{
  "$comment": "Per-component version registry (SoT). Components are versioned independently, and these values are not forced to stay identical. The single point for 'reading the current version from one place'.",
  "host": "0.4.12",
  "ide": "0.1.0",
  "screen-engine": "0.1.0"
}
