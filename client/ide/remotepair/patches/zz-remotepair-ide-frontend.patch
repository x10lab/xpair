diff --git a/src/vs/workbench/electron-browser/desktop.contribution.ts b/src/vs/workbench/electron-browser/desktop.contribution.ts
index be899620..be899621 100644
--- a/src/vs/workbench/electron-browser/desktop.contribution.ts
+++ b/src/vs/workbench/electron-browser/desktop.contribution.ts
@@ -164,7 +164,8 @@ const registry = Registry.as<IConfigurationRegistry>(ConfigurationExtensions.Con
 			'properties': {
 				'window.confirmSaveUntitledWorkspace': {
 					'type': 'boolean',
-					'default': true,
+					// Xpair: the client builds an untitled multi-root workspace via updateWorkspaceFolders, so VSCode otherwise prompts "save your workspace configuration as a file?" on every close. Default off → always discard the untitled workspace, no prompt.
+					'default': false,
 					'description': localize('confirmSaveUntitledWorkspace', "Controls whether a confirmation dialog shows asking to save or discard an opened untitled workspace in the window when switching to another workspace. Disabling the confirmation dialog will always discard the untitled workspace."),
 				},
 				'window.openWithoutArgumentsInNewWindow': {
diff --git a/src/vs/workbench/browser/layout.ts b/src/vs/workbench/browser/layout.ts
index 3480283a..edd00a3d 100644
--- a/src/vs/workbench/browser/layout.ts
+++ b/src/vs/workbench/browser/layout.ts
@@ -1589,6 +1589,11 @@ export abstract class Layout extends Disposable implements IWorkbenchLayoutServi
 	private setStatusBarHidden(hidden: boolean): void {
+		// RemotePair: the native status bar (git branch, screen-reader, bell, problems, etc.)
+		// has no place in the locked Sessions-first surface. Force it permanently hidden so no
+		// caller (config change, zen-mode exit, restore) can ever show it; the schema default
+		// alone (workbench.statusBar.visible=false) is overridable and does NOT force-hide.
+		hidden = true;
 		this.stateModel.setRuntimeValue(LayoutStateKeys.STATUSBAR_HIDDEN, hidden);

 		// Adjust CSS
 		if (hidden) {
 			this.mainContainer.classList.add(LayoutClasses.STATUSBAR_HIDDEN);
@@ -2224,6 +2224,12 @@ export abstract class Layout extends Disposable implements IWorkbenchLayoutServi
 	}
 
 	private setAuxiliaryBarHidden(hidden: boolean, skipLayout?: boolean): void {
+		// RemotePair: the secondary side bar (auxiliary bar / right side bar) is removed from
+		// the layout entirely — the RemotePair user has no use for the CHAT / "Build with Agent"
+		// surface. Force it permanently hidden so it never reserves grid space and cannot be
+		// shown by ANY caller (openViewContainer, session restore, toggle command, drag). The
+		// part and its grid node stay in code (rebase-safety); a hidden grid view takes 0 width.
+		hidden = true;
 		if (hidden && this.setAuxiliaryBarMaximized(false) && !this.isVisible(Parts.AUXILIARYBAR_PART)) {
 			return; // return: leaving maximised auxiliary bar made this part hidden
 		}
@@ -2669,8 +2674,10 @@ export abstract class Layout extends Disposable implements IWorkbenchLayoutServi
 					{
 						type: 'leaf',
 						data: { type: Parts.STATUSBAR_PART },
 						size: statusBarHeight,
-						visible: !this.stateModel.getRuntimeValue(LayoutStateKeys.STATUSBAR_HIDDEN)
+						// RemotePair: never construct the grid with a visible status bar — pair with the
+						// setStatusBarHidden force-hide so the part starts hidden on first render too.
+						visible: false
 					}
 				]
 			},
@@ -2941,7 +2947,8 @@ class LayoutStateModel extends Disposable {
 		const auxiliaryBarForceMaximized = this.configurationService.getValue(WorkbenchLayoutSettings.AUXILIARYBAR_FORCE_MAXIMIZED);
 		const workbenchState = this.contextService.getWorkbenchState();
 		const mainContainerDimension = configuration.mainContainerDimension;
-		LayoutStateKeys.SIDEBAR_SIZE.defaultValue = Math.min(300, mainContainerDimension.width / 4);
+		// RemotePair: default the left side bar to 1/3 of the window width (stock was min(300, w/4)).
+		LayoutStateKeys.SIDEBAR_SIZE.defaultValue = Math.round(mainContainerDimension.width / 3);
 		LayoutStateKeys.SIDEBAR_HIDDEN.defaultValue = workbenchState === WorkbenchState.EMPTY || auxiliaryBarForceMaximized === true;
 		LayoutStateKeys.AUXILIARYBAR_SIZE.defaultValue = auxiliaryBarForceMaximized ? Math.max(300, mainContainerDimension.width / 2) : Math.min(300, mainContainerDimension.width / 4);
 		LayoutStateKeys.AUXILIARYBAR_HIDDEN.defaultValue = (() => {
@@ -2980,7 +2987,10 @@ class LayoutStateModel extends Disposable {
 					return false;
 			}
 		})();
-		LayoutStateKeys.PANEL_SIZE.defaultValue = (this.stateCache.get(LayoutStateKeys.PANEL_POSITION.name) ?? isHorizontal(LayoutStateKeys.PANEL_POSITION.defaultValue)) ? mainContainerDimension.height / 3 : mainContainerDimension.width / 4;
+		// RemotePair: the bottom panel hosts only the compact Session Manager (3 thin
+		// horizontal-scroll rows, title hidden), so default it to a short fixed height instead of
+		// height/3 — the bottom area should waste minimal vertical space. (Side position keeps width/4.)
+		LayoutStateKeys.PANEL_SIZE.defaultValue = (this.stateCache.get(LayoutStateKeys.PANEL_POSITION.name) ?? isHorizontal(LayoutStateKeys.PANEL_POSITION.defaultValue)) ? 90 : mainContainerDimension.width / 4;
 		LayoutStateKeys.PANEL_POSITION.defaultValue = positionFromString(this.configurationService.getValue(WorkbenchLayoutSettings.PANEL_POSITION) ?? 'bottom');
 
 		// Apply all defaults
@@ -3039,7 +3049,7 @@ class LayoutStateModel extends Disposable {
 
 		// Restrict auxiliary bar size in case of small window dimensions
 		if (this.isNew[StorageScope.WORKSPACE] && configuration.mainContainerDimension.width <= DEFAULT_WORKSPACE_WINDOW_DIMENSIONS.width) {
-			this.setInitializationValue(LayoutStateKeys.SIDEBAR_SIZE, Math.min(300, configuration.mainContainerDimension.width / 4));
+			this.setInitializationValue(LayoutStateKeys.SIDEBAR_SIZE, Math.round(configuration.mainContainerDimension.width / 3)); // RemotePair: 1/3 default
 			this.setInitializationValue(LayoutStateKeys.AUXILIARYBAR_SIZE, Math.min(300, configuration.mainContainerDimension.width / 4));
 		}
 	}
diff --git a/src/vs/workbench/browser/parts/activitybar/activitybarPart.ts b/src/vs/workbench/browser/parts/activitybar/activitybarPart.ts
index bfd30e64..1d973f00 100644
--- a/src/vs/workbench/browser/parts/activitybar/activitybarPart.ts
+++ b/src/vs/workbench/browser/parts/activitybar/activitybarPart.ts
@@ -34,7 +34,7 @@ import { ContextKeyExpr, IContextKeyService } from '../../../../platform/context
 import { IsSessionsWindowContext } from '../../../common/contextkeys.js';
 import { Categories } from '../../../../platform/action/common/actionCommonCategories.js';
 import { getContextMenuActions } from '../../../../platform/actions/browser/menuEntryActionViewItem.js';
-import { IViewDescriptorService, ViewContainerLocation, ViewContainerLocationToString } from '../../../common/views.js';
+import { IViewDescriptorService, ViewContainer, ViewContainerLocation, ViewContainerLocationToString } from '../../../common/views.js';
 import { IExtensionService } from '../../../services/extensions/common/extensions.js';
 import { IWorkbenchEnvironmentService } from '../../../services/environment/common/environmentService.js';
 import { IViewsService } from '../../../services/views/common/viewsService.js';
@@ -47,9 +47,11 @@ export class ActivitybarPart extends Part {
 	static readonly COMPACT_ACTION_HEIGHT = 32;
 	static readonly COMPACT_ACTION_HEIGHT_RATIO = 32/48;
 
-	static readonly ACTIVITYBAR_WIDTH = 48;
-	static readonly COMPACT_ACTIVITYBAR_WIDTH = 36;
-	static readonly COMPACT_ACTIVITYBAR_WIDTH_RATIO = 36/48;
+	// RemotePair: the left rail is a thin vertical-text strip (no icons), so it
+	// does not need the default 48px icon-sized width. Use a slim fixed width.
+	static readonly ACTIVITYBAR_WIDTH = 30;
+	static readonly COMPACT_ACTIVITYBAR_WIDTH = 24;
+	static readonly COMPACT_ACTIVITYBAR_WIDTH_RATIO = 24/30;
 
 	static readonly ICON_SIZE = 24;
 	static readonly COMPACT_ICON_SIZE = 16;
@@ -61,8 +63,10 @@ export class ActivitybarPart extends Part {
 
 	//#region IView
 
-	get minimumWidth(): number { return this._isCompact ? FONT.activityBarSize48 * ActivitybarPart.COMPACT_ACTIVITYBAR_WIDTH_RATIO : FONT.activityBarSize48; }
-	get maximumWidth(): number { return this._isCompact ? FONT.activityBarSize48 * ActivitybarPart.COMPACT_ACTIVITYBAR_WIDTH_RATIO : FONT.activityBarSize48; }
+	// RemotePair: thin vertical-text rail. Width is a fixed slim strip rather than
+	// the icon-sized FONT.activityBarSize48 (48px) used by upstream.
+	get minimumWidth(): number { return this._isCompact ? ActivitybarPart.COMPACT_ACTIVITYBAR_WIDTH : ActivitybarPart.ACTIVITYBAR_WIDTH; }
+	get maximumWidth(): number { return this._isCompact ? ActivitybarPart.COMPACT_ACTIVITYBAR_WIDTH : ActivitybarPart.ACTIVITYBAR_WIDTH; }
 	readonly minimumHeight: number = 0;
 	readonly maximumHeight: number = Number.POSITIVE_INFINITY;
 
@@ -138,7 +142,8 @@ export class ActivitybarPart extends Part {
 			placeholderViewContainersKey: ActivitybarPart.placeholderViewContainersKey,
 			viewContainersWorkspaceStateKey: ActivitybarPart.viewContainersWorkspaceStateKey,
 			orientation: ActionsOrientation.VERTICAL,
-			icon: true,
+			// RemotePair: text-only left rail (Terminal/Browser/Settings) instead of icons.
+			icon: false,
 			iconSize,
 			activityHoverOptions: {
 				position: () => this.layoutService.getSideBarPosition() === Position.LEFT ? HoverPosition.RIGHT : HoverPosition.LEFT,
@@ -439,6 +444,9 @@ export class ActivityBarCompositeBar extends PaneCompositeBar {
 		// View Containers action bar
 		this.compositeBarContainer = super.create(this.element);
 
+		// RemotePair: the rail "+" was removed — new sessions are created from the
+		// Sessions container's own title-bar "+" instead (see remotePairTerminalSidebar).
+
 		// Global action bar
 		if (this.globalCompositeBar) {
 			this.globalCompositeBar.create(this.element);
@@ -495,6 +503,43 @@ export class ActivityBarCompositeBar extends PaneCompositeBar {
 		return actions;
 	}
 
+	// RemotePair: restrict the left rail to an allowlist. The default rail shows
+	// Explorer/Search/SCM/Run-Debug/Extensions plus extension containers; the
+	// native Terminal rail entry is also excluded because Sessions owns terminal
+	// creation and hosting. Only RemotePair-owned containers stay visible.
+	private static readonly RAIL_ALLOWED_CONTAINER_IDS = new Set<string>([
+	]);
+
+	private static isRailAllowedContainer(container: ViewContainer): boolean {
+		if (ActivityBarCompositeBar.RAIL_ALLOWED_CONTAINER_IDS.has(container.id)) {
+			return true;
+		}
+		// Allow any RemotePair-owned view container (e.g. the embedded terminal host).
+		return container.id.startsWith('remotepair') || container.id.startsWith('workbench.view.remotepair');
+	}
+
+	protected override getViewContainers(): readonly ViewContainer[] {
+		return super.getViewContainers().filter(container => ActivityBarCompositeBar.isRailAllowedContainer(container));
+	}
+
+	// Also gate the single-container lookup (used by shouldBeHidden + the cache/storage seeding)
+	// so extension-contributed rail containers (e.g. "Claude Code") that arrive after startup —
+	// invisible under dev `--disable-extensions` but present in the packaged app — are filtered
+	// too, not just the registered-enumeration path above.
+	protected override getViewContainer(id: string): ViewContainer | undefined {
+		const container = super.getViewContainer(id);
+		return container && ActivityBarCompositeBar.isRailAllowedContainer(container) ? container : undefined;
+	}
+
+	// Hard intercept: never add a non-allowlisted container to the rail, even via the
+	// unconditional addComposite() in onDidRegisterViewContainers (late extension viewlets).
+	protected override addComposite(viewContainer: ViewContainer): void {
+		if (!ActivityBarCompositeBar.isRailAllowedContainer(viewContainer)) {
+			return;
+		}
+		super.addComposite(viewContainer);
+	}
+
 }
 
 registerAction2(class extends Action2 {
diff --git a/src/vs/workbench/browser/parts/activitybar/media/activityaction.css b/src/vs/workbench/browser/parts/activitybar/media/activityaction.css
index 51eb067d..8ff87c48 100644
--- a/src/vs/workbench/browser/parts/activitybar/media/activityaction.css
+++ b/src/vs/workbench/browser/parts/activitybar/media/activityaction.css
@@ -12,7 +12,7 @@
 .monaco-workbench .activitybar > .content .composite-bar > .monaco-action-bar .action-item::after {
 	position: absolute;
 	content: '';
-	width: var(--activity-bar-width, 48px);
+	width: var(--activity-bar-width, 30px);
 	height: 2px;
 	display: none;
 	background-color: transparent;
@@ -46,7 +46,7 @@
 	z-index: 1;
 	display: flex;
 	overflow: hidden;
-	width: var(--activity-bar-width, 48px);
+	width: var(--activity-bar-width, 30px);
 	height: var(--activity-bar-action-height, 48px);
 	margin-right: 0;
 	box-sizing: border-box;
@@ -56,7 +56,7 @@
 .monaco-workbench .activitybar > .content :not(.monaco-menu) > .monaco-action-bar .action-label:not(.codicon) {
 	font-size: 15px;
 	line-height: var(--activity-bar-action-height, 48px);
-	padding: 0 0 0 var(--activity-bar-width, 48px);
+	padding: 0 0 0 var(--activity-bar-width, 30px);
 }
 
 .monaco-workbench .activitybar > .content :not(.monaco-menu) > .monaco-action-bar .action-label.codicon {
@@ -286,4 +286,61 @@
 .monaco-workbench .activitybar > .content :not(.monaco-menu) > .monaco-action-bar .badge .codicon.badge-content {
     font-size: calc(var(--vscode-workbench-activitybar-font-size) * 0.8125);
     padding: 0
-}
\ No newline at end of file
+}
+/* ----------------------------------------------------------------------------
+ * RemotePair: text-only activity bar rail (Terminal / Browser / Settings).
+ * When the composite bar is created with icon:false, the action-item container
+ * does NOT get the `.icon` class, but updateLabel() still adds the codicon
+ * classNames AND the text label. Suppress the glyph and show the text.
+ * -------------------------------------------------------------------------- */
+.monaco-workbench .activitybar > .content :not(.monaco-menu) > .monaco-action-bar .action-item:not(.icon) .action-label.codicon::before {
+	display: none !important;
+}
+.monaco-workbench .activitybar > .content :not(.monaco-menu) > .monaco-action-bar .action-item:not(.icon) .action-label {
+	font-family: var(--vscode-font-family);
+	font-size: 10px;
+	font-weight: 500;
+	letter-spacing: 0.02em;
+	line-height: 1;
+	box-sizing: border-box;
+	color: var(--vscode-foreground);
+
+	/* RemotePair: thin vertical text rail. The label reads bottom-to-top along
+	   the slim rail. vertical-rl + 180deg rotation gives an upright, readable
+	   sideways label; the rail itself stays narrow (~30px). Tight padding and a
+	   small font keep the text crisp on the thin vertical strip. */
+	writing-mode: vertical-rl;
+	transform: rotate(180deg);
+	text-orientation: mixed;
+	white-space: nowrap;
+	overflow: hidden;
+	text-overflow: ellipsis;
+	display: flex;
+	align-items: center;
+	justify-content: center;
+	width: 100%;
+	height: auto;
+	padding: 6px 0;
+}
+
+/* RemotePair: "+" rail action appended after the container list. */
+.monaco-workbench .activitybar > .content .remotepair-rail-add {
+	display: flex;
+	align-items: center;
+	justify-content: center;
+	width: 100%;
+	height: var(--activity-bar-action-height, 48px);
+	font-size: 16px;
+	font-weight: 400;
+	line-height: 1;
+	cursor: pointer;
+	color: var(--vscode-activityBar-inactiveForeground, var(--vscode-foreground));
+	box-sizing: border-box;
+	user-select: none;
+}
+
+.monaco-workbench .activitybar > .content .remotepair-rail-add:hover,
+.monaco-workbench .activitybar > .content .remotepair-rail-add:focus {
+	color: var(--vscode-activityBar-foreground, var(--vscode-foreground));
+	outline: none;
+}
diff --git a/src/vs/workbench/browser/parts/activitybar/media/activitybarpart.css b/src/vs/workbench/browser/parts/activitybar/media/activitybarpart.css
index b3d7e506..6687fde8 100644
--- a/src/vs/workbench/browser/parts/activitybar/media/activitybarpart.css
+++ b/src/vs/workbench/browser/parts/activitybar/media/activitybarpart.css
@@ -4,7 +4,7 @@
  *--------------------------------------------------------------------------------------------*/
 
 .monaco-workbench .part.activitybar {
-	width: var(--activity-bar-width, 48px);
+	width: var(--activity-bar-width, 30px);
 	height: 100%;
 	font-family: var(--vscode-workbench-activitybar-font-family, inherit);
 	font-size: var(--vscode-workbench-activitybar-font-size);
diff --git a/src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarPart.ts b/src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarPart.ts
index 88c43a91..bd65d165 100644
--- a/src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarPart.ts
+++ b/src/vs/workbench/browser/parts/auxiliarybar/auxiliaryBarPart.ts
@@ -15,7 +15,7 @@ import { contrastBorder } from '../../../../platform/theme/common/colorRegistry.
 import { IThemeService } from '../../../../platform/theme/common/themeService.js';
 import { ActiveAuxiliaryContext, AuxiliaryBarFocusContext } from '../../../common/contextkeys.js';
 import { ACTIVITY_BAR_BADGE_BACKGROUND, ACTIVITY_BAR_BADGE_FOREGROUND, ACTIVITY_BAR_TOP_ACTIVE_BORDER, ACTIVITY_BAR_TOP_DRAG_AND_DROP_BORDER, ACTIVITY_BAR_TOP_FOREGROUND, ACTIVITY_BAR_TOP_INACTIVE_FOREGROUND, PANEL_ACTIVE_TITLE_BORDER, PANEL_ACTIVE_TITLE_FOREGROUND, PANEL_DRAG_AND_DROP_BORDER, PANEL_INACTIVE_TITLE_FOREGROUND, SIDE_BAR_BACKGROUND, SIDE_BAR_BORDER, SIDE_BAR_TITLE_BORDER, SIDE_BAR_FOREGROUND } from '../../../common/theme.js';
-import { IViewDescriptorService, ViewContainerLocation } from '../../../common/views.js';
+import { IViewDescriptorService, ViewContainer, ViewContainerLocation } from '../../../common/views.js';
 import { IExtensionService } from '../../../services/extensions/common/extensions.js';
 import { ActivityBarPosition, IWorkbenchLayoutService, LayoutSettings, Parts, Position } from '../../../services/layout/browser/layoutService.js';
 import { HoverPosition } from '../../../../base/browser/ui/hover/hoverWidget.js';
@@ -27,7 +27,8 @@ import { ToggleSidebarPositionAction } from '../../actions/layoutActions.js';
 import { ICommandService } from '../../../../platform/commands/common/commands.js';
 import { AbstractPaneCompositePart, CompositeBarPosition } from '../paneCompositePart.js';
 import { ActionsOrientation } from '../../../../base/browser/ui/actionbar/actionbar.js';
-import { IPaneCompositeBarOptions } from '../paneCompositeBar.js';
+import { IPaneCompositeBarOptions, PaneCompositeBar } from '../paneCompositeBar.js';
+import { isRemotePairAuxBarContainerAllowed } from '../remotePairPrune.js';
 import { IMenuService, MenuId } from '../../../../platform/actions/common/actions.js';
 import { IConfigurationService } from '../../../../platform/configuration/common/configuration.js';
 import { getContextMenuActions } from '../../../../platform/actions/browser/menuEntryActionViewItem.js';
@@ -44,6 +45,28 @@ interface IAuxiliaryBarPartConfiguration {
 	showLabels: boolean;
 }
 
+// RemotePair: secondary side bar composite bar restricted to an allowlist (only RemotePair-owned
+// containers). The native CHAT / "Build with Agent" container stays registered but is filtered out
+// of the bar so it never surfaces. Mirrors the left-rail allowlist in activitybarPart.
+class RemotePairAuxBarCompositeBar extends PaneCompositeBar {
+	protected override getViewContainers(): readonly ViewContainer[] {
+		return super.getViewContainers().filter(isRemotePairAuxBarContainerAllowed);
+	}
+	// Also gate the single-container lookup (used by shouldBeHidden + the storage/cache
+	// seeding) so the pinned native CHAT panel never resurfaces from cache.
+	protected override getViewContainer(id: string): ViewContainer | undefined {
+		const container = super.getViewContainer(id);
+		return container && isRemotePairAuxBarContainerAllowed(container) ? container : undefined;
+	}
+	// Hard intercept at the add point (late extension aux views / "Additional Views" placeholder).
+	protected override addComposite(viewContainer: ViewContainer): void {
+		if (!isRemotePairAuxBarContainerAllowed(viewContainer)) {
+			return;
+		}
+		super.addComposite(viewContainer);
+	}
+}
+
 export class AuxiliaryBarPart extends AbstractPaneCompositePart {
 
 	static readonly activeViewSettingsKey = 'workbench.auxiliarybar.activepanelid';
@@ -272,6 +295,11 @@ export class AuxiliaryBarPart extends AbstractPaneCompositePart {
 		};
 	}
 
+	// RemotePair: use the allowlist-filtered composite bar so the CHAT panel stays hidden.
+	protected override createCompositeBar(): PaneCompositeBar {
+		return this.instantiationService.createInstance(RemotePairAuxBarCompositeBar, this.location, this.getCompositeBarOptions(), this.partId, this);
+	}
+
 	private fillExtraContextMenuActions(actions: IAction[]): void {
 		const currentPositionRight = this.layoutService.getSideBarPosition() === Position.LEFT;
 
diff --git a/src/vs/workbench/browser/parts/editor/media/multieditortabscontrol.css b/src/vs/workbench/browser/parts/editor/media/multieditortabscontrol.css
index 4928dac9..f56a5b37 100644
--- a/src/vs/workbench/browser/parts/editor/media/multieditortabscontrol.css
+++ b/src/vs/workbench/browser/parts/editor/media/multieditortabscontrol.css
@@ -685,4 +685,24 @@
 }
 .monaco-workbench .part.editor > .content .editor-group-container > .title.tabs .tabs-container > .tab.last-in-row.drop-target-left::after, .monaco-workbench .part.editor > .content .editor-group-container > .title.tabs .tabs-container > .tab.last-in-row + .tab.drop-target-right::before, .monaco-workbench .part.editor > .content .editor-group-container > .title.tabs .tabs-container > .tab:last-child.drop-target-left::after, .monaco-workbench .part.editor > .content .editor-group-container > .title.tabs .tabs-container > .tab:first-child.drop-target-right::before {
     width: calc(var(--vscode-workbench-tabs-font-size) * 0.153846)
-}
\ No newline at end of file
+}
+
+
+/* =============================================================================
+   RemotePair — responsive tabs
+   ============================================================================= */
+
+/* Smooth horizontal scroll when tabs overflow */
+.monaco-workbench .part.editor > .content .editor-group-container > .title .tabs-container {
+    scroll-behavior: smooth;
+    overflow-x: auto;
+}
+
+/* Minimum tab width so tabs don't collapse to nothing when many are open */
+.monaco-workbench .part.editor > .content .editor-group-container > .title .tabs-container > .tab {
+    min-width: 80px;
+}
+
+/* =============================================================================
+   End RemotePair — responsive tabs
+   ============================================================================= */
\ No newline at end of file
diff --git a/src/vs/workbench/browser/parts/globalCompositeBar.ts b/src/vs/workbench/browser/parts/globalCompositeBar.ts
index 48220d63..0e7493e5 100644
--- a/src/vs/workbench/browser/parts/globalCompositeBar.ts
+++ b/src/vs/workbench/browser/parts/globalCompositeBar.ts
@@ -53,7 +53,7 @@ export class GlobalCompositeBar extends Disposable {
 	readonly element: HTMLElement;
 
 	private readonly globalActivityAction = this._register(new Action(GLOBAL_ACTIVITY_ID));
-	private readonly accountAction = this._register(new Action(ACCOUNTS_ACTIVITY_ID));
+	// RemotePair: accountAction removed — Accounts activity is never shown in the rail.
 	private readonly globalActivityActionBar: ActionBar;
 
 	constructor(
@@ -75,7 +75,13 @@ export class GlobalCompositeBar extends Disposable {
 		this.globalActivityActionBar = this._register(new ActionBar(this.element, {
 			actionViewItemProvider: (action, options) => {
 				if (action.id === GLOBAL_ACTIVITY_ID) {
-					return this.instantiationService.createInstance(GlobalActivityActionViewItem, this.contextMenuActionsProvider, { ...options, colors: this.colors, hoverOptions: this.activityHoverOptions }, contextMenuAlignmentOptions);
+					// RemotePair (S1): render the global (settings) activity as a vertical
+					// TEXT label to match the Sessions/Browser rail items. icon:false makes
+					// CompositeBarActionViewItem show the action name ("Settings") instead of
+					// the gear glyph, so the rail stays a thin all-text strip. This is scoped
+					// to the activity-bar rail's global bar only — the titlebar's
+					// SimpleGlobalActivityActionViewItem path is created elsewhere and untouched.
+					return this.instantiationService.createInstance(GlobalActivityActionViewItem, this.contextMenuActionsProvider, { ...options, colors: this.colors, hoverOptions: this.activityHoverOptions, icon: false }, contextMenuAlignmentOptions);
 				}
 
 				if (action.id === ACCOUNTS_ACTIVITY_ID) {
@@ -102,9 +108,9 @@ export class GlobalCompositeBar extends Disposable {
 			preventLoopNavigation: true
 		}));
 
-		if (this.accountsVisibilityPreference) {
-			this.globalActivityActionBar.push(this.accountAction, { index: GlobalCompositeBar.ACCOUNTS_ACTION_INDEX });
-		}
+		// RemotePair: never show the Accounts activity in the rail. Only the
+		// global (Manage / settings gear) action is kept at the bottom.
+		// (Original code pushed accountAction when accountsVisibilityPreference was true.)
 
 		this.globalActivityActionBar.push(this.globalActivityAction);
 
@@ -136,13 +142,11 @@ export class GlobalCompositeBar extends Disposable {
 	}
 
 	private toggleAccountsActivity() {
-		if (this.globalActivityActionBar.length() === 2 && this.accountsVisibilityPreference) {
-			return;
-		}
+		// RemotePair: Accounts activity is permanently hidden from the rail, so
+		// ignore storage-driven visibility toggles. Only pull a stray account
+		// action if one somehow got added; never push it back.
 		if (this.globalActivityActionBar.length() === 2) {
 			this.globalActivityActionBar.pull(GlobalCompositeBar.ACCOUNTS_ACTION_INDEX);
-		} else {
-			this.globalActivityActionBar.push(this.accountAction, { index: GlobalCompositeBar.ACCOUNTS_ACTION_INDEX });
 		}
 	}
 
@@ -603,7 +607,11 @@ export class GlobalActivityActionViewItem extends AbstractGlobalActivityActionVi
 	) {
 		const action = instantiationService.createInstance(CompositeBarAction, {
 			id: GLOBAL_ACTIVITY_ID,
-			name: localize('manage', "Manage"),
+			// RemotePair (S1): the rail renders this activity as a text label (icon:false),
+			// so the name doubles as the rail caption. "Settings" matches the RemotePair
+			// rail vocabulary (Sessions / Browser / Settings); the click still opens the
+			// global Manage menu (Settings, Keyboard Shortcuts, …).
+			name: localize('remotepairSettings', "Settings"),
 			classNames: ThemeIcon.asClassNameArray(userDataProfileService.currentProfile.icon ? ThemeIcon.fromId(userDataProfileService.currentProfile.icon) : DEFAULT_ICON)
 		});
 		super(MenuId.GlobalActivity, action, options, contextMenuActionsProvider, contextMenuAlignmentOptions, themeService, hoverService, menuService, contextMenuService, contextKeyService, configurationService, keybindingService, activityService);
diff --git a/src/vs/workbench/browser/parts/media/paneCompositePart.css b/src/vs/workbench/browser/parts/media/paneCompositePart.css
index 195267cf..172890e4 100644
--- a/src/vs/workbench/browser/parts/media/paneCompositePart.css
+++ b/src/vs/workbench/browser/parts/media/paneCompositePart.css
@@ -352,8 +352,21 @@
 
 .monaco-workbench .pane-composite-part .empty-pane-message-area {
 	display: none;
+	/* RemotePair: overlay (not in-flow). The empty message is height:100% and was
+	   laid out IN FLOW, so if it is ever shown while a composite is also present
+	   (e.g. the embedded-EditorPart Sessions sidebar, whose activeComposite can lag
+	   the DOM), it shoves the real viewlet off the bottom of the sidebar -> blank.
+	   Absolute positioning keeps it a centered overlay that never displaces siblings. */
+	position: absolute;
+	inset: 0;
 	height: 100%;
 	width: 100%;
+	pointer-events: none;
+	z-index: 1;
+}
+
+.monaco-workbench .pane-composite-part .content {
+	position: relative;
 }
 
 .monaco-workbench .pane-composite-part.empty .empty-pane-message-area {
diff --git a/src/vs/workbench/browser/parts/paneCompositeBar.ts b/src/vs/workbench/browser/parts/paneCompositeBar.ts
index bc6003e2..dcc7ae99 100644
--- a/src/vs/workbench/browser/parts/paneCompositeBar.ts
+++ b/src/vs/workbench/browser/parts/paneCompositeBar.ts
@@ -458,7 +458,12 @@ export class PaneCompositeBar extends Disposable {
 		return true;
 	}
 
-	private addComposite(viewContainer: ViewContainer): void {
+	// RemotePair: changed from private to protected so the rail/panel/auxbar composite bars can
+	// override and hard-skip non-allowlisted containers at the add point. This is the reliable
+	// intercept: onDidRegisterViewContainers() calls addComposite() unconditionally for
+	// late-registering EXTENSION containers (e.g. "Claude Code"), bypassing the getViewContainers
+	// filter; filtering here keeps them out of the bar entirely.
+	protected addComposite(viewContainer: ViewContainer): void {
 		this.compositeBar.addComposite({ id: viewContainer.id, name: typeof viewContainer.title === 'string' ? viewContainer.title : viewContainer.title.value, order: viewContainer.order, requestedIndex: viewContainer.requestedIndex });
 	}
 
@@ -515,12 +520,18 @@ export class PaneCompositeBar extends Disposable {
 		this.compositeBar.layout(new Dimension(width, height));
 	}
 
-	private getViewContainer(id: string): ViewContainer | undefined {
+	// RemotePair: changed from private to protected so panel/auxbar composite bars can
+	// override and apply the same allowlist as getViewContainers(). This is the gate
+	// shouldBeHidden() uses, so filtering it here hides cached/pinned native containers
+	// (Problems/Output/Terminal/Ports/Debug, CHAT) — getViewContainers() alone does not.
+	protected getViewContainer(id: string): ViewContainer | undefined {
 		const viewContainer = this.viewDescriptorService.getViewContainerById(id);
 		return viewContainer && this.viewDescriptorService.getViewContainerLocation(viewContainer) === this.location ? viewContainer : undefined;
 	}
 
-	private getViewContainers(): readonly ViewContainer[] {
+	// RemotePair: changed from private to protected so ActivityBarCompositeBar
+	// can override and filter the container allowlist (Explorer/Terminal/RemotePair only).
+	protected getViewContainers(): readonly ViewContainer[] {
 		return this.viewDescriptorService.getViewContainersByLocation(this.location);
 	}
 
diff --git a/src/vs/workbench/browser/parts/panel/panelPart.ts b/src/vs/workbench/browser/parts/panel/panelPart.ts
index cdd90d95..291e7956 100644
--- a/src/vs/workbench/browser/parts/panel/panelPart.ts
+++ b/src/vs/workbench/browser/parts/panel/panelPart.ts
@@ -22,31 +22,57 @@ import { Dimension } from '../../../../base/browser/dom.js';
 import { IContextKeyService } from '../../../../platform/contextkey/common/contextkey.js';
 import { assertReturnsDefined } from '../../../../base/common/types.js';
 import { IExtensionService } from '../../../services/extensions/common/extensions.js';
-import { IViewDescriptorService, ViewContainerLocation } from '../../../common/views.js';
+import { IViewDescriptorService, ViewContainer, ViewContainerLocation } from '../../../common/views.js';
 import { HoverPosition } from '../../../../base/browser/ui/hover/hoverWidget.js';
 import { IMenuService, MenuId } from '../../../../platform/actions/common/actions.js';
 import { AbstractPaneCompositePart, CompositeBarPosition } from '../paneCompositePart.js';
 import { ICommandService } from '../../../../platform/commands/common/commands.js';
 import { getContextMenuActions } from '../../../../platform/actions/browser/menuEntryActionViewItem.js';
-import { IPaneCompositeBarOptions } from '../paneCompositeBar.js';
+import { IPaneCompositeBarOptions, PaneCompositeBar } from '../paneCompositeBar.js';
+import { isRemotePairPanelContainerAllowed } from '../remotePairPrune.js';
 import { IHoverService } from '../../../../platform/hover/browser/hover.js';
 import { IConfigurationService } from '../../../../platform/configuration/common/configuration.js';
 import { Extensions } from '../../panecomposite.js';
 import { FONT, getFontSize, updatePanelSize as updateBottomPaneSize } from '../../../../base/common/font.js';
 
+// RemotePair: bottom panel composite bar restricted to an allowlist (only RemotePair-owned
+// containers). Native panels (Problems/Output/Debug/Terminal/Ports) stay registered but are
+// filtered out of the bar so they never surface. Mirrors the left-rail allowlist in activitybarPart.
+class RemotePairPanelCompositeBar extends PaneCompositeBar {
+	protected override getViewContainers(): readonly ViewContainer[] {
+		return super.getViewContainers().filter(isRemotePairPanelContainerAllowed);
+	}
+	// Also gate the single-container lookup (used by shouldBeHidden + the storage/cache
+	// seeding) so pinned native panels never resurface from cache.
+	protected override getViewContainer(id: string): ViewContainer | undefined {
+		const container = super.getViewContainer(id);
+		return container && isRemotePairPanelContainerAllowed(container) ? container : undefined;
+	}
+	// Hard intercept at the add point (late extension panels bypass the getViewContainers filter).
+	protected override addComposite(viewContainer: ViewContainer): void {
+		if (!isRemotePairPanelContainerAllowed(viewContainer)) {
+			return;
+		}
+		super.addComposite(viewContainer);
+	}
+}
+
 export class PanelPart extends AbstractPaneCompositePart {
 
 	//#region IView
 
 	readonly minimumWidth: number = 300;
 	readonly maximumWidth: number = Number.POSITIVE_INFINITY;
-	readonly minimumHeight: number = 77;
+	// RemotePair: the panel hosts only the compact Session Manager (3 thin horizontal-scroll
+	// rows, title hidden), so allow it to shrink very short to avoid wasting vertical space.
+	readonly minimumHeight: number = 44;
 	readonly maximumHeight: number = Number.POSITIVE_INFINITY;
 
 	get preferredHeight(): number | undefined {
-		// Don't worry about titlebar or statusbar visibility
-		// The difference is minimal and keeps this function clean
-		return this.layoutService.mainContainerDimension.height * 0.4;
+		// RemotePair: the panel hosts only the compact Session Manager (3 thin horizontal-scroll
+		// rows, title hidden), so default to a short height instead of 40% of the window — the
+		// bottom area should waste minimal vertical space. The user can still drag it taller.
+		return 90;
 	}
 
 	get preferredWidth(): number | undefined {
@@ -204,6 +230,11 @@ export class PanelPart extends AbstractPaneCompositePart {
 		};
 	}
 
+	// RemotePair: use the allowlist-filtered composite bar so native panels stay hidden.
+	protected override createCompositeBar(): PaneCompositeBar {
+		return this.instantiationService.createInstance(RemotePairPanelCompositeBar, this.location, this.getCompositeBarOptions(), this.partId, this);
+	}
+
 	private fillExtraContextMenuActions(actions: IAction[]): void {
 		if (this.getCompositeBarPosition() === CompositeBarPosition.TITLE) {
 			const viewsSubmenuAction = this.getViewsSubmenuAction();
diff --git a/src/vs/workbench/contrib/files/browser/explorerViewlet.ts b/src/vs/workbench/contrib/files/browser/explorerViewlet.ts
index ecc6af48..285e075b 100644
--- a/src/vs/workbench/contrib/files/browser/explorerViewlet.ts
+++ b/src/vs/workbench/contrib/files/browser/explorerViewlet.ts
@@ -144,7 +144,13 @@ export class ExplorerViewletViewsContribution extends Disposable implements IWor
 			ctorDescriptor: new SyncDescriptor(ExplorerView),
 			order: 1,
 			canMoveView: true,
-			canToggleVisibility: false,
+			// RemotePair (C4.3): make the Explorer file-tree view toggleable so the Explorer<->Search
+			// inner router can hide it (viewContainerModel.updateViewDescriptorItemVisibility no-ops
+			// when canToggleVisibility is false — viewContainerModel.ts:431). Hiding the primary view
+			// is safe: the container stays alive because hideIfEmpty gates on ACTIVE (not visible)
+			// view descriptors. The view remains non-collapsible-away by default; the router drives
+			// visibility programmatically via IViewDescriptorService.getViewContainerModel(...).setVisible.
+			canToggleVisibility: true,
 			focusCommand: {
 				id: 'workbench.explorer.fileView.focus'
 			}
@@ -171,7 +177,16 @@ export class ExplorerViewPaneContainer extends ViewPaneContainer {
 		@ILogService logService: ILogService,
 	) {
 
-		super(VIEWLET_ID, { mergeViewWithContainerWhenSingleView: true }, instantiationService, configurationService, layoutService, contextMenuService, telemetryService, extensionService, themeService, storageService, contextService, viewDescriptorService, logService);
+		// RemotePair (C4): force the Browser container into a 2-row header (container-title Row-1 +
+		// active-view-title Row-2), symmetric to the Sessions container. With the single-view merge
+		// enabled the container collapses to ONE row whenever only one view is active (the
+		// isViewMergedWithContainer() `paneItems.length === 1` guard at viewPaneContainer.ts:1102).
+		// C2 added a Favorites view so there are usually >=2 views, but Favorites is
+		// canToggleVisibility:true and the Explorer<->Search router (C4.3) deliberately hides views,
+		// so the active-view count can drop to 1 and re-merge — collapsing back to a single row.
+		// Disabling the merge unconditionally guarantees the 2-row header is stable across every
+		// router state (Explorer-only, Search-only, with/without Favorites). See ADR-3 / AC14/AC20.
+		super(VIEWLET_ID, { mergeViewWithContainerWhenSingleView: false }, instantiationService, configurationService, layoutService, contextMenuService, telemetryService, extensionService, themeService, storageService, contextService, viewDescriptorService, logService);
 
 		this.viewletVisibleContextKey = ExplorerViewletVisibleContext.bindTo(contextKeyService);
 		this._register(this.contextService.onDidChangeWorkspaceName(e => this.updateTitleArea()));
@@ -256,7 +271,7 @@ const viewContainerRegistry = Registry.as<IViewContainersRegistry>(Extensions.Vi
  */
 export const VIEW_CONTAINER: ViewContainer = viewContainerRegistry.registerViewContainer({
 	id: VIEWLET_ID,
-	title: localize2('explore', "Explorer"),
+	title: localize2('explore', "Browser"),
 	ctorDescriptor: new SyncDescriptor(ExplorerViewPaneContainer),
 	storageId: 'workbench.explorer.views.state',
 	icon: explorerViewIcon,
@@ -270,7 +285,7 @@ export const VIEW_CONTAINER: ViewContainer = viewContainerRegistry.registerViewC
 		keybindings: { primary: KeyMod.CtrlCmd | KeyMod.Shift | KeyCode.KeyE },
 		order: 0
 	},
-}, ViewContainerLocation.Sidebar, { isDefault: true });
+}, ViewContainerLocation.Sidebar, { isDefault: false });

 const openFolder = localize('openFolder', "Open Folder");
 const addAFolder = localize('addAFolder', "add a folder");
@@ -327,6 +342,11 @@ viewsRegistry.registerViewWelcomeContent(EmptyView.ID, {
 	content: localize({ key: 'noFolderButEditorsHelp', comment: ['Please do not translate the word "command", it is part of our internal syntax which must not change'] },
 		"You have not yet opened a folder.\n{0}\nOpening a folder will close all currently open editors. To keep them open, {1} instead.", openFolderButton, addAFolderButton),
 	when: ContextKeyExpr.and(
+		// Xpair: this is not a local IDE — the empty Browser offers "Add Mapping" (mount a host
+		// folder), not the native "Open Folder". Gate the native local no-folder welcomes off the
+		// 'empty' workbench state (the only state EmptyView appears in for a RemotePair client) so
+		// only the Xpair Add Mapping welcome shows.
+		WorkbenchStateContext.notEqualsTo('empty'),
 		// editors are opened
 		ContextKeyExpr.has('editorIsOpen'),
 		ContextKeyExpr.or(
@@ -344,6 +364,9 @@ viewsRegistry.registerViewWelcomeContent(EmptyView.ID, {
 	content: localize({ key: 'noFolderHelp', comment: ['Please do not translate the word "command", it is part of our internal syntax which must not change'] },
 		"You have not yet opened a folder.\n{0}", openFolderButton),
 	when: ContextKeyExpr.and(
+		// Xpair: see noFolderButEditorsHelp above — suppress the native "Open Folder" welcome in
+		// the 'empty' state so only the Xpair Add Mapping welcome shows.
+		WorkbenchStateContext.notEqualsTo('empty'),
 		// no editor is open
 		ContextKeyExpr.has('editorIsOpen')?.negate(),
 		ContextKeyExpr.or(
diff --git a/src/vs/workbench/contrib/files/browser/files.contribution.ts b/src/vs/workbench/contrib/files/browser/files.contribution.ts
index 574c2d8e..60ac1f4c 100644
--- a/src/vs/workbench/contrib/files/browser/files.contribution.ts
+++ b/src/vs/workbench/contrib/files/browser/files.contribution.ts
@@ -19,6 +19,9 @@ import { ServicesAccessor } from '../../../../platform/instantiation/common/inst
 import { SyncDescriptor } from '../../../../platform/instantiation/common/descriptors.js';
 import { isNative, isWeb, isWindows } from '../../../../base/common/platform.js';
 import { ExplorerViewletViewsContribution } from './explorerViewlet.js';
+import './remotePairBrowserActions.js'; // RemotePair (G005): Browser title actions + per-folder session creation
+import './remotePairFavorites.js'; // RemotePair (C2): Favorites store + bottom-of-Browser Favorites view
+import './remotePairBrowserRouter.js'; // RemotePair (C4.3): Explorer<->Search inner router + hosted Search view
 import { IEditorPaneRegistry, EditorPaneDescriptor } from '../../../browser/editor.js';
 import { ILabelService } from '../../../../platform/label/common/label.js';
 import { InstantiationType, registerSingleton } from '../../../../platform/instantiation/common/extensions.js';
diff --git a/src/vs/workbench/contrib/files/browser/media/explorerviewlet.css b/src/vs/workbench/contrib/files/browser/media/explorerviewlet.css
index 0f75b632..a70c75f9 100644
--- a/src/vs/workbench/contrib/files/browser/media/explorerviewlet.css
+++ b/src/vs/workbench/contrib/files/browser/media/explorerviewlet.css
@@ -128,4 +128,131 @@
 .monaco-workbench .part.sidebar .explorer-folders-view .explorer-item .monaco-inputbox input[type="text"], .monaco-workbench .part.auxiliarybar .explorer-folders-view .explorer-item .monaco-inputbox input[type="text"] {
     outline-width: calc(var(--vscode-workbench-sidebar-font-size) * 0.076923);
     outline-offset: calc(var(--vscode-workbench-sidebar-font-size) * -0.076923)
-}
\ No newline at end of file
+}
+/* --- Xpair (C1): below-list "Add Mapping" footer --- */
+/* The explorer pane body becomes a flex column so the folder list (flex:1) and the
+   Add Mapping footer (fixed) stack; the tree height is reserved in layoutBody so the
+   footer is always visible ~20px below the list. */
+.rp-explorer-body {
+    display: flex;
+    flex-direction: column;
+    height: 100%;
+}
+.rp-explorer-body > .explorer-folders-view {
+    flex: 1 1 auto;
+    min-height: 0;
+}
+.rp-add-root-footer {
+    flex: 0 0 auto;
+    display: flex;
+    margin-top: 20px;
+    padding: 0 8px 8px;
+}
+.rp-add-root-button {
+    display: flex;
+    align-items: center;
+    gap: 6px;
+    cursor: pointer;
+    padding: 2px 4px;
+    border-radius: 4px;
+    color: var(--vscode-textLink-foreground);
+    user-select: none;
+}
+.rp-add-root-button:hover {
+    color: var(--vscode-textLink-activeForeground);
+    text-decoration: underline;
+}
+.rp-add-root-button:focus {
+    outline: 1px solid var(--vscode-focusBorder);
+    outline-offset: 2px;
+}
+.rp-add-root-icon {
+    flex: 0 0 auto;
+}
+/* --- RemotePair (C2): Favorites view (bottom of Browser container) --- */
+.monaco-workbench .remotepair-favorites .remotepair-favorites-list {
+    display: flex;
+    flex-direction: column;
+    padding: 2px 0;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-empty {
+    padding: 6px 12px;
+    opacity: 0.6;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-row {
+    display: flex;
+    align-items: center;
+    height: 22px;
+    padding: 0 8px 0 12px;
+    cursor: pointer;
+    user-select: none;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-row:hover,
+.monaco-workbench .remotepair-favorites .remotepair-favorites-row:focus {
+    background: var(--vscode-list-hoverBackground);
+    outline: none;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-label {
+    flex: 1 1 auto;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+}
+/* Inline actions ('+' / 'X'): right-aligned, revealed on row hover/focus. */
+.monaco-workbench .remotepair-favorites .remotepair-favorites-actions {
+    flex: 0 0 auto;
+    display: flex;
+    align-items: center;
+    gap: 2px;
+    margin-left: 6px;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-action {
+    opacity: 0;
+    cursor: pointer;
+    padding: 2px;
+    border-radius: 4px;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-row:hover .remotepair-favorites-action,
+.monaco-workbench .remotepair-favorites .remotepair-favorites-row:focus .remotepair-favorites-action {
+    opacity: 0.8;
+}
+.monaco-workbench .remotepair-favorites .remotepair-favorites-action:hover {
+    opacity: 1;
+    background: var(--vscode-toolbar-hoverBackground, rgba(128, 128, 128, 0.2));
+}
+
+/* --- RemotePair (C3): per-folder-row hover controls (star + '+') --- */
+/* Rendered by the IExplorerFileContribution into each row's contents container; absolute,
+   right-aligned, revealed on row hover/focus/selection (like native explorer inline actions).
+   Only present on folder rows (the contribution hides itself on files/unresolved rows — DR-6). */
+.monaco-workbench .explorer-folders-view .remotepair-folder-row-actions {
+    position: absolute;
+    right: 4px;
+    top: 0;
+    height: 100%;
+    display: flex;
+    align-items: center;
+    gap: 2px;
+    z-index: 12;
+}
+.monaco-workbench .explorer-folders-view .remotepair-folder-row-action {
+    opacity: 0;
+    cursor: pointer;
+    padding: 2px;
+    border-radius: 4px;
+    font-size: 14px;
+}
+/* Keep the star visible (dimmed) when the folder is already favorited, even without hover, so
+   the favorite state is discoverable; the '+' stays hover-only. */
+.monaco-workbench .explorer-folders-view .remotepair-folder-row-action.remotepair-folder-row-action-active {
+    opacity: 0.65;
+}
+.monaco-workbench .explorer-folders-view .monaco-list-row:hover .remotepair-folder-row-action,
+.monaco-workbench .explorer-folders-view .monaco-list-row.focused .remotepair-folder-row-action,
+.monaco-workbench .explorer-folders-view .monaco-list-row.selected .remotepair-folder-row-action {
+    opacity: 0.8;
+}
+.monaco-workbench .explorer-folders-view .remotepair-folder-row-action:hover {
+    opacity: 1;
+    background: var(--vscode-toolbar-hoverBackground, rgba(128, 128, 128, 0.2));
+}
diff --git a/src/vs/workbench/contrib/files/browser/views/explorerView.ts b/src/vs/workbench/contrib/files/browser/views/explorerView.ts
index d276b5c9..2ed4089c 100644
--- a/src/vs/workbench/contrib/files/browser/views/explorerView.ts
+++ b/src/vs/workbench/contrib/files/browser/views/explorerView.ts
@@ -49,6 +49,7 @@ import { IUriIdentityService } from '../../../../../platform/uriIdentity/common/
 import { EditorResourceAccessor, SideBySideEditor } from '../../../../common/editor.js';
 import { IExplorerService, IExplorerView } from '../files.js';
 import { Codicon } from '../../../../../base/common/codicons.js';
+import { ThemeIcon } from '../../../../../base/common/themables.js'; // Xpair (C1): below-list Add Mapping footer icon
 import { ICommandService } from '../../../../../platform/commands/common/commands.js';
 import { IEditorResolverService } from '../../../../services/editor/common/editorResolverService.js';
 import { EditorOpenSource } from '../../../../../platform/editor/common/editor.js';
@@ -171,6 +172,7 @@ export class ExplorerView extends ViewPane implements IExplorerView {
 
 	private treeContainer!: HTMLElement;
 	private container!: HTMLElement;
+	private rpAddRootFooter: HTMLElement | undefined; // Xpair (C1): below-list Add Mapping footer
 	private compressedFocusContext: IContextKey<boolean>;
 	private compressedFocusFirstContext: IContextKey<boolean>;
 	private compressedFocusLastContext: IContextKey<boolean>;
@@ -296,7 +298,10 @@ export class ExplorerView extends ViewPane implements IExplorerView {
 
 	protected override layoutBody(height: number, width: number): void {
 		super.layoutBody(height, width);
-		this.tree.layout(height, width);
+		// Xpair (C1): reserve space for the below-list "Add Mapping" footer so it is always
+		// visible under the folder list (the tree otherwise fills 100% of the body height).
+		const rpFooterHeight = this.rpAddRootFooter ? this.rpAddRootFooter.clientHeight || 44 : 0;
+		this.tree.layout(Math.max(0, height - rpFooterHeight), width);
 	}
 
 	protected override renderBody(container: HTMLElement): void {
@@ -307,6 +312,31 @@ export class ExplorerView extends ViewPane implements IExplorerView {
 
 		this.createTree(this.treeContainer);
 
+		// Xpair (C1): below-list "Add Mapping" footer. Appended into the renderBody container
+		// AFTER the tree (later sibling) so it sits ~20px below the folder list (see
+		// .rp-add-root-footer in explorerviewlet.css). Runs the mount-first
+		// `remotepair.browser.addRoot` command (registered in the RemotePair extension). The
+		// icon is Codicon.rootFolderOpened — visibly distinct from Codicon.newFolder (AC6). This
+		// is the single accepted marked core edit per the rebase-safety invariant.
+		container.classList.add('rp-explorer-body');
+		const rpFooter = DOM.append(container, DOM.$('.rp-add-root-footer'));
+		this.rpAddRootFooter = rpFooter;
+		const rpButton = DOM.append(rpFooter, DOM.$('a.rp-add-root-button'));
+		rpButton.setAttribute('role', 'button');
+		rpButton.setAttribute('tabindex', '0');
+		rpButton.title = nls.localize('remotepairAddRootTooltip', "Mount a host folder and add it as a Browser root");
+		const rpIcon = DOM.append(rpButton, DOM.$('span.rp-add-root-icon'));
+		rpIcon.classList.add(...ThemeIcon.asClassNameArray(Codicon.rootFolderOpened));
+		DOM.append(rpButton, DOM.$('span.rp-add-root-label')).textContent = nls.localize('remotepairAddRoot', "Add Mapping");
+		const rpRun = () => this.commandService.executeCommand('remotepair.browser.addRoot');
+		this._register(DOM.addDisposableListener(rpButton, DOM.EventType.CLICK, rpRun));
+		this._register(DOM.addDisposableListener(rpButton, DOM.EventType.KEY_DOWN, (e: KeyboardEvent) => {
+			if (e.key === 'Enter' || e.key === ' ') {
+				e.preventDefault();
+				rpRun();
+			}
+		}));
+
 		this._register(this.labelService.onDidChangeFormatters(() => {
 			this._onDidChangeTitleArea.fire();
 		}));
@@ -629,6 +659,24 @@ export class ExplorerView extends ViewPane implements IExplorerView {
 		}
 	}
 
+	/**
+	 * RemotePair (C4.2): set the Row-2 dynamic root label from the focused explorer item. The label
+	 * is the name of the root that owns the focused resource (IExplorerService.findClosestRoot). It
+	 * is rendered as the ViewPane title description (the `span.description` after the view title), so
+	 * the native new-file/new-folder/refresh/collapse actions stay untouched in Row-2. Cleared when
+	 * nothing is focused or there is only a single root (the label would be redundant with the view
+	 * title). AC16/AC17.
+	 */
+	private rpUpdateRootLabel(stat: ExplorerItem | undefined): void {
+		// With 0/1 roots the root name adds no information beyond the view title — clear the label.
+		if (!stat || this.explorerService.roots.length <= 1) {
+			this.updateTitleDescription(undefined);
+			return;
+		}
+		const root = this.explorerService.findClosestRoot(stat.resource);
+		this.updateTitleDescription(root ? root.name : undefined);
+	}
+
 	private async onContextMenu(e: ITreeContextMenuEvent<ExplorerItem>): Promise<void> {
 		if (DOM.isEditableElement(e.browserEvent.target as HTMLElement)) {
 			return;
@@ -687,6 +735,13 @@ export class ExplorerView extends ViewPane implements IExplorerView {
 		const stat = elements.at(0);
 		this.setContextKeys(stat);
 
+		// RemotePair (C4.2): drive the Row-2 dynamic label from the explorer selection. On every
+		// focus/selection change compute the root that owns the focused resource via
+		// IExplorerService.findClosestRoot and render its name as the view's title description (the
+		// `span.description` next to the view title — ViewPane.updateTitleDescription). When nothing
+		// is focused, or with <=1 root (label adds no info), clear it. AC16/AC17.
+		this.rpUpdateRootLabel(stat);
+
 		if (stat) {
 			const enableTrash = Boolean(this.configurationService.getValue<IFilesConfiguration>().files?.enableTrash);
 			const hasCapability = this.fileService.hasCapability(stat.resource, FileSystemProviderCapabilities.Trash);
diff --git a/src/vs/workbench/contrib/outline/browser/outline.contribution.ts b/src/vs/workbench/contrib/outline/browser/outline.contribution.ts
index bb8dd656..7f83843c 100644
--- a/src/vs/workbench/contrib/outline/browser/outline.contribution.ts
+++ b/src/vs/workbench/contrib/outline/browser/outline.contribution.ts
@@ -14,6 +14,7 @@ import { Codicon } from '../../../../base/common/codicons.js';
 import { registerIcon } from '../../../../platform/theme/common/iconRegistry.js';
 import { OutlineConfigKeys } from '../../../services/outline/browser/outline.js';
 import { IOutlinePane } from './outline.js';
+import { ContextKeyExpr } from '../../../../platform/contextkey/common/contextkey.js';
 
 // --- actions
 
@@ -34,7 +35,10 @@ Registry.as<IViewsRegistry>(ViewExtensions.ViewsRegistry).registerViews([{
 	collapsed: true,
 	order: 2,
 	weight: 30,
-	focusCommand: { id: 'outline.focus' }
+	focusCommand: { id: 'outline.focus' },
+	// RemotePair: hide the Outline view (not needed in the RemotePair Browser sidebar). Code is
+	// kept registered for rebase-safety; only `when` is forced false so it never surfaces.
+	when: ContextKeyExpr.false()
 }], VIEW_CONTAINER);
 
 // --- configurations
diff --git a/src/vs/workbench/contrib/remote/browser/remote.contribution.ts b/src/vs/workbench/contrib/remote/browser/remote.contribution.ts
index 95960da2..72367ab6 100644
--- a/src/vs/workbench/contrib/remote/browser/remote.contribution.ts
+++ b/src/vs/workbench/contrib/remote/browser/remote.contribution.ts
@@ -9,7 +9,9 @@ import { ShowCandidateContribution } from './showCandidate.js';
 import { LifecyclePhase } from '../../../services/lifecycle/common/lifecycle.js';
 import { TunnelFactoryContribution } from './tunnelFactory.js';
 import { RemoteAgentConnectionStatusListener, RemoteMarkers } from './remote.js';
-import { RemoteStatusIndicator } from './remoteIndicator.js';
+// RemotePair: remote status indicator (green "><" bottom-left) removed — replaced
+// by our own host-connect button. Import + registration disabled (see below).
+// import { RemoteStatusIndicator } from './remoteIndicator.js';
 import { AutomaticPortForwarding, ForwardedPortsView, PortRestore } from './remoteExplorer.js';
 import { InitialRemoteConnectionHealthContribution } from './remoteConnectionHealth.js';
 
@@ -17,7 +19,8 @@ const workbenchContributionsRegistry = Registry.as<IWorkbenchContributionsRegist
 registerWorkbenchContribution2(ShowCandidateContribution.ID, ShowCandidateContribution, WorkbenchPhase.BlockRestore);
 registerWorkbenchContribution2(TunnelFactoryContribution.ID, TunnelFactoryContribution, WorkbenchPhase.BlockRestore);
 workbenchContributionsRegistry.registerWorkbenchContribution(RemoteAgentConnectionStatusListener, LifecyclePhase.Eventually);
-registerWorkbenchContribution2(RemoteStatusIndicator.ID, RemoteStatusIndicator, WorkbenchPhase.BlockStartup);
+// RemotePair: RemoteStatusIndicator registration disabled (no green "><" indicator).
+// registerWorkbenchContribution2(RemoteStatusIndicator.ID, RemoteStatusIndicator, WorkbenchPhase.BlockStartup);
 workbenchContributionsRegistry.registerWorkbenchContribution(ForwardedPortsView, LifecyclePhase.Restored);
 workbenchContributionsRegistry.registerWorkbenchContribution(PortRestore, LifecyclePhase.Eventually);
 workbenchContributionsRegistry.registerWorkbenchContribution(AutomaticPortForwarding, LifecyclePhase.Eventually);
diff --git a/src/vs/workbench/contrib/terminal/browser/media/remotePairSessionManager.css b/src/vs/workbench/contrib/terminal/browser/media/remotePairSessionManager.css
new file mode 100644
index 00000000..d5f156ec
--- /dev/null
+++ b/src/vs/workbench/contrib/terminal/browser/media/remotePairSessionManager.css
@@ -0,0 +1,114 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair session tabs: each bottom-panel tab (Attached / Detached / History) renders ONLY
+ *  its own category as a single horizontally-scrolling row of session cards. Since only one
+ *  category shows at a time, the cards are substantial (room to breathe). The panel stays short:
+ *  composite tab bar + one card row fits comfortably in ~90px.
+ *--------------------------------------------------------------------------------------------*/
+
+.monaco-workbench .remotepair-session-manager {
+	display: flex;
+	flex-direction: column;
+	box-sizing: border-box;
+	height: 100%;
+	padding: 8px 12px;
+	overflow: hidden;
+}
+
+/* One horizontally-scrollable row of cards for the active category. */
+.monaco-workbench .remotepair-session-row {
+	display: flex;
+	flex: 1 1 auto;
+	align-items: center;
+	gap: 8px;
+	overflow-x: auto;
+	overflow-y: hidden;
+	padding: 2px 0;
+	/* Thin horizontal scrollbar so the row stays short. */
+	scrollbar-width: thin;
+}
+.monaco-workbench .remotepair-session-row::-webkit-scrollbar { height: 6px; }
+.monaco-workbench .remotepair-session-row::-webkit-scrollbar-thumb { background: var(--vscode-scrollbarSlider-background, rgba(128,128,128,0.4)); border-radius: 3px; }
+
+/* Substantial session cards — only one category shows at a time, so there is room. */
+.monaco-workbench .remotepair-session-card {
+	flex: 0 0 auto;
+	display: flex;
+	align-items: center;
+	box-sizing: border-box;
+	min-width: 120px;
+	max-width: 220px;
+	overflow: hidden;
+	white-space: nowrap;
+	border: 1px solid var(--vscode-widget-border, rgba(128, 128, 128, 0.45));
+	border-radius: 6px;
+	padding: 6px 12px;
+	font-size: 13px;
+	line-height: 18px;
+	cursor: pointer;
+	color: var(--vscode-foreground);
+	background: var(--vscode-editorWidget-background, rgba(128, 128, 128, 0.08));
+	user-select: none;
+}
+
+/* The card label takes the available room and ellipsizes; the close button stays pinned right. */
+.monaco-workbench .remotepair-session-card .remotepair-session-card-label {
+	flex: 1 1 auto;
+	overflow: hidden;
+	text-overflow: ellipsis;
+	white-space: nowrap;
+}
+
+.monaco-workbench .remotepair-session-card:hover,
+.monaco-workbench .remotepair-session-card:focus {
+	background: var(--vscode-toolbar-hoverBackground, rgba(128, 128, 128, 0.2));
+	border-color: var(--vscode-focusBorder, rgba(128, 128, 128, 0.6));
+	outline: none;
+}
+
+/* Active session: the same rounded card, highlighted with a focus-colored outline so the
+ * currently-active session stands out. */
+.monaco-workbench .remotepair-session-card.remotepair-session-card-active {
+	border-color: var(--vscode-focusBorder, rgba(128, 128, 128, 0.6));
+	outline: 1px solid var(--vscode-focusBorder, rgba(128, 128, 128, 0.6));
+	outline-offset: -1px;
+}
+
+/* Close "X": always visible (muted) with a real click target. RemotePair C3(c): the old rule used
+ * opacity:0 + a zero-area bare glyph, so the X was effectively un-clickable ("does nothing"). Give
+ * it padding + a min hit box and a visible-but-muted default; full opacity on hover/active. */
+.monaco-workbench .remotepair-session-card .remotepair-session-card-close {
+	flex: 0 0 auto;
+	margin-left: 6px;
+	font-size: 14px;
+	line-height: 16px;
+	display: flex;
+	align-items: center;
+	justify-content: center;
+	min-width: 18px;
+	min-height: 18px;
+	padding: 2px;
+	border-radius: 4px;
+	opacity: 0.55;
+	cursor: pointer;
+	color: inherit;
+	pointer-events: auto;
+	position: relative;
+}
+.monaco-workbench .remotepair-session-card:hover .remotepair-session-card-close,
+.monaco-workbench .remotepair-session-card:focus .remotepair-session-card-close,
+.monaco-workbench .remotepair-session-card.remotepair-session-card-active .remotepair-session-card-close {
+	opacity: 1;
+}
+.monaco-workbench .remotepair-session-card .remotepair-session-card-close:hover {
+	opacity: 1;
+	background: var(--vscode-toolbar-hoverBackground, rgba(128, 128, 128, 0.2));
+	border-radius: 4px;
+}
+
+.monaco-workbench .remotepair-session-empty {
+	flex: 0 0 auto;
+	font-size: 13px;
+	font-style: italic;
+	opacity: 0.45;
+	user-select: none;
+}
diff --git a/src/vs/workbench/contrib/terminal/browser/media/remotePairSessions.css b/src/vs/workbench/contrib/terminal/browser/media/remotePairSessions.css
new file mode 100644
index 00000000..c226ad4d
--- /dev/null
+++ b/src/vs/workbench/contrib/terminal/browser/media/remotePairSessions.css
@@ -0,0 +1,48 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair Sessions sidebar: custom "+" (New Session) overlay + hide the embedded
+ *  editor-group's default toolbar (new/split/more) which we don't want for sessions.
+ *--------------------------------------------------------------------------------------------*/
+
+.monaco-workbench .remotepair-terminal-sidebar {
+	position: relative;
+}
+
+/* The embedded EditorPart renders its own editor-group action toolbar; for the
+   Sessions sidebar we replace it with our single "+" New Session button. */
+.monaco-workbench .remotepair-terminal-sidebar .editor-actions {
+	display: none !important;
+}
+
+/* "+" New Session button, top-right of the Sessions body. */
+.monaco-workbench .remotepair-sessions-add {
+	position: absolute;
+	top: 2px;
+	right: 8px;
+	z-index: 12;
+	width: 24px;
+	height: 24px;
+	line-height: 22px;
+	text-align: center;
+	font-size: 17px;
+	font-weight: 400;
+	cursor: pointer;
+	border-radius: 5px;
+	color: var(--vscode-icon-foreground, var(--vscode-foreground));
+	user-select: none;
+}
+
+.monaco-workbench .remotepair-sessions-add:hover,
+.monaco-workbench .remotepair-sessions-add:focus {
+	background: var(--vscode-toolbar-hoverBackground, rgba(128, 128, 128, 0.2));
+	outline: none;
+}
+
+/* RemotePair (A-polish): Browser container title bar. Scoped via the Back button — only the Browser
+   container's title has `.codicon-arrow-left` (registered in remotePairBrowserActions.ts). Hide the
+   "BROWSER" text label, center the Files/Search/Extensions menu icons, and pin the Back (<-) button
+   to the right edge — the same corner as the Sessions "+" button. */
+.monaco-workbench .part.sidebar .composite.title:has(a.action-label.codicon-arrow-left) .title-label { display: none !important; }
+.monaco-workbench .part.sidebar .composite.title:has(a.action-label.codicon-arrow-left) { position: relative; }
+.monaco-workbench .part.sidebar .composite.title:has(a.action-label.codicon-arrow-left) .title-actions { flex: 1 1 auto; width: 100%; }
+.monaco-workbench .part.sidebar .composite.title:has(a.action-label.codicon-arrow-left) .title-actions .monaco-action-bar .actions-container { justify-content: center; }
+.monaco-workbench .part.sidebar .composite.title:has(a.action-label.codicon-arrow-left) .action-item:has(a.action-label.codicon-arrow-left) { position: absolute; right: 6px; top: 0; bottom: 0; display: flex; align-items: center; }
diff --git a/src/vs/workbench/contrib/terminal/browser/terminal.contribution.ts b/src/vs/workbench/contrib/terminal/browser/terminal.contribution.ts
index 97485af0..96c21a11 100644
--- a/src/vs/workbench/contrib/terminal/browser/terminal.contribution.ts
+++ b/src/vs/workbench/contrib/terminal/browser/terminal.contribution.ts
@@ -29,6 +29,13 @@ import './media/terminal.css';
 import './media/terminalVoice.css';
 import './media/widgets.css';
 import './media/xterm.css';
+// RemotePair: "Terminal" sidebar container that embeds an EditorPart (editor tabs).
+import './remotePairTerminalSidebar.js';
+import './media/remotePairSessions.css';
+// RemotePair: bottom "Session Manager" panel (Attached/Detached/History rows).
+import './remotePairSessionManager.js';
+import './media/remotePairSessionManager.css';
+import { SessionPickerInput, SessionPickerPane } from './remotePairSessionPicker.js';
 import { RemoteTerminalBackendContribution } from './remoteTerminalBackend.js';
 import { ITerminalConfigurationService, ITerminalEditingService, ITerminalEditorService, ITerminalGroupService, ITerminalInstanceService, ITerminalService, TerminalDataTransfers, terminalEditorId } from './terminal.js';
 import { registerTerminalActions } from './terminalActions.js';
@@ -82,6 +89,17 @@ Registry.as<IEditorPaneRegistry>(EditorExtensions.EditorPane).registerEditorPane
 		new SyncDescriptor(TerminalEditorInput)
 	]
 );
+// RemotePair (G002): in-tab "New Session" picker editor for the Sessions sidebar "+".
+Registry.as<IEditorPaneRegistry>(EditorExtensions.EditorPane).registerEditorPane(
+	EditorPaneDescriptor.create(
+		SessionPickerPane,
+		SessionPickerPane.ID,
+		nls.localize('remotepairSessionPicker', "New Session")
+	),
+	[
+		new SyncDescriptor(SessionPickerInput)
+	]
+);
 Registry.as<IDragAndDropContributionRegistry>(DragAndDropExtensions.DragAndDropContribution).register({
 	dataFormatKey: TerminalDataTransfers.Terminals,
 	getEditorInputs(data) {
@@ -105,6 +123,10 @@ Registry.as<IDragAndDropContributionRegistry>(DragAndDropExtensions.DragAndDropC
 });
 
 // Register views
+// RemotePair: keep the Panel container registered so terminalGroupService / terminalActions /
+// TERMINAL_VIEW_ID references compile and run without errors, but mark it non-default and
+// hideIfEmpty so the native TERMINAL panel tab never auto-appears.  The actual terminal UI lives
+// in our "Sessions" sidebar (remotePairTerminalSidebar.ts / task A).
 const VIEW_CONTAINER = Registry.as<IViewContainersRegistry>(ViewContainerExtensions.ViewContainersRegistry).registerViewContainer({
 	id: TERMINAL_VIEW_ID,
 	title: nls.localize2('terminal', "Terminal"),
@@ -114,7 +136,7 @@ const VIEW_CONTAINER = Registry.as<IViewContainersRegistry>(ViewContainerExtensi
 	hideIfEmpty: true,
 	order: 3,
 	windowEnablement: WindowEnablement.Both
-}, ViewContainerLocation.Panel, { doNotRegisterOpenCommand: true, isDefault: true });
+}, ViewContainerLocation.Panel, { doNotRegisterOpenCommand: true, isDefault: false });
 Registry.as<IViewsRegistry>(ViewContainerExtensions.ViewsRegistry).registerViews([{
 	id: TERMINAL_VIEW_ID,
 	name: nls.localize2('terminal', "Terminal"),
diff --git a/src/vs/workbench/contrib/timeline/browser/timeline.contribution.ts b/src/vs/workbench/contrib/timeline/browser/timeline.contribution.ts
index 64c6b1a5..ee3e4775 100644
--- a/src/vs/workbench/contrib/timeline/browser/timeline.contribution.ts
+++ b/src/vs/workbench/contrib/timeline/browser/timeline.contribution.ts
@@ -36,7 +36,10 @@ export class TimelinePaneDescriptor implements IViewDescriptor {
 	readonly canToggleVisibility = true;
 	readonly hideByDefault = false;
 	readonly canMoveView = true;
-	readonly when = TimelineHasProviderContext;
+	// RemotePair: hide the Timeline view (not needed in the RemotePair Browser sidebar). Code is
+	// kept registered for rebase-safety; only the `when` is forced false so it never surfaces.
+	// Original: ContextKeyExpr.and(... TimelineHasProviderContext) — restore to re-enable.
+	readonly when = ContextKeyExpr.false();
 
 	focusCommand = { id: 'timeline.focus' };
 }
diff --git a/src/vs/workbench/browser/parts/remotePairPrune.ts b/src/vs/workbench/browser/parts/remotePairPrune.ts
new file mode 100644
index 0000000..4fd3c0e
--- /dev/null
+++ b/src/vs/workbench/browser/parts/remotePairPrune.ts
@@ -0,0 +1,62 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair: UI prune allowlists.
+ *
+ *  We hide IDE surfaces the RemotePair user does not need, but we KEEP the underlying code
+ *  registered (rebase-safety) and only filter what the composite bars actually show. This
+ *  mirrors the left-rail allowlist in `activitybar/activitybarPart.ts`
+ *  (`ActivityBarCompositeBar.getViewContainers()` override) and applies the same idea to the
+ *  bottom panel and the secondary side bar (auxiliary bar).
+ *
+ *  Net effect:
+ *   - Bottom panel: native Problems / Output / Debug Console / Terminal / Ports tabs are hidden.
+ *     Only RemotePair-owned containers (e.g. the session-manager added in a later goal) remain.
+ *   - Auxiliary bar: the native CHAT / "Build with Agent" panel is hidden.
+ *--------------------------------------------------------------------------------------------*/
+
+import { ViewContainer } from '../../common/views.js';
+import { rpLog } from '../../contrib/terminal/browser/remotePairLog.js';
+
+/** A view container owned by RemotePair (the only kind we keep in pruned composite bars). */
+function isRemotePairOwned(id: string): boolean {
+	return id.startsWith('remotepair') || id.startsWith('workbench.view.remotepair');
+}
+
+/**
+ * Shared best-effort predicate for the composite-bar allowlists below. Both bars keep ONLY
+ * RemotePair-owned containers, so the decision is identical; `where` identifies the calling bar
+ * for the log line. This is on the composite-bar rebuild hot path, hence DEBUG (gated by the
+ * resolved file threshold, INFO-default per docs/logging.md §4, so it stays silent in practice).
+ *
+ * Best-effort / never-throw: the upstream code path must not break if a malformed container (no
+ * `id`) ever reaches us — the original `id.startsWith(...)` would throw and propagate into the
+ * vendor composite-bar render. We guard, log ERROR once-shaped (per call), and fall back to the
+ * dominant non-RemotePair outcome (deny → filtered out), preserving the "only RemotePair-owned
+ * pass" invariant rather than crashing the bar.
+ */
+function isAllowedInBar(container: ViewContainer, where: string): boolean {
+	const id = container?.id;
+	if (typeof id !== 'string') {
+		// App-level UI event (no session correlation id) -> session omitted -> '-' per §3/§5.
+		rpLog('error', `prune ${where}: container has no string id (got ${typeof id}); filtering out`);
+		return false;
+	}
+	const allowed = isRemotePairOwned(id);
+	rpLog('debug', `prune ${where}: ${allowed ? 'keep' : 'hide'} container '${id}'`);
+	return allowed;
+}
+
+/**
+ * Containers allowed in the bottom panel composite bar. Native panels stay registered but are
+ * filtered out of the bar so they never surface; RemotePair-owned containers pass through.
+ */
+export function isRemotePairPanelContainerAllowed(container: ViewContainer): boolean {
+	return isAllowedInBar(container, 'panel');
+}
+
+/**
+ * Containers allowed in the secondary side bar (auxiliary bar) composite bar. The native CHAT /
+ * "Build with Agent" container is filtered out; RemotePair-owned containers pass through.
+ */
+export function isRemotePairAuxBarContainerAllowed(container: ViewContainer): boolean {
+	return isAllowedInBar(container, 'auxbar');
+}
diff --git a/src/vs/workbench/contrib/files/browser/remotePairBrowserActions.ts b/src/vs/workbench/contrib/files/browser/remotePairBrowserActions.ts
new file mode 100644
index 00000000..6887d7cb
--- /dev/null
+++ b/src/vs/workbench/contrib/files/browser/remotePairBrowserActions.ts
@@ -0,0 +1,452 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair (G005): top-of-Browser actions and per-folder session creation.
+ *
+ *  The "Browser" is the relabeled Explorer view container (VIEWLET_ID). This file owns the
+ *  action contributions so they stay out of core explorer files:
+ *    - Browser title bar: Search / AI Extensions helper (MenuId.ViewContainerTitle). The row-1
+ *      "Add Mapping" action was removed (C1): the sole add entry point is now the
+ *      below-list / empty-state "Add Mapping" affordance which runs the mount-first
+ *      `remotepair.browser.addRoot` command (registered in the RemotePair extension).
+ *    - Empty-state "Add Mapping" button: a viewsWelcome entry on the explorer EmptyView.
+ *    - Per-folder inline "+": creates a Sessions terminal rooted at that folder.
+ *    - Per-folder inline star (C3): toggles the folder in the C2 Favorites store.
+ *    - Per-folder-row hover controls (C3): a star + '+' rendered on EVERY folder row (roots and
+ *      subfolders, never files) via the native IExplorerFileContribution mechanism — the
+ *      explorer file tree wires MenuId.ExplorerContext only to its right-click context menu
+ *      (explorerView.ts:701 showContextMenu), so group:'inline' there does NOT produce inline
+ *      hover icons on tree rows. The renderer instead invokes registered file contributions
+ *      (explorerViewer.ts:896/1015 contr.setResource(uri)) per row, which is the supported
+ *      inline-row hook. The contributions just dispatch the two Action2 commands below.
+ *  All work goes through public services / the existing Sessions helpers; core terminal,
+ *  explorer-tree, and EditorPart behavior is untouched (the file-contribution registry is a
+ *  public extension point — no core file is edited).
+ *--------------------------------------------------------------------------------------------*/
+
+import { localize, localize2 } from '../../../../nls.js';
+import { Action2, registerAction2, MenuId } from '../../../../platform/actions/common/actions.js';
+import { ContextKeyExpr } from '../../../../platform/contextkey/common/contextkey.js';
+import { ServicesAccessor, IInstantiationService } from '../../../../platform/instantiation/common/instantiation.js';
+import { IViewsService } from '../../../services/views/common/viewsService.js';
+import { ICommandService } from '../../../../platform/commands/common/commands.js';
+import { IListService } from '../../../../platform/list/browser/listService.js';
+import { Codicon } from '../../../../base/common/codicons.js';
+import { URI } from '../../../../base/common/uri.js';
+import { Disposable } from '../../../../base/common/lifecycle.js';
+import { $, append, addDisposableListener, EventType } from '../../../../base/browser/dom.js';
+import { IEditorService } from '../../../services/editor/common/editorService.js';
+import { VIEWLET_ID } from '../common/files.js';
+import { ExplorerFolderContext } from '../common/files.js';
+import { getResourceForCommand, IExplorerService } from './files.js';
+import { IExplorerFileContribution, explorerFileContribRegistry } from './explorerFileContrib.js';
+import { favoritesStore } from './remotePairFavorites.js';
+import { routeToExplorer, routeToSearch } from './remotePairBrowserRouter.js'; // RemotePair (C4.3): Explorer<->Search inner router
+import { Registry } from '../../../../platform/registry/common/platform.js';
+import { IViewsRegistry, Extensions as ViewExtensions, ViewContentGroups } from '../../../common/views.js';
+import { EmptyView } from './views/emptyView.js';
+import { WorkbenchStateContext } from '../../../common/contextkeys.js';
+import { rpLog } from '../../terminal/browser/remotePairLog.js'; // RemotePair: unified workbench logger (docs/logging.md §8)
+
+/**
+ * Browser title-bar action: Back → return the primary sidebar to the Sessions container. This is the
+ * counterpart to the Sessions "+" button (which fills the sidebar with this Browser). Rendered
+ * leftmost (order 0) in the Browser title nav cluster — the same affordance position as "+".
+ */
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.backToSessions',
+			title: localize2('remotepairBrowserBack', "Back to Sessions"),
+			icon: Codicon.arrowLeft,
+			f1: false,
+			menu: [{
+				id: MenuId.ViewContainerTitle,
+				when: ContextKeyExpr.equals('viewContainer', VIEWLET_ID),
+				group: 'navigation',
+				order: 0
+			}]
+		});
+	}
+	run(accessor: ServicesAccessor): void {
+		// Counterpart to the Sessions "+": switch the primary sidebar back to the Sessions container
+		// (id = REMOTEPAIR_TERMINAL_SIDEBAR_ID in remotePairTerminalSidebar.ts).
+		rpLog('debug', 'browser title-bar action: back to Sessions');
+		accessor.get(IViewsService).openViewContainer('remotepair.terminalSidebar', true);
+	}
+});
+
+/**
+ * Browser title-bar (Row-1) action: Explorer → route the Browser inner content back to the
+ * Explorer file tree (C4.3 inner router). Stays within the Browser frame — does NOT navigate to a
+ * different viewlet. Ordered before Search so the pair reads "Explorer | Search".
+ */
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.routeExplorer',
+			title: localize2('remotepairBrowserExplorer', "Explorer"),
+			icon: Codicon.files,
+			f1: false,
+			menu: [{
+				id: MenuId.ViewContainerTitle,
+				when: ContextKeyExpr.equals('viewContainer', VIEWLET_ID),
+				group: 'navigation',
+				order: 9
+			}]
+		});
+	}
+	run(accessor: ServicesAccessor): void {
+		// RemotePair (C4.3): inner-route to Explorer (no global-viewlet navigation).
+		rpLog('debug', 'browser title-bar action: route to Explorer');
+		routeToExplorer(accessor);
+	}
+});
+
+/**
+ * Browser title-bar (Row-1) action: Search → route the Browser inner content to the hosted Search
+ * view (C4.3 inner router), swapping it in WITHIN the Browser frame. This replaces the previous
+ * `workbench.action.findInFiles` call, which navigated to the global Search viewlet covering the
+ * sidebar — the AC19 bug. The router keeps the active viewlet = Browser and escalates (logs) rather
+ * than ever falling back to that global navigation.
+ */
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.search',
+			title: localize2('remotepairBrowserSearch', "Search"),
+			icon: Codicon.search,
+			f1: false,
+			menu: [{
+				id: MenuId.ViewContainerTitle,
+				when: ContextKeyExpr.equals('viewContainer', VIEWLET_ID),
+				group: 'navigation',
+				order: 10
+			}]
+		});
+	}
+	run(accessor: ServicesAccessor): void {
+		// RemotePair (C4.3): inner-route to Search (no global-viewlet navigation).
+		rpLog('debug', 'browser title-bar action: route to Search');
+		routeToSearch(accessor);
+	}
+});
+
+/**
+ * Browser title-bar action: Extensions → runs the Xpair-owned AI extension installer helper.
+ * This intentionally does NOT open the native Extensions viewlet, which would leave the Browser /
+ * Sessions path for a stock VSCodium surface.
+ */
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.extensions',
+			title: localize2('remotepairBrowserExtensions', "Extensions"),
+			icon: Codicon.extensions,
+			f1: false,
+			menu: [{
+				id: MenuId.ViewContainerTitle,
+				when: ContextKeyExpr.equals('viewContainer', VIEWLET_ID),
+				group: 'navigation',
+				order: 11
+			}]
+		});
+	}
+	run(accessor: ServicesAccessor): void {
+		rpLog('debug', 'browser title-bar action: install AI extensions helper');
+		accessor.get(ICommandService).executeCommand('remotepair.ensureExtensions');
+	}
+});
+
+/**
+ * Per-folder "New Session Here": on Explorer folders, create a Sessions terminal rooted at
+ * that folder. Contributed to MenuId.ExplorerContext (the file explorer's right-click menu —
+ * the same menu New File/New Folder use); the generic MenuId.ViewItemContext is NOT honored by
+ * the file-tree renderer. Resolves the folder uri via getResourceForCommand and delegates to the
+ * Sessions command (which makes the sidebar visible first, then opens the terminal).
+ */
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.newSessionInFolder',
+			title: localize2('remotepairBrowserNewSessionInFolder', "New Session Here"),
+			icon: Codicon.add,
+			f1: false,
+			menu: [
+				// Right-click context menu entry.
+				{
+					id: MenuId.ExplorerContext,
+					when: ExplorerFolderContext,
+					group: 'navigation',
+					order: 5
+				},
+				// Inline (hover) entry on folder rows. The explorer tree's right-click menu honors
+				// group:'inline', and the C3 per-folder-row contribution below dispatches this same
+				// command so the '+' appears as a hover icon on the row itself.
+				{
+					id: MenuId.ExplorerContext,
+					when: ExplorerFolderContext,
+					group: 'inline',
+					order: 20
+				}
+			]
+		});
+	}
+	run(accessor: ServicesAccessor, resource?: unknown): void {
+		const editorService = accessor.get(IEditorService);
+		const listService = accessor.get(IListService);
+		const uri = getResourceForCommand(resource, editorService, listService);
+		// Log-only: do NOT alter control flow. openSessionInFolder(cwd: URI | undefined) accepts an
+		// undefined cwd by contract, so we still dispatch (best-effort) but flag the unresolved case.
+		if (uri) {
+			rpLog('info', `newSessionInFolder: opening Sessions terminal rooted at ${uri.toString()}`);
+		} else {
+			rpLog('warn', 'newSessionInFolder: could not resolve a folder uri; dispatching with undefined cwd');
+		}
+		accessor.get(ICommandService).executeCommand('remotepair.terminalSidebar.openSessionInFolder', uri);
+	}
+});
+
+/**
+ * C3 — Per-folder "Toggle Favorite" (the star). Resolves the folder uri (getResourceForCommand)
+ * and toggles it in the C2 Favorites store via the module-level accessor (favoritesStore()), so
+ * there is no hard import of the store class and no import cycle. Contributed to the explorer
+ * right-click menu (group:'inline') and dispatched inline by the per-folder-row contribution
+ * below. Folder-only via ExplorerFolderContext (AC13 / DR-6 primary gate).
+ */
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.toggleFavorite',
+			title: localize2('remotepairBrowserToggleFavorite', "Toggle Favorite"),
+			icon: Codicon.star,
+			f1: false,
+			menu: [{
+				id: MenuId.ExplorerContext,
+				when: ExplorerFolderContext,
+				group: 'inline',
+				order: 21
+			}]
+		});
+	}
+	run(accessor: ServicesAccessor, resource?: unknown): void {
+		const editorService = accessor.get(IEditorService);
+		const listService = accessor.get(IListService);
+		const uri = getResourceForCommand(resource, editorService, listService);
+		if (uri) {
+			const store = favoritesStore();
+			if (store) {
+				rpLog('debug', `toggleFavorite: ${uri.toString()}`);
+				store.toggleFavorite(uri);
+			} else {
+				// Store not yet created (eager startup contribution missing); nothing to toggle.
+				rpLog('warn', `toggleFavorite: favorites store unavailable; ignoring ${uri.toString()}`);
+			}
+		} else {
+			rpLog('warn', 'toggleFavorite: could not resolve a folder uri; ignoring');
+		}
+	}
+});
+
+/**
+ * C1 empty-state "Add Mapping" button. When there are no FOLDER_MAPS roots the Browser removes all
+ * workspace roots (extension reconcileBrowserRoots), leaving zero folders → the workbench enters
+ * the 'empty' state and the explorer EmptyView shows. This viewsWelcome entry renders a single
+ * button that runs the mount-first `remotepair.browser.addRoot` command (registered in the
+ * RemotePair extension). It is the same entry point as the populated below-list footer
+ * (explorerView.ts append-hook). The command-link uses the rocket codicon ($(rocket)) so it is
+ * visibly distinct from the native New Folder icon (AC6).
+ *
+ * `when`: gated to the 'empty' workbench state (zero workspace folders, no .code-workspace open) —
+ * the exact state a RemotePair client lands in with zero FOLDER_MAPS roots (the extension manages
+ * roots via updateWorkspaceFolders on a plain window, never a .code-workspace). WITHOUT this gate
+ * the welcome had no `when`, so it was ALWAYS visible and STACKED on top of every native EmptyView
+ * welcome (explorerViewlet.ts:301-373): the viewsWelcome panel renders EVERY descriptor whose
+ * `when` matches (viewPane.ts onDidChangeViewWelcomeContent), so an un-gated entry duplicated
+ * content in the 'workspace'/'folder' states too — exactly where it never belonged (e.g. when a
+ * client HAS folders). Gating to 'empty' confines it to the no-roots Browser and removes the
+ * stacking on the 'workspace'-gated native welcomes (#1/#2, explorerViewlet.ts:301/314). The
+ * remaining native local-empty welcomes (#4/#5, gated on notWorkspace + remoteName=='') are
+ * core-owned and cannot be suppressed from here without editing core; this gate is the maximal
+ * de-stacking achievable in a RemotePair-owned file.
+ */
+Registry.as<IViewsRegistry>(ViewExtensions.ViewsRegistry).registerViewWelcomeContent(EmptyView.ID, {
+	content: localize(
+		{ key: 'remotepairAddRootWelcome', comment: ['Please do not translate the word "command", it is part of our internal syntax which must not change'] },
+		"No mapped folders yet.\n[$(rocket) Add Mapping](command:remotepair.browser.addRoot)\nMounts a host folder and registers it as a Browser mapping."
+	),
+	when: WorkbenchStateContext.isEqualTo('empty'),
+	group: ViewContentGroups.Open,
+	order: 1
+});
+
+/*---------------------------------------------------------------------------------------------
+ *  C3 — Per-folder-row hover controls (star + '+').
+ *
+ *  The native, supported way to put inline controls on EVERY explorer tree row is an
+ *  IExplorerFileContribution: the FilesRenderer instantiates one per row template and calls
+ *  setResource(uri) on every render (explorerViewer.ts:896/1015), including rapid scroll, and
+ *  setResource(undefined) to clear (explorerViewer.ts:924/991). We render a star + '+' anchored
+ *  to the right of the row, revealed on hover via CSS.
+ *
+ *  Folder-ness / DR-6 flicker guard: each row may be a file, a folder, or an unresolved row
+ *  during async model population. We confirm folder-ness with IExplorerService.findClosest(uri)
+ *  and render ONLY when it resolves to a directory. When findClosest(uri) is null (not yet in the
+ *  model) OR resolves to a file, the controls are HIDDEN — re-evaluated on the next setResource.
+ *  This prevents star/'+' flicker on files and unresolved rows during initial paint.
+ *
+ *  The star reflects live favorite state from the C2 store (favoritesStore()): filled star when
+ *  favorited, outline star otherwise; clicking it dispatches remotepair.browser.toggleFavorite and
+ *  the store's onDidChangeFavorites repaints the icon. The '+' dispatches
+ *  remotepair.browser.newSessionInFolder (single open contract). Picking a non-root folder row
+ *  itself also dispatches newSessionInFolder so nested folder selection is not a no-op. Both
+ *  commands resolve the uri via getResourceForCommand(uri) and we pass the row uri explicitly.
+ *--------------------------------------------------------------------------------------------*/
+class RemotePairFolderRowActions extends Disposable implements IExplorerFileContribution {
+
+	private readonly actions: HTMLElement;
+	private readonly starEl: HTMLElement;
+	private readonly addEl: HTMLElement;
+	private currentResource: URI | undefined;
+	// True once we have subscribed to the C2 store's onDidChangeFavorites. The store is created
+	// eagerly at startup, but this row template may (in principle) be constructed before that
+	// contribution runs — in which case favoritesStore() is undefined in the ctor and the
+	// subscription would never be made. We therefore (re)try the subscription lazily in
+	// setResource so the star auto-updates as soon as a store exists.
+	private favoritesSubscribed = false;
+
+	constructor(
+		container: HTMLElement,
+		@IExplorerService private readonly explorerService: IExplorerService,
+		@ICommandService private readonly commandService: ICommandService
+	) {
+		super();
+
+		this.actions = append(container, $('.remotepair-folder-row-actions'));
+		this.actions.style.display = 'none';
+
+		// Base classes only; the filled/outline star codicon class is set by updateStarIcon so the
+		// two glyphs never stack.
+		this.starEl = append(this.actions, $('.remotepair-folder-row-action.codicon'));
+		this.starEl.title = localize('remotepairFolderRowFavorite', "Toggle Favorite");
+		this.starEl.setAttribute('role', 'button');
+
+		this.addEl = append(this.actions, $('.remotepair-folder-row-action.codicon.codicon-' + Codicon.add.id));
+		this.addEl.title = localize('remotepairFolderRowNewSession', "New Session Here");
+		this.addEl.setAttribute('role', 'button');
+
+		// Dispatch on MOUSE_DOWN (not CLICK). The explorer tree changes list focus/selection on the
+		// row's native `click` (listWidget MouseController.onViewPointer), and that focus change
+		// re-renders the row template — which calls our setResource(undefined)/setResource(other),
+		// clearing this.currentResource BEFORE a CLICK handler would read it. Result: on a not-yet
+		// list-focused row the command saw an undefined uri and getResourceForCommand fell through to
+		// the (empty) focus branch, so nothing happened until the row was selected first. mousedown
+		// fires before that focus change, while this.currentResource is still the live row uri, and
+		// stopPropagation here keeps the tree from focusing/re-rendering the row out from under us.
+		// We snapshot the uri locally so the value passed to the command is exactly the row's uri.
+		this._register(addDisposableListener(this.starEl, EventType.MOUSE_DOWN, (e: MouseEvent) => {
+			e.stopPropagation();
+			e.preventDefault();
+			const resource = this.currentResource;
+			if (resource) {
+				rpLog('debug', `folder-row star clicked -> toggleFavorite ${resource.toString()}`);
+				this.commandService.executeCommand('remotepair.browser.toggleFavorite', resource);
+			}
+		}));
+		this._register(addDisposableListener(this.addEl, EventType.MOUSE_DOWN, (e: MouseEvent) => {
+			e.stopPropagation();
+			e.preventDefault();
+			const resource = this.currentResource;
+			if (resource) {
+				rpLog('debug', `folder-row '+' clicked -> newSessionInFolder ${resource.toString()}`);
+				this.commandService.executeCommand('remotepair.browser.newSessionInFolder', resource);
+			}
+		}));
+		// In the Sessions -> Browser picker flow, selecting a nested mapped folder must advance the
+		// flow instead of only leaving the native Explorer row selected. Treat a plain left-click on
+		// non-root folder content as choosing that folder; root rows still keep native expand/select.
+		this._register(addDisposableListener(container, EventType.MOUSE_DOWN, (e: MouseEvent) => {
+			if (e.button !== 0 || e.metaKey || e.ctrlKey || e.altKey || e.shiftKey) {
+				return;
+			}
+			if (e.target instanceof Node && this.actions.contains(e.target)) {
+				return;
+			}
+			const resource = this.currentResource;
+			if (!resource || !this.isNestedFolderResource(resource)) {
+				return;
+			}
+			e.stopPropagation();
+			e.preventDefault();
+			rpLog('debug', `nested folder row picked -> newSessionInFolder ${resource.toString()}`);
+			this.commandService.executeCommand('remotepair.browser.newSessionInFolder', resource);
+		}));
+		// Also swallow the trailing CLICK so a stray click (e.g. synthetic, or if mousedown was
+		// missed) never reaches the tree row and triggers open/expand. No command runs here.
+		this._register(addDisposableListener(this.starEl, EventType.CLICK, (e: MouseEvent) => {
+			e.stopPropagation();
+			e.preventDefault();
+		}));
+		this._register(addDisposableListener(this.addEl, EventType.CLICK, (e: MouseEvent) => {
+			e.stopPropagation();
+			e.preventDefault();
+		}));
+
+		// Keep the star icon in sync with the C2 store while a folder row is shown. Subscribe now if
+		// the store already exists; otherwise setResource will retry (lazy late-binding).
+		this.ensureFavoritesSubscription();
+	}
+
+	/**
+	 * Subscribe (once) to the C2 store's onDidChangeFavorites so the star auto-updates when
+	 * favorites change elsewhere. Idempotent and leak-free: the subscription is registered with
+	 * this row (disposed with it), and the guard prevents re-subscribing on every setResource.
+	 */
+	private ensureFavoritesSubscription(): void {
+		if (this.favoritesSubscribed) {
+			return;
+		}
+		const store = favoritesStore();
+		if (store) {
+			this.favoritesSubscribed = true;
+			this._register(store.onDidChangeFavorites(() => this.updateStarIcon()));
+		}
+	}
+
+	/** Called by the FilesRenderer on every row render (and with undefined to clear). */
+	setResource(resource: URI | undefined): void {
+		// Late-bind the favorites subscription in case the store was not yet created at ctor time.
+		this.ensureFavoritesSubscription();
+		// DR-6: hide on unresolved/cleared rows. Confirm folder-ness via the explorer model;
+		// findClosest === null (not yet populated) OR a non-directory (file) → hide.
+		const item = resource ? this.explorerService.findClosest(resource) : null;
+		if (!resource || !item || !item.isDirectory) {
+			this.currentResource = undefined;
+			this.actions.style.display = 'none';
+			return;
+		}
+		this.currentResource = resource;
+		this.actions.style.display = '';
+		this.updateStarIcon();
+	}
+
+	private updateStarIcon(): void {
+		const favorited = !!this.currentResource && !!favoritesStore()?.isFavorite(this.currentResource);
+		// Toggle between the filled (favorited) and outline (not favorited) star codicons.
+		this.starEl.classList.toggle('codicon-' + Codicon.starFull.id, favorited);
+		this.starEl.classList.toggle('codicon-' + Codicon.starEmpty.id, !favorited);
+		this.starEl.classList.toggle('remotepair-folder-row-action-active', favorited);
+	}
+
+	private isNestedFolderResource(resource: URI): boolean {
+		const root = this.explorerService.findClosestRoot(resource);
+		return !!root && root.resource.toString() !== resource.toString();
+	}
+}
+
+// Register the per-folder-row contribution on the public explorer file-contribution registry
+// (no core explorer file is edited — the FilesRenderer already instantiates registered
+// contributions per row).
+explorerFileContribRegistry.register({
+	create(insta: IInstantiationService, container: HTMLElement): IExplorerFileContribution {
+		return insta.createInstance(RemotePairFolderRowActions, container);
+	}
+});
diff --git a/src/vs/workbench/contrib/files/browser/remotePairBrowserRouter.ts b/src/vs/workbench/contrib/files/browser/remotePairBrowserRouter.ts
new file mode 100644
index 0000000..ba861e2
--- /dev/null
+++ b/src/vs/workbench/contrib/files/browser/remotePairBrowserRouter.ts
@@ -0,0 +1,196 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair (C4.3): Explorer <-> Search inner ROUTER inside the Browser VIEW_CONTAINER.
+ *
+ *  GOAL (AC19/AC20): the Row-1 "Explorer" / "Search" buttons swap the inner content WITHIN the
+ *  Browser frame. The active viewlet/composite stays = Browser the entire time — we do NOT
+ *  navigate to a global Search viewlet (that navigation is the AC19 bug). The Browser 2-row
+ *  header persists across switches; Favorites (C2) stays registered at the bottom.
+ *
+ *  MECHANISM (deviation from the plan's literal wording — see the note below):
+ *    The plan (ADR-1 / Step C4.3) said "relocate the Search VIEW into the Browser container via
+ *    viewDescriptorService.moveViewsToContainer(...) + per-view visibility toggle". Grounding that
+ *    in the actual code surfaced TWO hard blockers that make the literal mechanism unsafe:
+ *      1. moveViewsToContainer() PERSISTS the relocation (viewDescriptorService.ts:411 ->
+ *         saveViewCustomizations -> views.customizations in PROFILE storage). Combined with the
+ *         search container's hideIfEmpty:true, this yanks Search out of its OWN viewlet
+ *         PERMANENTLY across reloads — a real footgun, not a transient state.
+ *      2. Both the Explorer file-tree view (explorerViewlet.ts) and the core Search view
+ *         (search.contribution.ts:67) are registered canToggleVisibility:false, so
+ *         viewContainerModel.setVisible() NO-OPS for them (viewContainerModel.ts:431). Visibility
+ *         routing is dead on arrival unless they are toggleable.
+ *
+ *    Faithful-to-intent fix (Option A spirit: HOST the Search view inside the Browser container,
+ *    keep active viewlet = Browser, no global navigation), but NON-PERSISTENT and without mutating
+ *    any core descriptor:
+ *      - Register a RemotePair-OWNED view descriptor (REMOTEPAIR_SEARCH_VIEW_ID) directly INTO the
+ *        Browser VIEW_CONTAINER whose ctorDescriptor instantiates the native SearchView class
+ *        (search browser/searchView.ts). Because its DEFAULT container is the Browser container,
+ *        viewDescriptorService never writes a customization for it (saveViewCustomizations skips
+ *        views already at their default location, viewDescriptorService.ts:686) — nothing persists,
+ *        and the native global Search viewlet is left fully intact.
+ *      - canToggleVisibility:true on OUR descriptor + the C4.3 edit making the Explorer file-tree
+ *        view canToggleVisibility:true (explorerViewlet.ts) let the router flip per-view visibility.
+ *      - Explorer C4.1 already disabled the single-view merge, so hiding either view never collapses
+ *        the 2-row header.
+ *
+ *  ROUTER:
+ *      showExplorer(): show Explorer file-tree view, hide the hosted Search view.
+ *      showSearch():   show the hosted Search view, hide the Explorer file-tree view.
+ *    Both via IViewDescriptorService.getViewContainerModel(BROWSER_CONTAINER).setVisible(id, vis).
+ *
+ *  DEFENSIVE / ESCALATION (mandated): every operation that could fail to resolve the container or
+ *  the view is wrapped. On failure we LOG a clear escalation marker and DO NOTHING further — we
+ *  NEVER silently fall back to opening the global Search viewlet (that would be the AC19 bug).
+ *
+ *  RUNTIME UNCERTAINTY (flagged for the orchestrator's CDP spike — NOT verifiable by tsc):
+ *    SearchView consumes a SINGLETON search model (ISearchViewModelWorkbenchService.searchModel,
+ *    searchView.ts:297) and binds GLOBAL search context keys in its ctor (searchView.ts:249-265).
+ *    Hosting a SECOND SearchView instance (ours, under a new id) alongside the native one MAY:
+ *      (a) share/contend on the one search model, and
+ *      (b) double-bind global search context keys / mis-target search keybindings bound to the
+ *          native VIEW_ID 'workbench.view.search'.
+ *    This is the #1 thing to validate at runtime via CDP. If the hosted Search view fails to
+ *    render or the singleton coupling breaks behavior, ESCALATE for redesign (e.g. host only the
+ *    Search WIDGET, or gate the native instance) — do NOT fall back to the global viewlet.
+ *--------------------------------------------------------------------------------------------*/
+
+import { localize2 } from '../../../../nls.js';
+import { SyncDescriptor } from '../../../../platform/instantiation/common/descriptors.js';
+import { ServicesAccessor } from '../../../../platform/instantiation/common/instantiation.js';
+import { IFileService } from '../../../../platform/files/common/files.js';
+import { ILogService } from '../../../../platform/log/common/log.js';
+import { Registry } from '../../../../platform/registry/common/platform.js';
+import { IViewDescriptor, IViewDescriptorService, IViewsRegistry, Extensions as ViewContainerExtensions } from '../../../common/views.js';
+import { IViewsService } from '../../../services/views/common/viewsService.js';
+import { IPathService } from '../../../services/path/common/pathService.js';
+import { SearchView } from '../../search/browser/searchView.js';
+import { VIEW_ID as EXPLORER_FILE_VIEW_ID } from '../common/files.js';
+import { VIEW_CONTAINER } from './explorerViewlet.js';
+import { configureRemotePairLog, rpLog } from '../../terminal/browser/remotePairLog.js';
+
+/** Distinct id (NOT the native 'workbench.view.search') for the Browser-hosted Search instance. */
+export const REMOTEPAIR_SEARCH_VIEW_ID = 'remotepair.browser.search';
+
+const RP_ROUTER_LOG_PREFIX = 'RemotePair C4:';
+const RP_ROUTER_ESCALATION = 'RemotePair C4: Search relocation failed — needs redesign';
+
+/**
+ * Wire the shared workbench logger (remotePairLog.ts) from the per-call ServicesAccessor so every
+ * router line is emitted in the unified docs/logging.md §3 format and best-effort mirrored to
+ * ~/.xpair/host/logs/workbench.log. Best-effort and never-throw: ILogService is required (resolve
+ * defensively), the file-mirror sinks are optional and degrade to ILogService-only when absent.
+ * configureRemotePairLog() is idempotent (last call wins, same renderer singletons), so calling it
+ * on each route is a cheap re-bind — it never changes control flow.
+ */
+function ensureRpLog(accessor: ServicesAccessor): void {
+	try {
+		const logService = accessor.get(ILogService);
+		let fileService: IFileService | undefined;
+		let pathService: IPathService | undefined;
+		try { fileService = accessor.get(IFileService); } catch { /* optional mirror sink */ }
+		try { pathService = accessor.get(IPathService); } catch { /* optional mirror sink */ }
+		configureRemotePairLog({ logService, fileService, pathService });
+	} catch {
+		// If even ILogService can't be resolved, rpLog stays a safe no-op; never throw into the UI.
+	}
+}
+
+// --- hosted Search view descriptor (registered into the Browser container, non-persistent) ------
+
+/** localize2 first arg MUST be a static string literal. */
+const remotePairSearchViewDescriptor: IViewDescriptor = {
+	id: REMOTEPAIR_SEARCH_VIEW_ID,
+	name: localize2('remotepairBrowserSearch', "Search"),
+	// Host the native SearchView class under our own id, directly in the Browser container, so the
+	// global Search viewlet is untouched and no view-location customization is persisted.
+	ctorDescriptor: new SyncDescriptor(SearchView),
+	// Order between the file tree (order:1) and Favorites (order:100). It is hidden by default and
+	// only revealed by the router when the user clicks Row-1 "Search".
+	order: 2,
+	canToggleVisibility: true,
+	canMoveView: false,
+	hideByDefault: true,
+	collapsed: false,
+};
+
+try {
+	Registry.as<IViewsRegistry>(ViewContainerExtensions.ViewsRegistry).registerViews([remotePairSearchViewDescriptor], VIEW_CONTAINER);
+} catch (err) {
+	// Registration failure must not crash workbench startup; the router below degrades to a no-op
+	// (showSearch logs the escalation marker). Do NOT fall back to the global viewlet.
+	// eslint-disable-next-line no-console
+	console.error(`${RP_ROUTER_ESCALATION} (descriptor registration threw)`, err);
+}
+
+// --- router ----------------------------------------------------------------------------------
+
+/**
+ * Toggle per-view visibility inside the Browser container. Resolves the Browser container + its
+ * model defensively; on any failure logs the escalation marker and returns false (the caller then
+ * does nothing — never navigates to the global viewlet).
+ */
+function setBrowserViewVisible(accessor: ServicesAccessor, viewId: string, visible: boolean): boolean {
+	const viewDescriptorService = accessor.get(IViewDescriptorService);
+	ensureRpLog(accessor);
+	try {
+		const container = viewDescriptorService.getViewContainerById(VIEW_CONTAINER.id);
+		if (!container) {
+			rpLog('error', `${RP_ROUTER_ESCALATION} (Browser container ${VIEW_CONTAINER.id} not resolved)`);
+			return false;
+		}
+		const model = viewDescriptorService.getViewContainerModel(container);
+		if (!model) {
+			rpLog('error', `${RP_ROUTER_ESCALATION} (no view container model for ${VIEW_CONTAINER.id})`);
+			return false;
+		}
+		if (!model.activeViewDescriptors.some(v => v.id === viewId)) {
+			// The target view is not active in the container — for the hosted Search view this means
+			// its registration failed above. Escalate; never silently open the global viewlet.
+			rpLog('error', `${RP_ROUTER_ESCALATION} (view ${viewId} not active in Browser container)`);
+			return false;
+		}
+		model.setVisible(viewId, visible);
+		return true;
+	} catch (err) {
+		rpLog('error', `${RP_ROUTER_ESCALATION} (setVisible ${viewId}=${visible} threw): ${err instanceof Error ? err.message : String(err)}`);
+		return false;
+	}
+}
+
+/**
+ * Route to the Explorer file tree: show Explorer, hide the hosted Search view. Keeps the active
+ * viewlet = Browser (we only flip inner view visibility; we never call openViewContainer for a
+ * different viewlet). Ensures the Browser container itself is open/focused.
+ */
+export function routeToExplorer(accessor: ServicesAccessor): void {
+	const viewsService = accessor.get(IViewsService);
+	ensureRpLog(accessor);
+	rpLog('trace', `${RP_ROUTER_LOG_PREFIX} routeToExplorer`);
+	// Hide Search first, then show Explorer (order avoids a transient 0-visible-views flicker).
+	setBrowserViewVisible(accessor, REMOTEPAIR_SEARCH_VIEW_ID, false);
+	const shown = setBrowserViewVisible(accessor, EXPLORER_FILE_VIEW_ID, true);
+	if (shown) {
+		// Reveal the Explorer view inside the (already-active) Browser container — openView focuses
+		// the view WITHOUT switching the active viewlet away from Browser.
+		viewsService.openView(EXPLORER_FILE_VIEW_ID, true).catch(err => rpLog('error', `${RP_ROUTER_LOG_PREFIX} openView(explorer) failed: ${err instanceof Error ? err.message : String(err)}`));
+	}
+}
+
+/**
+ * Route to Search: show the hosted Search view, hide the Explorer file tree. Active viewlet stays
+ * = Browser. On any failure the escalation marker is logged and we do NOTHING (no global-viewlet
+ * fallback — that is the AC19 bug we exist to avoid).
+ */
+export function routeToSearch(accessor: ServicesAccessor): void {
+	const viewsService = accessor.get(IViewsService);
+	ensureRpLog(accessor);
+	rpLog('trace', `${RP_ROUTER_LOG_PREFIX} routeToSearch`);
+	const shown = setBrowserViewVisible(accessor, REMOTEPAIR_SEARCH_VIEW_ID, true);
+	if (!shown) {
+		// Escalation already logged by setBrowserViewVisible. Do NOT open the global Search viewlet.
+		return;
+	}
+	// Only hide Explorer once Search is confirmed visible, so the container is never 0-visible.
+	setBrowserViewVisible(accessor, EXPLORER_FILE_VIEW_ID, false);
+	viewsService.openView(REMOTEPAIR_SEARCH_VIEW_ID, true).catch(err => rpLog('error', `${RP_ROUTER_LOG_PREFIX} openView(search) failed: ${err instanceof Error ? err.message : String(err)}`));
+}
diff --git a/src/vs/workbench/contrib/files/browser/remotePairFavorites.ts b/src/vs/workbench/contrib/files/browser/remotePairFavorites.ts
new file mode 100644
index 0000000..515cbd0
--- /dev/null
+++ b/src/vs/workbench/contrib/files/browser/remotePairFavorites.ts
@@ -0,0 +1,375 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair (C2): Favorites store + Favorites view at the bottom of the Browser container.
+ *
+ *  The "Browser" is the relabeled Explorer view container (VIEWLET_ID / VIEW_CONTAINER). This
+ *  file owns:
+ *    - RemotePairFavoritesStore: a tiny store persisting favorite folder URIs via
+ *      IStorageService. Reads = union of WORKSPACE + PROFILE (global) scopes so favorites
+ *      survive across workspaces; writes update BOTH scopes (default write through workspace,
+ *      mirrored to global). Exposes onDidChangeFavorites + a small API.
+ *    - A module-level accessor (setFavoritesStore / favoritesStore) mirroring the
+ *      setSessionLauncher / setAttachedSessionsProvider injection pattern in the terminal
+ *      sidebar, so C3's per-folder star can call toggleFavorite/isFavorite without a hard
+ *      import cycle.
+ *    - RemotePairFavoritesView (extends ViewPane): renders the current Browser-mapped favorites
+ *      as a simple list. Clicking a row or its inline '+' starts a new Sessions terminal rooted at
+ *      that folder via the existing `remotepair.terminalSidebar.openSessionInFolder` command
+ *      (single open contract — never creates terminals directly). Each row also has an inline
+ *      remove (X). Persisted favorites outside current mapped roots stay stored but are hidden so
+ *      Favorites never becomes a second folder-mapping source.
+ *    - Registration of the Favorites view into the Browser VIEW_CONTAINER, ordered AFTER the
+ *      file tree (mirroring how Outline/Timeline register as sibling views).
+ *
+ *  All logic stays in this RemotePair-owned file (rebase-safety); the only touch elsewhere is
+ *  the single import from files.contribution.ts so this module loads.
+ *--------------------------------------------------------------------------------------------*/
+
+import { localize, localize2 } from '../../../../nls.js';
+import { $, append, clearNode, addDisposableListener, EventType } from '../../../../base/browser/dom.js';
+import { StandardKeyboardEvent } from '../../../../base/browser/keyboardEvent.js';
+import { KeyCode } from '../../../../base/common/keyCodes.js';
+import { Emitter, Event } from '../../../../base/common/event.js';
+import { Disposable, DisposableStore } from '../../../../base/common/lifecycle.js';
+import { URI } from '../../../../base/common/uri.js';
+import { basename } from '../../../../base/common/resources.js';
+import { Codicon } from '../../../../base/common/codicons.js';
+import { SyncDescriptor } from '../../../../platform/instantiation/common/descriptors.js';
+import { IInstantiationService } from '../../../../platform/instantiation/common/instantiation.js';
+import { IThemeService } from '../../../../platform/theme/common/themeService.js';
+import { IConfigurationService } from '../../../../platform/configuration/common/configuration.js';
+import { IStorageService, StorageScope, StorageTarget } from '../../../../platform/storage/common/storage.js';
+import { IContextKeyService } from '../../../../platform/contextkey/common/contextkey.js';
+import { IKeybindingService } from '../../../../platform/keybinding/common/keybinding.js';
+import { IContextMenuService } from '../../../../platform/contextview/browser/contextView.js';
+import { IOpenerService } from '../../../../platform/opener/common/opener.js';
+import { IHoverService } from '../../../../platform/hover/browser/hover.js';
+import { ICommandService } from '../../../../platform/commands/common/commands.js';
+import { IWorkspaceContextService } from '../../../../platform/workspace/common/workspace.js';
+import { IUriIdentityService } from '../../../../platform/uriIdentity/common/uriIdentity.js';
+import { Registry } from '../../../../platform/registry/common/platform.js';
+import { ViewPane, IViewPaneOptions } from '../../../browser/parts/views/viewPane.js';
+import { IViewDescriptor, IViewDescriptorService, IViewsRegistry, Extensions as ViewContainerExtensions } from '../../../common/views.js';
+import { registerWorkbenchContribution2, WorkbenchPhase } from '../../../common/contributions.js';
+import { VIEW_CONTAINER } from './explorerViewlet.js';
+// RemotePair workbench logger (docs/logging.md §8). Sibling contrib dir: files/browser -> terminal/browser.
+// rpLog is a safe no-op until configureRemotePairLog() is wired by a consumer; never throws.
+import { rpLog } from '../../terminal/browser/remotePairLog.js';
+
+// --- store ------------------------------------------------------------------
+
+const FAVORITES_STORAGE_KEY = 'remotepair.browser.favorites';
+export const REMOTEPAIR_FAVORITES_VIEW_ID = 'remotepair.browser.favorites';
+
+/**
+ * Persists favorite folder URIs via IStorageService across BOTH the WORKSPACE and PROFILE
+ * (global) scopes. `getFavorites()` returns the de-duplicated union of both scopes so a folder
+ * favorited in any workspace is visible everywhere; writes update both scopes so the entry both
+ * survives reload in this workspace and follows the user across workspaces.
+ */
+export class RemotePairFavoritesStore extends Disposable {
+
+	private readonly _onDidChangeFavorites = this._register(new Emitter<void>());
+	readonly onDidChangeFavorites: Event<void> = this._onDidChangeFavorites.event;
+
+	// >0 while THIS store is performing its own multi-scope write (add/removeFavorite). storage's
+	// onDidChangeValue fires synchronously on same-window writes, so without this guard a single
+	// toggle that writes WORKSPACE+PROFILE would fire onDidChangeFavorites up to 3x (one per scope
+	// write + one explicit). We suppress the storage-listener fires during our own writes and emit
+	// exactly once at the end; cross-window storage changes (flag === 0) still fire normally.
+	private _selfWriteDepth = 0;
+
+	constructor(private readonly storageService: IStorageService) {
+		super();
+		// React to writes made in another window/scope so the view stays in sync.
+		// RemotePair: `this._store` is the protected DisposableStore inherited from Disposable
+		// (lifecycle.ts) — the same store this._register() adds to. It is the required 3rd
+		// `disposable` arg of onDidChangeValue (it scopes the Event.filter wrapper); the listener
+		// subscription itself is scoped via this._register(...). This matches the canonical
+		// onDidChangeValue usage elsewhere in the workbench (e.g. agentSessionsFilter.ts).
+		const onStorageChange = () => { if (this._selfWriteDepth === 0) { this._onDidChangeFavorites.fire(); } };
+		this._register(this.storageService.onDidChangeValue(StorageScope.WORKSPACE, FAVORITES_STORAGE_KEY, this._store)(onStorageChange));
+		this._register(this.storageService.onDidChangeValue(StorageScope.PROFILE, FAVORITES_STORAGE_KEY, this._store)(onStorageChange));
+	}
+
+	private readScope(scope: StorageScope.WORKSPACE | StorageScope.PROFILE): string[] {
+		const raw = this.storageService.get(FAVORITES_STORAGE_KEY, scope);
+		if (!raw) {
+			return [];
+		}
+		try {
+			const parsed = JSON.parse(raw);
+			return Array.isArray(parsed) ? parsed.filter((s): s is string => typeof s === 'string') : [];
+		} catch (err) {
+			// Corrupt persisted JSON for this scope -> the whole scope's favorites are unreadable.
+			// Best-effort: log and recover as empty (never throw; the store stays usable).
+			rpLog('error', `favorites: failed to parse persisted ${scope === StorageScope.WORKSPACE ? 'workspace' : 'profile'}-scope favorites; treating as empty: ${err instanceof Error ? err.message : String(err)}`);
+			return [];
+		}
+	}
+
+	private writeScope(scope: StorageScope.WORKSPACE | StorageScope.PROFILE, list: string[]): void {
+		this.storageService.store(FAVORITES_STORAGE_KEY, JSON.stringify(list), scope, StorageTarget.USER);
+	}
+
+	/** De-duplicated union of WORKSPACE + PROFILE favorites, in stable (workspace-first) order. */
+	getFavorites(): URI[] {
+		const seen = new Set<string>();
+		const out: URI[] = [];
+		for (const s of [...this.readScope(StorageScope.WORKSPACE), ...this.readScope(StorageScope.PROFILE)]) {
+			if (seen.has(s)) {
+				continue;
+			}
+			seen.add(s);
+			try {
+				out.push(URI.parse(s));
+			} catch (err) {
+				// Skip a single malformed persisted entry (best-effort; the rest still load).
+				rpLog('warn', `favorites: skipping malformed persisted favorite entry: ${err instanceof Error ? err.message : String(err)}`);
+			}
+		}
+		return out;
+	}
+
+	isFavorite(uri: URI): boolean {
+		const key = uri.toString();
+		return this.getFavorites().some(f => f.toString() === key);
+	}
+
+	addFavorite(uri: URI): void {
+		const key = uri.toString();
+		let changed = false;
+		// Suppress the synchronous per-scope storage-change fires; emit once below (see _selfWriteDepth).
+		this._selfWriteDepth++;
+		try {
+			for (const scope of [StorageScope.WORKSPACE, StorageScope.PROFILE] as const) {
+				const list = this.readScope(scope);
+				if (!list.includes(key)) {
+					list.push(key);
+					this.writeScope(scope, list);
+					changed = true;
+				}
+			}
+		} finally {
+			this._selfWriteDepth--;
+		}
+		if (changed) {
+			this._onDidChangeFavorites.fire();
+		}
+	}
+
+	removeFavorite(uri: URI): void {
+		const key = uri.toString();
+		let changed = false;
+		// Suppress the synchronous per-scope storage-change fires; emit once below (see _selfWriteDepth).
+		this._selfWriteDepth++;
+		try {
+			for (const scope of [StorageScope.WORKSPACE, StorageScope.PROFILE] as const) {
+				const list = this.readScope(scope);
+				const next = list.filter(s => s !== key);
+				if (next.length !== list.length) {
+					this.writeScope(scope, next);
+					changed = true;
+				}
+			}
+		} finally {
+			this._selfWriteDepth--;
+		}
+		if (changed) {
+			this._onDidChangeFavorites.fire();
+		}
+	}
+
+	toggleFavorite(uri: URI): void {
+		if (this.isFavorite(uri)) {
+			this.removeFavorite(uri);
+		} else {
+			this.addFavorite(uri);
+		}
+	}
+}
+
+// --- module-level accessor (mirrors setSessionLauncher / setAttachedSessionsProvider) --------
+
+let _store: RemotePairFavoritesStore | undefined;
+
+/** Register the singleton store (set eagerly by the workbench contribution below). */
+export function setFavoritesStore(store: RemotePairFavoritesStore): void { _store = store; }
+
+/** Accessor for C3's per-folder star (toggleFavorite / isFavorite) — no hard import cycle. */
+export function favoritesStore(): RemotePairFavoritesStore | undefined { return _store; }
+
+// --- eager store lifecycle --------------------------------------------------
+//
+// The store USED to be created in RemotePairFavoritesView's constructor. But that view is
+// registered with `collapsed: true` and is constructed lazily (only when first rendered/expanded),
+// so on a session where the user never opens Favorites, favoritesStore() stayed undefined — making
+// the C3 per-folder star a silent no-op (toggleFavorite()?. short-circuits) and the icon never
+// reflecting favorite state. We now create the store EAGERLY in a tiny IWorkbenchContribution at
+// startup and publish it via setFavoritesStore; the Favorites view USES this singleton rather than
+// owning it. Both the star and the view share one store instance.
+
+/**
+ * Eagerly creates the singleton RemotePairFavoritesStore at workbench startup and publishes it via
+ * setFavoritesStore, so C3's per-folder star can toggle/read favorites even if the (collapsed)
+ * Favorites view is never opened. Registered with registerWorkbenchContribution2 below.
+ */
+export class RemotePairFavoritesStoreContribution extends Disposable {
+	static readonly ID = 'workbench.contrib.remotePairFavoritesStore';
+
+	constructor(@IStorageService storageService: IStorageService) {
+		super();
+		setFavoritesStore(this._register(new RemotePairFavoritesStore(storageService)));
+		// Lifecycle: eager singleton store published at BlockStartup so C3's per-folder star works
+		// even if the (collapsed) Favorites view is never opened.
+		rpLog('info', 'favorites: store published at startup (eager singleton)');
+	}
+}
+
+// --- view -------------------------------------------------------------------
+
+/**
+ * Favorites ViewPane: renders favorite folders as a simple list. Each row is label + an inline
+ * '+' (start a new Sessions terminal rooted there) + an inline remove (X / unfavorite). Clicking
+ * the row itself also starts a session. Sessions are opened ONLY through the existing
+ * `remotepair.terminalSidebar.openSessionInFolder` command (single open contract).
+ */
+export class RemotePairFavoritesView extends ViewPane {
+
+	private list: HTMLElement | undefined;
+	private readonly store: RemotePairFavoritesStore;
+	// Per-render row listener store, cleared at the top of each render so listeners do not
+	// accumulate across re-renders.
+	private readonly rowDisposables = this._register(new DisposableStore());
+
+	constructor(
+		options: IViewPaneOptions,
+		@IKeybindingService keybindingService: IKeybindingService,
+		@IContextMenuService contextMenuService: IContextMenuService,
+		@IConfigurationService configurationService: IConfigurationService,
+		@IContextKeyService contextKeyService: IContextKeyService,
+		@IViewDescriptorService viewDescriptorService: IViewDescriptorService,
+		@IInstantiationService instantiationService: IInstantiationService,
+		@IOpenerService openerService: IOpenerService,
+		@IThemeService themeService: IThemeService,
+		@IHoverService hoverService: IHoverService,
+		@IStorageService storageService: IStorageService,
+		@IWorkspaceContextService private readonly workspaceContextService: IWorkspaceContextService,
+		@IUriIdentityService private readonly uriIdentityService: IUriIdentityService,
+		@ICommandService private readonly commandService: ICommandService
+	) {
+		super(options, keybindingService, contextMenuService, configurationService, contextKeyService, viewDescriptorService, instantiationService, openerService, themeService, hoverService);
+		// USE the eagerly-created singleton store (RemotePairFavoritesStoreContribution) so the view
+		// and C3's per-folder star share one instance. Fall back to creating + publishing one only if
+		// the contribution somehow has not run yet (defensive; should not happen in practice).
+		let store = favoritesStore();
+		if (!store) {
+			store = this._register(new RemotePairFavoritesStore(storageService));
+			setFavoritesStore(store);
+		}
+		this.store = store;
+	}
+
+	protected override renderBody(container: HTMLElement): void {
+		super.renderBody(container);
+		container.classList.add('remotepair-favorites');
+		this.list = append(container, $('.remotepair-favorites-list'));
+		this._register(this.store.onDidChangeFavorites(() => this.refresh()));
+		this._register(this.workspaceContextService.onDidChangeWorkspaceFolders(() => this.refresh()));
+		this.refresh();
+	}
+
+	private getMappedFavorites(): URI[] {
+		const roots = this.workspaceContextService.getWorkspace().folders.map(folder => folder.uri);
+		if (roots.length === 0) {
+			return [];
+		}
+		return this.store.getFavorites().filter(uri => roots.some(root => this.uriIdentityService.extUri.isEqualOrParent(uri, root)));
+	}
+
+	private refresh(): void {
+		const list = this.list;
+		if (!list) {
+			return;
+		}
+		this.rowDisposables.clear();
+		clearNode(list);
+
+		const favorites = this.getMappedFavorites();
+		if (favorites.length === 0) {
+			const empty = append(list, $('.remotepair-favorites-empty'));
+			empty.textContent = localize('remotepairFavoritesEmpty', "Star a mapped folder to add it here.");
+			return;
+		}
+
+		for (const uri of favorites) {
+			this.addRow(uri);
+		}
+	}
+
+	/** Append one favorite row: label + inline '+' (new session) + inline remove (X). */
+	private addRow(uri: URI): void {
+		const list = this.list!;
+		const row = append(list, $('.remotepair-favorites-row'));
+		const text = basename(uri) || uri.fsPath || uri.toString();
+		row.title = uri.fsPath || uri.toString();
+		row.setAttribute('role', 'button');
+		row.setAttribute('tabindex', '0');
+
+		const label = append(row, $('.remotepair-favorites-label'));
+		label.textContent = text;
+
+		const actions = append(row, $('.remotepair-favorites-actions'));
+
+		const add = append(actions, $('.remotepair-favorites-action.codicon.codicon-' + Codicon.add.id));
+		add.title = localize('remotepairFavoritesNewSession', "New Session Here");
+		add.setAttribute('role', 'button');
+		add.setAttribute('tabindex', '0');
+
+		const remove = append(actions, $('.remotepair-favorites-action.codicon.codicon-' + Codicon.close.id));
+		remove.title = localize('remotepairFavoritesRemove', "Remove from Favorites");
+		remove.setAttribute('role', 'button');
+		remove.setAttribute('tabindex', '0');
+
+		const openSession = () => this.commandService.executeCommand('remotepair.terminalSidebar.openSessionInFolder', uri);
+
+		// Clicking the row or '+' starts a new Sessions terminal rooted at this folder.
+		this.rowDisposables.add(addDisposableListener(row, EventType.CLICK, openSession));
+		this.rowDisposables.add(addDisposableListener(row, EventType.KEY_DOWN, (e) => {
+			const k = new StandardKeyboardEvent(e as KeyboardEvent);
+			if (k.equals(KeyCode.Enter) || k.equals(KeyCode.Space)) { k.preventDefault(); openSession(); }
+		}));
+		// '+' opens a new session via the same single open contract.
+		this.rowDisposables.add(addDisposableListener(add, EventType.CLICK, (e) => {
+			e.stopPropagation();
+			openSession();
+		}));
+		// 'X' unfavorites; stopPropagation so it does not also fire the row's open-session click.
+		this.rowDisposables.add(addDisposableListener(remove, EventType.CLICK, (e) => {
+			e.stopPropagation();
+			this.store.removeFavorite(uri);
+		}));
+	}
+}
+
+// --- registration: Favorites view at the bottom of the Browser VIEW_CONTAINER ----------------
+
+const viewsRegistry = Registry.as<IViewsRegistry>(ViewContainerExtensions.ViewsRegistry);
+
+/** localize2 first arg MUST be a static string literal (dynamic NLS keys break the prod build). */
+const favoritesViewDescriptor: IViewDescriptor = {
+	id: REMOTEPAIR_FAVORITES_VIEW_ID,
+	name: localize2('remotepairFavorites', "Favorites"),
+	ctorDescriptor: new SyncDescriptor(RemotePairFavoritesView),
+	// Order AFTER the file tree (VIEW_ID order:1, OpenEditors order:0) so it sits at the bottom
+	// like Outline/Timeline.
+	order: 100,
+	canToggleVisibility: true,
+	canMoveView: true,
+	collapsed: true,
+};
+
+viewsRegistry.registerViews([favoritesViewDescriptor], VIEW_CONTAINER);
+
+// Create the Favorites store eagerly at startup so the C3 per-folder star works even when the
+// collapsed Favorites view is never opened (BlockStartup: before any view is constructed).
+registerWorkbenchContribution2(RemotePairFavoritesStoreContribution.ID, RemotePairFavoritesStoreContribution, WorkbenchPhase.BlockStartup);
diff --git a/src/vs/workbench/contrib/terminal/browser/remotePairLog.ts b/src/vs/workbench/contrib/terminal/browser/remotePairLog.ts
new file mode 100644
index 0000000..f57f54f
--- /dev/null
+++ b/src/vs/workbench/contrib/terminal/browser/remotePairLog.ts
@@ -0,0 +1,219 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair workbench logger (comp tag `workbench`).
+ *
+ *  This is the workbench (RENDERER process) logger named in docs/logging.md §8:
+ *    | workbench | client/ide/.../remotePairLog.ts | ILogService primary + mirror appender to
+ *      workbench.log using the SAME formatter |
+ *
+ *  It produces the unified RemotePair log line (docs/logging.md §3):
+ *
+ *      [<ISO-8601 ts>] [<LEVEL>] [workbench] [<session ?? '-'>] <message>
+ *
+ *  and routes it to ILogService.{trace,debug,info,warn,error} (the same DI service used by
+ *  remotePairBrowserRouter.ts), which persists to the workbench's user-data-dir/logs/.
+ *
+ *  ~/.xpair/host/logs/workbench.log MIRROR — FEASIBLE (and implemented here).
+ *  ---------------------------------------------------------------------------
+ *  The renderer is sandboxed and has NO direct Node `fs` (and this file deliberately imports
+ *  none). HOWEVER, the desktop workbench renderer registers an IPC-backed DiskFileSystemProvider
+ *  for the `file://` scheme (workbench/electron-browser/desktop.main.ts:259-260 ->
+ *  workbench/services/files/electron-browser/diskFileSystemProvider.ts, which proxies disk ops to
+ *  the main/utility process). That means IFileService can write ARBITRARY `file://` paths from the
+ *  renderer WITHOUT Node fs in this layer. So we mirror the exact same formatted line to
+ *  ~/.xpair/host/logs/workbench.log via IFileService:
+ *    - home dir resolved from IPathService.userHome({ preferLocal: true }) (a local file:// URI on
+ *      desktop), then joined to .xpair/host/logs/workbench.log;
+ *    - the logs dir is created with IFileService.createFolder (idempotent `mkdir -p`);
+ *    - each record is appended with writeFile(uri, buf, { append: true }) when the provider has the
+ *      FileAppend capability, else falls back to read+concat+write.
+ *
+ *  GRACEFUL DEGRADATION (the limitation, scoped): the mirror is BEST-EFFORT and OPT-IN. If
+ *  IFileService / IPathService are not supplied (e.g. a non-desktop/web build where no local
+ *  file:// disk provider is registered, or a caller that only wires ILogService), this logger
+ *  falls back to ILogService-ONLY — which still persists to user-data-dir/logs/. The mirror also
+ *  silently no-ops (never throws into caller UI code) if the file:// provider is absent, lacks
+ *  capabilities, or any write fails; failures are reported once to ILogService. We do NOT implement
+ *  §7 rotation/flock here: ILogService owns rotation for its own sink, and the renderer cannot take
+ *  the cross-process advisory lock that §7 requires for the multi-writer files — the
+ *  ~/.xpair/host/workbench.log mirror is single-writer (one renderer per window) and unbounded
+ *  growth is bounded in practice by INFO-default volume; hardening (size-check rotate) is a
+ *  follow-up if this file proves hot.
+ *--------------------------------------------------------------------------------------------*/
+
+import { VSBuffer } from '../../../../base/common/buffer.js';
+import { URI } from '../../../../base/common/uri.js';
+import { FileSystemProviderCapabilities, IFileService } from '../../../../platform/files/common/files.js';
+import { ILogService } from '../../../../platform/log/common/log.js';
+import { IPathService } from '../../../services/path/common/pathService.js';
+
+/** docs/logging.md §3 comp tag for the workbench renderer. */
+const COMP = 'workbench';
+
+export type RemotePairLogLevel = 'trace' | 'debug' | 'info' | 'warn' | 'error';
+
+/**
+ * Optional sinks for the workbench logger. `logService` is required for any output. The mirror to
+ * ~/.xpair/host/logs/workbench.log only runs when BOTH `fileService` and `pathService` are
+ * supplied AND a local file:// disk provider is available (desktop). Wire these from a consumer's
+ * DI constructor, e.g.:
+ *
+ *     constructor(..., @ILogService logService: ILogService,
+ *                      @IFileService fileService: IFileService,
+ *                      @IPathService pathService: IPathService) {
+ *         configureRemotePairLog({ logService, fileService, pathService });
+ *     }
+ */
+export interface IRemotePairLogSinks {
+	readonly logService: ILogService;
+	readonly fileService?: IFileService;
+	readonly pathService?: IPathService;
+}
+
+let sinks: IRemotePairLogSinks | undefined;
+
+/** Memoized ~/.xpair/host/logs/workbench.log file URI + its parent dir (resolved on first use). */
+let mirrorFileUri: URI | undefined;
+let mirrorDirUri: URI | undefined;
+/** Serialize mirror writes so concurrent rpLog() calls append in order and don't race create/write. */
+let mirrorChain: Promise<void> = Promise.resolve();
+/** Latches so we report a given mirror failure class to ILogService at most once (avoid log spam). */
+let mirrorDirEnsured = false;
+let mirrorReportedUnavailable = false;
+let mirrorReportedWriteError = false;
+
+/**
+ * Install the logger's sinks. Idempotent and safe to call from multiple consumer constructors; the
+ * last call wins (all consumers in one renderer share the same ILogService/IFileService singletons,
+ * so this is effectively a no-op re-bind). Without this, rpLog() is a safe no-op.
+ */
+export function configureRemotePairLog(next: IRemotePairLogSinks): void {
+	sinks = next;
+}
+
+/** Local-tz ISO-8601 with offset, second precision — matches docs/logging.md §3 (e.g. 2026-06-15T10:45:16+0900). */
+function isoTimestamp(d: Date = new Date()): string {
+	const pad = (n: number, w = 2) => String(Math.floor(Math.abs(n))).padStart(w, '0');
+	const offMin = -d.getTimezoneOffset(); // getTimezoneOffset is minutes BEHIND UTC, so negate.
+	const sign = offMin >= 0 ? '+' : '-';
+	return (
+		`${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}` +
+		`T${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}` +
+		`${sign}${pad(offMin / 60)}${pad(offMin % 60)}`
+	);
+}
+
+/** Build the canonical unified line (docs/logging.md §3). Single logical line: collapse newlines. */
+function formatLine(level: RemotePairLogLevel, msg: string, session: string | undefined): string {
+	const sess = session && session.length > 0 ? session : '-';
+	const oneLine = msg.replace(/\r?\n/g, ' ');
+	return `[${isoTimestamp()}] [${level.toUpperCase()}] [${COMP}] [${sess}] ${oneLine}`;
+}
+
+/**
+ * Primary RemotePair workbench log entry point.
+ *
+ *   rpLog('info', 'session attached', 'proj-main@1718430000')
+ *
+ * Routes the unified-format line to ILogService at the matching level, and best-effort mirrors the
+ * identical line to ~/.xpair/host/logs/workbench.log when the file sinks are wired. Safe no-op if
+ * configureRemotePairLog() has not been called. Never throws.
+ */
+export function rpLog(level: RemotePairLogLevel, msg: string, session?: string): void {
+	const s = sinks;
+	if (!s) {
+		return;
+	}
+	const line = formatLine(level, msg, session);
+
+	// 1) ILogService primary sink. Level threshold/rotation are owned by ILogService.
+	try {
+		switch (level) {
+			case 'trace': s.logService.trace(line); break;
+			case 'debug': s.logService.debug(line); break;
+			case 'info': s.logService.info(line); break;
+			case 'warn': s.logService.warn(line); break;
+			case 'error': s.logService.error(line); break;
+		}
+	} catch {
+		// ILogService should never throw; swallow to protect caller UI code regardless.
+	}
+
+	// 2) Best-effort ~/.xpair/host/logs/workbench.log mirror (desktop only; opt-in via sinks).
+	if (s.fileService && s.pathService) {
+		void mirrorAppend(s.logService, s.fileService, s.pathService, line);
+	}
+}
+
+/** Resolve (and memoize) the ~/.xpair/host/logs dir + workbench.log file URIs from userHome. */
+function resolveMirrorUris(pathService: IPathService): { dir: URI; file: URI } | undefined {
+	if (mirrorFileUri && mirrorDirUri) {
+		return { dir: mirrorDirUri, file: mirrorFileUri };
+	}
+	// preferLocal:true returns the LOCAL file:// home synchronously on desktop (NativePathService).
+	const home = pathService.userHome({ preferLocal: true });
+	if (!home || home.scheme !== 'file') {
+		return undefined; // No local file home (e.g. web/remote) -> mirror not applicable.
+	}
+	mirrorDirUri = URI.joinPath(home, '.xpair/host', 'logs');
+	mirrorFileUri = URI.joinPath(mirrorDirUri, 'workbench.log');
+	return { dir: mirrorDirUri, file: mirrorFileUri };
+}
+
+/**
+ * Append one line to the mirror file. Chained so calls serialize. Best-effort: any failure is
+ * reported to ILogService at most once and otherwise swallowed (the ILogService sink already has
+ * the record, so the mirror missing is non-fatal).
+ */
+function mirrorAppend(logService: ILogService, fileService: IFileService, pathService: IPathService, line: string): Promise<void> {
+	mirrorChain = mirrorChain.then(async () => {
+		const uris = resolveMirrorUris(pathService);
+		if (!uris) {
+			return;
+		}
+		const { dir, file } = uris;
+
+		// Require a local file:// disk provider in this layer (present on desktop renderer; absent
+		// on web/sandbox-without-IPC). Report once, then give up on the mirror permanently.
+		if (!fileService.hasProvider(file)) {
+			if (!mirrorReportedUnavailable) {
+				mirrorReportedUnavailable = true;
+				logService.info(`[${COMP}] ~/.xpair/host/logs/workbench.log mirror unavailable (no file:// provider); ILogService-only`);
+			}
+			return;
+		}
+
+		try {
+			// Idempotent mkdir -p of the logs dir (once per session unless it later vanishes).
+			if (!mirrorDirEnsured) {
+				if (!(await fileService.exists(dir))) {
+					await fileService.createFolder(dir);
+				}
+				mirrorDirEnsured = true;
+			}
+
+			const payload = VSBuffer.fromString(line + '\n');
+			if (fileService.hasCapability(file, FileSystemProviderCapabilities.FileAppend)) {
+				// Atomic-ish append (single provider write) when supported.
+				await fileService.writeFile(file, payload, { append: true });
+			} else {
+				// Portable fallback: read existing (if any), concat, overwrite.
+				let existing: VSBuffer | undefined;
+				if (await fileService.exists(file)) {
+					existing = (await fileService.readFile(file)).value;
+				}
+				const combined = existing ? VSBuffer.concat([existing, payload]) : payload;
+				await fileService.writeFile(file, combined);
+			}
+		} catch (err) {
+			// Allow re-ensuring the dir on a later attempt (it may have been transiently absent).
+			mirrorDirEnsured = false;
+			if (!mirrorReportedWriteError) {
+				mirrorReportedWriteError = true;
+				logService.warn(`[${COMP}] ~/.xpair/host/logs/workbench.log mirror write failed; ILogService-only`, err);
+			}
+		}
+	}).catch(() => {
+		// Never let the chain reject (would drop subsequent appends); already reported above.
+	});
+	return mirrorChain;
+}
diff --git a/src/vs/workbench/contrib/terminal/browser/remotePairSessionManager.ts b/src/vs/workbench/contrib/terminal/browser/remotePairSessionManager.ts
new file mode 100644
index 0000000..6e5d586
--- /dev/null
+++ b/src/vs/workbench/contrib/terminal/browser/remotePairSessionManager.ts
@@ -0,0 +1,689 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair: bottom-panel session tabs. Three Panel view containers — Attached / Detached /
+ *  History — each surfaced as a horizontal TAB in the bottom-panel composite bar (exactly like
+ *  the native Problems / Output / Terminal tabs). Selecting a tab switches the shown content.
+ *  Each tab renders ONLY its own category as a single horizontally-scrollable row of cards.
+ *
+ *  - Attached cards come from the Sessions sidebar's embedded EditorPart (sourced via
+ *    `setAttachedSessionsProvider`). The sidebar tracks its own terminals because they are
+ *    created through the low-level ITerminalInstanceService path (S0 reverse-leak isolation)
+ *    and therefore never appear in ITerminalService.instances. ITerminalService.instances is
+ *    kept as a fallback when no provider is registered (dev reload, future uses).
+ *  - Detached / History cards come from a pluggable `SessionDataProvider` (default empty),
+ *    so the renderer never has to child_process. Click a card → reattach the session into
+ *    the Sessions sidebar via the `setSessionReattacher` module hook.
+ *
+ *  Hosting-layer only — it never touches core terminal behavior.
+ *--------------------------------------------------------------------------------------------*/
+
+import { localize, localize2 } from '../../../../nls.js';
+import { $, append, clearNode, addDisposableListener, EventType } from '../../../../base/browser/dom.js';
+import { StandardKeyboardEvent } from '../../../../base/browser/keyboardEvent.js';
+import { KeyCode, KeyMod } from '../../../../base/common/keyCodes.js';
+import { Emitter, Event } from '../../../../base/common/event.js';
+import { ICommandService } from '../../../../platform/commands/common/commands.js';
+import { KeybindingsRegistry, KeybindingWeight } from '../../../../platform/keybinding/common/keybindingsRegistry.js';
+import { DisposableStore } from '../../../../base/common/lifecycle.js';
+import { SyncDescriptor } from '../../../../platform/instantiation/common/descriptors.js';
+import { IInstantiationService, ServicesAccessor } from '../../../../platform/instantiation/common/instantiation.js';
+import { IThemeService } from '../../../../platform/theme/common/themeService.js';
+import { IConfigurationService } from '../../../../platform/configuration/common/configuration.js';
+import { IStorageService, StorageScope, StorageTarget } from '../../../../platform/storage/common/storage.js';
+import { IContextKeyService } from '../../../../platform/contextkey/common/contextkey.js';
+import { IKeybindingService } from '../../../../platform/keybinding/common/keybinding.js';
+import { IContextMenuService } from '../../../../platform/contextview/browser/contextView.js';
+import { IOpenerService } from '../../../../platform/opener/common/opener.js';
+import { IHoverService } from '../../../../platform/hover/browser/hover.js';
+import { Registry } from '../../../../platform/registry/common/platform.js';
+import { ViewPane, IViewPaneOptions } from '../../../browser/parts/views/viewPane.js';
+import { IViewDescriptor, IViewDescriptorService, IViewsRegistry, IViewContainersRegistry, Extensions as ViewContainerExtensions, ViewContainerLocation } from '../../../common/views.js';
+import { ViewPaneContainer } from '../../../browser/parts/views/viewPaneContainer.js';
+import { ITerminalService } from './terminal.js';
+import { ITerminalInstance } from './terminal.js';
+import { rpLog } from './remotePairLog.js';
+
+// Container ids MUST start with `remotepair` so they pass the G003 panel allowlist
+// (isRemotePairPanelContainerAllowed in remotePairPrune.ts → id.startsWith('remotepair')).
+export const REMOTEPAIR_SESSIONS_ATTACHED_ID = 'remotepair.sessions.attached';
+export const REMOTEPAIR_SESSIONS_DETACHED_ID = 'remotepair.sessions.detached';
+export const REMOTEPAIR_SESSIONS_HISTORY_ID = 'remotepair.sessions.history';
+
+/** Which session category a `RemotePairSessionCategoryView` renders. */
+export type SessionCategory = 'attached' | 'detached' | 'history';
+
+// --- module hooks -----------------------------------------------------------
+
+/**
+ * A single entry in the Attached tab: a terminal the Sessions sidebar currently hosts.
+ * The id is `instance.resource.toString()` so reveal can look it up in the sidebar map.
+ */
+export interface AttachedSessionInfo {
+	id: string;
+	title: string;
+}
+
+/**
+ * Implemented by `RemotePairTerminalSidebarView` and registered via
+ * `setAttachedSessionsProvider`. Provides the Attached tab's data and the reveal action.
+ * Separate from `ITerminalService.instances` because the sidebar creates terminals through
+ * the low-level ITerminalInstanceService path (S0 isolation) that bypasses the service.
+ */
+export interface AttachedSessionsProvider {
+	getAttached(): readonly AttachedSessionInfo[];
+	reveal(id: string): boolean;
+	/** Close (dispose) the attached session identified by `id`. Optional → default no-op. */
+	close?(id: string): void;
+	/** The id of the currently-active attached session, if known. Optional → default undefined. */
+	getActiveId?(): string | undefined;
+}
+
+const attachedChangeEmitter = new Emitter<void>();
+/** Fires whenever the sidebar's set of attached sessions changes (open / close / rename). */
+export const onDidChangeAttachedSessions: Event<void> = attachedChangeEmitter.event;
+
+let attachedProvider: AttachedSessionsProvider | undefined;
+export function setAttachedSessionsProvider(p: AttachedSessionsProvider): void {
+	attachedProvider = p;
+	attachedChangeEmitter.fire();
+}
+/** Called by the sidebar whenever its attached set changes; triggers an Attached-tab refresh. */
+export function notifyAttachedSessionsChanged(): void { attachedChangeEmitter.fire(); }
+
+/**
+ * Provides the Detached/History session-name lists for the manager. The core renderer
+ * cannot child_process (`xpair ls`), so the extension-host command path owns CLI access.
+ * Defaults to empty (dev: no host) → tabs render an empty state.
+ */
+export interface SessionDataProvider {
+	/** Host/local sessions that exist but are NOT currently attached. */
+	getDetached(): readonly string[];
+	/** Previously-seen session names that are no longer live. */
+	getHistory(): readonly string[];
+}
+
+let dataProvider: SessionDataProvider | undefined;
+const sessionDataChangeEmitter = new Emitter<void>();
+export const onDidChangeSessionData: Event<void> = sessionDataChangeEmitter.event;
+export function setSessionDataProvider(provider: SessionDataProvider): void { dataProvider = provider; sessionDataChangeEmitter.fire(); }
+
+interface SessionListEntry {
+	readonly name: string;
+	readonly attached: number;
+}
+
+let liveSessionCache: readonly SessionListEntry[] = [];
+let sessionRefreshInFlight = false;
+let sessionRefreshTimer: ReturnType<typeof setTimeout> | undefined;
+let sessionListUnavailable = false;
+let sessionCardCause: string | undefined;
+const SESSION_REFRESH_RETRY_MS = 1000;
+const SESSION_REFRESH_INTERVAL_MS = 10000;
+const SESSION_NAME_RE = /^[A-Za-z0-9_.-]+$/;
+
+function isRecord(value: unknown): value is Record<string, unknown> {
+	return typeof value === 'object' && value !== null;
+}
+
+function normalizedAttached(value: unknown): number {
+	const n = typeof value === 'number' ? value : Number(value);
+	return Number.isFinite(n) && n > 0 ? Math.trunc(n) : 0;
+}
+
+function normalizeSessionCommandResult(value: unknown): readonly SessionListEntry[] {
+	if (!isRecord(value) || !Array.isArray(value.sessions)) {
+		return [];
+	}
+	const sessions: SessionListEntry[] = [];
+	for (const raw of value.sessions) {
+		if (!isRecord(raw) || typeof raw.name !== 'string') {
+			continue;
+		}
+		const name = raw.name.trim();
+		if (!SESSION_NAME_RE.test(name)) {
+			continue;
+		}
+		sessions.push({ name, attached: normalizedAttached(raw.attached) });
+	}
+	return sessions;
+}
+
+function commandResultUnavailable(value: unknown): boolean {
+	return isRecord(value) && value.unavailable === true;
+}
+
+function commandResultCause(value: unknown): string | undefined {
+	if (!isRecord(value) || typeof value.cause !== 'string') {
+		return undefined;
+	}
+	const cause = value.cause.trim();
+	return cause || undefined;
+}
+
+export function setSessionCardCause(cause: string | undefined): void {
+	if (sessionCardCause === cause) {
+		return;
+	}
+	sessionCardCause = cause;
+	sessionDataChangeEmitter.fire();
+}
+
+function cachedDetachedSessions(): readonly string[] {
+	return liveSessionCache.filter(s => s.attached === 0).map(s => s.name);
+}
+
+function cachedHistorySessions(): readonly string[] {
+	return liveSessionCache.filter(s => s.attached > 0).map(s => s.name);
+}
+
+function cachedSessionNames(): readonly string[] {
+	return liveSessionCache.map(s => s.name);
+}
+
+function sameSessionCache(a: readonly SessionListEntry[], b: readonly SessionListEntry[]): boolean {
+	return a.length === b.length && a.every((entry, index) => entry.name === b[index]?.name && entry.attached === b[index]?.attached);
+}
+
+function scheduleSessionRefresh(commandService: ICommandService, delayMs: number): void {
+	if (sessionRefreshTimer !== undefined) {
+		return;
+	}
+	sessionRefreshTimer = setTimeout(() => {
+		sessionRefreshTimer = undefined;
+		refreshSessionData(commandService);
+	}, delayMs);
+}
+
+function refreshSessionData(commandService: ICommandService): void {
+	if (sessionRefreshInFlight) {
+		return;
+	}
+	if (sessionRefreshTimer !== undefined) {
+		clearTimeout(sessionRefreshTimer);
+		sessionRefreshTimer = undefined;
+	}
+	sessionRefreshInFlight = true;
+	commandService.executeCommand('remotepair.sessions.listJson').then((value) => {
+		sessionRefreshInFlight = false;
+		const nextUnavailable = commandResultUnavailable(value);
+		const unavailableChanged = sessionListUnavailable !== nextUnavailable;
+		sessionListUnavailable = nextUnavailable;
+		const next = normalizeSessionCommandResult(value);
+		if (unavailableChanged || !sameSessionCache(liveSessionCache, next)) {
+			liveSessionCache = next;
+			sessionDataChangeEmitter.fire();
+		}
+		scheduleSessionRefresh(commandService, SESSION_REFRESH_INTERVAL_MS);
+	}, (error) => {
+		sessionRefreshInFlight = false;
+		sessionListUnavailable = true;
+		liveSessionCache = [];
+		rpLog('warn', `session list refresh failed: ${error instanceof Error ? error.message : String(error)}`);
+		sessionDataChangeEmitter.fire();
+		scheduleSessionRefresh(commandService, SESSION_REFRESH_RETRY_MS);
+	});
+}
+
+/** Reattach a session by name into the Sessions sidebar (wired by the sidebar view). */
+export type SessionReattacher = (name: string) => void;
+
+let reattacher: SessionReattacher | undefined;
+export function setSessionReattacher(fn: SessionReattacher): void { reattacher = fn; }
+
+function reattach(name: string): void {
+	if (!SESSION_NAME_RE.test(name)) {
+		rpLog('warn', `reattach rejected invalid session name: ${name}`);
+		return;
+	}
+	if (!reattacher) {
+		rpLog('warn', `reattach requested but no reattacher wired (host layer not registered); ignoring: ${name}`);
+		return;
+	}
+	reattacher(name);
+}
+
+function applySessionCheckResult(value: unknown): string | undefined {
+	const nextUnavailable = commandResultUnavailable(value);
+	const unavailableChanged = sessionListUnavailable !== nextUnavailable;
+	sessionListUnavailable = nextUnavailable;
+	const next = normalizeSessionCommandResult(value);
+	const cacheChanged = !sameSessionCache(liveSessionCache, next);
+	const cause = commandResultCause(value);
+	const causeChanged = sessionCardCause !== cause;
+	if (cacheChanged) {
+		liveSessionCache = next;
+	}
+	if (causeChanged) {
+		sessionCardCause = cause;
+	}
+	if (unavailableChanged || cacheChanged || causeChanged) {
+		sessionDataChangeEmitter.fire();
+	}
+	return cause;
+}
+
+function validateAndReattach(name: string, commandService: ICommandService): void {
+	if (!SESSION_NAME_RE.test(name)) {
+		setSessionCardCause(localize('remotepairInvalidSessionCard', "This session card is invalid. The list was refreshed; choose another session or start a new one."));
+		refreshSessionData(commandService);
+		return;
+	}
+	if (!reattacher) {
+		setSessionCardCause(localize('remotepairSessionAttachUnavailable', "Session attach is not ready yet. Reopen Sessions or retry in a moment."));
+		refreshSessionData(commandService);
+		return;
+	}
+	commandService.executeCommand('remotepair.sessions.checkAttach', name).then((value) => {
+		const cause = applySessionCheckResult(value);
+		if (isRecord(value) && value.ok === true) {
+			setSessionCardCause(undefined);
+			reattach(name);
+			return;
+		}
+		setSessionCardCause(cause ?? localize('remotepairSessionAttachCheckFailed', "Session is no longer available. The list was refreshed; choose another session or start a new one."));
+		scheduleSessionRefresh(commandService, SESSION_REFRESH_RETRY_MS);
+	}, (error) => {
+		rpLog('warn', `session attach check failed for ${name}: ${error instanceof Error ? error.message : String(error)}`);
+		setSessionCardCause(localize('remotepairSessionStatusUnavailable', "Session status unavailable. Check the host connection, then retry."));
+		refreshSessionData(commandService);
+	});
+}
+
+// --- view -------------------------------------------------------------------
+
+const HISTORY_STORAGE_KEY = 'remotepair.sessionManager.history.v2';
+
+/**
+ * Shared Panel ViewPane rendering ONE session category (attached / detached / history) as a
+ * single horizontally-scrollable row of cards. The category is supplied per-container by a thin
+ * subclass (`category` getter), so the same body logic backs all three bottom-panel tabs.
+ */
+export abstract class RemotePairSessionCategoryView extends ViewPane {
+
+	/** The category this view renders. Supplied by the concrete subclass per container. */
+	protected abstract get category(): SessionCategory;
+
+	private row: HTMLElement | undefined;
+
+	// Per-render card listener store, cleared at the top of each render so click/keydown
+	// listeners do not accumulate across the (frequent) re-renders.
+	private readonly cardDisposables = this._register(new DisposableStore());
+
+	constructor(
+		options: IViewPaneOptions,
+		@IKeybindingService keybindingService: IKeybindingService,
+		@IContextMenuService contextMenuService: IContextMenuService,
+		@IConfigurationService configurationService: IConfigurationService,
+		@IContextKeyService contextKeyService: IContextKeyService,
+		@IViewDescriptorService viewDescriptorService: IViewDescriptorService,
+		@IInstantiationService instantiationService: IInstantiationService,
+		@IOpenerService openerService: IOpenerService,
+		@IThemeService themeService: IThemeService,
+		@IHoverService hoverService: IHoverService,
+		@ITerminalService private readonly terminalService: ITerminalService,
+		@IStorageService private readonly storageService: IStorageService,
+		@ICommandService private readonly commandService: ICommandService
+	) {
+		super(options, keybindingService, contextMenuService, configurationService, contextKeyService, viewDescriptorService, instantiationService, openerService, themeService, hoverService);
+	}
+
+	protected override renderBody(container: HTMLElement): void {
+		super.renderBody(container);
+
+		container.classList.add('remotepair-session-manager');
+		this.row = append(container, $('.remotepair-session-row'));
+
+		if (this.category === 'attached') {
+			// Primary: refresh when the Sessions sidebar's attached-sessions set changes.
+			this._register(onDidChangeAttachedSessions(() => this.refresh()));
+			// Fallback: also react to ITerminalService.instances for any other terminal sources.
+			this._register(this.terminalService.onDidChangeInstances(() => this.refresh()));
+			this._register(onDidChangeSessionData(() => this.refresh()));
+			this._register(onDidChangeAttachedSessions(() => refreshSessionData(this.commandService)));
+			refreshSessionData(this.commandService);
+		} else {
+			this._register(onDidChangeSessionData(() => this.refresh()));
+			this._register(onDidChangeAttachedSessions(() => refreshSessionData(this.commandService)));
+			refreshSessionData(this.commandService);
+		}
+
+		this.refresh();
+	}
+
+	private refresh(): void {
+		switch (this.category) {
+			case 'attached': this.renderAttached(); return;
+			case 'detached': this.renderDetached(); return;
+			case 'history': this.renderHistory(); return;
+		}
+	}
+
+	/** Append a clickable tab to the row. Each tab is a label span + a native-style close "X".
+	 *  Listeners go on `cardDisposables` (cleared per render). When `active` is set the tab gets
+	 *  the `.remotepair-session-card-active` highlight; when `onClose` is set the X disposes it. */
+	private addCard(text: string, onClick: () => void, opts?: { active?: boolean; onClose?: () => void }): void {
+		const row = this.row!;
+		const card = append(row, $('.remotepair-session-card'));
+		if (opts?.active) {
+			card.classList.add('remotepair-session-card-active');
+		}
+		card.title = text;
+		card.setAttribute('role', 'button');
+		card.setAttribute('tabindex', '0');
+		const label = append(card, $('.remotepair-session-card-label'));
+		label.textContent = text;
+		this.cardDisposables.add(addDisposableListener(card, EventType.CLICK, onClick));
+		this.cardDisposables.add(addDisposableListener(card, EventType.KEY_DOWN, (e) => {
+			const k = new StandardKeyboardEvent(e as KeyboardEvent);
+			if (k.equals(KeyCode.Enter) || k.equals(KeyCode.Space)) { k.preventDefault(); onClick(); }
+		}));
+		const onClose = this.cardCloseHandler(text, opts);
+		if (onClose) {
+			const close = append(card, $('.remotepair-session-card-close.codicon.codicon-close'));
+			close.title = this.cardCloseTitle();
+			close.setAttribute('role', 'button');
+			close.tabIndex = 0;
+			this.cardDisposables.add(addDisposableListener(close, EventType.MOUSE_DOWN, (e) => {
+				e.preventDefault();
+				e.stopPropagation();
+			}));
+			this.cardDisposables.add(addDisposableListener(close, EventType.CLICK, (e) => {
+				e.preventDefault();
+				e.stopPropagation();
+				onClose();
+			}));
+			this.cardDisposables.add(addDisposableListener(close, EventType.KEY_DOWN, (e) => {
+				const k = new StandardKeyboardEvent(e as KeyboardEvent);
+				if (k.equals(KeyCode.Enter) || k.equals(KeyCode.Space)) {
+					k.preventDefault();
+					e.stopPropagation();
+					onClose();
+				}
+			}));
+		}
+	}
+
+	/** Render an empty-state message in the row. */
+	private addEmpty(text: string): void {
+		const empty = append(this.row!, $('.remotepair-session-empty'));
+		empty.textContent = text;
+	}
+
+	private renderAttached(): void {
+		const row = this.row;
+		if (!row) {
+			return;
+		}
+		clearNode(row);
+		this.cardDisposables.clear();
+		const cause = sessionCardCause;
+
+		if (attachedProvider) {
+			// Primary source: the Sessions sidebar tracks its own terminals (S0 isolation means
+			// those instances never appear in ITerminalService.instances).
+			const provider = attachedProvider;
+			const sessions = provider.getAttached();
+			this.recordHistory(this.reattachableHistoryNames());
+			if (sessions.length === 0) {
+				this.addEmpty(cause ?? localize('remotepairNoAttached', "No attached sessions"));
+				return;
+			}
+			if (cause) {
+				this.addEmpty(cause);
+			}
+			const activeId = provider.getActiveId?.();
+			for (const session of sessions) {
+				this.addCard(session.title, () => {
+					if (provider.reveal(session.id) === false) {
+						setSessionCardCause(localize('remotepairAttachedSessionGone', "That attached terminal is gone. The Attached list was refreshed."));
+					} else {
+						setSessionCardCause(undefined);
+					}
+				}, {
+					active: !!activeId && session.id === activeId,
+					onClose: provider.close ? () => provider.close!(session.id) : undefined,
+				});
+			}
+		} else {
+			// Fallback: read from ITerminalService for any other terminal sources.
+			const instances = this.terminalService.instances;
+			this.recordHistory(this.reattachableHistoryNames());
+			if (instances.length === 0) {
+				this.addEmpty(cause ?? localize('remotepairNoAttached', "No attached sessions"));
+				return;
+			}
+			if (cause) {
+				this.addEmpty(cause);
+			}
+			const active = this.terminalService.activeInstance;
+			for (const instance of instances) {
+				this.addCard(instance.title, () => this.focusInstance(instance), {
+					active: !!active && instance === active,
+					onClose: () => { try { instance.dispose(); } catch (e) { rpLog('warn', `attached fallback dispose failed: ${e instanceof Error ? e.message : String(e)}`); /* best-effort */ } },
+				});
+			}
+		}
+	}
+
+	private renderDetached(): void {
+		const row = this.row;
+		if (!row) {
+			return;
+		}
+		clearNode(row);
+		this.cardDisposables.clear();
+		if (sessionListUnavailable) {
+			this.addEmpty(sessionCardCause ?? localize('remotepairSessionListUnavailable', "Session list unavailable; retrying..."));
+			return;
+		}
+		const names = dataProvider?.getDetached() ?? cachedDetachedSessions();
+		this.recordHistory(names);
+		if (names.length === 0) {
+			this.addEmpty(sessionCardCause ?? localize('remotepairNoDetached', "No detached sessions"));
+			return;
+		}
+		if (sessionCardCause) {
+			this.addEmpty(sessionCardCause);
+		}
+		for (const name of names) {
+			this.addCard(name, () => validateAndReattach(name, this.commandService));
+		}
+	}
+
+	private renderHistory(): void {
+		const row = this.row;
+		if (!row) {
+			return;
+		}
+		clearNode(row);
+		this.cardDisposables.clear();
+		if (sessionListUnavailable) {
+			this.addEmpty(sessionCardCause ?? localize('remotepairSessionListUnavailable', "Session list unavailable; retrying..."));
+			return;
+		}
+		// Live provider names are real, reattachable session names → clickable.
+		const providerNames = dataProvider?.getHistory() ?? cachedHistorySessions();
+		this.recordHistory(providerNames);
+		if (providerNames && providerNames.length > 0) {
+			if (sessionCardCause) {
+				this.addEmpty(sessionCardCause);
+			}
+			for (const name of providerNames) {
+				this.addCard(name, () => validateAndReattach(name, this.commandService));
+			}
+			return;
+		}
+		const persisted = this.readHistory();
+		if (persisted.length === 0) {
+			this.addEmpty(sessionCardCause ?? localize('remotepairNoHistory', "No history"));
+			return;
+		}
+		for (const name of persisted) {
+			this.addCard(name, () => reattach(name));
+		}
+	}
+
+	/** Reveal/focus a live terminal session (best-effort, public APIs only). */
+	private focusInstance(instance: ITerminalInstance): void {
+		try {
+			this.terminalService.setActiveInstance(instance);
+			instance.focus(true);
+		} catch (e) {
+			rpLog('warn', `focus attached instance failed: ${e instanceof Error ? e.message : String(e)}`);
+			// best-effort
+		}
+	}
+
+	// --- history persistence (workspace scope) -----------------------------
+
+	private reattachableHistoryNames(): readonly string[] {
+		return dataProvider ? [...dataProvider.getDetached(), ...dataProvider.getHistory()] : cachedSessionNames();
+	}
+
+	private readHistory(): string[] {
+		const raw = this.storageService.get(HISTORY_STORAGE_KEY, StorageScope.WORKSPACE);
+		if (!raw) {
+			return [];
+		}
+		try {
+			const parsed = JSON.parse(raw);
+			return Array.isArray(parsed) ? parsed.filter((n): n is string => typeof n === 'string' && SESSION_NAME_RE.test(n)) : [];
+		} catch (e) {
+			rpLog('warn', `history storage parse failed; resetting to empty: ${e instanceof Error ? e.message : String(e)}`);
+			return [];
+		}
+	}
+
+	/** Merge newly-seen real tmux session names into the persisted last-seen set. */
+	private recordHistory(names: readonly string[]): void {
+		const meaningful = names.filter(n => SESSION_NAME_RE.test(n));
+		if (meaningful.length === 0) {
+			return;
+		}
+		const existing = this.readHistory();
+		const merged = Array.from(new Set([...existing, ...meaningful]));
+		if (merged.length === existing.length) {
+			return; // nothing new
+		}
+		this.storageService.store(HISTORY_STORAGE_KEY, JSON.stringify(merged), StorageScope.WORKSPACE, StorageTarget.MACHINE);
+		// Only the History view owns the History row; re-render it if that's us.
+		if (this.category === 'history') {
+			this.renderHistory();
+		}
+	}
+
+}
+
+/** Attached tab view. */
+class RemotePairAttachedSessionsView extends RemotePairSessionCategoryView {
+	protected get category(): SessionCategory { return 'attached'; }
+}
+/** Detached tab view. */
+class RemotePairDetachedSessionsView extends RemotePairSessionCategoryView {
+	protected get category(): SessionCategory { return 'detached'; }
+}
+/** History tab view. */
+class RemotePairHistorySessionsView extends RemotePairSessionCategoryView {
+	protected get category(): SessionCategory { return 'history'; }
+}
+
+// --- registration: three bottom-panel tabs (one Panel container each) --------
+
+const viewContainerRegistry = Registry.as<IViewContainersRegistry>(ViewContainerExtensions.ViewContainersRegistry);
+const viewsRegistry = Registry.as<IViewsRegistry>(ViewContainerExtensions.ViewsRegistry);
+
+/** Register one Panel view container (a tab) hosting a single category view.
+ *  `title` must be a statically-keyed localize2() result (the NLS build analyzer cannot parse a
+ *  dynamic localize key such as localize2(id, ...) — that fails the production build). */
+function registerSessionTab(id: string, title: ReturnType<typeof localize2>, order: number, ctor: new (...args: any[]) => RemotePairSessionCategoryView): void {
+	const container = viewContainerRegistry.registerViewContainer({
+		id,
+		title,
+		ctorDescriptor: new SyncDescriptor(ViewPaneContainer, [id, { mergeViewWithContainerWhenSingleView: true }]),
+		storageId: id,
+		hideIfEmpty: false,
+		order,
+	}, ViewContainerLocation.Panel, { isDefault: false });
+
+	const viewDescriptor: IViewDescriptor = {
+		id: id + '.view',
+		name: title,
+		ctorDescriptor: new SyncDescriptor(ctor),
+		canToggleVisibility: false,
+		canMoveView: false,
+	};
+	viewsRegistry.registerViews([viewDescriptor], container);
+}
+
+registerSessionTab(REMOTEPAIR_SESSIONS_ATTACHED_ID, localize2('remotepairAttached', "Attached"), 1, RemotePairAttachedSessionsView);
+registerSessionTab(REMOTEPAIR_SESSIONS_DETACHED_ID, localize2('remotepairDetached', "Detached"), 2, RemotePairDetachedSessionsView);
+registerSessionTab(REMOTEPAIR_SESSIONS_HISTORY_ID, localize2('remotepairHistory', "History"), 3, RemotePairHistorySessionsView);
+
+// --- Ctrl+Tab: cycle Xpair sessions (override the native editor-tab switch) ------------------
+//
+// Xpair owns the IDE: there is no user-facing native editor-tab strip (RD / Welcome) to switch
+// between — the only meaningful "tabs" are the attached Xpair session terminals living in the
+// Sessions sidebar's embedded EditorPart. So Ctrl+Tab must cycle THOSE, not fire the native
+// `quickOpenPreviousRecentlyUsedEditorInGroup` (Ctrl+Tab) / its Shift sibling.
+//
+// Both source + target are the SAME attached-session set the bottom Sessions "Attached" tab
+// renders (the `attachedProvider` module hook: getAttached()/getActiveId()/reveal()). reveal(id)
+// is exactly the click-a-card path — it opens the session's terminal editor in the embedded
+// group AND focuses its xterm — so "cycle to next session" reuses the proven open/focus path.
+
+/**
+ * Focus/open the next (forward=true) or previous attached Xpair session, wrapping around.
+ * No-ops gracefully when there is no provider or no attached sessions; with a single session it
+ * simply (re)reveals it. Returns true when a reveal was attempted so the keybinding can report
+ * whether it handled the chord (it always claims the chord on the locked product regardless).
+ */
+function cycleAttachedSession(forward: boolean): boolean {
+	const provider = attachedProvider;
+	if (!provider) {
+		return false;
+	}
+	const sessions = provider.getAttached();
+	if (sessions.length === 0) {
+		return false;
+	}
+	if (sessions.length === 1) {
+		provider.reveal(sessions[0].id);
+		return true;
+	}
+	const activeId = provider.getActiveId?.();
+	// When the active session is unknown (focus elsewhere), treat -1 so forward lands on the
+	// first card and backward on the last — a sensible "enter the cycle" entry point.
+	const currentIndex = activeId ? sessions.findIndex(s => s.id === activeId) : -1;
+	const count = sessions.length;
+	const delta = forward ? 1 : -1;
+	const nextIndex = ((currentIndex + delta) % count + count) % count;
+	provider.reveal(sessions[nextIndex].id);
+	return true;
+}
+
+export const REMOTEPAIR_SESSIONS_CYCLE_NEXT_ID = 'remotepair.sessions.cycleNext';
+export const REMOTEPAIR_SESSIONS_CYCLE_PREV_ID = 'remotepair.sessions.cyclePrev';
+
+// Weight WorkbenchContrib + 100 (= 300) outranks the native Ctrl+Tab editor bindings
+// (`quickOpenPreviousRecentlyUsedEditorInGroup`, weight WorkbenchContrib = 200, and the
+// `quickOpenNavigateNext/PreviousInEditorPicker` rules at WorkbenchContrib + 50 = 250). When
+// multiple rules bind the same chord the highest weight wins and the others do NOT fire, so the
+// native editor-tab switch is effectively disabled on Ctrl+Tab / Ctrl+Shift+Tab. We claim the
+// chord globally (no `when`) and match the native binding on every platform: primary
+// CtrlCmd|Tab plus the mac WinCtrl|Tab variant the native rule actually occupies on macOS.
+const REMOTEPAIR_SESSIONS_CYCLE_WEIGHT = KeybindingWeight.WorkbenchContrib + 100;
+
+KeybindingsRegistry.registerCommandAndKeybindingRule({
+	id: REMOTEPAIR_SESSIONS_CYCLE_NEXT_ID,
+	weight: REMOTEPAIR_SESSIONS_CYCLE_WEIGHT,
+	primary: KeyMod.CtrlCmd | KeyCode.Tab,
+	mac: { primary: KeyMod.WinCtrl | KeyCode.Tab },
+	handler: (_accessor: ServicesAccessor) => { cycleAttachedSession(true); }
+});
+
+KeybindingsRegistry.registerCommandAndKeybindingRule({
+	id: REMOTEPAIR_SESSIONS_CYCLE_PREV_ID,
+	weight: REMOTEPAIR_SESSIONS_CYCLE_WEIGHT,
+	primary: KeyMod.CtrlCmd | KeyMod.Shift | KeyCode.Tab,
+	mac: { primary: KeyMod.WinCtrl | KeyMod.Shift | KeyCode.Tab },
+	handler: (_accessor: ServicesAccessor) => { cycleAttachedSession(false); }
+});
diff --git a/src/vs/workbench/contrib/terminal/browser/remotePairSessionPicker.ts b/src/vs/workbench/contrib/terminal/browser/remotePairSessionPicker.ts
new file mode 100644
index 00000000..4d355d93
--- /dev/null
+++ b/src/vs/workbench/contrib/terminal/browser/remotePairSessionPicker.ts
@@ -0,0 +1,137 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair: in-tab "New Session" Browser editor.
+ *
+ *  The Sessions sidebar "+" opens this as an editor tab inside the embedded EditorPart group.
+ *  It hosts the REAL Browser/Explorer viewlet — the registered Explorer view CONTAINER
+ *  (VIEWLET_ID = 'workbench.view.explorer', class ExplorerViewPaneContainer) instantiated as a
+ *  PaneComposite and laid out host-agnostically inside this EditorPane. That means the tab shows
+ *  the full Browser: the file tree PLUS its title toolbar (New File / New Folder / Refresh /
+ *  Collapse All) PLUS Search — exactly what the Browser sidebar viewlet shows, because the toolbar
+ *  actions (MenuId.ViewTitle, gated `when: view == workbench.explorer.fileView`) travel WITH the
+ *  ExplorerView pane header and render as long as the container is not merged-single-view.
+ *
+ *  Starting a Claude session rooted at a folder is NOT done by clicking a row here (the real
+ *  Explorer click opens files / expands folders). The Browser already exposes a per-folder hover
+ *  action `remotepair.terminalSidebar.openSessionInFolder` for that.
+ *
+ *  Blank-render note: the only gotcha hosting a composite in an EditorPane is driving setVisible +
+ *  layout so the ViewPaneContainer's panes load and get a real size. We do that in
+ *  setEditorVisible() and layout() with the FULL dimension.
+ *--------------------------------------------------------------------------------------------*/
+
+import { localize } from '../../../../nls.js';
+import { append, $, Dimension } from '../../../../base/browser/dom.js';
+import { URI } from '../../../../base/common/uri.js';
+import { DisposableStore } from '../../../../base/common/lifecycle.js';
+import { Registry } from '../../../../platform/registry/common/platform.js';
+import { EditorInputCapabilities, IUntypedEditorInput } from '../../../common/editor.js';
+import { EditorInput } from '../../../common/editor/editorInput.js';
+import { EditorPane } from '../../../browser/parts/editor/editorPane.js';
+import { IEditorGroup } from '../../../services/editor/common/editorGroupsService.js';
+import { ITelemetryService } from '../../../../platform/telemetry/common/telemetry.js';
+import { IThemeService } from '../../../../platform/theme/common/themeService.js';
+import { IStorageService } from '../../../../platform/storage/common/storage.js';
+import { IInstantiationService } from '../../../../platform/instantiation/common/instantiation.js';
+import { PaneComposite, Extensions as PaneCompositeExtensions, PaneCompositeRegistry } from '../../../browser/panecomposite.js';
+import { rpLog } from './remotePairLog.js';
+
+// The real Browser/Explorer view container id (see contrib/files/common/files.ts VIEWLET_ID).
+const EXPLORER_VIEWLET_ID = 'workbench.view.explorer';
+
+// Session kind kept as a type for the Session Manager's reattach path (remotePairTerminalSidebar.ts).
+export type SessionKind = 'claude' | 'shell' | 'codex' | 'gemini';
+
+export const SESSION_PICKER_RESOURCE = URI.from({ scheme: 'remotepair-session-picker', path: '/new' });
+
+export class SessionPickerInput extends EditorInput {
+	static readonly ID = 'remotepair.sessionPickerInput';
+	override get typeId(): string { return SessionPickerInput.ID; }
+	override get resource(): URI { return SESSION_PICKER_RESOURCE; }
+	override get capabilities(): EditorInputCapabilities { return EditorInputCapabilities.Readonly | EditorInputCapabilities.Singleton; }
+	override getName(): string { return localize('rpNewSession', "New Session"); }
+	override matches(other: EditorInput | IUntypedEditorInput): boolean { return other instanceof SessionPickerInput; }
+}
+
+export class SessionPickerPane extends EditorPane {
+	static readonly ID = 'remotepair.sessionPickerPane';
+	private holder: HTMLElement | undefined;
+	private composite: PaneComposite | undefined;
+	private lastDimension: Dimension | undefined;
+	private readonly compositeDisposables = this._register(new DisposableStore());
+
+	constructor(
+		group: IEditorGroup,
+		@ITelemetryService telemetryService: ITelemetryService,
+		@IThemeService themeService: IThemeService,
+		@IStorageService storageService: IStorageService,
+		@IInstantiationService private readonly instantiationService: IInstantiationService,
+	) {
+		super(SessionPickerPane.ID, group, telemetryService, themeService, storageService);
+	}
+
+	protected createEditor(parent: HTMLElement): void {
+		rpLog('debug', 'session picker: hosting real Explorer viewlet (PaneComposite)');
+
+		// Full-size holder. Deliberately NOT classed `part`/`editor` (that was an unrelated
+		// embedded-EditorPart hack) — this holder just gives the composite a sized box.
+		const holder = this.holder = append(parent, $('div.remotepair-session-picker'));
+		holder.style.cssText = 'height:100%;width:100%;position:relative;box-sizing:border-box;';
+
+		const reg = Registry.as<PaneCompositeRegistry>(PaneCompositeExtensions.Viewlets);
+		const desc = reg.getPaneComposite(EXPLORER_VIEWLET_ID);
+		if (!desc) {
+			rpLog('warn', `session picker: Explorer viewlet descriptor '${EXPLORER_VIEWLET_ID}' not found; tab will be empty`);
+			return;
+		}
+
+		const composite = this.composite = desc.instantiate(this.instantiationService);
+		this.compositeDisposables.add(composite);
+		composite.create(holder);
+		composite.setVisible(true);
+
+		// If we already have a size (createEditor can run after a layout), apply it now.
+		if (this.lastDimension) {
+			composite.layout(this.lastDimension);
+		}
+	}
+
+	protected override setEditorVisible(visible: boolean): void {
+		super.setEditorVisible(visible);
+		this.composite?.setVisible(visible);
+		// On first real visibility, ensure the composite is laid out so its panes load + size.
+		if (visible && this.composite && this.lastDimension) {
+			this.composite.layout(this.lastDimension);
+		}
+	}
+
+	override focus(): void {
+		super.focus();
+		this.composite?.focus();
+	}
+
+	layout(dimension: Dimension): void {
+		this.lastDimension = dimension;
+		if (this.holder) {
+			this.holder.style.height = `${dimension.height}px`;
+			this.holder.style.width = `${dimension.width}px`;
+		}
+		this.composite?.layout(dimension);
+	}
+
+	override clearInput(): void {
+		this.disposeComposite();
+		super.clearInput();
+	}
+
+	override dispose(): void {
+		this.disposeComposite();
+		super.dispose();
+	}
+
+	private disposeComposite(): void {
+		if (this.composite) {
+			this.compositeDisposables.clear();
+			this.composite = undefined;
+		}
+	}
+}
diff --git a/src/vs/workbench/contrib/terminal/browser/remotePairTerminalSidebar.ts b/src/vs/workbench/contrib/terminal/browser/remotePairTerminalSidebar.ts
new file mode 100644
index 00000000..820a8de4
--- /dev/null
+++ b/src/vs/workbench/contrib/terminal/browser/remotePairTerminalSidebar.ts
@@ -0,0 +1,689 @@
+/*---------------------------------------------------------------------------------------------
+ *  RemotePair: "Terminal" sidebar container that embeds a real EditorPart so its
+ *  content is editor-tab-based (horizontal tabs), terminal-specialized.
+ *
+ *  Two conditions (per design): (1) it IS a primary-sidebar view container that the
+ *  activity-bar rail switches with the file "Browser"; (2) inside it lives a real
+ *  EditorPart (editor tabs), not a vertical list. VS Code has no native "editor
+ *  group inside a sidebar", so we instantiate a standalone EditorPart (same machine
+ *  AuxiliaryEditorPart uses for auxiliary windows) and mount it into a ViewPane body.
+ *--------------------------------------------------------------------------------------------*/
+
+import { localize, localize2 } from '../../../../nls.js';
+import { URI } from '../../../../base/common/uri.js';
+import { mainWindow } from '../../../../base/browser/window.js';
+import { getWindow, scheduleAtNextAnimationFrame, addDisposableListener, EventType } from '../../../../base/browser/dom.js';
+import { DisposableStore, MutableDisposable, IDisposable } from '../../../../base/common/lifecycle.js';
+import { SyncDescriptor } from '../../../../platform/instantiation/common/descriptors.js';
+import { IInstantiationService, ServicesAccessor } from '../../../../platform/instantiation/common/instantiation.js';
+import { IThemeService } from '../../../../platform/theme/common/themeService.js';
+import { IConfigurationService } from '../../../../platform/configuration/common/configuration.js';
+import { IStorageService } from '../../../../platform/storage/common/storage.js';
+import { IContextKeyService, ContextKeyExpr } from '../../../../platform/contextkey/common/contextkey.js';
+import { IKeybindingService } from '../../../../platform/keybinding/common/keybinding.js';
+import { IContextMenuService } from '../../../../platform/contextview/browser/contextView.js';
+import { IOpenerService } from '../../../../platform/opener/common/opener.js';
+import { IHoverService } from '../../../../platform/hover/browser/hover.js';
+import { IClipboardService } from '../../../../platform/clipboard/common/clipboardService.js';
+import { INotificationService } from '../../../../platform/notification/common/notification.js';
+import { Registry } from '../../../../platform/registry/common/platform.js';
+import { ViewPane, IViewPaneOptions } from '../../../browser/parts/views/viewPane.js';
+import { IViewDescriptor, IViewDescriptorService, IViewsRegistry, IViewContainersRegistry, Extensions as ViewContainerExtensions, ViewContainerLocation } from '../../../common/views.js';
+import { ViewPaneContainer } from '../../../browser/parts/views/viewPaneContainer.js';
+import { EditorPart } from '../../../browser/parts/editor/editorPart.js';
+import { IEditorPartsView, IEditorGroupView } from '../../../browser/parts/editor/editor.js';
+import { IEditorGroupsService, IEditorGroup } from '../../../services/editor/common/editorGroupsService.js';
+import { IWorkbenchLayoutService } from '../../../services/layout/browser/layoutService.js';
+import { IHostService } from '../../../services/host/browser/host.js';
+import { TerminalLocation } from '../../../../platform/terminal/common/terminal.js';
+import { ITerminalInstanceService, ITerminalInstance } from './terminal.js';
+import { TerminalEditorInput } from './terminalEditorInput.js';
+import { Action2, registerAction2, MenuId } from '../../../../platform/actions/common/actions.js';
+import { Codicon } from '../../../../base/common/codicons.js';
+import { IViewsService } from '../../../services/views/common/viewsService.js';
+import { EditorInput } from '../../../common/editor/editorInput.js';
+import { SessionPickerInput, SessionKind } from './remotePairSessionPicker.js';
+import { VIEWLET_ID as BROWSER_VIEWLET_ID } from '../../files/common/files.js';
+import { setSessionReattacher, setAttachedSessionsProvider, setSessionCardCause, notifyAttachedSessionsChanged } from './remotePairSessionManager.js';
+import { rpLog } from './remotePairLog.js';
+
+export const REMOTEPAIR_TERMINAL_SIDEBAR_ID = 'remotepair.terminalSidebar';
+
+/**
+ * A standalone EditorPart instance hosted inside a sidebar ViewPane.
+ * Modeled on AuxiliaryEditorPartImpl (auxiliaryEditorPart.ts) — that class proves
+ * EditorPart can be instantiated outside the main part.
+ */
+class EmbeddedEditorPart extends EditorPart {
+
+	private static COUNTER = 1;
+
+	constructor(
+		editorPartsView: IEditorPartsView,
+		@IInstantiationService instantiationService: IInstantiationService,
+		@IThemeService themeService: IThemeService,
+		@IConfigurationService configurationService: IConfigurationService,
+		@IStorageService storageService: IStorageService,
+		@IWorkbenchLayoutService layoutService: IWorkbenchLayoutService,
+		@IHostService hostService: IHostService,
+		@IContextKeyService contextKeyService: IContextKeyService
+	) {
+		const id = EmbeddedEditorPart.COUNTER++;
+		super(
+			editorPartsView,
+			`workbench.parts.remotepairTerminalEditor.${id}`,
+			localize('remotepairTerminal', "Sessions"),
+			mainWindow.vscodeWindowId,
+			instantiationService,
+			themeService,
+			configurationService,
+			storageService,
+			layoutService,
+			hostService,
+			contextKeyService
+		);
+	}
+}
+
+/**
+ * Sidebar ViewPane that hosts the embedded EditorPart. Step 1 (skeleton): render
+ * an empty EditorPart in the sidebar body. Step 2 (next): mount terminal editors
+ * into it via a scoped IEditorService.
+ */
+export class RemotePairTerminalSidebarView extends ViewPane {
+
+	private editorPart: EmbeddedEditorPart | undefined;
+	private readonly partDisposables = this._register(new DisposableStore());
+
+	/**
+	 * Tracks terminals opened in the embedded EditorPart. Keyed by instance.resource.toString().
+	 * Used to populate the Session Manager's Attached row (S0 isolation means these instances
+	 * never appear in ITerminalService.instances, so we source the Attached row from here).
+	 */
+	private readonly attached = new Map<string, { instance: ITerminalInstance; input: EditorInput; group: IEditorGroup }>();
+
+	/** Last body layout size, so we can re-layout the part once an editor mounts. */
+	private lastWidth = 0;
+	private lastHeight = 0;
+
+	/** Title-source subscription for whichever terminal is currently active. */
+	private readonly activeTitleListener: MutableDisposable<IDisposable> = this._register(new MutableDisposable());
+	/** Per-group active-editor subscription, swapped when the active group changes. */
+	private readonly activeGroupListener: MutableDisposable<IDisposable> = this._register(new MutableDisposable());
+
+	private static readonly DEFAULT_TITLE = localize('remotepairSessions', "Sessions");
+	private static readonly INPUT_RECOVERY_GUIDANCE_MIN_CHARS = 8192;
+	private static readonly INPUT_RECOVERY_GUIDANCE_MIN_LINES = 20;
+	private static readonly INPUT_RECOVERY_GUIDANCE_COOLDOWN_MS = 30000;
+
+	private lastInputRecoveryGuidanceAt = 0;
+
+	constructor(
+		options: IViewPaneOptions,
+		@IKeybindingService keybindingService: IKeybindingService,
+		@IContextMenuService contextMenuService: IContextMenuService,
+		@IConfigurationService configurationService: IConfigurationService,
+		@IContextKeyService contextKeyService: IContextKeyService,
+		@IViewDescriptorService viewDescriptorService: IViewDescriptorService,
+		@IInstantiationService instantiationService: IInstantiationService,
+		@IOpenerService openerService: IOpenerService,
+		@IThemeService themeService: IThemeService,
+		@IHoverService hoverService: IHoverService,
+		@IClipboardService private readonly clipboardService: IClipboardService,
+		@INotificationService private readonly notificationService: INotificationService,
+		@IEditorGroupsService private readonly editorGroupsService: IEditorGroupsService,
+		@ITerminalInstanceService private readonly terminalInstanceService: ITerminalInstanceService
+	) {
+		super(options, keybindingService, contextMenuService, configurationService, contextKeyService, viewDescriptorService, instantiationService, openerService, themeService, hoverService);
+
+		// G004: route the bottom Session Manager's reattach requests into this sidebar's
+		// embedded EditorPart group, reusing the same terminal-open path.
+		setSessionReattacher((name) => this.launchReattach(name));
+
+		// G004: supply the Session Manager's Attached row with the sidebar's own terminal
+		// set. These instances bypass ITerminalService (S0 isolation), so we source them here.
+		setAttachedSessionsProvider({
+			getAttached: () => [...this.attached.values()].map(v => ({
+				id: v.instance.resource.toString(),
+				title: v.instance.title || 'Session',
+			})),
+			reveal: (id) => {
+				const v = this.attached.get(id);
+				if (v) {
+					v.group.openEditor(v.input, { pinned: true }).then(() => setSessionCardCause(undefined), (e) => {
+						rpLog('warn', `reveal: openEditor failed for attached session id=${id}: ${e}`);
+						this.attached.delete(id);
+						setSessionCardCause(localize('remotepairAttachedRevealFailed', "That attached terminal could not be shown. The Attached list was refreshed."));
+						notifyAttachedSessionsChanged();
+					});
+					v.instance.focus(true);
+					return true;
+				}
+				rpLog('warn', `reveal: attached session id=${id} is stale; removing card`);
+				setSessionCardCause(localize('remotepairAttachedSessionGone', "That attached terminal is gone. The Attached list was refreshed."));
+				notifyAttachedSessionsChanged();
+				return false;
+			},
+			// Close (dispose) the attached session — the instance's onDisposed handler removes it
+			// from `this.attached` and notifies the Attached row. Public dispose() only.
+			close: (id) => {
+				const v = this.attached.get(id);
+				if (v) {
+					try { v.instance.dispose(); } catch (e) { rpLog('warn', `close: instance.dispose threw for attached session id=${id}: ${e}`); /* best-effort */ }
+				}
+			},
+			// Best-effort: the embedded part's active group's active terminal editor → its resource
+			// string, which is the same id used as the attached-map key. undefined when unknown.
+			getActiveId: () => {
+				const input = this.editorPart?.activeGroup?.activeEditor;
+				const instance = input instanceof TerminalEditorInput ? input.terminalInstance : undefined;
+				return instance?.resource.toString();
+			},
+		});
+	}
+
+	/**
+	 * G004: reattach a session by name into the embedded EditorPart group. Reuses the same
+	 * terminal-open path as launchSession and sends a quoted `xpair attach <name>` via the
+	 * terminal's public sendText API. Hosting-layer only — core terminal behavior untouched.
+	 */
+	private launchReattach(name: string): void {
+		const part = this.editorPart;
+		if (!part) {
+			return;
+		}
+		part.whenReady.then(() => {
+			if (this.editorPart !== part) {
+				return;
+			}
+			const group = part.activeGroup;
+			if (!group) {
+				return;
+			}
+			try {
+				const { instance, opened } = this.openTerminalInGroup(group);
+				opened.then(() => {
+					instance.sendText('xpair attach ' + shellSingleQuote(name), true);
+				}, (e) => { rpLog('warn', `launchReattach(${name}): terminal open rejected: ${e}`); /* best-effort */ });
+			} catch (e) {
+				rpLog('error', `launchReattach(${name}): openTerminalInGroup threw: ${e}`); // best-effort
+			}
+		}, () => { /* part disposed */ });
+	}
+
+	/**
+	 * Shared terminal-open path: create a terminal editor in `group`, record it in the
+	 * `attached` map, and notify the Session Manager's Attached row. Both launchSession and
+	 * launchReattach use this so terminal-creation logic is not duplicated. Public services only.
+	 */
+	private openTerminalInGroup(group: IEditorGroup, cwd?: URI): { instance: ITerminalInstance; opened: Promise<void> } {
+		const instance = this.terminalInstanceService.createInstance(cwd ? { cwd } : {}, TerminalLocation.Editor);
+		const input = this.instantiationService.createInstance(TerminalEditorInput, instance.resource, instance);
+		const key = instance.resource.toString();
+
+		// Per-instance lifecycle store so these listeners live only as long as the terminal,
+		// not the whole Sessions view (otherwise opening/closing N sessions leaks 2*N listeners
+		// and dead onTitleChanged closures keep firing). It is removed from partDisposables and
+		// disposed when the instance is disposed or the editor open fails.
+		const store = new DisposableStore();
+		this.partDisposables.add(store);
+		store.add(instance.onDisposed(() => {
+			this.attached.delete(key);
+			notifyAttachedSessionsChanged();
+			this.partDisposables.delete(store); // removes from set + disposes this store's listeners
+		}));
+		store.add(instance.onTitleChanged(() => notifyAttachedSessionsChanged()));
+
+		const opened = group.openEditor(input, { pinned: true }).then(() => {
+			// Record only after the editor is open so `group` is live.
+			this.attached.set(key, { instance, input, group });
+			notifyAttachedSessionsChanged();
+		}, (e) => {
+			// Open failed: tear down the orphaned instance + its listeners (don't strand it).
+			this.partDisposables.delete(store);
+			rpLog('warn', `openTerminalInGroup: editor open failed for ${key}; tearing down orphaned instance: ${e}`);
+			try { instance.dispose(); } catch (de) { rpLog('warn', `openTerminalInGroup: dispose of orphaned instance ${key} threw: ${de}`); /* best-effort */ }
+			throw e;
+		});
+
+		return { instance, opened };
+	}
+
+	/** Open the in-tab "New Session" picker as an editor in the embedded group. */
+	openSessionPicker(): void {
+		const part = this.editorPart;
+		if (!part) {
+			return;
+		}
+		part.whenReady.then(() => {
+			if (this.editorPart !== part) {
+				return;
+			}
+			const group = part.activeGroup;
+			if (!group) {
+				return;
+			}
+			const input = this.instantiationService.createInstance(SessionPickerInput);
+			group.openEditor(input, { pinned: false }).then(() => {
+				// Blank-fix (same as mountTerminal): re-fire visibility + layout now that the
+				// group + editor exist, so the embedded EditorPart actually renders. The
+				// setVisible in renderBody ran before any group existed and was missed; without
+				// this the picker (the launch editor) would open at 0×0 and stay blank.
+				if (this.editorPart === part) {
+					part.setVisible(this.isBodyVisible());
+					if (this.lastWidth > 0 && this.lastHeight > 0) {
+						part.layout(this.lastWidth, this.lastHeight, 0, 0);
+					}
+				}
+			}, (e) => { rpLog('warn', `openSessionPicker: openEditor(picker) failed: ${e}`); /* best-effort */ });
+		}, () => { /* part disposed */ });
+	}
+
+	/**
+	 * G005: open a session in the embedded group rooted at `cwd` (a folder uri from the
+	 * Browser's per-folder "+"). Reuses the shared openTerminalInGroup path with the cwd
+	 * threaded through, then launches the xpair launcher via the terminal's public
+	 * sendText API. Hosting-layer only — core terminal behavior untouched.
+	 */
+	openSessionInFolder(cwd: URI | undefined, kind?: SessionKind): void {
+		const part = this.editorPart;
+		if (!part) {
+			return;
+		}
+		part.whenReady.then(() => {
+			if (this.editorPart !== part) {
+				return;
+			}
+			const group = part.activeGroup;
+			if (!group) {
+				return;
+			}
+			try {
+				const { instance, opened } = this.openTerminalInGroup(group, cwd);
+				opened.then(() => {
+					// Folder-root launch contract: instance.sendText('xpair launch', true) runs in cwd
+					// from openTerminalInGroup(group, cwd). The "+" / folder-row path passes no kind:
+					// emit plain `xpair launch` and let the CLI's device-first engine picker decide.
+					// Only EXPLICIT picker choices append their engine flag.
+					if (kind === 'shell') {
+						return;
+					}
+					if (kind === 'codex') {
+						instance.sendText('xpair launch --engine codex', true);
+						return;
+					}
+					if (kind === 'gemini') {
+						instance.sendText('xpair launch --engine gemini', true);
+						return;
+					}
+					if (kind === 'claude') {
+						instance.sendText('xpair launch --engine claude', true);
+						return;
+					}
+					instance.sendText('xpair launch', true);
+				}, (e) => { rpLog('warn', `openSessionInFolder: terminal open rejected: ${e}`); /* best-effort */ });
+			} catch (e) {
+				rpLog('error', `openSessionInFolder: openTerminalInGroup threw: ${e}`); // best-effort
+			}
+		}, () => { /* part disposed */ });
+	}
+
+	/**
+	 * G005: open a terminal in the embedded group and stage `text` without a newline so the
+	 * user can complete the command (used by the Browser's "Add Mapping" action with the
+	 * staged `xpair map add ` prefix). Hosting-layer only — core terminal untouched.
+	 */
+	stageBrowserCommand(text: string): void {
+		const part = this.editorPart;
+		if (!part) {
+			return;
+		}
+		part.whenReady.then(() => {
+			if (this.editorPart !== part) {
+				return;
+			}
+			const group = part.activeGroup;
+			if (!group) {
+				return;
+			}
+			try {
+				const { instance, opened } = this.openTerminalInGroup(group);
+				opened.then(() => {
+					instance.sendText(text, false);
+				}, (e) => { rpLog('warn', `stageBrowserCommand: terminal open rejected: ${e}`); /* best-effort */ });
+			} catch (e) {
+				rpLog('error', `stageBrowserCommand: openTerminalInGroup threw: ${e}`); // best-effort
+			}
+		}, () => { /* part disposed */ });
+	}
+
+	/** Reflect the active terminal session's title in the view/container header. */
+	private syncTitleToActiveSession(): void {
+		const part = this.editorPart;
+		const input = part?.activeGroup?.activeEditor;
+		const instance = input instanceof TerminalEditorInput ? input.terminalInstance : undefined;
+
+		if (instance) {
+			this.activeTitleListener.value = instance.onTitleChanged(() => {
+				this.updateTitle(instance.title || RemotePairTerminalSidebarView.DEFAULT_TITLE);
+			});
+			this.updateTitle(instance.title || RemotePairTerminalSidebarView.DEFAULT_TITLE);
+		} else {
+			this.activeTitleListener.clear();
+			this.updateTitle(RemotePairTerminalSidebarView.DEFAULT_TITLE);
+		}
+	}
+
+	/** (Re)subscribe to the active group's active-editor changes. */
+	private trackActiveGroup(group: IEditorGroupView | undefined): void {
+		this.activeGroupListener.value = group?.onDidActiveEditorChange(() => {
+			this.syncTitleToActiveSession();
+			// Active session changed → refresh the Session Manager Attached row so its
+			// active-tab highlight (getActiveId) tracks the focused terminal.
+			notifyAttachedSessionsChanged();
+		});
+		this.syncTitleToActiveSession();
+	}
+
+	protected override renderBody(container: HTMLElement): void {
+		super.renderBody(container);
+
+		container.classList.add('remotepair-terminal-sidebar');
+		this.partDisposables.add(addDisposableListener(container, EventType.KEY_DOWN, (e) => this.handleTerminalShortcut(e as KeyboardEvent)));
+
+		// G002: New Session "+" lives ONLY in the view title (row 1) via the MenuId.ViewTitle
+		// action `remotepair.terminalSidebar.newSession`, which DOES render in this container's
+		// merged composite title. The earlier in-body custom "+" button (row 2) was a duplicate
+		// (its original "ViewTitle does not render here" assumption is no longer true) and was
+		// removed per user feedback — the single row-1 "+" is sufficient.
+
+		// Defer the embedded EditorPart creation OUT of the synchronous composite-open
+		// path. renderBody runs inside compositePart.doOpenComposite → createComposite;
+		// instantiating a full EditorPart (with its own grid/group/editor + events) there
+		// re-enters the composite machinery so the sidebar's `activeComposite` never
+		// settles on Sessions. That marks the pane-composite-part `.empty`, and the
+		// full-height empty-pane-message (paneCompositePart.ts:615) shoves our viewlet off
+		// the bottom of the sidebar → blank. Building the part on the next frame lets
+		// doOpenComposite finish (activeComposite = Sessions) first.
+		this.partDisposables.add(scheduleAtNextAnimationFrame(getWindow(container), () => this.createEmbeddedPart(container)));
+	}
+
+	private createEmbeddedPart(container: HTMLElement): void {
+		if (this.editorPart) {
+			return; // already built (or rebuilt) — avoid duplicates
+		}
+
+		// EditorParts (the IEditorGroupsService impl) also implements IEditorPartsView.
+		const editorPartsView = this.editorGroupsService as unknown as IEditorPartsView;
+
+		// S0 — routing isolation (reverse-leak fix). We deliberately do NOT call
+		// `editorPartsView.registerPart(part)` here. Registration would add this part
+		// to EditorParts._parts and its MRU list, which feeds `getPartByDocument`
+		// (editorParts.ts:329) and `get activeGroup` (editorParts.ts:676). The result:
+		// when the user focuses this sidebar, `activePart`/`activeGroup` would resolve
+		// to the embedded group, so any editor opened with ACTIVE_GROUP (settings,
+		// search results, diffs, welcome) would be sucked into the session sidebar —
+		// the dual of the "main leak". It would also fire global `onDidActiveGroupChange`
+		// on every sidebar click (breadcrumbs/title/active-editor consumers misfire).
+		//
+		// The session sidebar is NOT a first-class editor area (unlike auxiliary
+		// windows, which is why AuxiliaryEditorPart *does* register). Leaving the part
+		// unregistered keeps it invisible to global group queries; we drive it solely
+		// via `part.activeGroup.openEditor` on the part instance, which is part-local
+		// and registration-independent. `groupsLabel`/`count` come from the part's own
+		// fields, so group construction still works standalone.
+		// CRITICAL: the editor-group layout CSS (editorgroupview.css) is scoped to
+		// `.monaco-workbench .part.editor > .content ...`. The main editor part gets the
+		// `.part .editor` classes from the workbench grid; an embedded part does not, so
+		// without these classes NONE of the group/grid/title/watermark layout rules apply
+		// → the grid container collapses to 0, the group's children fall back to static
+		// flow, the empty watermark stays in-flow, and the editor/terminal renders blank.
+		// `createContentArea` sets `this.element = parent` (this container) and appends the
+		// `.content` directly under it, so tagging the container `.part.editor` makes
+		// `.part.editor > .content` match and the editor renders correctly.
+		container.classList.add('part', 'editor');
+
+		const part = this.instantiationService.createInstance(EmbeddedEditorPart, editorPartsView);
+		this.partDisposables.add(part);
+		part.create(container);
+
+		this.editorPart = part;
+
+		// Blank-fix: drive the embedded part's visibility from the ViewPane body.
+		// Editor panes (including TerminalEditor/xterm) only attach and size when their
+		// group is told it is visible: EditorPart.setVisible → onDidVisibilityChange →
+		// EditorGroupView.onDidVisibilityChange (editorGroupView.ts:868) →
+		// editorPane.setVisible → TerminalEditor.setVisible (terminalEditor.ts:172) →
+		// terminalInstance.setVisible. An embedded part has nobody to drive this, so the
+		// xterm opens at 0×0 and stays blank. Wire it to the pane's own visibility.
+		this.partDisposables.add(this.onDidChangeBodyVisibility(visible => part.setVisible(visible)));
+		part.setVisible(this.isBodyVisible());
+
+		// Lay out immediately with the last known body size (layoutBody may have already
+		// run while the part did not yet exist).
+		if (this.lastWidth > 0 && this.lastHeight > 0) {
+			part.layout(this.lastWidth, this.lastHeight, 0, 0);
+		}
+
+		// Keep the header title in sync with the active session, swapping the
+		// per-group subscription whenever the embedded part's active group changes.
+		this.partDisposables.add(part.onDidChangeActiveGroup(group => this.trackActiveGroup(group)));
+		this.trackActiveGroup(part.activeGroup);
+
+		// RemotePair: on launch the Sessions sidebar shows running session terminals (if any). The
+		// "+" New Session button fills the primary sidebar with the Browser container (folder picker);
+		// picking a folder starts a Claude session and returns here. (We no longer open an in-tab
+		// picker — hosting the Explorer viewlet in an editor pane could not populate its views.)
+	}
+
+	protected override layoutBody(height: number, width: number): void {
+		super.layoutBody(height, width);
+		this.lastWidth = width;
+		this.lastHeight = height;
+		this.editorPart?.layout(width, height, 0, 0);
+	}
+
+	/** The live terminal instance in the embedded part's active group, if any. */
+	private activeTerminalInstance(): ITerminalInstance | undefined {
+		const input = this.editorPart?.activeGroup?.activeEditor;
+		return input instanceof TerminalEditorInput ? input.terminalInstance : undefined;
+	}
+
+	private handleTerminalShortcut(e: KeyboardEvent): void {
+		if (!e.metaKey || e.ctrlKey || e.altKey || e.shiftKey) {
+			return;
+		}
+
+		const instance = this.activeTerminalInstance();
+		if (!instance) {
+			return;
+		}
+
+		const key = e.key.toLowerCase();
+		if (key === 'c') {
+			if (!instance.hasSelection()) {
+				return;
+			}
+			e.preventDefault();
+			e.stopPropagation();
+			instance.xterm?.copySelection();
+			return;
+		}
+
+		if (key === 'v') {
+			e.preventDefault();
+			e.stopPropagation();
+			this.clipboardService.readText().then(text => {
+				if (this.activeTerminalInstance() === instance) {
+					instance.sendText(text, false, true);
+					this.maybeOfferInputRecoveryGuidance(text);
+				}
+			}, e => {
+				rpLog('warn', `paste: clipboard read failed: ${e}`);
+			});
+		}
+	}
+
+	private maybeOfferInputRecoveryGuidance(text: string): void {
+		if (!this.shouldOfferInputRecoveryGuidance(text)) {
+			return;
+		}
+
+		const now = Date.now();
+		if (now - this.lastInputRecoveryGuidanceAt < RemotePairTerminalSidebarView.INPUT_RECOVERY_GUIDANCE_COOLDOWN_MS) {
+			return;
+		}
+		this.lastInputRecoveryGuidanceAt = now;
+
+		this.notificationService.info(localize(
+			'remotepairTerminalInputRecoveryGuidance',
+			"If a native terminal or agent input limit rejects or truncates this paste, edit it in place, shorten it, or retry paste. Xpair does not switch to file transfer for this flow."
+		));
+	}
+
+	private shouldOfferInputRecoveryGuidance(text: string): boolean {
+		if (text.length >= RemotePairTerminalSidebarView.INPUT_RECOVERY_GUIDANCE_MIN_CHARS) {
+			return true;
+		}
+
+		const lineBreaks = text.match(/\r\n|\r|\n/g)?.length ?? 0;
+		return lineBreaks >= RemotePairTerminalSidebarView.INPUT_RECOVERY_GUIDANCE_MIN_LINES;
+	}
+
+	/**
+	 * Move keyboard focus onto the active terminal's xterm textarea — the ONLY element that
+	 * receives keystrokes. `instance.focus(true)` restores xterm's internal focus state but
+	 * does not reliably move DOM focus onto `.xterm-helper-textarea`; we therefore also focus
+	 * the textarea explicitly on a microtask (after any pending layout/visibility work). The
+	 * textarea is a descendant of this view's element, so its focus is picked up by the
+	 * ViewPane FocusTracker (onDidFocus) — keeping the workbench's focused-view tracking
+	 * correct so focus is not "fallen back" to the main editor. Hosting-layer only; uses the
+	 * terminal's public API and does not touch core terminal behavior.
+	 */
+	private focusActiveTerminal(): boolean {
+		const instance = this.activeTerminalInstance();
+		if (!instance) {
+			return false;
+		}
+		instance.focus(true);
+		queueMicrotask(() => {
+			if (this.activeTerminalInstance() === instance) {
+				instance.xterm?.raw.textarea?.focus();
+			}
+		});
+		return true;
+	}
+
+	/**
+	 * Route focus into the embedded terminal. We focus the xterm textarea DIRECTLY and
+	 * deliberately do NOT call `super.focus()` first: `ViewPane.focus()` runs
+	 * `this.element.focus()`, which moves DOM focus to the view *container* and steals it
+	 * from the xterm textarea — the root cause of unreliable terminal key input and the
+	 * focus "fallback". Only when there is no live terminal do we fall back to the default
+	 * ViewPane focus + group delegation. Hosting-layer only — core terminal behavior untouched.
+	 */
+	override focus(): void {
+		if (!this.focusActiveTerminal()) {
+			super.focus();
+			this.editorPart?.activeGroup?.focus();
+		}
+	}
+
+	override dispose(): void {
+		this.partDisposables.dispose();
+		this.editorPart = undefined;
+		super.dispose();
+	}
+}
+
+// --- registration: "Terminal" sidebar view container + view ----------------
+
+const viewContainerRegistry = Registry.as<IViewContainersRegistry>(ViewContainerExtensions.ViewContainersRegistry);
+const container = viewContainerRegistry.registerViewContainer({
+	id: REMOTEPAIR_TERMINAL_SIDEBAR_ID,
+	title: localize2('remotepairTerminalSidebar', "Sessions"),
+	ctorDescriptor: new SyncDescriptor(ViewPaneContainer, [REMOTEPAIR_TERMINAL_SIDEBAR_ID, { mergeViewWithContainerWhenSingleView: true }]),
+	storageId: REMOTEPAIR_TERMINAL_SIDEBAR_ID,
+	hideIfEmpty: false,
+	order: 1,
+}, ViewContainerLocation.Sidebar, { isDefault: true });
+
+const viewDescriptor: IViewDescriptor = {
+	id: REMOTEPAIR_TERMINAL_SIDEBAR_ID + '.view',
+	name: localize2('remotepairTerminalSidebarView', "Sessions"),
+	ctorDescriptor: new SyncDescriptor(RemotePairTerminalSidebarView),
+	canToggleVisibility: false,
+	canMoveView: false,
+};
+Registry.as<IViewsRegistry>(ViewContainerExtensions.ViewsRegistry).registerViews([viewDescriptor], container);
+
+// G002: "+" on the Sessions container top bar → opens the in-tab New Session picker.
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.terminalSidebar.newSession',
+			title: localize2('remotepairNewSession', "New Session"),
+			icon: Codicon.add,
+			f1: false,
+			menu: [{
+				id: MenuId.ViewTitle,
+				when: ContextKeyExpr.equals('view', REMOTEPAIR_TERMINAL_SIDEBAR_ID + '.view'),
+				group: 'navigation',
+				order: 1
+			}]
+		});
+	}
+	run(accessor: ServicesAccessor): void {
+		// New Session: fill the primary sidebar with the real Browser container so the user can pick
+		// a folder (the Browser renders natively here). The Browser's "Back to Sessions" title button
+		// returns here; picking a folder starts a Claude session and also returns here.
+		accessor.get(IViewsService).openViewContainer(BROWSER_VIEWLET_ID, true);
+	}
+});
+
+// G005: open a session in a specific folder (from the Browser's per-folder "+"). Ensures
+// the Sessions sidebar is visible first (so the embedded EditorPart exists), then routes
+// the folder uri into the view's openSessionInFolder.
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.terminalSidebar.openSessionInFolder',
+			title: localize2('remotepairOpenSessionInFolder', "New Session Here"),
+			icon: Codicon.add,
+			f1: false
+		});
+	}
+	async run(accessor: ServicesAccessor, resource?: URI, kind?: SessionKind): Promise<void> {
+		const viewsService = accessor.get(IViewsService);
+		// Make the Sessions sidebar visible so the embedded EditorPart is constructed.
+		const view = await viewsService.openView(REMOTEPAIR_TERMINAL_SIDEBAR_ID + '.view', false);
+		if (view instanceof RemotePairTerminalSidebarView) {
+			view.openSessionInFolder(resource, kind);
+		}
+	}
+});
+
+// G005: stage the `xpair map add ` command in a Sessions terminal (from the Browser's
+// "Add Mapping" action). Ensures the Sessions sidebar is visible first, then stages the text.
+registerAction2(class extends Action2 {
+	constructor() {
+		super({
+			id: 'remotepair.browser.addMapping',
+			title: localize2('remotepairAddMapping', "Xpair: Add Mapping"),
+			icon: Codicon.add,
+			f1: true
+		});
+	}
+	async run(accessor: ServicesAccessor): Promise<void> {
+		const viewsService = accessor.get(IViewsService);
+		// Make the Sessions sidebar visible so the embedded EditorPart is constructed.
+		const view = await viewsService.openView(REMOTEPAIR_TERMINAL_SIDEBAR_ID + '.view', false);
+		if (view instanceof RemotePairTerminalSidebarView) {
+			view.stageBrowserCommand('xpair map add ');
+		}
+	}
+});
+
+function shellSingleQuote(value: string): string {
+	return `'${value.replace(/'/g, `'\\''`)}'`;
+}
diff --git a/extensions/git/package.json b/extensions/git/package.json
index 483589b3..d4310749 100644
--- a/extensions/git/package.json
+++ b/extensions/git/package.json
@@ -4366,13 +4366,13 @@
       {
         "view": "explorer",
         "contents": "%view.workbench.cloneRepository%",
-        "when": "config.git.enabled && git.state == initialized && scm.providerCount == 0",
+        "when": "config.git.enabled && git.state == initialized && scm.providerCount == 0 && workbenchState != 'empty'",
         "group": "5_scm@1"
       },
       {
         "view": "explorer",
         "contents": "%view.workbench.learnMore%",
-        "when": "config.git.enabled && git.state == initialized && scm.providerCount == 0",
+        "when": "config.git.enabled && git.state == initialized && scm.providerCount == 0 && workbenchState != 'empty'",
         "group": "5_scm@10"
       }
     ]
diff --git a/src/vs/workbench/browser/actions/workspaceActions.ts b/src/vs/workbench/browser/actions/workspaceActions.ts
index 795ac6e0..54a64cbd 100644
--- a/src/vs/workbench/browser/actions/workspaceActions.ts
+++ b/src/vs/workbench/browser/actions/workspaceActions.ts
@@ -353,7 +353,8 @@ MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
 		title: localize({ key: 'miOpenFolder', comment: ['&& denotes a mnemonic'] }, "Open &&Folder...")
 	},
 	order: 2,
-	when: OpenFolderWorkspaceSupportContext
+	// Xpair: not a local IDE — hide the stock "Open Folder" from the File menu (Add Mapping mounts host folders).
+	when: ContextKeyExpr.false()
 });
 
 MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
@@ -363,7 +364,8 @@ MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
 		title: localize({ key: 'miOpenFolder', comment: ['&& denotes a mnemonic'] }, "Open &&Folder...")
 	},
 	order: 2,
-	when: ContextKeyExpr.and(OpenFolderWorkspaceSupportContext.toNegated(), WorkbenchStateContext.isEqualTo('workspace'))
+	// RemotePair: hide the stock "Open Folder" (workspace fallback) from the File menu.
+	when: ContextKeyExpr.false()
 });
 
 MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
@@ -373,7 +375,8 @@ MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
 		title: localize({ key: 'miOpen', comment: ['&& denotes a mnemonic'] }, "&&Open...")
 	},
 	order: 1,
-	when: ContextKeyExpr.and(IsMacNativeContext, OpenFolderWorkspaceSupportContext)
+	// RemotePair: hide the stock macOS "Open..." (file-or-folder) from the File menu.
+	when: ContextKeyExpr.false()
 });
 
 MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
@@ -383,7 +386,8 @@ MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
 		title: localize({ key: 'miOpenWorkspace', comment: ['&& denotes a mnemonic'] }, "Open Wor&&kspace from File...")
 	},
 	order: 3,
-	when: EnterMultiRootWorkspaceSupportContext
+	// RemotePair: hide the stock "Open Workspace from File..." from the File menu.
+	when: ContextKeyExpr.false()
 });
 
 MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
diff --git a/src/vs/workbench/browser/actions/windowActions.ts b/src/vs/workbench/browser/actions/windowActions.ts
index 9db25c4a..0bb4bb9d 100644
--- a/src/vs/workbench/browser/actions/windowActions.ts
+++ b/src/vs/workbench/browser/actions/windowActions.ts
@@ -517,4 +517,6 @@ MenuRegistry.appendMenuItem(MenuId.MenubarFileMenu, {
 	submenu: MenuId.MenubarRecentMenu,
 	group: '2_open',
 	order: 4,
+	// RemotePair: not a local IDE — hide the stock "Open Recent" submenu from the File menu.
+	when: ContextKeyExpr.false()
 });
diff --git a/src/vs/workbench/contrib/markers/browser/markers.contribution.ts b/src/vs/workbench/contrib/markers/browser/markers.contribution.ts
index 43a883c5..df321091 100644
--- a/src/vs/workbench/contrib/markers/browser/markers.contribution.ts
+++ b/src/vs/workbench/contrib/markers/browser/markers.contribution.ts
@@ -689,7 +689,11 @@ class MarkersStatusBarContributions extends Disposable implements IWorkbenchCont
 	}
 }
 
-workbenchRegistry.registerWorkbenchContribution(MarkersStatusBarContributions, LifecyclePhase.Restored);
+// RemotePair C4(b): hide the native "Problems" status bar item — do NOT register the status-bar
+// contribution (the Problems panel/view, file decorations, and marker activity badge are
+// independent and unaffected). `void` keeps the class referenced so noUnusedLocals stays happy.
+void MarkersStatusBarContributions;
+// workbenchRegistry.registerWorkbenchContribution(MarkersStatusBarContributions, LifecyclePhase.Restored);
 
 registerWorkbenchContribution2(MarkerChatContextContribution.ID, MarkerChatContextContribution, WorkbenchPhase.AfterRestored);
 
diff --git a/src/vs/workbench/browser/parts/panel/panelActions.ts b/src/vs/workbench/browser/parts/panel/panelActions.ts
index 10fcbca0..2dbdd0e7 100644
--- a/src/vs/workbench/browser/parts/panel/panelActions.ts
+++ b/src/vs/workbench/browser/parts/panel/panelActions.ts
@@ -71,12 +71,16 @@ export class TogglePanelAction extends Action2 {
 
 registerAction2(TogglePanelAction);
 
+// RemotePair: the bottom panel hosts the always-on Sessions tabs (Attached/Detached/History),
+// which must never be collapsible. Gate the panel title "Hide Panel" (X) button off so the
+// only collapse affordance is gone. (The TogglePanelAction command itself stays registered.)
 MenuRegistry.appendMenuItem(MenuId.PanelTitle, {
 	command: {
 		id: TogglePanelAction.ID,
 		title: localize('closePanel', 'Hide Panel'),
 		icon: closeIcon
 	},
+	when: ContextKeyExpr.false(),
 	group: 'navigation',
 	order: 2
 });

diff --git a/src/vs/workbench/browser/workbench.contribution.ts b/src/vs/workbench/browser/workbench.contribution.ts
index 3c7242ab..d29f74fd 100644
--- a/src/vs/workbench/browser/workbench.contribution.ts
+++ b/src/vs/workbench/browser/workbench.contribution.ts
@@ -623,7 +623,7 @@ const registry = Registry.as<IConfigurationRegistry>(ConfigurationExtensions.Con
 			},
 			'workbench.statusBar.visible': {
 				'type': 'boolean',
-				'default': true,
+				'default': false, // RemotePair: status bar hidden by default (focused xpair surface, no rail/statusbar chrome)
 				'description': localize('statusBarVisibility', "Controls the visibility of the status bar at the bottom of the workbench."),
 				agentsWindow: { default: false, readOnly: true },
 			},
@@ -650,7 +650,7 @@ const registry = Registry.as<IConfigurationRegistry>(ConfigurationExtensions.Con
 			[LayoutSettings.ACTIVITY_BAR_LOCATION]: {
 				'type': 'string',
 				'enum': ['default', 'top', 'bottom', 'hidden'],
-				'default': 'default',
+				'default': 'hidden', // RemotePair: no activity-bar rail; Sessions is the only, fixed primary sidebar
 				'markdownDescription': localize({ comment: ['This is the description for a setting'], key: 'activityBarLocation' }, "Controls the location of the Activity Bar relative to the Primary and Secondary Side Bars."),
 				'enumDescriptions': [
 					localize('workbench.activityBar.location.default', "Show the Activity Bar on the side of the Primary Side Bar and on top of the Secondary Side Bar."),
@@ -861,7 +861,7 @@ const registry = Registry.as<IConfigurationRegistry>(ConfigurationExtensions.Con
 			},
 			[LayoutSettings.LAYOUT_ACTIONS]: {
 				'type': 'boolean',
-				'default': true,
+				'default': false, // RemotePair: hide the title-bar layout toggles (sidebar/panel are fixed open)
 				'markdownDescription': isWeb ?
 					localize('layoutControlEnabledWeb', "Controls whether the layout control in the title bar is shown.") :
 					localize({ key: 'layoutControlEnabled', comment: ['{0}, {1} is a placeholder for a setting identifier.'] }, "Controls whether the layout control is shown in the custom title bar. This setting only has an effect when {0} is not set to {1}.", '`#window.customTitleBarVisibility#`', '`never`'),
