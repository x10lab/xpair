# 312211

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 312211 | session base가 계산됨 | host 이름, readable folder, path hash로 안정적인 base 생성 | `REMOTE_PROJ=<host>_<slug>_<hash>` | continue |
| 3122110 | 사용자가 session base 계산 직후 터미널을 닫음 | session 상태 조회, tmux 생성, attach 없이 종료 | remote setup 본문 실행 전이면 no session | terminate |
| 3122111 | launcher가 계산된 base를 실행 이름으로 확정함 | 이후 session 번호는 같은 base 안에서만 고름 | host alias, folder slug, hash가 session prefix로 고정됨 | continue |
| 31221110 | base 확정 중 사용자가 종료함 | session 목록 조회 없이 종료 | session prefix만 계산되고 host 변경 없음 | terminate |
| 31221111 | base가 현재 mapped project와 일치함 | 같은 project session 조회로 진행 | `REMOTE_PROJ`와 `HOST_DIR`가 같은 mapping 결과를 가리킴 | continue |
| 312211110 | 일치 확인 뒤 사용자가 종료함 | 기존 session 조회나 생성 없이 종료 | remote tmux 호출 전 종료 | terminate |
| 312211111 | session 조회에 쓸 base가 준비됨 | no session, detached, attached 상태 판별로 넘어감 | base 이름으로 `_1`, `_2+` 후보를 확인할 준비 완료 | continue |
| 312211112 | base가 다른 mapped project와 충돌함 | 잘못된 project attach를 막고 remote setup 실패 안내로 이동 | hash 충돌이나 stale mapping이면 안전하게 실패 처리 필요 | route to: 314 |
| 31221112 | base가 Xpair launch 범위 밖의 이름으로 해석됨 | 접근 불가로 끝내고 VSCodium 기본 표면 탐색을 막음 | CLI launch branch 밖 표면은 이 흐름에서 다루지 않음 | terminate |
| 3122112 | launcher가 같은 base의 local attached 상태를 확인함 | 이미 붙은 낮은 번호가 있으면 fresh 번호로, 없으면 remote 상태 조회로 진행 | local `mosh-client`와 terminal attach 흔적을 기준으로 분기 | continue |
| 31221120 | attached 상태 확인 전에 사용자가 종료함 | 새 session 생성이나 attach 없이 종료 | local 확인 전 종료라 host side effect 없음 | terminate |
| 31221121 | 같은 base의 live client attach가 없음 | remote tmux의 no session 또는 detached 상태를 확인 | `_remote_next_n`이 낮은 번호부터 확인 가능 | continue |
| 312211210 | live attach 없음 확인 뒤 사용자가 종료함 | remote tmux 조회 없이 종료 | local 판별만 끝나고 no session | terminate |
| 312211211 | remote session 상태 조회로 진행함 | 기존 `_1`이 없으면 생성, detached면 take-over attach 판단으로 넘어감 | `tm has-session` 계열 확인 단계로 이동 | continue |
| 312211212 | local attach 판별 결과가 stale임 | 안전하게 remote 상태를 다시 묻거나 실패 복구로 보냄 | stale client 흔적은 실제 tmux 상태와 불일치할 수 있음 | route to: 3123 |
| 31221122 | 같은 base의 낮은 번호가 이미 client에 attached임 | 다음 번호 fresh session 생성 흐름으로 이동 | live attach 감지 시 `_2+`를 선택 | route to: 31232 |
| 3122113 | remote setup이 base와 host dir을 함께 받음 | host path에서 agent session을 만들거나 붙을 준비를 함 | remote script 입력값에 `REMOTE_PROJ`와 `HOST_DIR`가 전달됨 | continue |
| 31221130 | remote setup 입력 전달 전에 사용자가 종료함 | remote script 실행 없이 종료 | SSH command 실행 전이면 no session | terminate |
| 31221131 | host가 같은 base와 host dir을 수신함 | tmux-aqua session 목록 조회로 진행 | remote side에서 project cwd와 session prefix가 같은 값으로 쓰임 | continue |
| 312211310 | host 수신 직후 사용자가 종료함 | session 생성 없이 종료 | remote script 초기에 끊기면 no session | terminate |
| 312211311 | host에서 session 상태 조회가 시작됨 | no session, detached, attached 여부를 판별함 | `tmux-aqua` 조회 결과로 다음 branch를 고름 | continue |
| 312211312 | host가 base 또는 host dir을 사용할 수 없음 | remote setup 실패 안내로 이동 | quoting, 권한, cwd 실패는 setup 실패로 처리 | route to: 314 |
| 31221132 | remote setup 입력이 XpairHost-first 조작을 요구함 | Xpair 기준 launch 흐름에서는 접근 불가로 끝냄 | Host-first 조작은 이 base에서 새 하위 branch를 만들지 않음 | terminate |

다음 실행 ID
- 312211111
- 312211211
- 312211311
