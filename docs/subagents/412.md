# 412

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 412 | Host onboarding에서 권한과 엔진을 통과해 Connect/Done 범위에 도달함 | 클라이언트가 없으면 대기 안내에 머물고, 클라이언트가 보이면 Done으로 넘어갈 수 있음 | Connect는 3초마다 연결 클라이언트를 조회하고 Done은 완료 버튼을 제공 | continue |
| 4120 | 사용자가 Connect 또는 Done 범위에서 창을 닫음 | 완료 전이면 host serving을 시작하지 않고 flow 종료 | runGate에서 권한이 이미 있으면 종료하지 않을 수 있지만 `complete`가 없으면 `startServing`도 호출되지 않음 | terminate |
| 4121 | Connect 단계가 표시됨 | 다른 Mac에서 Xpair를 열라는 안내와 현재 Mac 이름을 보여줌 | `Waiting for a client...`, hostname 카드, 자동 연결 안내가 표시됨 | continue |
| 41210 | Connect 단계에서 사용자가 아무 조작 없이 창을 닫음 | client 연결, Done 전환, host serving 없이 끝남 | 권한 완료 상태에서는 앱이 남을 수 있으나 온보딩 완료 처리는 없음 | terminate |
| 41211 | 연결된 Xpair 클라이언트가 없음 | 대기 상태를 유지하고 클라이언트 실행 안내를 계속 보여줌 | `connectedClients()` 결과가 빈 배열이면 레이더와 대기 문구를 유지 | continue |
| 412110 | 사용자가 안내만 보고 기다림 | 새 클라이언트 heartbeat가 오기 전까지 화면 변화 없음 | 점 애니메이션만 바뀌고 클라이언트 목록은 비어 있음 | terminate |
| 412111 | 사용자가 안내에 따라 다른 Mac에서 Xpair 클라이언트를 실행함 | 이후 흐름은 Xpair 클라이언트 실행 상태에서 판단 | Host 창은 계속 polling하고 클라이언트 쪽 첫 실행 또는 기존 설정으로 분기 | route to: 1 |
| 412112 | 사용자가 Connect 화면에서 host 이름만 확인함 | 클라이언트 설정에 쓸 수 있는 host 이름을 확인하고 대기 유지 | 현재 Mac hostname만 표시되고 별도 복사 버튼은 없음 | terminate |
| 41212 | Connect 단계의 자동 새로고침이 돈다 | 연결 상태를 주기적으로 다시 확인함 | 3초마다 `connectedClients()`를 호출하고 실패는 화면 오류로 노출하지 않음 | continue |
| 412123 | 새로고침 결과가 계속 빈 목록임 | 대기 안내를 유지하고 다음 poll을 기다림 | 빈 배열이면 `Waiting for a client...` 상태 유지 | continue |
| 412121 | 새로고침 호출이 실패하거나 응답을 못 받음 | 대기 화면을 깨지 않고 다음 새로고침을 기다림 | catch가 오류를 무시해 직전 목록 상태가 유지됨 | continue |
| 412122 | 클라이언트 heartbeat가 90초보다 오래됨 | 연결된 클라이언트로 보지 않고 대기 상태로 유지 | freshness window 밖 파일은 목록에서 제외됨 | continue |
| 41213 | 사용자가 client setup 안내 행동을 찾음 | Xpair 클라이언트를 직접 열거나 README 설치 흐름으로 이동해야 함 | Connect 화면 자체에는 설치 실행, 복사, 열기 버튼이 없음 | continue |
| 412130 | 사용자가 안내 화면에서 설치 버튼을 기대함 | 접근 불가, Host onboarding은 client 설치를 실행하지 않음 | 해당 버튼 없음 | terminate |
| 412131 | 사용자가 Xpair IDE를 직접 엶 | 클라이언트 앱 flow로 이동 | Host는 대기 상태를 유지하고 클라이언트 heartbeat를 기다림 | route to: 1 |
| 412132 | 사용자가 `xpair` CLI 또는 Finder Quick Action으로 연결을 시도함 | 클라이언트 launch flow로 이동 | Host는 heartbeat가 생기면 연결 표시로 바뀜 | route to: 31 |
| 412133 | 사용자가 Host 화면에서 client를 원격 설치하려 함 | 접근 불가, Xpair 기준 client 쪽 설치/실행에서 처리해야 함 | Host Connect 화면은 원격 client 설치 기능 없음 | terminate |
| 4122 | 연결된 클라이언트 heartbeat가 나타남 | Connect 화면이 연결 완료 상태로 바뀌고 Done으로 진행 가능해야 함 | 최근 90초 안의 client 파일을 이름순 목록으로 표시 | continue |
| 41220 | 클라이언트가 연결됐지만 poll 전임 | 다음 새로고침 전까지 대기 화면 유지 | 즉시 push가 아니라 다음 3초 poll에서 반영 | terminate |
| 41221 | poll이 연결된 클라이언트를 감지함 | 연결 완료 문구와 연결된 Mac 정보를 표시 | `Client connected` 또는 `{n} clients connected`와 name/user 카드 표시 | continue |
| 412213 | 연결 클라이언트가 1대임 | 단일 클라이언트 연결 상태를 표시하고 사용자는 Next를 누를 수 있음 | 단수 제목과 클라이언트 카드 1개 표시 | continue |
| 412211 | 연결 클라이언트가 여러 대임 | 여러 클라이언트 연결 상태를 표시하고 사용자는 Next를 누를 수 있음 | 복수 제목과 각 클라이언트 카드 표시 | continue |
| 412212 | 표시된 클라이언트 heartbeat가 끊김 | stale 처리 뒤 다시 대기 상태로 돌아감 | 다음 poll에서 목록이 비면 대기 화면으로 바뀜 | route to: 41212 |
| 41222 | 사용자가 Connect에서 Next를 누름 | Done 단계로 이동 | Connect 단계의 Next는 연결 여부와 무관하게 활성화됨 | continue |
| 412220 | 사용자가 Next를 누르기 전에 중단함 | Done으로 이동하지 않음 | Connect 화면 상태만 남음 | terminate |
| 412221 | 클라이언트가 없는 상태에서 Next를 누름 | Host ready Done으로 이동하고 나중에 pair 가능하다고 안내 | 연결 여부 gate 없이 Done 단계로 이동 | continue |
| 412222 | 클라이언트가 표시된 상태에서 Next를 누름 | paired Done으로 이동해야 함 | `StepDone`에 `paired`가 전달되지 않아 항상 `Host is ready` 문구 | continue |
| 41223 | Connect에서 Previous를 누름 | Engine 단계로 돌아가 선택 엔진을 다시 볼 수 있음 | Wizard previous가 Engine step으로 이동 | route to: 411 |
| 4123 | Done 단계가 표시됨 | 완료 전 마지막 확인과 선택 동의 변경, 완료 버튼을 제공 | `Host is ready`, 동의 요약, `Open Xpair` 버튼 표시 | continue |
| 41230 | Done 단계에서 사용자가 창만 닫음 | 완료 처리를 하지 않았으므로 serving 시작 없이 끝남 | `complete`가 호출되지 않으면 `onComplete`와 `startServing`이 실행되지 않음 | terminate |
| 41231 | 사용자가 Done 내용을 확인함 | XpairHost가 메뉴바에서 실행될 예정임을 이해하고 완료할 수 있음 | 메뉴바 관리 안내와 선택 동의 요약이 표시됨 | continue |
| 412313 | 사용자가 Done에서 익명 사용 분석 체크를 바꿈 | 선택 상태를 저장하고 Done 화면에 머무름 | `setConsent`가 UserDefaults에 telemetry 값을 저장 | continue |
| 412311 | 사용자가 Done에서 crash report 체크를 바꿈 | 선택 상태를 저장하고 Done 화면에 머무름 | `setConsent`가 UserDefaults에 crash 값을 저장 | continue |
| 412312 | 사용자가 연결된 클라이언트가 있는데 paired 문구를 기대함 | 연결됨을 반영한 완료 문구가 나와야 함 | `paired` 기본값 false라 `Host is ready`로 표시 | terminate |
| 41232 | 사용자가 `Open Xpair` 완료 버튼을 누름 | 온보딩을 완료하고 Host serving을 시작해야 함 | 버튼은 `window.xpair.complete()`를 호출 | continue |
| 412323 | complete 호출이 성공함 | 창을 닫고 메뉴바 앱으로 전환한 뒤 host serving을 시작 | `finish()`가 창을 닫고 accessory 전환 후 `startServing()` 콜백 실행 | route to: 42 |
| 412321 | complete 뒤 host serving이 시작됨 | tmux host, screen sidecar, host 광고, watchdog이 켜짐 | `host.ensureServer()`, host role 광고, 5초 watchdog 실행 | route to: 42 |
| 412322 | 사용자가 버튼이 Xpair 클라이언트를 직접 열 것으로 기대함 | Host 완료와 client 실행은 분리되어야 하며 client는 별도로 열어야 함 | 버튼 라벨은 `Open Xpair`지만 실제로는 Host onboarding 완료만 수행 | route to: 1 |
| 41233 | 완료 뒤 메뉴바 상태를 확인함 | XpairHost 메뉴에서 권한, screen share, clients, sessions, Connect를 볼 수 있음 | 완료 후 accessory 앱으로 돌아가 메뉴가 동적으로 재구성됨 | route to: 42 |

다음 실행 ID
- 412123
- 412121
- 412122
- 412213
- 412211
- 412221
- 412222
- 412313
- 412311
