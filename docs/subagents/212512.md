# 212512

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 212512 | dir check 결과 marker가 깨지거나 비어 있어 남은 재시도를 계속함 | 사용자는 같은 Finder Quick Action 터미널에서 재시도 메시지를 봄 | marker가 없으면 attempt 메시지를 출력하고 retry budget을 소모함 | continue |
| 2125120 | 재시도 전에 사용자가 터미널을 닫거나 중단함 | host dir 판단, fallback, session 생성 없이 종료 | 이후 attempt가 실행되지 않음 | terminate |
| 2125121 | 다음 dir check 재시도가 시작됨 | host path 존재 여부를 다시 확인함 | 같은 host와 mapped path로 SSH dir check를 다시 실행함 | continue |
| 21251210 | 재시도 실행 중 사용자가 터미널을 닫음 | partial output만 남고 remote session은 생성되지 않음 | retry loop가 중단됨 | terminate |
| 21251211 | 재시도에서 `__YES__` marker를 받음 | host dir이 존재하므로 remote setup으로 진행 | marker를 인식하고 remote setup branch로 이동 | route to: 211311 |
| 21251212 | 재시도에서 `__NO__` marker를 받음 | stale host path 처리로 이동 | create 또는 remap 안내 branch로 이동 | route to: 2122 |
| 21251213 | 재시도에서도 marker가 깨지거나 비어 있음 | 마지막 재시도까지 계속함 | attempt 메시지를 다시 출력하고 retry budget을 소모함 | continue |
| 212512130 | 마지막 재시도 전에 사용자가 터미널을 닫음 | host dir 판단, fallback, session 생성 없이 종료 | retry loop가 중단됨 | terminate |
| 212512131 | 마지막 재시도에서 `__YES__` marker를 받음 | host dir이 존재하므로 remote setup으로 진행 | marker를 인식하고 remote setup branch로 이동 | route to: 211311 |
| 212512132 | 마지막 재시도에서 `__NO__` marker를 받음 | stale host path 처리로 이동 | create 또는 remap 안내 branch로 이동 | route to: 2122 |
| 212512133 | 마지막 재시도에서도 marker가 깨지거나 비어 있음 | host dir check 실패로 보고 local fallback으로 이동 | 3회 실패 처리 후 fallback branch로 이동 | route to: 21252 |
| 212512134 | 마지막 재시도에서 SSH transport 오류가 표시됨 | host가 불안정하므로 local fallback으로 이동 | dir-check 실패 budget을 모두 소모한 뒤 fallback branch로 이동 | route to: 21252 |
| 2125122 | marker는 없지만 사용자가 remote session 생성을 기대함 | 접근 불가, dir 존재 여부를 확정하기 전엔 attach하지 않음 | session marker나 attach 단계로 넘어가지 않음 | terminate |
| 2125123 | 사용자가 같은 Finder Quick Action을 새로 다시 실행함 | 새 실행은 stale mapping 또는 unreachable host flow를 처음부터 다시 판단 | 이전 retry 상태를 이어받지 않고 launch를 새로 시작함 | route to: 212 |

다음 실행 ID
없음
