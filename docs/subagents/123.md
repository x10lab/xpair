# 123

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 123 | 이미 설정된 Xpair workbench가 reachable host와 기존 세션을 가진 상태로 열림 | 사용자는 Sessions의 Attached, Detached, History, 새 세션 진입점에서 기존 세션을 확인하거나 이어 붙음 | 하위 항목에서 검증 | continue |
| 1230 | 사용자가 세션 표면을 조작하지 않고 앱을 둠 | 기존 세션은 host tmux에 남고 새 attach, detach, kill은 발생하지 않음 | 동일 | terminate |
| 1231 | 사용자가 하단 Session Manager의 Attached 탭을 선택함 | 현재 Xpair IDE 안에 붙어 있는 terminal session 카드만 표시됨 | `attachedProvider.getAttached()` 또는 terminal fallback으로 렌더링 | continue |
| 12310 | Attached 탭을 보고 아무 카드도 선택하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 12311 | Attached 카드 목록이 보임 | 각 카드는 세션 제목, active 강조, 닫기 X를 제공함 | `RemotePairTerminalSidebarView.attached` map 기준으로 표시 | continue |
| 123110 | Attached 카드 위에 머물고 조작하지 않음 | active 강조만 유지되고 세션 상태 변화 없음 | 동일 | terminate |
| 123111 | Attached 카드 본문을 클릭함 | 해당 terminal editor가 다시 열리고 terminal focus가 돌아감 | `provider.reveal(id)`가 `group.openEditor()`와 `instance.focus(true)`를 실행 | continue |
| 123112 | Attached 카드의 닫기 X를 클릭함 | IDE 안 terminal attach가 닫히고 Attached 목록에서 빠짐, host tmux 세션은 detached 상태로 남아야 함 | `instance.dispose()` 뒤 attached map에서 제거됨 | route to: 1232 |
| 123113 | Attached 카드에서 Enter 또는 Space를 누름 | 카드 클릭과 같은 reveal 동작으로 처리됨 | key handler가 click handler를 호출 | route to: 123111 |
| 12312 | Attached 탭이 비어 있지만 기존 세션이 Detached 또는 History에 있음 | `No attached sessions`를 보여주고 다른 탭에서 이어 붙을 수 있어야 함 | attached provider가 빈 배열이면 빈 상태 표시 | continue |
| 123120 | 빈 Attached 탭에서 멈춤 | 세션 상태 변화 없음 | 동일 | terminate |
| 123121 | 빈 Attached 탭에서 Detached 탭으로 이동함 | detached 세션 목록 확인으로 넘어감 | 하단 패널 탭 전환 | route to: 1232 |
| 123122 | 빈 Attached 탭에서 History 탭으로 이동함 | history 목록 확인으로 넘어감 | 하단 패널 탭 전환 | route to: 1233 |
| 12313 | Attached provider 또는 렌더러 상태가 실제 attached 세션과 어긋남 | 실제 attached 세션이 있으면 카드가 보여야 함 | provider map에 없으면 하단 Attached에는 보이지 않음 | continue |
| 123130 | 실제 attached 세션이 있는데 Attached 탭이 비어 있음 | 사용자가 세션을 잃어버린 것으로 보이지 않게 재동기화해야 함 | 자동 복구 경로 미확인 | terminate |
| 1232 | 사용자가 Detached 탭을 선택함 | host 또는 provider가 알려준 detached session 이름을 reattach 카드로 표시함 | `dataProvider.getDetached()`가 없으면 빈 상태 | continue |
| 12320 | Detached 탭에서 아무 카드도 선택하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 12321 | Detached provider가 live detached 세션 이름을 제공함 | 각 이름이 클릭 가능한 reattach 카드로 표시됨 | 이름별 `addCard(name, () => reattach(name))` | continue |
| 123210 | live detached 카드가 보이지만 사용자가 선택하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 123211 | live detached 카드를 클릭함 | 기존 host tmux 세션을 Xpair IDE terminal로 다시 attach해야 함 | terminal을 만들고 `xpair attach <name>`을 전송하지만 CLI dispatch에 `attach` 명령이 없어 `unknown command`로 끝남 | terminate |
| 123212 | live detached 카드에서 Enter 또는 Space를 누름 | 클릭과 같은 reattach 동작이어야 함 | key handler가 같은 reattach를 호출 | route to: 123211 |
| 123213 | 여러 detached 카드 중 다른 카드를 선택함 | 선택한 이름의 세션만 reattach 대상이어야 함 | 선택한 name을 그대로 `xpair attach <name>`에 넣음 | route to: 123211 |
| 12322 | Detached provider가 없거나 등록되지 않음 | reachable host에 기존 detached 세션이 있으면 목록을 가져와야 함 | `dataProvider` 기본값이 없어 `No detached sessions` 표시 | continue |
| 123220 | 기존 detached 세션이 있는데 빈 Detached 탭만 보임 | 사용자가 reattach 대상을 볼 수 있어야 함 | provider 미등록 상태에서는 숨겨짐 | terminate |
| 123221 | 빈 Detached 탭에서 History 탭으로 이동함 | 마지막으로 본 세션 또는 live history 확인으로 넘어감 | 하단 패널 탭 전환 | route to: 1233 |
| 123222 | 빈 Detached 탭에서 새 세션을 시작함 | 기존 세션이 안 보이는 상태에서도 새 session launch는 가능해야 함 | `+` 또는 Browser folder action 경로로 이동 | route to: 1234 |
| 12323 | Detached provider가 stale 세션 이름을 제공함 | stale 항목은 클릭 전에 제거되거나 클릭 시 명확한 실패로 끝나야 함 | 현재는 stale 여부를 검증하지 않고 reattach hook으로 전달 | continue |
| 123230 | stale detached 카드를 클릭함 | 세션 없음 오류를 보여주고 카드 갱신 또는 제거가 필요함 | `xpair attach <name>` 자체가 미구현이라 unknown command로 끝남 | terminate |
| 123231 | stale detached 카드를 선택하지 않고 무시함 | 세션 상태 변화 없음 | 동일 | terminate |
| 123232 | stale 이름이 History에도 남아 있어 History 탭으로 이동함 | history 쪽 stale 처리로 넘어감 | 하단 패널 탭 전환 | route to: 1233 |
| 1233 | 사용자가 History 탭을 선택함 | live provider history가 있으면 reattach 가능, fallback 저장 history면 표시와 삭제만 가능해야 함 | provider 유무에 따라 클릭 가능 카드 또는 readonly 카드 표시 | continue |
| 12330 | History 탭에서 아무 항목도 선택하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 12331 | live provider가 history 세션 이름을 제공함 | 각 history 이름은 실제 reattach 가능한 카드여야 함 | `getHistory()` 값이 있으면 클릭 카드로 렌더링 | continue |
| 123310 | live history 카드를 선택하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 123311 | live history 카드를 클릭함 | 해당 세션을 다시 attach해야 함 | detached reattach와 같은 `xpair attach <name>` 미구현 경로 | route to: 123211 |
| 123312 | live history 카드에서 Enter 또는 Space를 누름 | 클릭과 같은 reattach 동작이어야 함 | key handler가 같은 reattach 호출 | route to: 123211 |
| 123313 | live history 이름이 stale인 상태에서 클릭함 | 세션 없음 오류와 갱신이 필요함 | stale 검증 없이 reattach로 전달 | route to: 123230 |
| 12332 | live provider가 없고 workspace 저장 history만 있음 | 읽기 전용 카드로 보여주고 body 클릭으로 reattach하지 않아야 함 | `remotepair.sessionManager.history` 값을 readonly 카드로 표시 | continue |
| 123320 | readonly history 카드 본문을 클릭함 | 아무 동작도 하지 않음 | click listener 없음 | terminate |
| 123321 | readonly history 카드의 X를 클릭함 | 해당 이름을 local history에서 제거함 | storage 값에서 제거 후 History 탭 재렌더링 | terminate |
| 123322 | readonly history 카드에서 reattach를 기대함 | 접근 불가, 저장 title은 실제 attach 이름이 아님 | 의도대로 reattach 동작 없음 | terminate |
| 12333 | History 탭이 비어 있음 | `No history`를 표시하고 다른 세션 동작으로 이동할 수 있어야 함 | 저장값과 provider 값이 모두 없으면 빈 상태 | continue |
| 123330 | 빈 History에서 멈춤 | 세션 상태 변화 없음 | 동일 | terminate |
| 123331 | 빈 History에서 Attached 탭으로 이동함 | attached 확인으로 돌아감 | 하단 패널 탭 전환 | route to: 1231 |
| 123332 | 빈 History에서 Detached 탭으로 이동함 | detached 확인으로 이동 | 하단 패널 탭 전환 | route to: 1232 |
| 123333 | 빈 History에서 새 세션을 시작함 | 새 session launch 경로로 이동 | `+` 또는 Browser folder action 경로로 이동 | route to: 1234 |
| 1234 | 사용자가 Sessions 상단 `+` 또는 Browser의 `New Session Here`를 통해 다른 세션 시작을 시도함 | Browser에서 폴더를 고르고 `xpair launch`로 새 terminal session을 열어야 함 | `+`는 Browser 컨테이너를 열고 folder action은 terminal에 `xpair launch` 전송 | continue |
| 12340 | `+`를 눌렀지만 폴더를 고르지 않음 | 기존 세션 상태 변화 없음 | Browser 표면에 머무름 | terminate |
| 12341 | `+`가 Browser 컨테이너를 열어 폴더 선택 상태가 됨 | 사용자는 폴더별 `New Session Here`만 세션 시작 동작으로 사용함 | Browser viewlet으로 전환 | continue |
| 123410 | Browser에서 아무 폴더도 선택하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 123411 | mapped folder의 `New Session Here`를 클릭함 | Sessions로 돌아와 terminal을 열고 `xpair launch`를 실행함 | `openSessionInFolder(cwd)`가 terminal cwd를 잡고 `xpair launch`를 전송 | continue |
| 123412 | unmapped 또는 stale folder에서 `New Session Here`를 클릭함 | mapping 또는 host dir 확인 흐름으로 넘어가야 함 | launcher의 map, dir check, fallback 경로로 넘어감 | route to: 313 |
| 12342 | Browser 안에서 세션 시작과 무관한 VSCodium 기능을 선택함 | Xpair userflow에서는 접근 불가로 보고 하위 탐색하지 않음 | Browser 기본 기능은 남아 있을 수 있음 | continue |
| 123420 | Search, Extensions, SCM 같은 비세션 표면으로 빠짐 | 접근 불가 | VSCodium 기능이 노출되면 Xpair 범위 밖 | terminate |
| 123421 | Browser의 Back to Sessions로 돌아감 | Sessions 표면으로 복귀 | Sessions view container를 다시 엶 | route to: 1231 |
| 123422 | 파일 열기나 편집을 시도함 | 세션 시작 flow가 아니므로 접근 불가 | Browser가 기본 파일 열기를 수행할 수 있음 | terminate |
| 12343 | 기존 세션이 있는 같은 project에서 새 세션을 시작함 | live attach가 있으면 `_2` 이상 fresh, detached `_1`이면 take over attach가 되어야 함 | `xpair launch`의 `_remote_next_n`과 `attach -d` 정책이 처리 | continue |
| 123430 | 같은 project 새 세션 선택 직전 멈춤 | 세션 상태 변화 없음 | 동일 | terminate |
| 123431 | 같은 project의 `_1`이 현재 attached라 새 세션을 시작함 | `_2` 이상 새 대화로 열림 | live mosh client가 있으면 다음 번호와 `CL_CONTINUE=0` 선택 | continue |
| 123432 | 같은 project의 `_1`이 detached라 새 세션을 시작함 | 기존 `_1`을 `attach -d`로 take over함 | host tmux session이 있으면 `NEED_CREATE=0` | continue |
| 123433 | session list provider가 stale이어도 새 세션을 시작함 | provider 표시와 무관하게 launcher가 host tmux 사실을 기준으로 결정해야 함 | `xpair launch`가 SSH와 tmux facts를 다시 확인 | continue |
| 1235 | reachable host인데 session provider 상태가 실제 host session 상태와 맞지 않음 | 표면은 stale, provider missing, orphan attached를 구분해야 함 | core renderer는 provider 값을 신뢰하거나 없으면 빈 상태 | continue |
| 12350 | stale 또는 missing provider 상태를 보고 조작하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 12351 | provider가 없어 Detached와 live History를 만들 수 없음 | 기존 host 세션을 숨기지 않는 별도 로딩 또는 오류가 필요함 | `No detached sessions`, fallback history만 표시 | continue |
| 123510 | provider 미등록 상태에서 detached 세션 확인을 끝냄 | reattach 가능한 세션이 숨겨졌음을 알려야 함 | 조용히 빈 상태 | terminate |
| 123511 | provider 미등록 상태에서 새 세션을 시작함 | 기존 세션 목록 없이도 launch 가능 | Browser 또는 `xpair launch` 경로로 이동 | route to: 1234 |
| 123512 | provider 미등록 상태에서 host status를 확인하고 Sessions로 돌아옴 | reachable이면 session 목록 재조회가 필요함 | status button은 reachability만 표시하고 list provider를 채우지 않음 | route to: 1231 |
| 12352 | provider가 stale detached 또는 stale history 이름을 계속 제공함 | stale 항목은 클릭 시 실패를 명확히 드러내고 갱신되어야 함 | 렌더러는 이름을 그대로 카드화 | continue |
| 123520 | stale provider 카드를 보고 조작하지 않음 | 세션 상태 변화 없음 | 동일 | terminate |
| 123521 | stale provider 카드를 클릭함 | stale 세션 오류 후 목록 갱신 | 현재 reattach 미구현 또는 stale 검증 부재로 실패 | route to: 123230 |
| 123522 | stale history readonly 항목을 제거함 | local 저장 history에서만 제거됨 | X 버튼으로 storage 제거 | route to: 123321 |
| 123523 | stale provider를 새로고침하려고 탭을 다시 엶 | provider가 갱신되면 stale 카드가 사라져야 함 | core view에는 host 재조회 트리거가 없음 | terminate |
| 12353 | Attached 카드가 보이지만 내부 attached map에는 대상 terminal이 없음 | orphan 카드는 보이지 않거나 자동 제거되어야 함 | reveal과 close 모두 map miss면 실질 동작 없음 | continue |
| 123530 | orphan Attached 카드 본문을 클릭함 | 카드 제거 또는 오류 표시가 필요함 | `this.attached.get(id)` 실패 시 no-op | terminate |
| 123531 | orphan Attached 카드 X를 클릭함 | 카드 제거가 필요함 | map miss면 dispose할 instance가 없어 no-op | terminate |
| 123532 | orphan Attached 상태에서 Detached로 이동함 | host tmux 기준 세션 확인으로 넘어감 | 하단 패널 탭 전환 | route to: 1232 |

다음 실행 ID
- 123111
- 123411
- 123431
- 123432
- 123433
