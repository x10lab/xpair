# 211222

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 211222 | 같은 project의 `_1`이 없거나 detached임 | `_1` 생성 또는 take-over attach로 진행 | remote tmux 존재 시 attach, 없으면 new-session | continue |
| 2112220 | 사용자가 `_1` 상태 확인 뒤 터미널을 닫음 | 새 session 생성 또는 attach 없이 종료 | setup script 실행 전이면 변화 없음 | terminate |
| 2112221 | host에 같은 project의 `_1` session이 없음 | `_1` tmux session을 새로 만들 준비를 함 | `tm has-session` 실패 시 create 경로로 감 | continue |
| 21122210 | 사용자가 새 session 생성 전에 종료함 | host tmux session을 만들지 않고 종료 | setup script 중단 시 no session | terminate |
| 21122211 | XpairHost tmux-aqua server와 agent CLI가 준비됨 | mapped host path에서 `_1` session 생성으로 진행 | server와 CLI 확인 후 `tm new-session` 경로 사용 | continue |
| 211222110 | 사용자가 생성 명령 직후 attach 전에 종료함 | 생성 전이면 no session, 생성 후면 detached session으로 남음 | 타이밍에 따라 host session 잔존 가능 | terminate |
| 211222111 | 새 `_1` session 생성이 성공함 | terminal title을 `_1` session으로 맞추고 attach 단계로 진행 | `__SESSION__:<name>` 반환 후 attach 실행 | continue |
| 211222112 | 새 `_1` session 생성이 실패함 | CLI remote setup 실패 안내로 이동 | session marker 없거나 SSH_RC 실패 시 die | route to: 314 |
| 21122212 | XpairHost tmux-aqua server 또는 agent CLI가 준비되지 않음 | host app, 권한, agent 설치 안내 후 실패 흐름으로 이동 | exit 11 또는 exit 13 계열 오류 | route to: 314 |
| 2112222 | host에 같은 project의 `_1` session이 detached로 있음 | 기존 대화에 take-over attach할 준비를 함 | `NEED_CREATE=0`으로 기존 session attach | continue |
| 21122220 | 사용자가 detached session attach 전에 종료함 | 기존 host session은 detached 상태로 남음 | attach 실행 전 종료 | terminate |
| 21122221 | `mosh`가 설치되어 있음 | mosh로 기존 `_1` tmux session에 attach | `mosh --server ... attach -d` 사용 | continue |
| 211222210 | mosh attach 직전에 사용자가 종료함 | 기존 session은 detached 상태로 유지 | foreground attach 전 종료 | terminate |
| 211222211 | mosh attach가 성공함 | 사용자는 기존 `_1` agent 화면으로 복귀 | mosh roaming 가능한 attach 화면 표시 | continue |
| 211222212 | mosh attach가 실패함 | ssh fallback 또는 SSH 실패 흐름으로 분기 | 실패 원인에 따라 fallback 또는 오류 | route to: 21122222 |
| 21122222 | `mosh`가 없거나 사용할 수 없음 | ssh TTY로 기존 `_1` tmux session에 attach | `ssh -t ... tmux-aqua attach -d` fallback | continue |
| 211222220 | ssh attach 직전에 사용자가 종료함 | 기존 session은 detached 상태로 유지 | foreground attach 전 종료 | terminate |
| 211222221 | ssh fallback attach가 성공함 | 사용자는 기존 `_1` agent 화면으로 복귀 | ssh TTY attach 화면 표시 | continue |
| 211222222 | ssh fallback attach가 실패함 | SSH/auth/host reachability 실패 흐름으로 이동 | ssh attach 실패 시 오류 종료 | route to: 314 |
| 21122223 | detached로 보였던 `_1` session이 attach 직전 사라짐 | 같은 project의 fresh `_1` 생성 흐름으로 이동 | race 또는 cleanup 뒤 `tm has-session` 실패 | route to: 2112221 |
| 2112223 | 상태 확인 사이에 낮은 `_1`이 이미 client tab에 붙음 | `_2+` fresh session 선택 흐름으로 이동 | local live attach 감지 시 다음 번호 선택 | route to: 211221 |

다음 실행 ID
- 211222111
- 211222211
- 211222221
