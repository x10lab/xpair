# 211221

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 211221 | 같은 project의 낮은 `_N`이 client tab에 붙어 있음 | 붙어 있는 낮은 번호 세션을 건드리지 않고 새 `_N` fresh 세션을 준비 | local `mosh-client` fact로 다음 번호를 고르고 `_2+`는 `CL_CONTINUE=0` | continue |
| 2112210 | 사용자가 새 번호 계산 전에 터미널 또는 실행 표면을 닫음 | 새 session 생성 없이 기존 attached session만 유지 | remote setup 전 종료면 추가 변화 없음 | terminate |
| 2112211 | Xpair가 낮은 `_N`의 live attached client를 확인함 | 가장 낮은 빈 다음 번호를 fresh 대상 session으로 선택 | live `mosh-client`가 붙은 번호를 건너뛰어 `_2+` 선택 | continue |
| 2112212 | live attached 표시가 stale로 판명됨 | 새 번호를 만들지 않고 기존 `_1` 생성 또는 detached takeover 판단으로 이동 | host tmux 사실 기준으로 다시 분기 가능 | route to: 211222 |
| 2112213 | 사용자가 이미 attached된 낮은 번호를 강제로 takeover하려 함 | 현재 붙어 있는 client를 끊지 않고 fresh 번호 분기로만 진행 | attached 충돌은 takeover가 아니라 `_2+` 선택으로 처리 | route to: 21143 |
| 21122110 | live client 확인 중 사용자가 종료함 | 새 번호 선택과 remote setup 없이 종료 | 기존 attached session은 유지 | terminate |
| 21122111 | 빈 다음 번호가 선택됨 | 선택된 `_N`을 이전 대화 재개 없이 fresh session으로 만들 준비를 함 | `RCONT=0` 또는 `CL_CONTINUE=0` 성격의 remote setup으로 진행 | continue |
| 21122112 | 선택된 다음 번호가 이미 detached session으로 존재함 | fresh 생성 대신 detached session attach 판단으로 이동 | host tmux에 session이 있으면 생성 블록을 건너뛸 수 있음 | route to: 21142 |
| 21122113 | 낮은 번호들이 모두 live attached라 더 높은 번호를 찾아야 함 | 기존 attached session은 유지하고 사용 가능한 다음 `_N`을 계속 찾음 | 번호 탐색은 live client가 없는 번호까지 증가 | continue |
| 211221110 | 선택된 `_N` remote setup 직전에 사용자가 종료함 | 새 host session 생성 없이 종료 | setup script 실행 전이면 no new session | terminate |
| 211221111 | 선택된 `_N` session이 host에 없음 | fresh remote session을 만들고 attach 단계로 넘어감 | 새 tmux session marker가 반환되고 이전 attached session은 유지 | continue |
| 211221112 | 선택된 `_N` 생성 직전 다른 client가 같은 번호를 선점함 | stale 선택을 버리고 다시 다음 번호 선택으로 돌아감 | 재확인 결과 live client가 있으면 번호를 다시 올림 | route to: 2112211 |
| 211221113 | 선택된 `_N` fresh session 생성이 실패함 | remote setup 실패 안내로 이동 | SSH rc, session marker 누락, agent 준비 실패가 오류로 남음 | route to: 314 |
| 211221130 | 높은 번호 탐색 중 사용자가 종료함 | 새 session 생성 없이 기존 attached session만 유지 | 종료 시 탐색 중단 | terminate |
| 211221131 | 사용 가능한 높은 `_N`을 찾음 | 해당 번호를 fresh session 대상으로 확정하고 생성 단계로 이동 | live client가 없는 가장 낮은 번호로 `RCONT=0` 적용 | continue |
| 211221132 | 사용 가능한 번호를 찾지 못함 | 더 만들 수 없다는 오류를 보이고 종료 | 번호 충돌 또는 한계 상태면 session marker 없이 실패 | terminate |

다음 실행 ID
- 211221111
- 211221131
