# 421

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 421 | Host가 준비된 상태에서 XpairHost 메뉴를 조작함 | 메뉴에서 상태 확인, 권한, 연결 안내, 재설정, 업데이트, 세션 종료, 종료만 다룸 | `AppDelegate.rebuildMenu()` 기준 | continue |
| 4210 | 사용자가 메뉴를 열지 않거나 아무 항목도 선택하지 않음 | Host는 기존 준비 상태를 유지함 | 메뉴 action 호출 없음 | terminate |
| 4211 | 사용자가 `Permissions...`를 선택함 | 권한 단계로 열린 host onboarding에서 AX, SR, FDA 확인과 재요청을 진행함 | `grantPermissions()`가 `initialStep: "permissions"`로 `OnboardingWindow`를 엶 | continue |
| 42110 | Permissions 창을 닫음 | 이미 실행 중인 Host는 종료되지 않고 메뉴 대기 상태로 돌아감 | grantOnly mode라 닫아도 앱을 종료하지 않음 | terminate |
| 42111 | 권한 요청 또는 시스템 설정 열기를 진행함 | 권한 설정 후 host 권한 guard 흐름으로 이어짐 | bridge가 `requestPermission`, `openPermissionPane`을 처리 | route to: 411 |
| 42112 | 권한 화면에서 완료를 누름 | 권한 상태를 반영하고 Host 메뉴 대기로 돌아감 | `complete()`가 grant 창만 닫음 | terminate |
| 4212 | 사용자가 `Connect...`를 선택함 | 클라이언트 연결 안내 단계가 열림 | `connectClient()`가 `initialStep: "connect"`로 onboarding을 엶 | continue |
| 42120 | Connect 창을 닫음 | Host는 클라이언트 없이 준비 상태를 유지함 | grantOnly mode라 앱을 종료하지 않음 | terminate |
| 42121 | 안내대로 다른 Mac에서 Xpair 클라이언트를 엶 | Host 준비 후 클라이언트 연결 흐름으로 넘어감 | Host 쪽은 안내 창만 표시하고 실제 연결은 클라이언트가 시작 | route to: 422 |
| 42122 | Connect 안내만 확인하고 돌아감 | 연결된 client 없이 메뉴 대기로 돌아감 | `connectedClients()`는 읽기 전용 목록만 반환 | terminate |
| 4213 | 사용자가 `Set up...`를 선택함 | 전체 host onboarding을 처음부터 다시 열어 권한, 엔진, 연결 안내를 재검토함 | `openSetup()`이 `initialStep: nil`로 onboarding을 엶 | continue |
| 42130 | Set up 창을 닫음 | 실행 중인 Host는 종료되지 않고 기존 설정을 유지함 | grantOnly mode라 닫아도 앱을 종료하지 않음 | terminate |
| 42131 | Set up에서 권한 또는 엔진 설정을 진행함 | host onboarding 설정 흐름으로 이동함 | bridge가 permissions, engine guard, install status를 처리 | route to: 411 |
| 42132 | Set up에서 Connect 단계까지 진행함 | 클라이언트 실행 안내 뒤 host 대기 상태로 남음 | Connect step과 완료 bridge가 같은 창 안에서 처리됨 | route to: 412 |
| 4214 | 사용자가 `Check for Updates...`를 선택함 | 최신 릴리스 확인 후 실패, 최신, 업데이트 적용 여부로 분기함 | `Updater.checkForUpdates(interactive: true)` 호출 | continue |
| 42140 | 업데이트 확인 실패 | 실패 알림을 보이고 기존 Host 상태를 유지함 | `Update check failed` alert 표시 | terminate |
| 42141 | 최신 버전임 | 최신 버전 알림을 보이고 메뉴 대기로 돌아감 | `Latest version` alert 표시 | terminate |
| 42142 | 새 버전이 있고 세션이 없음 | 사용자 확인 후 앱 업데이트와 재시작을 진행함 | live session 0이어도 확인 alert 뒤 relaunch | terminate |
| 42143 | 새 버전이 있고 세션이 있음 | 세션 중단 가능성을 경고하고 사용자가 동의한 경우에만 재시작함 | `Sessions.liveSessionCount()`로 경고 alert 표시 | terminate |
| 4215 | 사용자가 `About XpairHost`를 선택함 | 버전, 역할, 저장소 정보를 확인함 | About alert 표시 | continue |
| 42150 | About에서 OK를 누름 | alert가 닫히고 Host 메뉴 대기로 돌아감 | `OK` 버튼으로 종료 | terminate |
| 42151 | About에서 Open GitHub를 누름 | 저장소 페이지를 브라우저로 엶 | `NSWorkspace.shared.open` 호출 | terminate |
| 4216 | 사용자가 Sessions 목록을 확인함 | 서버 상태와 Attached, Detached 세션을 읽고 세션별 종료만 선택할 수 있음 | `Sessions.list()`를 메뉴에 렌더링 | continue |
| 42160 | tmux host가 내려가 있음 | 서버 미동작 상태를 표시하고 세션 조작은 제공하지 않음 | `tmux host: down`, `server not running` 표시 | terminate |
| 42161 | active session이 없음 | 세션 없음 상태만 표시함 | `(no active sessions)` 표시 | terminate |
| 42162 | 세션 행을 클릭함 | 해당 세션 종료 확인 modal을 띄움 | `sessionClicked()`가 terminate alert 표시 | continue |
| 421620 | 종료 확인에서 Cancel을 누름 | 세션을 유지하고 메뉴 대기로 돌아감 | `Cancel`이면 `Sessions.kill` 호출 없음 | terminate |
| 421621 | 종료 확인에서 Terminate를 누름 | 해당 tmux session과 내부 프로세스를 정리함 | `Sessions.kill(name)`이 `tmux kill-session` 실행 | terminate |
| 421622 | detach session을 찾음 | 메뉴에서는 detach를 제공하지 않고 종료만 노출해야 함 | `Sessions.detachAll`은 있지만 메뉴 action에 연결되지 않음 | terminate |
| 4217 | 사용자가 `Quit`을 선택함 | XpairHost 앱을 종료함 | `NSApplication.terminate` 호출 | terminate |
| 4218 | 사용자가 메뉴의 읽기 전용 상태 행을 확인함 | 권한, 화면 공유, connected clients, session count를 조작 없이 확인함 | disabled menu item으로 표시 | continue |
| 42180 | 권한 상태 행을 확인함 | AX, SR, FDA의 granted 여부만 표시함 | `Accessibility`, `Screen Recording`, `Full Disk` 행 표시 | terminate |
| 42181 | 화면 공유 상태 행을 확인함 | viewer connected, ready, off 중 하나를 표시함 | `Screen share` 행은 disabled | terminate |
| 42182 | Clients 목록을 확인함 | 연결 client가 없거나 이름과 user만 표시함 | `ConnectedClients.list()` 결과를 disabled row로 표시 | terminate |
| 42183 | session count와 Attached, Detached header를 확인함 | 세션 상태를 읽고 조작은 세션 행 클릭으로만 진행함 | header는 disabled, 세션 행만 action 보유 | route to: 4216 |
| 4219 | 사용자가 Settings, logs, diagnostics, status 세부 화면을 메뉴에서 열려고 함 | XpairHost 메뉴에서는 직접 접근 불가로 끝남 | 현행 메뉴에 Settings, logs, diagnostics, status 창 항목 없음 | terminate |
| 421A | 사용자가 Restart tmux host를 메뉴에서 찾음 | 준비된 Host 메뉴에서는 노출되지 않으면 접근 불가로 끝남 | README에는 언급되나 현행 `rebuildMenu()`에 항목 없음 | terminate |
| 421B | 사용자가 repair 또는 install을 메뉴에서 찾음 | 준비된 Host 메뉴에서는 노출되지 않으면 접근 불가로 끝남 | self install은 launch 시 자동이고 repair menu action 없음 | terminate |

다음 실행 ID
- 없음
