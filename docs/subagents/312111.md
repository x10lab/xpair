# 312111

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 312111 | remote host가 reachable임 | mapped host dir 확인으로 진행 | SSH BatchMode reachability check 통과 | continue |
| 3121110 | dir 확인 전에 사용자가 launcher를 종료함 | host directory check와 tmux setup 없이 종료 | SSH reach 확인 뒤 중단이면 remote side effect 없음 | terminate |
| 3121111 | launcher가 mapped host dir 확인을 시작함 | `HOST_DIR` 기준으로 remote directory 존재 여부를 확인 | SSH로 `[ -d HOST_DIR ]` marker check 실행 | continue |
| 3121112 | reachable 판정 직후 host 연결이 다시 끊김 | remote attach 대신 SSH/auth 실패 복구 흐름으로 이동 | 후속 SSH command 실패는 launch 실패로 처리 | route to: 314 |
| 31211110 | directory check command 실행 전에 사용자가 종료함 | remote session 생성 없이 종료 | check command 전 종료면 marker와 session 없음 | terminate |
| 31211111 | host가 directory check command를 실행함 | host filesystem에서 mapped project path를 판정 | remote shell이 `[ -d "$HOST_DIR" ]` 결과를 marker로 반환 | continue |
| 31211112 | directory check command가 실행되지 못함 | remote setup 실패 안내로 이동 | SSH timeout, shell error, 권한 문제는 실패 branch로 처리 | route to: 314 |
| 312111110 | directory check 응답 전에 사용자가 종료함 | 판정 결과 없이 종료 | attach와 tmux 생성 없음 | terminate |
| 312111111 | mapped host dir이 존재함 | session base 계산과 remote setup 준비로 진행 | `__YES__` marker 수신 뒤 session naming 단계로 넘어감 | continue |
| 312111112 | mapped host dir이 없음 | missing dir 복구 또는 unmapped/stale mapping 흐름으로 이동 | `__NO__` 또는 marker 누락 시 create/cancel branch로 이동 | route to: 313 |
| 312111113 | mapped host dir이 Xpair 대상 project로 접근 불가함 | 잘못된 mapping 또는 권한 문제로 종료 안내 | directory는 있어도 cwd로 쓸 수 없으면 launch 실패 | terminate |

다음 실행 ID
- 312111111
