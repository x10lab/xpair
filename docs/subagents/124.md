# 124

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 124 | 사용자가 Xpair workbench 안에서 setup again 또는 force onboarding으로 진입함 | 기존 세션은 유지하고 다음 실행에서 첫 실행 온보딩으로 다시 들어감 | host 상태 버튼과 `remotepair.runSetup`이 `.force-onboarding` sentinel을 쓰는 구조 | continue |
| 1240 | 사용자가 setup again 진입점을 누르지 않음 | 현재 workbench와 기존 세션 상태 유지 | 동일 | terminate |
| 1241 | 사용자가 host 상태 버튼을 누름 | 세션 보존 안내와 함께 setup again 확인을 표시함 | `Set up Xpair again? Your sessions stay attached.` 메시지와 `Set up again` 버튼 표시 | continue |
| 12410 | 사용자가 확인 메시지에서 `Set up again`을 누르지 않음 | workbench에 그대로 남고 sentinel을 쓰지 않음 | choice가 다르면 return | terminate |
| 12411 | 사용자가 `Set up again`을 확정함 | 기존 세션을 끊지 않고 다음 실행 온보딩을 예약함 | sentinel write 후 `workbench.action.quit` 실행 | continue |
| 124110 | sentinel write가 실패함 | 재온보딩 예약 실패를 사용자에게 보여주고 종료하지 않아야 함 | warn log만 남기고 quit을 계속 실행할 수 있음 | terminate |
| 124111 | sentinel write가 성공하고 앱이 종료된 뒤 사용자가 Xpair를 다시 실행함 | first-run onboarding guard로 이동 | 다음 실행에서 sentinel을 검사 | route to: 1243 |
| 124112 | sentinel write가 성공했지만 사용자가 다시 실행하지 않음 | 기존 host tmux 세션은 보존되고 GUI flow는 멈춤 | 앱 종료 이후 추가 동작 없음 | terminate |
| 12412 | 사용자가 세션 보존 문구만 확인함 | setup again을 확정하기 전까지 attach, detach, kill은 발생하지 않음 | 메시지는 `Your sessions stay attached`로 표시 | continue |
| 124120 | 문구 확인 후 취소함 | 기존 세션과 workbench 유지 | 동일 | terminate |
| 124121 | 문구 확인 후 `Set up again`을 누름 | host 상태 버튼 확정 흐름으로 이동 | 같은 handler의 choice 처리 | route to: 12411 |
| 124122 | 사용자가 세션 종료 경고를 기대함 | 세션 종료 경고는 접근 불가, 이 흐름은 재온보딩만 처리 | 현재 문구도 세션 보존을 명시 | terminate |
| 1242 | 사용자가 command palette 등에서 Re-run setup 명령을 실행함 | 다음 실행에서 setup을 열도록 예약하고 즉시 재시작 여부를 묻음 | `runSetup()`이 sentinel을 쓴 뒤 `Restart now` 메시지 표시 | continue |
| 12420 | 사용자가 Re-run setup 명령을 실행하지 않고 빠져나감 | sentinel 생성과 restart prompt 없이 현재 workbench 유지 | 명령 미실행이면 변화 없음 | terminate |
| 12421 | sentinel write가 실패함 | 실패를 사용자에게 보여주고 재시작을 막아야 함 | warn log만 남기고 `Restart now` 선택지를 계속 표시 | continue |
| 124210 | 사용자가 실패 상태에서 재시작하지 않음 | 현재 workbench 유지, 재온보딩 예약 없음 | sentinel 없음 | terminate |
| 124211 | 사용자가 실패 상태에서 `Restart now`를 누름 | 온보딩 예약이 없으므로 정상 workbench로 돌아갈 수 있음을 알려야 함 | quit하면 다음 실행에서 `isOnboarded()`가 true라 온보딩을 건너뜀 | terminate |
| 12422 | sentinel write가 성공함 | 다음 실행 온보딩이 예약되고 사용자는 지금 재시작하거나 나중에 재실행 가능 | `.force-onboarding` 생성 후 메시지 표시 | continue |
| 124220 | 사용자가 `Restart now`를 누르지 않음 | 현재 workbench는 유지되고 다음 수동 재실행 때 온보딩 | sentinel은 남아 있음 | terminate |
| 124221 | 사용자가 `Restart now`를 누름 | 앱을 종료하고 다음 실행에서 온보딩으로 들어감 | `workbench.action.quit` 실행 | route to: 1243 |
| 124222 | 사용자가 메시지를 닫고 앱을 나중에 직접 종료함 | 다음 수동 실행에서 sentinel guard로 이동 | sentinel이 남아 있으면 다음 실행 때 소비됨 | route to: 1243 |
| 12423 | 사용자가 Re-run setup 메시지를 보고 세션 보존을 확인하려 함 | 이 명령은 세션 종료를 하지 않고 onboarding만 예약해야 함 | runSetup 메시지는 setup 재실행만 안내하고 세션 보존 문구는 status 버튼 쪽에만 있음 | continue |
| 124230 | 사용자가 세션 종료 없이 현재 workbench에 남음 | 기존 attached, detached host 세션 유지 | quit을 누르지 않으면 세션 조작 없음 | terminate |
| 124231 | 사용자가 즉시 재시작함 | host tmux 세션은 보존되고 GUI만 재실행됨 | quit만 실행, host session kill 없음 | route to: 1243 |
| 124232 | 사용자가 세션 종료 동작을 찾음 | 접근 불가, setup again flow는 세션 종료를 제공하지 않음 | 해당 handler에 kill, detach 명령 없음 | terminate |
| 1243 | Xpair가 `.force-onboarding` 상태로 다시 실행됨 | 이미 설정된 사용자라도 첫 실행 온보딩 창을 다시 표시함 | `shouldOnboard()`가 sentinel 존재를 true로 처리 | continue |
| 12430 | relaunch 시 sentinel이 없음 | 기존 설정이 있으면 일반 workbench로 복귀하고 이 forced onboarding flow는 종료 | `isOnboarded()`가 true면 onboarding skip | terminate |
| 12431 | relaunch 시 sentinel이 있음 | sentinel을 소비하고 pre-workbench onboarding window를 열어야 함 | `openOnboardingWindow()` 시작 시 sentinel 삭제 | continue |
| 124310 | onboarding window가 열렸지만 사용자가 멈춤 | 온보딩 창을 유지하고 아직 첫 실행 분기로 들어가지 않음 | 같은 onboarding webview가 표시됨 | terminate |
| 124311 | onboarding window가 열리고 사용자가 첫 실행 온보딩을 시작함 | 첫 실행 온보딩 root로 라우팅 | 같은 onboarding webview를 로드 | route to: 11 |
| 124312 | 사용자가 온보딩 창을 완료 전에 닫음 | 완료 전 닫기 처리로 이동 | `_completed` false면 onComplete로 workbench를 열 수 있음 | route to: 110 |
| 124313 | 사용자가 host discovery/setup으로 진행함 | host 탐색과 설치 온보딩으로 이동 | webview 선택 이후 bridge 흐름 | route to: 111 |
| 124314 | 사용자가 manual host/connect로 진행함 | 수동 host 입력 온보딩으로 이동 | webview 선택 이후 bridge 흐름 | route to: 112 |
| 12432 | sentinel 삭제가 실패하거나 남아 있음 | 반복 온보딩을 막기 위해 한 번만 소비되어야 함 | 삭제는 best-effort라 실패 시 다음 실행에서도 강제될 수 있음 | continue |
| 124320 | 사용자가 열린 온보딩에서 멈춤 | 현재 온보딩 창 유지 | sentinel 삭제 여부와 별개로 창은 열림 | terminate |
| 124321 | 온보딩 완료 후 다음 실행에서도 sentinel이 남음 | 반복 재온보딩을 피해야 함 | 삭제 실패 시 다시 `shouldOnboard()` true 가능 | terminate |
| 124322 | sentinel이 정상 삭제됨 | 이후 완료 또는 취소 결과에 따라 일반 흐름으로 이동 | `fs.rmSync(..., { force: true })` | route to: 11 |
| 1244 | setup again 도중 기존 세션 보존 상태를 사용자가 확인함 | GUI 재시작과 onboarding은 host tmux 세션을 종료하지 않아야 함 | handlers는 sentinel write, message, quit만 수행 | continue |
| 12440 | 사용자가 아무 세션 조작도 하지 않음 | attached, detached, history 상태 변화 없음 | 동일 | terminate |
| 12441 | setup again 확정 후 GUI가 종료됨 | host tmux 세션은 그대로 남고 다음 workbench에서 다시 확인 가능 | session kill 호출 없음 | continue |
| 124410 | 재실행하지 않음 | host tmux 세션은 남고 GUI attach만 끊긴 상태 | 앱 종료 이후 추가 GUI 동작 없음 | terminate |
| 124411 | 재실행 후 onboarding으로 들어감 | 세션 보존은 onboarding 완료 후 workbench에서 확인 | onboarding 자체는 세션 목록을 조작하지 않음 | route to: 11 |
| 124412 | 사용자가 세션이 종료됐다고 오해함 | 종료가 아니라 route to onboarding임을 보여줘야 함 | 메시지는 status 버튼 경로에서만 명확함 | terminate |

다음 실행 ID
- 없음
