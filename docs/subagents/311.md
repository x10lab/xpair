# 311

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 311 | `xpair launch`가 local/self-host target으로 결정됨 | 원격 SSH 없이 현재 Mac에서 세션을 열 준비를 함 | `TARGET=local`이면 `launch_local`을 호출 | continue |
| 3110 | 사용자가 local 실행 전에 종료함 | 엔진 검사, tmux 검사, 세션 생성 없음 | 프로세스 종료 시 이후 단계 없음 | terminate |
| 3111 | local target 결정 원인을 확인함 | 명시 옵션, 원격 미설정, self-host, 대화식 선택을 구분함 | 대상 결정 블록이 `TARGET=local`로 수렴 | continue |
| 31110 | local target으로 수렴하지 않음 | 이 파일 하위 탐색 대상 아님 | remote branch로 진행 | terminate |
| 31111 | `--local`이 지정됨 | 원격 설정과 무관하게 local 실행으로 고정 | `TARGET_PREF=local`이 우선 | route to: 3112 |
| 31112 | `REMOTE_HOST`가 비어 있음 | 원격 후보가 없으므로 local 실행 | `-z "$REMOTE_HOST"`이면 local 선택 | route to: 3112 |
| 31113 | `REMOTE_HOST`가 현재 호스트명과 같음 | 자기 자신에게 SSH하지 않고 local 실행 | `LOCAL_HOST = REMOTE_HOST`이면 local 선택 | route to: 3112 |
| 31114 | 대화식 선택에서 사용자가 local을 고름 | 프롬프트의 local 후보로 실행 | `ans=2`이면 local 선택 | route to: 3112 |
| 3112 | `launch_local`이 시작됨 | 선택 엔진과 현재 프로젝트 경로를 기준으로 local 세션을 준비함 | `command -v "$RP_ENGINE"`, `sync_claude_local`, `cd "$PROJECT_DIR"` 순서로 진행 | continue |
| 31120 | 선택 엔진이 없음 | 설치 안내를 보이고 종료 | `$RP_ENGINE not found on $LOCAL_HOST`와 코드 11로 종료 | terminate |
| 31121 | 선택 엔진이 있음 | local 세션 백엔드 선택으로 진행 | `command -v "$RP_ENGINE"` 통과 | continue |
| 311210 | 엔진 확인 뒤 사용자가 종료함 | tmux 세션 생성 없음 | 이후 guard 실행 전 종료 | terminate |
| 311211 | host role guard가 aqua 사용을 허용함 | XpairHost 권한 상속 경로를 먼저 시도 | `role`이 `host` 또는 `both`이고 `host.env`가 있거나, 구버전 조건을 만족 | route to: 3113 |
| 311212 | host role guard가 aqua 사용을 막음 | 일반 tmux fallback으로 진행 | `client` role, `host.env` 없음, 구버전 host 불일치가 `ensure_local_host` 실패로 이어짐 | route to: 3114 |
| 3113 | aqua tmux 경로를 시도함 | XpairHost subtree 아래 `tmux-aqua` 세션으로 붙거나 fallback으로 이동 | `ensure_local_host` 성공 여부와 `_local_next_n` 결과로 분기 | continue |
| 31130 | aqua 검사 전에 사용자가 종료함 | 세션 생성 없음 | 이후 `tmux-aqua` 호출 없음 | terminate |
| 31131 | `tmux-aqua` 실행 파일이 없거나 실행 불가 | 컴퓨터 사용 권한 경로를 쓰지 않고 일반 tmux로 이동 | `$LOCAL_BIN/tmux-aqua`가 실행 가능하지 않으면 guard 실패 | route to: 3114 |
| 31132 | `tmux-aqua` 소켓이 죽었고 XpairHost 깨우기도 실패함 | 일반 tmux fallback으로 이동 | 앱 open 또는 launchctl 뒤 재시도해도 `tm_local has-session` 실패 | route to: 3114 |
| 31133 | aqua 대상 세션이 없음 | 새 aqua 세션을 만들고 기본적으로 마지막 기록된 대화를 재개함 | `NEED_CREATE=1`, `CONT=1`, `tm_local new-session` 실행 | continue |
| 31134 | aqua 대상 세션이 detached 상태임 | 새 대화 없이 기존 세션을 인수 연결함 | `has-session` 성공으로 `NEED_CREATE=0`, `attach -d` 실행 | continue |
| 31135 | aqua 낮은 번호가 이미 attached이거나 `--fresh`가 지정됨 | 다음 번호 또는 `_1`을 fresh 대화로 열음 | `_N>1` 또는 `FRESH=1`이면 `CONT=0` | continue |
| 3114 | 일반 tmux fallback 경로로 진행함 | XpairHost 권한 상속 없이 현재 Mac의 plain tmux 세션을 사용함 | `ensure_local_host` 실패 뒤 `tmux` 명령으로 분기 | continue |
| 31140 | 일반 `tmux`가 없음 | 설치 안내를 보이고 종료 | `tmux not found on $LOCAL_HOST. Install it: brew install tmux`와 코드 10으로 종료 | terminate |
| 31141 | 일반 tmux 대상 세션이 없음 | 새 plain tmux 세션을 만들고 기본적으로 마지막 기록된 대화를 재개함 | `tmux has-session` 실패 뒤 `tmux new -s "$TSESS"` 실행 | continue |
| 31142 | 일반 tmux 대상 세션이 detached 상태임 | 새 재시작 파일 없이 기존 세션을 인수 연결함 | `tmux has-session` 성공 뒤 `attach -d` 실행 | continue |
| 31143 | 일반 tmux 낮은 번호가 이미 attached이거나 `--fresh`가 지정됨 | 다음 번호 또는 `_1`을 fresh 대화로 열음 | `TN>1` 또는 `FRESH=1`이면 `TCONT=0` | continue |

다음 실행 ID
- 31133
- 31134
- 31135
- 31141
- 31142
- 31143
