# 422

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 422 | Host 준비 후 사용자가 Xpair 클라이언트를 열어 연결 시도 | 클라이언트 설치 여부, 첫 실행 여부, 기존 설정 여부, discovery 또는 수동 연결, SSH guard, heartbeat 표시로 분기 | Host는 Bonjour 광고와 clients heartbeat 파일만 읽고, 클라이언트가 연결되기 전까지 대기 | continue |
| 4220 | 사용자가 Xpair 클라이언트를 열지 않음 | Host는 연결된 client 없이 준비 상태로 대기 | connectedClients 목록이 비어 있으면 메뉴와 온보딩 bridge가 빈 배열을 반환 | terminate |
| 4221 | 클라이언트 Mac에 Xpair IDE가 설치되어 있지 않음 | 클라이언트 설치 안내로 종료하고 Host는 대기 유지 | README의 `brew install --cask xpair` 또는 client bootstrap 전에는 IDE 실행 표면 없음 | terminate |
| 4222 | Xpair IDE가 설치되어 있고 사용자가 앱을 실행함 | 첫 실행, 기존 설정, 강제 재설정 sentinel로 분기 | `shouldOnboard()`가 `REMOTE_HOST`와 `.force-onboarding`을 기준으로 온보딩 여부를 판단 | continue |
| 4223 | Host가 클라이언트 연결을 기다리는 동안 discovery 표면에 노출됨 | Bonjour LAN discovery와 수동 host 입력 후보가 제공됨 | Host는 `_remotepair._tcp`를 광고하고 연결은 받지 않고 즉시 닫음 | continue |
| 4224 | 클라이언트가 Host를 선택하거나 수동 입력으로 연결을 시도함 | SSH reachable, auth 실패, host app guard, reconnect 결과로 분기 | onboarding이 `sshReachable`, `setHost`, reconnect probe로 상태를 갱신 | continue |
| 4225 | 클라이언트 연결 성공 후 Host가 connected client heartbeat를 보게 됨 | Host 메뉴와 onboarding bridge에 최근 client가 표시됨 | onboarding main과 workbench extension이 30초마다 `~/.xpair/host/clients/<id>.json`을 씀 | continue |
| 42220 | 사용자가 IDE 실행 직후 앱을 닫음 | Host 연결 시도 없이 종료 | 온보딩 또는 workbench가 열리기 전이면 heartbeat도 없음 | terminate |
| 42221 | 최초 실행 또는 `REMOTE_HOST`가 비어 있음 | 클라이언트 first-run onboarding으로 이동 | `isOnboarded()`가 false면 pre-workbench onboarding window를 열음 | route to: 111 |
| 42222 | 기존 `REMOTE_HOST`가 있고 강제 재설정 sentinel이 없음 | workbench를 열고 기존 host 상태를 평가 | onboarding 없이 extension activate 후 host probe, RD, Sessions 표면을 준비 | route to: 122 |
| 42223 | `.force-onboarding` sentinel 또는 force 인자가 있음 | 기존 세션을 유지한 채 재설정 온보딩으로 이동 | sentinel을 소비한 뒤 onboarding window를 열음 | route to: 124 |
| 42230 | Bonjour 광고를 기다리지 않고 중단 | 클라이언트 연결 없이 Host가 계속 대기 | 광고는 계속 유지되지만 heartbeat 파일은 생기지 않음 | terminate |
| 42231 | Bonjour LAN scan에서 Host가 발견됨 | 발견된 host row에서 Connect 또는 Reconnect 선택 가능 | TXT에 `hn`, `v`, `role`, `fp`가 실리고 peer row로 표시 | continue |
| 42232 | Bonjour에서 Host가 보이지 않음 | 같은 Wi-Fi 진단 뒤 Tailscale 또는 수동 입력으로 이동 | EmptyDiagnose가 Tailscale과 Enter manually fallback을 표시 | route to: 112 |
| 42233 | 사용자가 Enter manually 또는 Connect over Internet을 선택함 | 수동 host 입력과 SSH key 안내로 이동 | 두 fallback button 모두 `onManual`로 Connect 단계 진입 | route to: 112 |
| 42240 | 연결 시도 중 사용자가 취소하거나 창을 닫음 | Host는 connected client 없이 대기 유지 | onboarding 미완료면 workbench로 떨어질 수 있으나 유효 host 없이는 연결 없음 | terminate |
| 42241 | 발견된 Host가 기존 설정 host라 Reconnect를 선택함 | 기존 key로 SSH reachability만 확인 | reconnect step이 `setHost` 후 `sshReachable`을 실행 | continue |
| 42242 | 발견된 Host가 새 연결 후보라 Connect를 선택함 | host 입력 저장과 SSH BatchMode probe로 검증 | connect step이 `sshReachable` 성공 시 `setHost`를 호출 | continue |
| 42243 | SSH auth 또는 network guard가 실패함 | 실패 안내와 Retry를 표시하고 Host는 waiting 유지 | BatchMode 실패는 `Couldn't reach host` 또는 offline 상태로 노출 | continue |
| 42244 | SSH reachable이고 Host app guard를 통과함 | engine, 권한, 파일 접근 온보딩으로 넘어감 | reachable 상태에서 Next가 열리고 이후 onboarding 단계가 진행 | route to: 111117 |
| 42245 | 이미 설정된 workbench에서 host probe가 성공함 | usable host workbench flow로 이동 | status bar가 reachable로 바뀌고 RD와 Sessions가 사용 가능 | route to: 122 |
| 42250 | heartbeat 쓰기 전 클라이언트가 종료됨 | Host connected client 표시는 생기지 않음 | stop 또는 실패 시 파일이 없거나 90초 freshness 밖으로 빠짐 | terminate |
| 42251 | onboarding window가 열린 상태에서 heartbeat가 성공함 | Host는 연결 중인 클라이언트를 표시하고 onboarding은 계속 진행 | onboarding main이 `startHeartbeat()`를 호출 | continue |
| 42252 | workbench가 열린 상태에서 heartbeat가 성공함 | Host는 클라이언트 이름, 사용자, age를 최신순으로 표시 | extension activate가 heartbeat를 시작하고 Host가 90초 이내 파일만 읽음 | continue |
| 42253 | heartbeat SSH write가 실패함 | 클라이언트 UI는 계속 두고 Host는 waiting 상태 유지 | heartbeat는 fire-and-forget이라 실패를 무시하고 다음 tick에서 재시도 | continue |
| 422410 | reconnect 확인 중 사용자가 중단 | 기존 설정은 남지만 이번 연결은 확정되지 않음 | `onReady(false)` 상태로 Next가 막힘 | terminate |
| 422411 | reconnect SSH probe가 성공함 | 기존 설정 host로 온보딩 이후 단계 또는 workbench 복귀 가능 | `Reachable, your key works` 표시 | route to: 111118 |
| 422412 | reconnect SSH probe가 실패함 | Retry만 제공하고 Host는 연결된 client 없이 대기 | `Can't reach the host`와 Retry 표시 | continue |
| 422420 | 새 연결 host 입력 없이 대기 | Check connection이 비활성이라 진행 불가 | host trim 값이 없으면 button disabled | terminate |
| 422421 | 새 연결 host를 입력했지만 Check connection을 누르지 않음 | SSH 검증 전이므로 Host 연결 없음 | state는 idle로 유지 | terminate |
| 422422 | 새 연결 host로 Check connection을 누름 | SSH probe 결과로 성공 또는 실패 분기 | `sshReachable(host.trim())` 실행 | continue |
| 422430 | 실패 안내를 보고 Retry하지 않음 | Host는 waiting 상태 유지 | 실패 화면만 남음 | terminate |
| 422431 | Retry를 눌렀지만 SSH가 계속 실패함 | 같은 실패 안내를 유지 | 같은 probe를 재실행하고 failed로 돌아감 | continue |
| 422432 | Retry 후 SSH가 성공함 | 연결 성공 branch로 이동 | `setHost` 후 reachable 상태가 됨 | route to: 42244 |
| 422510 | onboarding 도중 heartbeat가 90초 넘게 갱신되지 않음 | Host connected client 표시에서 사라짐 | `ConnectedClients.freshnessSec`를 넘은 파일은 제외 | terminate |
| 422511 | onboarding heartbeat가 유지되고 사용자가 Host 메뉴를 봄 | client hostname과 user가 보임 | JSON의 `name`, `user`, `ts`를 읽어 age와 함께 정렬 | continue |
| 422512 | onboarding 완료 후 workbench가 열림 | heartbeat는 workbench 쪽에서 계속 유지 | onboarding complete 후 같은 앱 process가 workbench를 열음 | route to: 122 |
| 422520 | workbench heartbeat가 90초 넘게 갱신되지 않음 | Host connected client 표시에서 사라지고 waiting처럼 보임 | stale heartbeat는 필터링됨 | terminate |
| 422521 | workbench heartbeat가 유지되고 사용자가 Sessions를 엶 | connected client 상태와 별개로 세션 표면으로 이동 | Sessions sidebar와 panel이 강제 open됨 | route to: 131 |
| 422522 | workbench에서 Remote Desktop 또는 host 상태를 확인함 | usable host 표면에서 RD, connect, retry 흐름으로 이동 | reachable host flow의 RD와 status bar가 활성화됨 | route to: 122 |
| 422530 | heartbeat 실패 상태에서 사용자가 아무 조작도 하지 않음 | Host는 connected client 없이 계속 대기 | 실패는 UI를 막지 않고 다음 tick만 예약 | terminate |
| 422531 | heartbeat 실패 후 SSH가 회복됨 | 다음 heartbeat tick에서 Host connected client 표시가 생김 | 다음 write 성공 시 fresh JSON 파일이 생성됨 | route to: 4225 |

다음 실행 ID
- 42231
- 42241
- 42242
- 42243
- 42251
- 42252
- 42253
- 422412
- 422422
- 422431
- 422511
