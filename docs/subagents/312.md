# 312

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 312 | 사용자가 mapped remote target으로 `xpair launch`를 실행 | client path를 host path로 바꾸고 remote tmux attach로 진행 | `map_to_host`, SSH reach, dir check, remote setup, mosh 또는 ssh attach 순서 | continue |
| 3120 | launch 시작 뒤 사용자가 종료함 | mapping, SSH, tmux 생성 없이 종료 | 프로세스 종료 시 이후 단계 없음 | terminate |
| 3121 | launcher가 client path를 host path로 매핑함 | 가장 긴 mapping prefix를 적용해 host project path를 확정 | `FOLDER_MAPS` longest prefix로 `HOST_DIR` 계산 | continue |
| 31210 | path 확정 전에 사용자가 종료함 | remote 확인 없이 종료 | `PROJECT_DIR` 이후 중단이면 후속 없음 | terminate |
| 31211 | mapping이 host path로 계산됨 | 이후 모든 remote 명령은 host path 기준으로 실행 | `HOST_DIR`, `REMOTE_PROJ`가 client에서 계산됨 | continue |
| 312110 | host path 계산 뒤 실행을 취소함 | SSH reach와 dir check 없이 종료 | 중단 시 remote side effect 없음 | terminate |
| 312111 | remote host가 reachable임 | mapped host dir 확인으로 진행 | `ssh BatchMode ConnectTimeout` true 통과 | continue |
| 312112 | remote host가 reachable이 아님 | SSH/auth 실패 흐름으로 이동 | tailscale 재시도 후 실패하면 local fallback | route to: 314 |
| 31212 | mapping 대상이 Xpair launch 범위 밖임 | 접근 불가, 하위 탐색 차단 | mapped remote target 전제와 맞지 않음 | terminate |
| 3122 | launcher가 remote dir 존재를 확인함 | host에 project dir이 있으면 remote setup으로 진행 | SSH `[ -d HOST_DIR ]` marker 확인 | continue |
| 31220 | dir check 전에 사용자가 종료함 | remote tmux 생성 없이 종료 | check 전 종료면 no session | terminate |
| 31221 | remote dir이 존재함 | session 이름과 번호 결정으로 진행 | `__YES__` marker 수신 | continue |
| 312210 | dir 존재 확인 뒤 사용자가 종료함 | tmux 생성 없이 종료 | remote script 실행 전이면 no session | terminate |
| 312211 | session base가 계산됨 | host 이름, readable folder, path hash로 안정적인 base 생성 | `REMOTE_PROJ=<host>_<slug>_<hash>` | continue |
| 312212 | remote dir이 없음 | missing dir 복구 또는 unmapped/stale 흐름으로 이동 | create prompt 또는 cancel | route to: 313 |
| 31222 | dir check SSH가 3회 실패함 | remote attach 대신 실패 복구 흐름으로 이동 | 현행은 local fallback 실행 | route to: 314 |
| 3123 | launcher가 remote tmux session 번호와 상태를 결정함 | live client tab이 없는 가장 낮은 `_N`을 선택 | local `mosh-client` 탐색으로 `_remote_next_n` 결정 | continue |
| 31233 | 같은 session이 없음 | `_1`을 새로 만들고 이전 기록이 있으면 resume 시도 | `NEED_CREATE=1`, `CL_CONTINUE=1` | continue |
| 312331 | host에 agent CLI가 없음 | 설치 안내 후 종료 | `command -v` 실패 시 exit 11 | route to: 314 |
| 312332 | XpairHost tmux server가 없음 | host app 상태 확인 흐름으로 이동 | `tm has-session` 실패 시 exit 13 | route to: 314 |
| 312333 | 새 tmux session 생성 성공 | detached session을 만든 뒤 attach 단계로 진행 | `tm new-session -d`, `__SESSION__` 출력 | continue |
| 31231 | 같은 session이 detached 상태로 있음 | 새 대화가 아니라 기존 session을 take-over attach | `tm has-session`이면 `NEED_CREATE=0` | continue |
| 312310 | reattach 직전에 사용자가 종료함 | host session은 detached로 유지 | attach 전 종료 | terminate |
| 312311 | detached session attach 준비가 끝남 | 기존 tmux session을 `attach -d` 대상으로 사용 | `__SESSION__`이 기존 이름으로 반환 | continue |
| 312312 | detached session 이름이 stale임 | remote setup 실패 흐름으로 이동 | marker 누락 또는 attach 실패 가능 | route to: 314 |
| 31232 | 낮은 `_N`이 현재 client tab에 붙어 있음 | 다음 `_N`으로 fresh session 생성 | `mosh-client`가 있으면 `_2+`, `CL_CONTINUE=0` | continue |
| 312320 | fresh 생성 전 사용자가 종료함 | 추가 session 없이 기존 attach 유지 | remote script 전 종료면 no new session | terminate |
| 312321 | fresh session 생성 성공 | 기존 session과 분리된 새 agent 화면 attach로 진행 | `_2+` session 생성, `RCONT=0` | continue |
| 312322 | fresh session 생성 실패 | remote setup 실패 흐름으로 이동 | SSH rc 또는 session marker 실패 | route to: 314 |
| 3124 | launcher가 attach transport를 선택함 | mosh가 있으면 mosh, 없으면 ssh TTY로 tmux에 붙음 | `command -v mosh` 기준 분기 | continue |
| 31240 | attach transport 선택 전에 사용자가 종료함 | 생성된 session은 detached로 남을 수 있음 | setup 이후 종료면 host session 유지 | terminate |
| 31241 | client에 mosh가 있음 | mosh로 remote tmux session에 attach | `mosh --server ... tmux-aqua attach -d` 실행 | continue |
| 312413 | mosh attach가 실패함 | attach 실패를 표시하고 종료 | mosh exit code 그대로 종료 | route to: 314 |
| 312411 | mosh attach가 성공함 | roaming 가능한 terminal 화면이 열림 | foreground가 mosh client로 전환 | continue |
| 312412 | mosh attach 중 tab이 닫힘 | remote session을 detach하고 종료 | HUP/TERM trap이 `detach-client` 실행 | terminate |
| 31242 | client에 mosh가 없음 | ssh TTY fallback으로 remote tmux에 attach | `ssh -t ... attach -d` 실행 | continue |
| 312423 | ssh attach가 실패함 | 실패를 표시하고 종료 | ssh exit 또는 tmux attach 실패 | route to: 314 |
| 312421 | ssh attach가 성공함 | terminal에서 remote tmux session을 사용 | foreground가 ssh TTY로 전환 | continue |
| 312422 | ssh 연결이 끊김 | host session은 detached로 남고 다음 실행에서 재연결 가능 | ssh 종료 후 다음 launch는 detached branch | route to: 31231 |
| 3125 | 사용자가 attached agent 화면을 봄 | mapped host path 기준 agent session을 사용할 수 있음 | tmux cwd가 `HOST_DIR`, title이 session name | continue |
| 31250 | agent 화면을 바로 닫음 | remote session은 detached 상태로 남음 | mosh는 trap, ssh는 attach 종료 | terminate |
| 31251 | agent 화면이 정상 표시됨 | 사용자는 host path의 claude, codex, opencode에 입력 가능 | agent CLI가 tmux 안에서 실행됨 | continue |
| 312510 | 사용자가 입력하지 않음 | attach 상태만 유지 | 세션 상태 변화 없음 | terminate |
| 312511 | 사용자가 agent에 입력함 | 이후 대화는 agent 자체 flow로 넘어감 | Xpair launch 하위 탐색은 종료 | terminate |
| 312512 | 사용자가 tmux detach를 실행함 | host session은 detached로 남고 다음 launch에서 reattach | `attach -d` 정책으로 재연결 가능 | route to: 31231 |
| 31252 | agent 화면 대신 오류가 보임 | remote setup 또는 attach 실패 흐름으로 이동 | log path와 실패 rc가 표시됨 | route to: 314 |

다음 실행 ID
- 312111
- 312211
- 312333
- 312311
- 312321
- 312411
- 312421
