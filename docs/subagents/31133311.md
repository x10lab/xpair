# 31133311

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31133311 | session title을 출력함 | Xpair IDE와 terminal 표면에서 새 aqua session name을 식별할 수 있어야 함 | OSC title `\033]2;$SESS\007` 출력 | continue |
| 311333110 | 사용자가 title 출력 직후 attach 전에 종료함 | 새 aqua session은 detached 상태로 남고 이 분기는 끝남 | attach exec 전 종료하면 생성된 session만 남음 | terminate |
| 311333111 | 사용자가 표시된 session title을 확인함 | 현재 local aqua session으로 붙는 흐름임을 알 수 있어야 함 | terminal title 또는 Xpair 표면에 `$SESS`가 노출됨 | continue |
| 311333112 | terminal이 OSC title을 표시하지 않음 | title 표시만 누락되고 attach 흐름은 계속 진행해야 함 | terminal 설정에 따라 window title 반영이 보이지 않을 수 있음 | continue |
| 311333113 | 사용자가 표시된 title을 보고 다른 session으로 전환하려 함 | 이미 생성된 session을 이 분기에서 바꾸지 않고 기존 session 선택 흐름으로 보내야 함 | 새 session 선택 UI 없이 현재 `$SESS` attach만 이어짐 | route to: 131 |
| 3113331110 | title 확인 뒤 사용자가 attach 전에 종료함 | 새 aqua session은 detached 상태로 남고 이 분기는 끝남 | `attach -d` 호출 전 종료 | terminate |
| 3113331111 | aqua attach handoff가 실행됨 | 사용자는 새로 만든 agent shell을 보고 입력할 수 있어야 함 | `tmux-aqua ... attach -d -t "=$SESS"`로 foreground 전환 | continue |
| 3113331112 | 사용자가 title이 local aqua session임을 확인하고 원격 실행을 기대함 | local/self-host launch 범위를 벗어나므로 remote launch 흐름으로 다시 보내야 함 | 현재 경로는 remote host를 다시 고르지 않음 | route to: 312 |
| 3113331120 | title 미표시 상태에서 사용자가 종료함 | session은 detached 상태로 남고 title 미표시는 이후 흐름을 만들지 않음 | attach 전 종료면 생성된 session만 남음 | terminate |
| 3113331121 | title은 보이지 않지만 attach handoff가 실행됨 | title 표시 여부와 무관하게 새 agent shell로 붙어야 함 | OSC title 표시 실패 뒤에도 attach 명령은 계속 실행됨 | continue |
| 3113331122 | title 미표시 때문에 사용자가 session 식별을 포기함 | 새 aqua session은 남기고 사용자는 session 목록에서 다시 선택해야 함 | session name은 내부 로그와 tmux session에는 남음 | route to: 131 |
| 31133311110 | attach가 실패함 | agent shell로 넘어가지 않고 실패 안내를 확인한 뒤 이 분기는 끝남 | `attach -d` 실패 시 tmux 오류와 로그 안내가 남음 | terminate |
| 31133311111 | attach가 성공하고 fresh client가 붙음 | 사용자는 새 aqua agent shell에서 입력을 시작할 수 있어야 함 | launcher 프로세스가 tmux client로 대체되고 shell 화면이 표시됨 | continue |
| 31133311112 | attach가 성공하며 stale client를 떼어냄 | 같은 session에는 현재 사용자 client만 남아야 함 | `attach -d`가 기존 client를 detach하고 현재 terminal을 붙임 | continue |
| 31133311113 | attach 직후 사용자가 terminal 탭을 닫음 | aqua session은 detached 상태로 유지되고 다음 실행에서 인수 연결 가능해야 함 | tmux client 종료 뒤 session 자체는 유지됨 | route to: 31134 |
| 31133311210 | title 미표시 상태의 attach가 실패함 | title 문제가 아니라 attach 실패로 안내한 뒤 이 분기는 끝남 | `tmux-aqua attach` 오류가 표시됨 | terminate |
| 31133311211 | title 미표시 상태에서도 attach가 성공함 | 사용자는 title 없이도 새 agent shell 화면에서 입력할 수 있어야 함 | tmux client 전환은 title 표시 여부와 독립적으로 성공 | continue |
| 31133311212 | attach 후에도 session name을 확인할 수 없음 | session 내부 상태는 유지하고 session 목록 또는 로그에서 식별해야 함 | shell 화면에는 별도 title 재출력이 없을 수 있음 | continue |

다음 실행 ID
- 31133311111
- 31133311112
- 31133311211
- 31133311212
