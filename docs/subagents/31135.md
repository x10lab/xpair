# 31135

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31135 | aqua local target에서 낮은 번호가 이미 attached이거나 `--fresh`가 지정됨 | 기존 attached 세션을 건드리지 않고 다음 번호 또는 fresh 대상 세션을 새 대화로 열 준비를 함 | `_local_next_n` 결과와 `FRESH=1` 조건으로 `CONT=0`이 됨 | continue |
| 311350 | 사용자가 fresh aqua 세션 준비 전에 종료함 | 새 세션 생성, attach, 기존 attached 세션 변경 없음 | `tm_local new-session`과 `attach -d` 호출 전 종료 | terminate |
| 311351 | 낮은 `_N`에 live client가 붙어 있음 | 가장 낮은 빈 다음 번호를 선택하고 이전 attached 세션은 그대로 둠 | `list-clients`가 있는 번호를 건너뛰어 `N>1`, `CONT=0`으로 설정 | continue |
| 3113510 | 다음 번호 선택 뒤 사용자가 종료함 | 기존 attached 세션은 유지되고 새 번호 세션은 생기지 않음 | 생성 블록 전 종료 | terminate |
| 3113511 | 선택된 다음 번호 세션이 없음 | fresh `CONT=0` 재시작 파일로 새 aqua 세션을 만들고 attach를 시도함 | `NEED_CREATE=1`, `CL_CONTINUE=0`, `tm_local new-session` 실행 | continue |
| 31135110 | 새 aqua 세션 생성이 실패함 | 실패 원인을 보이고 attach로 넘어가지 않음 | `new-session` nonzero로 launcher가 실패 종료 | terminate |
| 31135111 | 새 aqua 세션 생성과 attach가 성공함 | 기존 attached 세션은 유지되고 사용자는 새 번호의 fresh agent shell을 봄 | `RP_SESSION`과 title을 새 `SESS`로 둔 뒤 `tmux-aqua attach -d` 성공 | route to: 21151 |
| 31135112 | 새 aqua 세션은 생겼지만 attach가 실패함 | 세션 상호작용으로 넘어가지 않고 실패 안내로 이동 | `attach -d -t "=$SESS"` nonzero 이후 오류와 로그 안내가 남음 | route to: 3147 |
| 311352 | `--fresh`가 지정되어 fresh 실행을 강제함 | 선택된 대상이 없으면 `_1`도 이전 대화 재개 없이 새 세션으로 시작함 | `[ "$FRESH" = 1 ] && CONT=0` 적용 | continue |
| 3113520 | fresh 지정 뒤 사용자가 종료함 | 새 세션 생성, attach 없음 | 생성 블록 전 종료 | terminate |
| 3113521 | fresh 대상 세션이 없음 | `CL_CONTINUE=0`으로 새 aqua 세션을 만들고 attach를 시도함 | `NEED_CREATE=1`, temp script에 `CL_CONTINUE=0` 기록 | continue |
| 31135210 | fresh 대상 새 세션 생성이 실패함 | 실패 원인을 보이고 종료 | `tm_local new-session` 실패로 EXIT trap이 동작 | terminate |
| 31135211 | fresh 대상 새 세션 생성과 attach가 성공함 | 사용자는 이전 기록을 재개하지 않는 fresh agent shell을 봄 | agent respawn script가 `CL_CONTINUE=0`으로 시작되고 attach 성공 | route to: 21151 |
| 31135212 | fresh 대상 새 세션은 생겼지만 attach가 실패함 | 실패 branch에서 원인을 확인함 | attach nonzero 뒤 stderr와 `claude-launch.err.log` 안내가 남음 | route to: 3147 |
| 3113522 | `--fresh`였지만 선택된 세션이 이미 detached 상태임 | 이 파일에서 새로 탐색하지 않고 detached takeover 분기로 보냄 | `tm_local has-session -t "=$SESS"`로 `NEED_CREATE=0`이 되어 생성 블록을 건너뜀 | route to: 31134 |

다음 실행 ID
