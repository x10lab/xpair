# 313

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 313 | `xpair launch`를 unmapped local folder에서 실행함 | remote host path 확인 뒤 등록, 생성, 취소, 자동 생성으로 분기 | `cmd_launch`가 먼저 mapping prompt를 열고, 이후 `xpair-launch`가 host dir을 다시 확인 | continue |
| 3130 | 사용자가 unmapped launch를 중단함 | mapping 등록, host dir 생성, 세션 생성 없음 | `cancelled.` 또는 directory missing 오류 후 종료 | terminate |
| 3131 | remote에 같은 path가 이미 있음 | 사용자에게 mapping 등록 여부를 묻고 launch는 계속 가능 | `exists on $REMOTE_HOST` 후 `[Y/n]` prompt 표시 | continue |
| 31310 | 등록 prompt에서 창을 닫거나 프로세스를 중단함 | mapping 등록, 세션 생성 없음 | 중단 신호가 들어오면 종료 | terminate |
| 31311 | 사용자가 mapping 등록을 수락함 | 현재 local folder와 같은 host path를 mapping에 추가 | 기본값 Y, 빈 입력도 `cmd_map add "$dir" "$hostdir"` 실행 | continue |
| 313110 | mapping add 전 사용자가 중단함 | mapping 등록, 세션 생성 없음 | 중단 시 종료 | terminate |
| 313111 | mapping add가 성공함 | 다음 실행부터 mapped remote launch로 처리 | `mapping added` 후 launcher 실행 | route to: 312 |
| 313112 | mapping add가 실패함 | 오류를 보여주고 세션 시작하지 않음 | local path 확인 실패나 env 저장 실패 시 종료 가능 | terminate |
| 31312 | 사용자가 mapping 등록을 거절함 | 이번 launch는 같은 host path가 존재하므로 계속 진행 | `[nN]*`이면 등록 없이 launcher 실행 | continue |
| 313120 | 거절 직후 사용자가 launch를 중단함 | mapping 없음, 세션 없음 | 중단 시 종료 | terminate |
| 313121 | 거절 후 그대로 진행함 | host dir이 있으므로 remote session 생성으로 이동 | launcher dir check가 `__YES__`를 받고 remote launch 진행 | route to: 312 |
| 31313 | prompt 중 다른 설정이 이미 mapping을 추가함 | 중복 등록 없이 mapped launch로 이어져야 함 | `cmd_map add`가 already mapped를 출력하고 성공 취급 | route to: 312 |
| 3132 | remote에 같은 path가 없음 | map to actual host path, create empty dir, cancel 중 선택 | `[m]`, `[c]`, `[N]` prompt 표시 | continue |
| 31320 | 사용자가 missing path prompt를 취소함 | host dir 생성, mapping 등록, 세션 생성 없음 | 기본값 N, 기타 입력도 `cancelled.` 후 종료 | terminate |
| 31321 | 사용자가 실제 host path로 mapping하기를 선택함 | host에 있는 content path를 입력하고 mapping을 저장해야 함 | `map_register_interactive "$dir"` 실행 | continue |
| 313210 | map 입력을 취소함 | mapping 등록, 세션 생성 없음 | client 또는 host path 입력 실패, cancel 선택 시 return 1 | terminate |
| 313211 | 입력한 host path가 존재하고 mapping add가 성공함 | folder only 또는 parent root 등록 뒤 remote launch로 이동 | this 또는 parent root 선택 후 `cmd_map add` 성공 | route to: 312 |
| 313212 | 입력한 host path는 존재하지만 mapping add가 실패함 | 오류를 보여주고 launch하지 않음 | `cmd_map add` 실패가 return 1로 전파 | terminate |
| 313213 | 입력한 host path가 없어 생성을 수락하고 성공함 | 생성된 host path를 mapping에 추가한 뒤 remote launch로 이동 | `[c]`에서 `mkdir -p` 성공 후 등록 범위 prompt로 이어짐 | route to: 312 |
| 313214 | 입력한 host path 생성을 수락했지만 실패함 | 오류를 보여주고 mapping 저장하지 않음 | `mkdir failed` 경고 후 같은 prompt에 남음 | terminate |
| 31322 | 사용자가 같은 path에 빈 dir 생성을 선택함 | 새 프로젝트용 빈 host dir을 만들고 launch 계속 | `[c]` 입력 시 `ssh mkdir -p '$hostdir'` 실행 | continue |
| 313220 | create 선택 전 중단함 | host dir 생성, mapping 등록, 세션 생성 없음 | 중단 시 종료 | terminate |
| 313221 | 같은 path mkdir가 성공함 | host dir이 생겼으므로 remote launch로 이동 | `created on $REMOTE_HOST` 후 launcher 실행 | route to: 312 |
| 313222 | 같은 path mkdir가 실패함 | 오류를 보여주고 세션 생성하지 않음 | `mkdir failed on $REMOTE_HOST` 후 return 1 | terminate |
| 31323 | 사용자가 missing path prompt에서 다른 값을 입력함 | 안전하게 취소 처리 | case 기본 분기가 `cancelled.` 반환 | continue |
| 313230 | 취소 처리 후 멈춤 | host dir 생성, mapping 등록, 세션 생성 없음 | 종료 코드 1 | terminate |
| 3133 | `RP_YES=1` 또는 `--yes`로 noninteractive 실행함 | prompt 없이 remote를 선택하고 missing host dir은 자동 생성 | `cmd_launch` mapping prompt를 건너뛰고 launcher가 `TARGET=remote` 선택 | continue |
| 31330 | `RP_YES` 자동 launch를 사용하지 않음 | interactive 흐름으로만 처리 | 해당 없음 | terminate |
| 31331 | `RP_YES`에서 host dir check가 성공함 | prompt 없이 remote session 생성 | `__YES__`이면 바로 remote script 생성 | continue |
| 313310 | dir check 후 사용자가 프로세스를 중단함 | 세션 생성 없음 | 중단 시 종료 | terminate |
| 313311 | host dir이 이미 있음 | remote mapped launch와 같은 session 흐름으로 이동 | mapping 저장 없이 같은 path 기준 session name 계산 | route to: 312 |
| 31332 | `RP_YES`에서 host dir이 없음 | prompt 없이 host dir을 생성해야 함 | `mk=y`로 강제되어 `mkdir -p` 실행 | continue |
| 313320 | 자동 mkdir 전 실행이 중단됨 | host dir 생성, 세션 생성 없음 | 프로세스 중단 시 종료 | terminate |
| 313321 | 자동 mkdir가 성공함 | 생성된 host dir에서 remote session 생성 | mkdir 성공 후 remote script 실행 | route to: 312 |
| 313322 | 자동 mkdir가 실패함 | mkdir 실패를 보여주고 종료 | `die "mkdir failed"`로 종료 코드 4 | terminate |
| 31333 | `RP_YES`에서 ssh dir check가 3회 실패함 | 무인 실행에서 local fallback으로 명확히 전환 | 현행은 `launch_local`로 fallback | continue |
| 313330 | fallback을 시작하지 못함 | 오류를 보여주고 종료 | local engine 또는 tmux 없음이면 종료 | terminate |
| 313331 | local fallback이 성공함 | remote 대신 local session으로 이어짐 | `launch_local` 실행 | route to: 311 |

다음 실행 ID
- 313
- 3131
- 31311
- 31312
- 3132
- 31321
- 31322
- 31323
- 3133
- 31331
- 31332
- 31333
