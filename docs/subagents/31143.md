# 31143

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31143 | 일반 tmux에서 낮은 번호가 이미 attached이거나 `--fresh`가 지정됨 | 기존 attached 세션을 건드리지 않고 다음 번호 또는 fresh 대상 세션을 새 대화로 열 준비를 함 | `TN>1` 또는 `FRESH=1`이면 `TCONT=0` | continue |
| 311430 | 사용자가 fresh plain tmux 세션 준비 전에 종료함 | 새 세션 생성, attach, 기존 attached 세션 변경 없음 | `tmux new`와 `tmux attach -d` 호출 전 종료 | terminate |
| 311431 | 낮은 `_N`에 live client가 붙어 있음 | 가장 낮은 빈 다음 번호를 선택하고 이전 attached 세션은 그대로 둠 | plain tmux client가 있는 번호를 건너뛰어 `TN>1`, `TCONT=0`으로 설정 | continue |
| 3114310 | 다음 번호 선택 뒤 사용자가 종료함 | 기존 attached 세션은 유지되고 새 번호 세션은 생기지 않음 | 생성 블록 전 종료 | terminate |
| 3114311 | 선택된 다음 번호 세션이 없음 | fresh 재시작 파일로 새 plain tmux 세션을 만들고 attach를 시도함 | `TCONT=0` 상태에서 `tmux new -s "$TSESS"` 실행 | continue |
| 31143110 | 새 plain tmux 세션 생성이 실패함 | 실패 원인을 보이고 attach로 넘어가지 않음 | `tmux new` 실패로 launcher가 종료 | terminate |
| 31143111 | 새 plain tmux 세션 생성과 attach가 성공함 | 기존 attached 세션은 유지되고 사용자는 새 번호의 fresh agent shell을 봄 | `RP_SESSION`을 새 `TSESS`로 둔 뒤 `tmux attach -d` 성공 | route to: 21151 |
| 31143112 | 새 plain tmux 세션은 생겼지만 attach가 실패함 | 세션 상호작용으로 넘어가지 않고 실패 안내로 이동 | `tmux attach -d -t "=$TSESS"` 실패 뒤 오류와 로그 안내가 남음 | route to: 3147 |
| 311432 | `--fresh`가 지정되어 fresh 실행을 강제함 | 대상이 `_1`이어도 이전 대화를 재개하지 않고 새 세션으로 시작함 | `[ "$FRESH" = 1 ]` 조건으로 `TCONT=0` 적용 | continue |
| 3114320 | fresh 지정 뒤 사용자가 종료함 | 새 세션 생성, attach 없음 | 생성 블록 전 종료 | terminate |
| 3114321 | fresh 대상 세션이 없음 | `TCONT=0`으로 새 plain tmux 세션을 만들고 attach를 시도함 | 재시작 파일이 이전 대화 재개 없이 agent를 시작하도록 작성됨 | continue |
| 31143210 | fresh 대상 새 세션 생성이 실패함 | 실패 원인을 보이고 종료 | `tmux new` 실패로 오류가 남음 | terminate |
| 31143211 | fresh 대상 새 세션 생성과 attach가 성공함 | 사용자는 이전 기록을 재개하지 않는 fresh agent shell을 봄 | plain tmux 세션이 `TCONT=0`으로 시작되고 attach 성공 | route to: 21151 |
| 31143212 | fresh 대상 새 세션은 생겼지만 attach가 실패함 | 실패 branch에서 원인을 확인함 | attach 실패 뒤 stderr와 launch 오류 안내가 남음 | route to: 3147 |
| 3114322 | `--fresh`였지만 선택된 세션이 이미 detached 상태임 | 새로 만들지 않고 plain tmux detached takeover 분기로 보냄 | `tmux has-session -t "=$TSESS"` 통과로 생성 블록을 건너뜀 | route to: 31142 |
| 311433 | 사용자가 plain tmux 대신 XpairHost 권한 상속이나 Remote Desktop 표면을 기대함 | 이 branch에서는 접근 불가, host 권한 상속은 aqua 또는 host setup 경로로만 다룸 | 일반 tmux fallback에는 XpairHost subtree와 Remote Desktop surface가 없음 | terminate |

다음 실행 ID
