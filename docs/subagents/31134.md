# 31134

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31134 | aqua local target의 대상 세션이 detached 상태임 | 새 세션을 만들지 않고 기존 세션을 `attach -d`로 인수 연결함 | `tm_local has-session -t "=$SESS"` 통과로 `NEED_CREATE=0`이 됨 | continue |
| 311340 | 사용자가 attach 전 터미널이나 Xpair 표면을 닫음 | 새 세션 생성 없음, 기존 detached 세션은 그대로 남음 | `exec tmux-aqua attach -d` 전 종료되면 후속 attach가 없음 | terminate |
| 311341 | launcher가 기존 세션 인수 연결 준비를 확인함 | `NEED_CREATE=0`을 유지하고 respawn 파일이나 새 tmux session을 만들지 않음 | `if [ "$NEED_CREATE" = 1 ]` 블록을 건너뜀 | continue |
| 3113410 | 준비 확인 뒤 사용자가 종료함 | attach 없이 기존 detached 세션만 남음 | 이후 `attach -d` 호출 전 종료 | terminate |
| 3113411 | 새 세션 생성 경로가 비활성화됨 | detached 세션을 그대로 이어 붙고 새 대화나 `_2` 세션으로 가지 않음 | `tm_local new-session`이 실행되지 않음 | continue |
| 31134110 | 준비 중 대상 세션이 사라진 stale tmux 상태가 됨 | 새 세션으로 대체하지 않고 실패로 끝남 | attach 시점에는 재생성 경로가 없어 `attach -d` 실패로 이어짐 | terminate |
| 31134111 | respawn temp 파일 생성이 생략됨 | 기존 tmux 안의 agent 프로세스와 화면을 그대로 보존함 | `mktemp` 파일은 만들어졌더라도 `NEED_CREATE=0`이라 세션 시작 스크립트로 쓰이지 않음 | continue |
| 31134112 | 사용자가 detached takeover 대신 새 세션을 기대함 | 이 분기에서는 새 세션을 만들 수 없고 새 aqua 세션 분기로 보내야 함 | 새 세션은 대상 세션 없음 또는 fresh 조건에서만 처리됨 | route to: 31133 |
| 311342 | 대상이 plain tmux detached 세션으로 판명됨 | aqua takeover 문서에서 탐색하지 않고 plain tmux attach 분기로 보냄 | plain tmux는 `launch_local` fallback의 별도 `tmux attach -d` 경로 | route to: 31142 |
| 311343 | `tmux-aqua attach -d` 실행이 시작됨 | 기존 detached 세션에 연결하고 stale client가 있으면 끊음 | `exec "$LOCAL_BIN/tmux-aqua" -S "$AQUA_SOCK" attach -d -t "=$SESS"` 실행 | continue |
| 3113430 | 사용자가 attach 명령 실행 직전에 닫음 | 기존 세션은 detached로 남고 새 세션 없음 | 프로세스 종료로 attach argv가 실행되지 않음 | terminate |
| 3113431 | attach가 성공함 | 사용자 터미널이 기존 aqua 세션 화면으로 전환됨 | `attach -d` 성공 시 launcher 프로세스가 tmux 클라이언트로 대체됨 | continue |
| 31134310 | attach 직후 사용자가 탭을 닫음 | tmux 세션은 다시 detached 상태로 남음 | tmux client 종료 뒤 세션 자체는 유지됨 | terminate |
| 31134311 | stale client가 남아 있었지만 takeover가 성공함 | stale 연결을 끊고 같은 세션에 하나의 활성 client만 남김 | `attach -d`가 기존 client를 detach함 | continue |
| 31134312 | 사용자가 재개된 agent 화면을 봄 | 마지막 상태 그대로 이어서 local agent interaction으로 넘어감 | 새 `CL_CONTINUE` 실행 없이 기존 tmux 화면이 표시됨 | continue |
| 3113432 | attach가 실패함 | 실패 원인을 보여주고 세션 상호작용으로 넘어가지 않음 | `exec`된 `tmux-aqua attach`가 nonzero로 끝나면 terminal에 tmux 오류가 남음 | continue |
| 31134320 | 실패 원인이 stale tmux 세션 없음임 | 세션 없음 오류로 끝내고 새 세션을 자동 생성하지 않음 | `can't find session`류 오류 후 종료 | terminate |
| 31134321 | 실패 원인이 aqua socket 또는 XpairHost tmux 서버 손실임 | host guard 실패 분기로 되돌려 aqua 복구 또는 plain fallback 판단을 다시 함 | attach 단계에서 socket 연결이 끊기면 이전 guard 결과가 무효가 됨 | route to: 31132 |
| 31134322 | 사용자가 attach 실패 메시지와 로그 안내를 확인함 | 실패 branch에서 원인 확인 후 종료 또는 재시도를 결정함 | stderr tee와 EXIT trap이 실패 로그 위치를 보여줌 | route to: 3147 |

다음 실행 ID
- 31134111
- 31134311
- 31134312
