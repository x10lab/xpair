# 211

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 211 | Finder Quick Action을 mapped folder에서 실행 | 선택된 folder를 `xpair launch <dir>`로 넘기고 mapped host path의 세션 attach로 이어짐 | `Launch Xpair` Service는 Finder의 `public.folder` 입력만 받음 | continue |
| 2110 | Quick Action 호출 뒤 사용자가 터미널 또는 실행 표면을 닫음 | 세션 생성, SSH, mosh attach 없이 종료 | 실행 전 또는 실패 pause에서 닫으면 launcher 진행 없음 | terminate |
| 2111 | Finder가 mapped folder 선택을 Service에 전달함 | 선택된 folder path 하나를 CLI launch 대상으로 사용 | workflow service가 Finder folder 입력을 받음 | continue |
| 21110 | Finder 선택이 folder가 아니거나 사라짐 | Quick Action이 실행되지 않거나 CLI가 `folder not found`로 종료 | `NSSendFileTypes=public.folder`, CLI는 `cd` 실패 시 종료 | terminate |
| 21111 | Finder 선택이 mapped folder로 유지됨 | `xpair launch <dir>`가 절대 client path를 계산 | `cmd_launch`가 `cd`로 절대 path를 확정 | continue |
| 211110 | 사용자가 path 확정 뒤 실행을 중단함 | mapping, host probe, session 생성 없이 종료 | 중단 시 이후 launcher 호출 없음 | terminate |
| 211111 | folder가 등록된 mapping 아래에 있음 | unmapped 등록 prompt 없이 launcher로 이동 | `is_mapped` true면 interactive map 등록 단계를 건너뜀 | continue |
| 211112 | mapping은 있지만 selected path가 존재하지 않음 | folder 없음 오류로 종료 | `cd` 실패 후 `folder not found` | terminate |
| 21112 | Quick Action이 Xpair 범위 밖 Finder 기능으로 빠짐 | 접근 불가, 하위 탐색 차단 | Finder 기본 메뉴와 Service 외 기능은 Xpair flow 아님 | terminate |
| 2112 | CLI가 mapped folder의 host path와 session base를 계산함 | client path를 host path로 치환하고 host path 기준 session 이름을 만듦 | `map_to_host`, `_proj_base`, `REMOTE_PROJ` 사용 | continue |
| 21120 | 사용자가 session 이름 계산 전 실행을 중단함 | 세션 생성 없이 종료 | 프로세스 종료 시 launcher 진행 없음 | terminate |
| 21121 | mapped host path가 계산됨 | host의 실제 project path만 이후 remote 실행 기준으로 사용 | longest-prefix mapping으로 `HOST_DIR` 확정 | continue |
| 211210 | host path 계산만 끝나고 attach를 시작하지 않음 | 세션 생성 없음 | 이후 target 또는 reach 단계 전 중단이면 종료 | terminate |
| 211211 | host path가 host에 존재함 | directory check를 통과하고 remote setup으로 진행 | SSH `[ -d HOST_DIR ]`가 `__YES__`면 계속 | continue |
| 211212 | host path가 host에 없음 | mapped stale 또는 missing dir CLI flow로 이동 | create prompt 또는 cancel이 뜸 | route to: 212 |
| 21122 | session base가 host path와 hostname으로 만들어짐 | 같은 host path는 같은 base, 이미 attached면 다음 번호로 분기 | `<REMOTE_HOST>_<slug>_<hash>` 뒤 `_N` 결정 | continue |
| 211220 | 사용자가 session base 표시 전 종료함 | terminal title, tmux attach 없음 | 종료 시 no session | terminate |
| 211221 | 같은 project의 낮은 `_N`이 client tab에 붙어 있음 | 새 `_N`을 fresh 세션으로 연다 | local `mosh-client` fact로 다음 번호 선택, `_2+`는 `CL_CONTINUE=0` | continue |
| 211222 | 같은 project의 `_1`이 없거나 detached임 | `_1` 생성 또는 take-over attach | remote tmux 존재 시 attach, 없으면 new-session | continue |
| 2113 | launcher가 remote/local target을 결정함 | 기본은 remote, 명시 local 또는 self-host면 local branch로 이동 | `REMOTE_HOST`, `--local`, `--remote`, prompt로 결정 | continue |
| 21130 | target 선택 prompt에서 사용자가 종료함 | remote/local 세션 생성 없이 종료 | 선택 전 종료면 launcher 진행 없음 | terminate |
| 21131 | 사용자가 remote target을 선택하거나 기본값을 둠 | host reachability와 mapped host dir 확인으로 진행 | prompt 기본 `[1] remote`, `RP_YES=1`도 remote | continue |
| 211313 | remote 선택 뒤 SSH reach가 실패함 | CLI reachability 실패 flow로 이동 | tailscale 시도 후 실패하면 local fallback | route to: 314 |
| 211311 | remote 선택 뒤 SSH reach가 성공함 | remote setup script 실행으로 진행 | `ssh REMOTE_HOST true` 통과 | continue |
| 211312 | remote 선택 뒤 dir check가 실패함 | stale mapping 또는 unreachable check flow로 이동 | `__NO__`나 3회 실패는 create, cancel, local fallback | route to: 212 |
| 21132 | 사용자가 local target을 선택함 | local tmux 또는 local XpairHost session flow로 이동 | `TARGET=local`이면 `launch_local` 실행 | route to: 311 |
| 21133 | `--remote`가 명시됨 | prompt 없이 remote branch로 고정 | `TARGET_PREF=remote`가 우선 | continue |
| 211332 | 명시 remote에서 host가 unreachable | CLI SSH/auth 실패 branch로 이동 | reach 실패와 fallback 처리 | route to: 314 |
| 211331 | 명시 remote에서 host와 dir check가 통과함 | remote session setup으로 이동 | remote script 실행 가능 상태 | route to: 211311 |
| 2114 | remote setup이 tmux session을 만들거나 기존 session에 붙음 | 사용자는 terminal에서 mosh 또는 ssh attach 화면을 봄 | remote script가 `__SESSION__`을 만들고 attach 실행 | continue |
| 21140 | remote setup 전에 사용자가 터미널을 닫음 | host tmux 생성 없이 종료 | ssh setup 전 종료면 no session | terminate |
| 21141 | host에 같은 session이 없음 | host tmux session을 새로 만들고 agent CLI를 시작 | `tm new-session -d`, `CL_CONTINUE=1` 또는 fresh 조건 반영 | continue |
| 211413 | agent CLI가 host에 없음 | 설치 안내 오류 후 종료 | `command -v claude/codex/opencode` 실패 시 exit 11 | route to: 314 |
| 211411 | XpairHost tmux-aqua server가 없음 | host app 또는 권한 문제 안내 후 종료 | `tm has-session` 실패 시 exit 13 | route to: 314 |
| 211412 | host tmux session 생성 성공 | terminal title과 attach 단계로 진행 | `__SESSION__:<name>` 반환 | continue |
| 21142 | host에 같은 session이 detached로 있음 | 새 대화가 아니라 기존 tmux session에 take-over attach | `tm has-session`이면 `NEED_CREATE=0` | continue |
| 211420 | 사용자가 detached session attach 직전 닫음 | 기존 host session은 detached 상태로 남음 | attach 실행 전 종료 | terminate |
| 211421 | `mosh`가 설치되어 있음 | mosh로 remote tmux에 attach | `mosh --server ... tmux-aqua attach -d` | continue |
| 211422 | `mosh`가 없음 | ssh TTY로 remote tmux에 attach | `ssh -t ... tmux-aqua attach -d` fallback | continue |
| 21143 | 같은 project의 낮은 번호가 이미 client tab에 붙어 있음 | 다음 `_N` fresh session을 생성 후 attach | `_remote_next_n`이 live `mosh-client`를 보고 `_2+` 선택 | continue |
| 211430 | 사용자가 새 번호 session attach 전 닫음 | 새 session 생성 전이면 변화 없음, 생성 후면 detached로 남음 | 단계에 따라 setup script 실행 여부가 갈림 | terminate |
| 211431 | `_2+` session 생성 성공 | 이전 session과 별개 fresh 대화로 attach | `RCONT=0`, 새 tmux session 생성 | continue |
| 211432 | `_2+` session 생성 실패 | CLI remote setup 실패 branch로 이동 | SSH_RC 또는 session marker 없음이면 die | route to: 314 |
| 2115 | 사용자가 terminal attach 화면을 봄 | mapped host path에서 agent session이 열려 입력 가능 | mosh 또는 ssh attach가 foreground를 차지 | continue |
| 21150 | terminal attach 화면을 닫음 | remote session은 detach되고 다음 실행에서 이어 붙을 수 있음 | HUP/TERM trap이 remote detach-client 시도 | terminate |
| 21151 | attach된 shell에 agent 화면이 보임 | 사용자는 해당 mapped host path 기준 세션을 사용 | tmux cwd가 `HOST_DIR`, title은 session name | continue |
| 211510 | 사용자가 agent 화면에서 아무 입력도 하지 않음 | session은 attached 상태로 유지 | 동일 | terminate |
| 211511 | 사용자가 agent에 입력함 | 기존 agent CLI flow로 진행, Finder Quick Action 하위 탐색은 종료 | Claude/Codex/OpenCode 자체 대화는 Xpair launch 밖 | terminate |
| 211512 | 사용자가 tmux detach를 실행함 | host session은 detached로 남고 다음 mapped launch에서 take-over 가능 | `attach -d` 정책상 다음 실행에서 재연결 | route to: 21142 |
| 21152 | attach 중 네트워크가 끊김 | mosh면 roaming 복구, ssh fallback이면 attach 종료 가능 | mosh는 복구, ssh는 연결 종료될 수 있음 | continue |
| 211523 | 네트워크가 복구되지 않음 | terminal attach는 끊기고 host session은 남음 | 다음 실행에서 detached 또는 stale cleanup 대상 | route to: 314 |
| 211521 | mosh 연결이 복구됨 | 같은 tmux session 화면으로 돌아옴 | mosh roaming | route to: 21151 |
| 211522 | ssh fallback 연결이 끊김 | terminal 종료, host session은 detached로 남아야 함 | ssh attach 종료 | route to: 21142 |

다음 실행 ID
- 211111
- 211211
- 211221
- 211222
- 211311
- 211412
- 211421
- 211422
- 211431
