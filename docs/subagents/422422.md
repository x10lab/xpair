# 422422

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 422422 | 새 연결 host로 Check connection을 누름 | SSH probe 결과와 host 준비 상태로 분기 | `sshReachable(host.trim())` 실행 | continue |
| 4224220 | Check connection 직후 사용자가 창을 닫거나 취소함 | host 저장 없이 Host는 waiting 상태를 유지 | probe 완료 전 종료면 `setHost` 호출 없음 | terminate |
| 4224221 | 입력한 host에 SSH probe가 시작됨 | network, auth, success, 지연 상태로 분기 | BatchMode SSH reachability 확인이 진행됨 | continue |
| 4224222 | Check connection 중 입력값을 바꾸거나 다른 host를 선택하려 함 | 현재 probe를 확정하지 않고 host 입력 단계로 돌아감 | 입력 변경은 다음 check 전까지 저장되지 않음 | route to: 42242 |
| 4224223 | 사용자가 연결 확인 중 VSCodium 기본 표면으로 빠짐 | Xpair 연결 흐름 밖이므로 접근 불가 | 기본 IDE 표면은 남아 있을 수 있으나 host 연결 판단과 무관 | terminate |
| 42242210 | probe 대기 중 사용자가 중단함 | host 저장 없이 failure나 success 표시도 하지 않음 | 창 종료 또는 back 동작이면 이번 check 결과를 쓰지 않음 | terminate |
| 42242211 | SSH probe가 실패함 | 실패 안내와 Retry, host 재입력, 설치 안내 선택지로 분기 | `Couldn't reach host` 또는 offline 상태로 표시 | continue |
| 42242212 | SSH probe가 성공함 | host 저장, Ready 상태, 다음 단계 진입으로 분기 | 성공 시 `setHost` 후 reachable 상태가 됨 | continue |
| 42242213 | SSH probe가 즉시 끝나지 않고 대기 상태가 길어짐 | 사용자는 기다리거나 중단하고, 결과가 오면 성공/실패로 이동 | 연결 지연 동안 checking 상태가 유지될 수 있음 | continue |
| 422422110 | 실패 안내를 보고 Retry하지 않음 | Host는 connected client 없이 waiting 상태를 유지 | 실패 화면만 남고 host 저장 없음 | terminate |
| 422422111 | Retry를 눌러 같은 host를 다시 확인함 | 같은 host에 대해 SSH probe 결과를 다시 판단 | probe를 재실행하고 실패 또는 성공 상태로 갱신 | continue |
| 422422112 | 실패 뒤 host 값을 고쳐 다시 입력하려 함 | 수동 host 입력 branch로 돌아감 | 새 trim 값은 다음 Check connection에서만 검증 | route to: 42242 |
| 422422113 | 실패 원인이 Host 미설치 또는 Host 미실행이라고 판단해 안내를 선택함 | XpairHost 설치, 실행, 권한 안내 흐름으로 이동 | 실패 화면에서 setup 안내로 이어질 수 있음 | route to: 111117 |
| 422422120 | 성공 표시 후 사용자가 Next를 누르지 않고 닫음 | 저장된 host는 남을 수 있지만 이번 onboarding 진행은 중단 | `setHost` 이후 종료면 `REMOTE_HOST`는 남을 수 있음 | terminate |
| 422422121 | 성공 표시를 확인하고 Ready 상태에 머무름 | 사용자는 Next, host 변경, 창 닫기를 선택할 수 있음 | `Reachable, your key works` 상태가 유지됨 | continue |
| 422422122 | 성공 표시 뒤 Next를 누름 | Host app guard, engine, 권한, 파일 접근 온보딩으로 이동 | reachable 상태에서 다음 onboarding 단계가 열린다 | route to: 42244 |
| 422422123 | 성공 표시 뒤 다른 host로 바꾸려 함 | 새 host connect branch에서 다시 검증 | 기존 성공 host 대신 입력 단계로 돌아감 | route to: 42242 |
| 422422124 | 성공 뒤 XpairHost 앱을 직접 조작하려 함 | Xpair client 연결 흐름 밖이므로 현재 branch 종료 | Host-first 조작은 client-to-host path의 하위 flow가 아님 | terminate |
| 422422130 | 대기 상태에서 창을 닫거나 중단함 | host 저장 없이 연결 확인을 끝냄 | checking 상태가 사라지고 이후 UI 갱신 없음 | terminate |
| 422422131 | 대기 끝에 SSH 실패가 돌아옴 | 실패 안내 branch로 이동 | 실패 상태로 갱신됨 | route to: 42242211 |
| 422422132 | 대기 끝에 SSH 성공이 돌아옴 | 성공 branch로 이동 | reachable 상태로 갱신됨 | route to: 42242212 |
| 422422133 | 대기 상태가 유지되는 동안 사용자가 기다림 | 다음 판단은 성공, 실패, 중단 중 하나로 이어짐 | checking 표시가 계속 보임 | continue |

다음 실행 ID
- 422422111
- 422422121
- 422422133
