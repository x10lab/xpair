# 132

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 132 | 사용자가 Xpair Remote Desktop을 엶 | RD 탭이 열리거나 기존 RD 탭이 앞으로 오고, host 화면 연결 상태를 보여줌 | activation에서 RD를 자동 reveal하고 명령도 `panel.reveal()` 실행 | continue |
| 1320 | 사용자가 RD를 열지 않거나 바로 닫음 | RD 하위 흐름 없이 종료 | 패널 dispose 시 tunnel을 정리하고 panel 참조를 비움 | terminate |
| 1321 | RD 탭 reveal 또는 복원 경로에 들어감 | 새 RD는 하나만 만들고, 기존 RD는 재사용해야 함 | singleton panel과 serializer restore가 있음 | continue |
| 13210 | RD reveal 뒤 사용자가 아무 조작도 하지 않음 | 열린 RD 상태만 유지하고 추가 세션 동작 없음 | visible이면 stream 시작, 숨겨지면 정리 | terminate |
| 13211 | 기존 RD panel이 이미 있음 | 새 탭을 만들지 않고 기존 panel을 reveal함 | `this.panel.reveal()` 후 return | continue |
| 132110 | 기존 RD 탭을 앞으로 가져온 뒤 멈춤 | 탭 중복 없이 기존 화면만 보임 | 동일 | terminate |
| 132111 | 기존 RD가 visible이 됨 | v2 stream 시작 조건으로 넘어감 | view state에서 `_startStream()` 호출 | route to: 1322 |
| 132112 | 기존 RD가 숨겨짐 | signaling tunnel과 stream 상태를 끊음 | `_stopAll()`이 `_stopV2()` 실행 | terminate |
| 13212 | RD panel이 없고 새로 만들어야 함 | `RD` pinned editor tab을 하나 생성함 | `createWebviewPanel`, `_adopt`, `pinEditor` 실행 | continue |
| 132120 | 새 RD 생성 중 사용자가 닫음 | 생성된 panel을 dispose하고 tunnel 없음 상태로 종료 | dispose handler가 상태 정리 | terminate |
| 132121 | 새 RD 생성이 완료됨 | webview HTML, JS, CSS가 붙고 visible이면 stream 시작 | `_adopt()`가 html과 message handler를 연결 | route to: 1322 |
| 132122 | 새 RD가 생성됐지만 pin 명령이 실패함 | RD 동작은 유지되어야 함 | pin 실패는 무시하고 panel은 남음 | continue |
| 13213 | VSCode reload 뒤 RD webview가 복원됨 | 복원 panel을 singleton으로 채택해야 함 | serializer가 `panel.restore(restored)` 호출 | continue |
| 132130 | 복원된 RD를 사용자가 바로 닫음 | 복원 상태를 버리고 tunnel 정리 | dispose handler가 정리 | terminate |
| 132131 | 복원 panel이 첫 panel임 | HTML과 handler를 다시 붙이고 visible 상태를 유지 | `_adopt(restored)` 실행 | route to: 1322 |
| 132132 | 복원 panel이 중복 panel임 | 중복 RD를 닫고 기존 RD 하나만 남김 | `_adopt()`가 incoming panel dispose | terminate |
| 1322 | RD가 visible 상태에서 host 연결을 시작함 | 유효 host가 있으면 v2 signaling tunnel을 시작함 | `_startStream()`이 `getValidHost()` 후 `_startV2()` 호출 | continue |
| 13220 | host 설정이 없거나 유효하지 않음 | RD overlay에 host 미설정 상태를 보여주고 종료 | `status no-host` 메시지 표시 | terminate |
| 13221 | 유효 host가 있음 | 빈 local port를 잡고 ssh tunnel을 준비함 | `getFreePort()` 호출 | continue |
| 132210 | local port 확보 실패 | RD 오류 overlay를 보여주고 종료 | `status error` 전송 | terminate |
| 132211 | local port 확보 성공 | host `screen serve-webrtc` signaling port로 ssh local-forward를 시작함 | `spawnTunnel(host, localPort, SIGNAL_REMOTE_PORT)` 실행 | continue |
| 132212 | stream start가 중복 호출됨 | 이미 active면 새 tunnel을 만들지 않음 | `_v2Active` guard로 return | terminate |
| 13222 | ssh tunnel child가 시작됨 | foreground ssh가 살아 있는 동안 signaling 연결 가능해야 함 | `ssh -N -L local:127.0.0.1:remote host` spawn | continue |
| 132220 | tunnel process가 바로 종료됨 | RD 오류를 사용자에게 보여줘야 함 | close는 log만 남기고 overlay 오류 전환 없음 | terminate |
| 132221 | tunnel settle 시간이 지남 | webview에 loopback signaling URL을 전달함 | 1200ms 뒤 `v2Connect` post | route to: 1323 |
| 132222 | settle 전에 RD가 닫히거나 숨겨짐 | webview connect를 보내지 않고 종료 | `_v2Active`와 panel guard가 return | terminate |
| 13223 | RD refresh 또는 visibility 복귀가 tunnel 시작과 겹침 | tunnel은 하나만 유지되어야 함 | active guard와 stop 후 start 경로가 있음 | continue |
| 132230 | 겹친 호출을 무시함 | 기존 tunnel 하나만 유지 | `_v2Active`면 return | terminate |
| 132231 | refresh가 먼저 기존 tunnel을 끊음 | 새 tunnel 하나로 재연결해야 함 | `_stopAll()` 후 `_startStream()` | route to: 1324 |
| 132232 | 숨김 처리와 start가 겹침 | connect 메시지를 보내지 않음 | timeout guard가 return | terminate |
| 1323 | webview가 v2 signaling과 첫 frame을 처리함 | video만 표시하고 view-only badge를 유지함 | `connectV2()`가 WebRTC receive-only로 연결 | continue |
| 13230 | webview ready 뒤 사용자가 기다리지 않고 RD를 닫음 | 연결 중단 후 종료 | extension dispose가 tunnel 정리 | terminate |
| 13231 | `v2Connect` 메시지를 받음 | WebSocket을 열고 receive-only PeerConnection을 만듦 | `connectV2(signalUrl)` 실행 | continue |
| 132310 | WebSocket 생성자가 throw함 | v2 error로 보고 overlay 오류 표시 | `v2Error` post 후 status error | terminate |
| 132311 | signaling WebSocket이 열림 | negotiating overlay를 보여주고 offer를 기다림 | `v2: signaling connected, negotiating...` 표시 | continue |
| 132312 | signaling WebSocket이 error 또는 비정상 close됨 | RD 오류 overlay를 보여줌 | `v2Error` post 후 status error | terminate |
| 13232 | host offer가 도착함 | answer를 만들어 signaling으로 돌려보냄 | `setRemoteDescription`, `createAnswer`, `setLocalDescription`, answer send | continue |
| 132320 | offer를 받았지만 사용자가 조작하지 않음 | negotiation만 진행되고 입력은 보내지 않음 | input DataChannel handler 없음 | terminate |
| 132321 | ICE candidate가 오감 | 연결 후보를 추가하고 media track을 기다림 | candidate send, addIceCandidate 처리 | continue |
| 132322 | candidate 추가가 실패함 | 치명 오류로 보지 않고 계속 대기 | catch 후 무시 | continue |
| 13233 | 첫 media track이 도착함 | overlay를 숨기고 host 화면 영상을 표시함 | `video.srcObject`, `hideOverlay()`, `v2FirstFrame` post | continue |
| 132330 | 첫 frame 뒤 사용자가 보기만 함 | host 화면만 표시, 원격 입력 없음 | video 표시와 view-only badge 유지 | terminate |
| 132331 | 첫 frame 이벤트가 extension으로 전달됨 | 정상 렌더링 로그만 남김 | `v2: media track rendering` log | continue |
| 132332 | 추가 track 또는 이후 frame이 계속 옴 | overlay 재표시 없이 video가 갱신됨 | `haveFrame` true라 overlay 숨김 유지 | continue |
| 1324 | 사용자가 RD refresh를 실행함 | 기존 v2 연결을 끊고 새 signaling tunnel로 다시 연결함 | command가 `panel.refresh()` 실행 | continue |
| 13240 | refresh 명령을 누르지 않음 | 기존 RD 상태 유지 | 변화 없음 | terminate |
| 13241 | refresh 명령이 panel 없는 상태에서 호출됨 | 아무 동작 없이 종료 | `if (this.panel)` guard | terminate |
| 13242 | refresh 명령이 panel 있는 상태에서 호출됨 | 현재 tunnel을 정리하고 start stream으로 돌아감 | `onMessage({ type: "refresh" })` | continue |
| 132420 | refresh 직후 host가 없음 | no-host overlay로 종료 | `status no-host` | terminate |
| 132421 | refresh 직후 host가 유효함 | 새 port와 tunnel을 잡고 재연결 | route to: 1322 | route to: 1322 |
| 132422 | refresh 중 사용자가 RD를 숨김 | 새 connect를 보내지 않고 종료 | stop과 timeout guard로 차단 | terminate |
| 13243 | refresh 후 webview가 새 `v2Connect`를 받음 | 기존 ws와 pc를 닫고 새 연결만 유지 | `closeWs()`, `closePc2()` 후 connect | continue |
| 132430 | refresh 후 새 연결을 기다리다 멈춤 | connecting overlay 유지 | 표시만 유지 | terminate |
| 132431 | refresh 후 첫 frame이 다시 도착함 | overlay를 숨기고 최신 화면을 보여줌 | route to: 13233 | route to: 13233 |
| 132432 | refresh 후 v2 error가 남 | 오류 overlay 표시 | route to: 1325 | route to: 1325 |
| 1325 | v2 오류가 발생함 | RD overlay에 오류와 host 실행 확인 안내를 보여줌 | `status error`가 Error 메시지와 XpairHost 안내 표시 | continue |
| 13250 | 오류 overlay에서 아무 조작도 하지 않음 | 오류 상태로 종료 | overlay 유지 | terminate |
| 13251 | peer connection failed가 발생함 | v2 error로 보고 오류 overlay 표시 | connectionState failed에서 `v2Error` post | continue |
| 132510 | 사용자가 오류만 확인함 | 추가 동작 없음 | 동일 | terminate |
| 132511 | 사용자가 refresh로 재시도함 | 새 tunnel과 signaling으로 돌아감 | route to: 1324 | route to: 1324 |
| 132512 | 사용자가 RD를 닫음 | tunnel을 정리하고 종료 | dispose handler와 `_stopAll()` | terminate |
| 13252 | signaling WebSocket 오류가 발생함 | v2 error로 보고 오류 overlay 표시 | error listener가 detail 포함 post | continue |
| 132520 | WebSocket 오류 뒤 멈춤 | 오류 overlay 유지 | 동일 | terminate |
| 132521 | WebSocket 오류 뒤 refresh | 재연결 경로로 이동 | route to: 1324 | route to: 1324 |
| 132522 | WebSocket 오류 뒤 host 설정을 바꾸려 함 | RD 흐름 밖 host 설정으로 이동 | status bar host 또는 connectHost 경로 | route to: 1225 |
| 13253 | webview가 오류 detail을 보냄 | extension은 detail을 문자열화해 status error를 돌려줌 | `this.post({ type: "status", state: "error" })` | continue |
| 132530 | detail 없는 unknown error | unknown 또는 webrtc error 문구 표시 | fallback detail 사용 | terminate |
| 132531 | detail 있는 error | detail을 overlay에 표시 | 동일 | terminate |
| 132532 | 오류 후 첫 frame이 뒤늦게 옴 | 화면이 보이면 overlay를 숨기는 게 맞음 | webview `haveFrame`이 false면 hideOverlay 실행 | continue |
| 1326 | 사용자가 RD 화면에 키보드나 마우스 입력을 시도함 | Remote Desktop은 영구 view-only라 host 입력을 보내면 안 됨 | 입력 forwarding 코드가 없음 | continue |
| 13260 | 사용자가 입력을 시도하지 않음 | host 화면 보기만 유지 | 동일 | terminate |
| 13261 | 사용자가 RD video를 클릭함 | remote mouse click이 전송되지 않아야 함 | click handler 없음 | continue |
| 132610 | 클릭 뒤 화면만 유지됨 | host 입력 없음 | 동일 | terminate |
| 132611 | 클릭 중 DataChannel이 있어도 입력을 보내려 함 | 접근 불가, view-only라 제어 채널을 쓰지 않음 | `ondatachannel`은 채널을 닫고 입력을 보내지 않음 | terminate |
| 132612 | 클릭으로 focus만 이동함 | IDE focus 변화 외 remote side effect 없음 | 브라우저 기본 focus만 가능 | terminate |
| 13262 | 사용자가 키를 누름 | remote keyboard input이 전송되지 않아야 함 | key handler 없음 | continue |
| 132620 | 키 입력 뒤 화면만 유지됨 | host 입력 없음 | 동일 | terminate |
| 132621 | shortcut이 VSCode 자체 기능으로 처리됨 | Xpair RD 입력 흐름에서는 접근 불가 | workbench shortcut은 남을 수 있음 | terminate |
| 132622 | text 입력을 기대함 | 접근 불가, RD는 display only | 입력 전송 기능 없음 | terminate |
| 13263 | 사용자가 view-only badge를 확인함 | badge가 view-only로 표시되어 제어 불가를 알려야 함 | `badge.textContent = "view-only"`, class off | continue |
| 132630 | badge만 보고 멈춤 | 화면 보기 상태 유지 | 동일 | terminate |
| 132631 | badge를 클릭하려 함 | badge는 조작 불가 | `pointer-events: none` | terminate |
| 132632 | badge가 숨거나 on 상태가 됨 | view-only 의미가 깨지면 안 됨 | 코드상 항상 off 설정 | terminate |
| 1327 | RD 중복 탭 guard가 작동함 | 같은 창에 RD 탭은 하나만 남아야 함 | tabGroups guard와 restore adopt guard가 있음 | continue |
| 13270 | 중복 상황이 없음 | 기존 singleton 흐름 유지 | 동일 | terminate |
| 13271 | 다른 extension host가 이미 RD 탭을 열었음 | 새 RD를 만들지 않고 중단 | tabGroups에서 viewType 확인 후 return | continue |
| 132710 | guard 뒤 사용자가 기존 RD를 보기만 함 | 기존 RD 하나만 유지하고 종료 | 동일 | terminate |
| 132711 | guard 뒤 새 RD 생성을 기대함 | 접근 불가, 중복 생성은 차단 | log만 남기고 생성 안 함 | terminate |
| 132712 | guard 확인 중 tabGroups 접근이 실패함 | 실패해도 app은 멈추지 않아야 함 | catch 후 새 panel 생성 경로 가능 | continue |
| 13272 | reload restore와 startup reveal이 동시에 옴 | 복원 RD를 채택하거나 중복을 dispose해야 함 | serializer와 `_adopt()`가 처리 | continue |
| 132720 | restore RD가 먼저 채택된 상태에서 멈춤 | 복원 RD 하나만 유지하고 종료 | 동일 | terminate |
| 132721 | startup reveal이 먼저 새 RD를 만듦 | 뒤늦은 restore duplicate는 dispose | route to: 132132 | route to: 132132 |
| 132722 | 둘 다 visible 전환을 발생시킴 | tunnel 중복 start를 막아야 함 | `_v2Active` guard | route to: 132212 |
| 13273 | 사용자가 RD 명령을 반복 실행함 | 기존 RD만 reveal되고 중복 탭 없음 | command가 같은 `panel.reveal()` 사용 | continue |
| 132730 | 반복 실행 뒤 멈춤 | 탭 하나만 유지 | 동일 | terminate |
| 132731 | 반복 실행 중 기존 panel이 visible | stream은 중복 시작하지 않음 | route to: 132212 | route to: 132212 |
| 132732 | 반복 실행 중 panel이 dispose된 뒤 재실행 | 새 RD 하나를 만들고 다시 연결 | route to: 13212 | route to: 13212 |

다음 실행 ID
- 132122
- 132311
- 132321
- 132322
- 132331
- 132332
- 132532
- 132712
