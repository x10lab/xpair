# 123431

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 123431 | 같은 project의 `_1`이 현재 attached라 새 세션을 시작함 | `_2` 이상 새 대화로 열림 | live mosh client가 있으면 다음 번호와 `CL_CONTINUE=0` 선택 | continue |
| 1234310 | 사용자가 새 세션 시작 직후 Xpair 터미널이나 실행 표면을 닫음 | 기존 `_1` attached 세션은 유지되고 새 세션 생성이나 attach는 발생하지 않음 | 실행 중단 시 이후 launcher 단계로 진행하지 않음 | terminate |
| 1234311 | Xpair가 같은 project의 live attached 세션을 확인함 | 이미 붙어 있는 낮은 번호 세션을 건드리지 않고 가장 낮은 빈 `_N`을 새 fresh 대상로 고름 | live client가 붙은 번호를 건너뛰고 `_2` 이상을 선택함 | continue |
| 12343110 | 사용자가 다음 번호 선택 뒤 세션 생성 전에 중단함 | 기존 `_1` attached 세션만 남고 새 번호 세션은 생기지 않음 | 생성 단계 전 종료되면 상태 변화 없음 | terminate |
| 12343111 | Xpair가 선택된 `_2` 이상 세션을 fresh 대화로 생성하려고 함 | 이전 대화를 재개하지 않도록 `continue` 없이 새 agent shell을 준비함 | `CL_CONTINUE=0` 성격의 fresh 생성 경로로 진행 | continue |
| 123431110 | 새 번호 세션 생성이 실패함 | 실패 안내를 보여주고 기존 `_1` attached 세션은 유지함 | setup 실패나 session marker 누락이면 launch 실패로 끝남 | terminate |
| 123431111 | 새 번호 세션 생성과 attach가 성공함 | 사용자는 기존 `_1`과 별개인 `_2` 이상 fresh agent 화면을 봄 | 새 session title로 terminal attach가 열리고 이전 attached 세션은 유지됨 | continue |
| 123431112 | 새 번호 세션은 생겼지만 attach가 실패함 | 세션 상호작용으로 넘어가지 않고 연결 실패 안내로 이동함 | attach 실패 후 오류와 로그 안내가 남을 수 있음 | route to: 314 |
| 12343112 | 다음 번호 선택 중 이미 사라진 stale live client 상태가 드러남 | stale 표시를 믿지 않고 host tmux 사실 기준으로 detached takeover 또는 새 번호 결정을 다시 해야 함 | 재확인 결과에 따라 detached면 takeover 경로, 빈 번호면 fresh 생성 경로로 갈 수 있음 | route to: 123432 |
| 1234312 | 새 세션 시작 중 사용자가 Xpair 범위 밖 VSCodium 표면으로 빠짐 | 접근 불가, 세션 생성 flow 하위 탐색을 차단함 | 기본 VSCodium 표면이 열릴 수 있어도 Xpair flow가 아님 | terminate |

다음 실행 ID
- 123431111
