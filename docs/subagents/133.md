# 133

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 133 | 사용자가 Xpair Browser, Add Root, Settings, Logs 표면을 엶 | Browser는 FOLDER_MAPS 기반 root만 보이고, Add Root는 host folder mount와 map add로 root를 추가하며, Settings와 Logs는 Xpair 보조 표면으로 열린다 | 하위 항목에서 검증 | continue |
| 1330 | 사용자가 해당 표면을 열지 않거나 아무 조작도 하지 않음 | Browser root, mount, map, settings, logs 상태 변화 없음 | 동일 | terminate |
| 1331 | 사용자가 Browser를 열고 FOLDER_MAPS 기반 root 목록을 봄 | `client.env`의 FOLDER_MAPS clientDir 중 실제 존재하는 경로만 선언 순서대로 root가 된다 | `reconcileBrowserRoots()`가 missing clientDir을 건너뛰고 workspace folders를 맞춤 | continue |
| 13310 | Browser root 목록을 보고 아무 항목도 선택하지 않음 | root, session, mapping 상태 변화 없음 | 동일 | terminate |
| 13311 | Browser에 하나 이상의 mapped root가 보임 | root row와 하위 folder row에서 탐색, favorite, New Session Here만 Xpair 범위로 허용됨 | Explorer tree와 RemotePair row action이 표시됨 | continue |
| 133110 | mapped root row를 펼치거나 접지 않음 | root 목록만 유지 | 동일 | terminate |
| 133111 | mapped root 또는 하위 folder를 펼침 | 해당 root 안의 folder 탐색으로 이어짐 | Explorer tree가 folder children을 표시 | continue |
| 133112 | folder row의 New Session Here를 누름 | 선택 folder에서 새 session 시작 흐름으로 이동 | `remotepair.terminalSidebar.openSessionInFolder` 호출 | route to: 1234 |
| 133113 | folder row의 star를 누름 | 해당 folder favorite 상태만 토글 | favorites store가 있으면 toggle, 없으면 warn 후 무시 | terminate |
| 13312 | FOLDER_MAPS에 있지만 clientDir이 로컬에 없음 | Browser root에 표시하지 않고 Add Root 또는 setup 경로로 복구해야 함 | missing client dir은 log 후 skip | continue |
| 133120 | missing root 상태를 보고 조작하지 않음 | 표시되지 않은 root는 그대로 숨김 | 동일 | terminate |
| 133121 | missing root 대신 Add Root를 누름 | 새 host folder mount 입력으로 이동 | `remotepair.browser.addRoot` 실행 | route to: 1333 |
| 133122 | missing root를 직접 열려고 함 | 접근 불가, 존재하지 않는 clientDir은 Browser root가 아님 | root 자체가 표시되지 않음 | terminate |
| 13313 | Browser에서 Xpair 범위 밖 VSCodium 기능으로 빠짐 | userflow는 접근 불가로 종료하거나 허용된 Xpair 표면으로 돌아와야 함 | Search, Extensions 등 일부 기본 표면 진입점이 남아 있음 | continue |
| 133130 | Search를 눌러 Browser 내부 search로 이동함 | Xpair root 탐색 보조까지만 허용, 하위 userflow 탐색은 차단 | inner router가 Search view를 Browser 안에 표시 | terminate |
| 133131 | Extensions를 누름 | 접근 불가 | Extensions viewlet이 열릴 수 있음 | terminate |
| 133132 | 파일 편집, SCM, Debug 등 일반 IDE 기능을 찾음 | 접근 불가 | prune allowlist 밖이면 rail에서 숨김, 우회 진입은 범위 밖 | terminate |
| 1332 | Browser가 빈 상태로 열림 | native Open Folder 대신 Xpair Add Root만 보여야 함 | empty workbench welcome에서 native Open Folder는 suppress, Add Root welcome 표시 | continue |
| 13320 | 빈 Browser에서 아무 조작도 하지 않음 | root, mount, map 상태 변화 없음 | 동일 | terminate |
| 13321 | 빈 Browser의 Add Root 버튼을 봄 | host folder를 mount해 Browser root로 추가하는 단일 진입점이어야 함 | EmptyView welcome과 아래쪽 footer가 `remotepair.browser.addRoot`를 실행 | continue |
| 133210 | Add Root 버튼을 누르지 않음 | 빈 Browser 유지 | 동일 | terminate |
| 133211 | Add Root 버튼을 클릭함 | host folder 입력 상자가 열린다 | `showInputBox`로 Add Root prompt 표시 | route to: 1333 |
| 133212 | Add Root 버튼에서 Enter 또는 Space를 누름 | 클릭과 같은 입력 상자가 열린다 | footer button key handler가 같은 command 실행 | route to: 1333 |
| 13322 | 빈 Browser에서 native Open Folder를 찾음 | 접근 불가, local folder open으로 빠지면 안 됨 | RemotePair patch가 empty state native welcome을 숨김 | continue |
| 133220 | native Open Folder 버튼이 보이지 않음 | Add Root만 선택 가능 | 동일 | terminate |
| 133221 | command palette 등으로 local Open Folder를 우회 실행함 | 접근 불가 | Xpair userflow 밖, 하위 탐색 차단 | terminate |
| 133222 | Browser를 닫고 Sessions로 돌아감 | 세션 표면으로 복귀 | Back to Sessions action이 terminal sidebar를 연다 | route to: 1231 |
| 1333 | Add Root 입력 상자가 열림 | 사용자는 host 절대경로를 입력하거나 취소한다 | `Xpair Add Root` input box가 표시됨 | continue |
| 13330 | 사용자가 입력을 취소함 | mount, map add, root reconcile 없음 | `showInputBox`가 falsy면 return | terminate |
| 13331 | 사용자가 잘못된 host path를 입력함 | 입력 단계에서 막고 mount를 실행하지 않음 | validateInput이 빈 값과 비절대경로를 거부 | continue |
| 133310 | 빈 값을 입력함 | `Enter a host folder path.` 안내 후 진행 불가 | 동일 | terminate |
| 133311 | 상대경로를 입력함 | `Host path must be absolute` 안내 후 진행 불가 | 동일 | terminate |
| 133312 | 잘못된 값을 고치지 않고 입력 상자를 닫음 | mount, map add 없음 | cancel 처리 | terminate |
| 13332 | 절대경로를 제출했지만 mount가 실패함 | 오류 메시지를 보여주고 map add를 실행하지 않음 | `xpair mount <host>` nonzero면 error message 후 return | continue |
| 133320 | mount command가 nonzero로 끝남 | 실패 상세를 보여주고 종료 | stderr 또는 stdout 마지막 줄을 error에 포함 | terminate |
| 133321 | mount command가 timeout 또는 spawn error로 끝남 | 실패로 종료하고 root를 추가하지 않음 | code -2 또는 -1도 nonzero 실패 처리 | terminate |
| 133322 | 사용자가 mount 실패 메시지만 확인함 | Browser root는 추가되지 않음 | 동일 | terminate |
| 13333 | 절대경로 제출 후 mount가 성공함 | mountpoint를 파싱하고 FOLDER_MAPS 등록으로 넘어감 | stdout의 `Mountpoint:` 줄을 읽음 | continue |
| 133330 | mount는 성공했지만 mountpoint를 파싱하지 못함 | 오류를 보여주고 map add를 실행하지 않음 | `mountpoint could not be determined` 표시 | terminate |
| 133331 | mountpoint는 있지만 map add가 실패함 | mount 위치를 알려주고 root 등록 실패를 표시해야 함 | `mounted at ... but registering the folder map failed` 표시 | terminate |
| 133332 | map add가 성공함 | 새 mountpoint가 Browser root로 추가되고 Browser가 다시 보임 | `reconcileBrowserRoots()` 후 Explorer reveal, success message 표시 | continue |
| 133333 | map add가 이미 있는 mapping으로 성공 처리됨 | 중복 없이 기존 mapping 기준으로 Browser root를 맞춤 | CLI code 0이면 성공과 같은 경로 | route to: 133332 |
| 1334 | 사용자가 Settings 표면을 엶 | Xpair 설정 또는 VSCodium settings가 열리되 Xpair userflow 밖 설정은 차단한다 | `remotepair.openSettings`는 `workbench.action.openSettings` 실행 | continue |
| 13340 | Settings를 열지 않음 | 설정 변경 없음 | 동일 | terminate |
| 13341 | Xpair Settings rail 또는 command로 Settings를 엶 | settings UI가 열리고 사용자는 필요한 설정만 확인한다 | stock Settings editor가 열린다 | continue |
| 133413 | Settings editor가 열린 뒤 아무 설정도 바꾸지 않음 | 설정 상태 변화 없음 | 동일 | continue |
| 133411 | Xpair 전용 설정을 찾음 | 있다면 Xpair 관련 항목만 수정 가능해야 함 | package는 configurationDefaults만 제공하고 Xpair 설정 schema는 없음 | terminate |
| 133412 | 일반 VSCodium 설정을 수정하려 함 | 접근 불가 | stock settings는 열릴 수 있으나 Xpair flow 밖 | terminate |
| 13342 | Manage 메뉴에서 Settings를 선택함 | 같은 Settings editor로 이동 | global manage action 이름이 Settings로 표시됨 | route to: 13341 |
| 13343 | command palette에서 Xpair Open Settings를 실행함 | 같은 Settings editor로 이동 | contributed command가 openSettings 실행 | route to: 13341 |
| 1335 | 사용자가 Show Logs 표면을 엶 | 로그 폴더를 Finder에 보여주고 선택 시 collect command를 terminal에서 실행한다 | `showLogs()`가 log dir 생성, reveal, collect prompt를 처리 | continue |
| 13350 | Show Logs를 실행하지 않음 | 로그 폴더 reveal, collect 없음 | 동일 | terminate |
| 13351 | Show Logs를 실행함 | `~/.xpair/host/logs`를 만들고 OS file manager로 연다 | mkdir 후 `revealFileInOS` 실행 | continue |
| 133510 | 로그 폴더 reveal이 성공함 | Finder가 log dir을 보여줌 | 동일 | terminate |
| 133511 | revealFileInOS가 실패하고 openExternal이 성공함 | 대체 경로로 log dir을 연다 | fallback openExternal 실행 | terminate |
| 133512 | reveal과 openExternal이 모두 실패함 | 오류 로그를 남기고 collect 여부만 물을 수 있음 | error log 후 다음 prompt로 진행 가능 | terminate |
| 13352 | collect logs 확인 메시지가 표시됨 | 사용자가 tarball 수집 여부를 선택한다 | `Collect logs (--collect)` 버튼을 제공 | continue |
| 133520 | collect를 선택하지 않음 | terminal 실행 없이 종료 | picked가 다르면 return | terminate |
| 133521 | collect를 선택함 | `xpair logs --collect` terminal이 열리고 command가 실행된다 | `Xpair Collect Logs` terminal 생성 후 sendText 실행 | continue |
| 133522 | collect terminal 생성이 실패함 | 오류 로그를 남기고 종료 | catch에서 error log | terminate |
| 13353 | collect terminal에서 command 결과가 나옴 | 성공이면 bug report용 tarball 위치가 출력되고, 실패면 terminal 오류로 끝난다 | extension은 terminal 출력을 추적하지 않음 | continue |
| 133533 | collect command가 실행 중임 | 사용자는 terminal 출력으로 진행 상태를 본다 | terminal surface에 맡김 | continue |
| 133531 | collect command가 성공함 | 생성된 로그 묶음 위치를 확인하고 종료 | CLI 출력 기준 | terminate |
| 133532 | collect command가 실패함 | terminal 오류를 보고 종료 | CLI 출력 기준 | terminate |

다음 실행 ID
- 133111
- 133332
- 133413
- 133521
- 133533
