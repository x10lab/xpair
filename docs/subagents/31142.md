# 31142

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31142 | 일반 tmux 대상 세션이 detached 상태임 | 새 세션을 만들지 않고 기존 plain tmux 세션을 `attach -d`로 인수 연결함 | `tmux has-session` 성공 뒤 `tmux attach -d` 경로로 진행 | continue |
| 311420 | 사용자가 attach 전에 터미널이나 Xpair 실행 흐름을 닫음 | 새 세션 생성 없음, 기존 detached 세션은 그대로 남음 | attach 호출 전 종료되면 후속 동작 없음 | terminate |
| 311421 | launcher가 기존 세션 인수 연결 준비를 확인함 | 대상 세션을 재사용하고 재시작 파일이나 새 tmux session을 만들지 않음 | plain tmux fallback은 기존 세션 발견 시 생성 블록을 건너뜀 | continue |
| 3114210 | 준비 확인 뒤 사용자가 종료함 | attach 없이 기존 detached 세션만 남음 | `tmux attach -d` 호출 전 종료 | terminate |
| 3114211 | 새 세션 생성 경로가 비활성화됨 | detached 세션을 그대로 이어 붙고 새 대화나 `_2` 세션으로 가지 않음 | `tmux new -s`가 실행되지 않음 | continue |
| 31142110 | 준비 중 대상 세션이 사라진 stale tmux 상태가 됨 | 새 세션으로 대체하지 않고 실패로 끝남 | attach 시점에는 재생성 경로가 없어 세션 없음 오류로 이어짐 | terminate |
| 31142111 | 기존 세션 재사용이 확정됨 | plain tmux attach 실행 단계로 넘김 | 동일 | route to: 311423 |
| 31142112 | 사용자가 detached takeover 대신 새 세션을 기대함 | 이 분기에서는 새 세션을 만들지 않고 fresh 또는 낮은 번호 충돌 분기로 보냄 | 새 세션은 `--fresh` 또는 번호 증가 조건에서만 처리됨 | route to: 31143 |
| 311422 | 사용자가 XpairHost 권한 상속 또는 aqua 세션 인수를 기대함 | plain tmux fallback에서는 접근 불가, aqua 경로에서 다시 판단해야 함 | 현재 분기는 XpairHost subtree 밖의 plain tmux attach만 수행 | route to: 31134 |
| 311423 | `tmux attach -d` 실행이 시작됨 | 기존 detached plain tmux 세션에 연결함 | `exec tmux attach -d -t "$TSESS"` 성격의 attach로 launcher가 대체됨 | continue |
| 3114230 | 사용자가 attach 명령 실행 직전에 닫음 | 기존 세션은 detached로 남고 새 세션 없음 | 프로세스 종료로 attach argv가 실행되지 않음 | terminate |
| 3114231 | attach가 성공함 | 사용자 터미널이 기존 plain tmux 세션 화면으로 전환됨 | attach 성공 시 launcher 프로세스가 tmux client로 대체됨 | continue |
| 31142310 | attach 직후 사용자가 탭을 닫음 | tmux 세션은 다시 detached 상태로 남음 | tmux client 종료 뒤 세션 자체는 유지됨 | terminate |
| 31142311 | 사용자가 재개된 agent 화면을 봄 | 마지막 상태 그대로 local agent interaction으로 넘어감 | 새 continue 실행 없이 기존 tmux 화면이 표시됨 | continue |
| 31142312 | 사용자가 기존 세션에 입력을 계속함 | 같은 plain tmux 세션에서 기존 agent 대화가 이어짐 | 동일 | continue |
| 3114232 | attach가 실패함 | 실패 원인을 보여주고 세션 상호작용으로 넘어가지 않음 | tmux 오류가 터미널에 남고 attach가 끝남 | continue |
| 31142320 | 실패 원인이 stale tmux 세션 없음임 | 세션 없음 오류로 끝내고 새 세션을 자동 생성하지 않음 | `can't find session`류 오류 후 종료 | terminate |
| 31142321 | 실패 원인이 tmux 서버 또는 클라이언트 오류임 | 오류를 표시하고 plain tmux attach 흐름을 끝냄 | tmux nonzero 종료 상태와 stderr만 남음 | terminate |
| 31142322 | 사용자가 attach 실패 화면에서 VSCodium 기본 기능이나 Xpair 범위 밖 표면으로 이동함 | 접근 불가로 처리하고 이 유저플로우를 끝냄 | Xpair CLI attach 실패 뒤 해당 표면은 이 branch에서 다루지 않음 | terminate |

다음 실행 ID
- 31142311
- 31142312
