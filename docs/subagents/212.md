# 212

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 212 | Finder Quick Action이 stale mapping 또는 unreachable host 상태에서 실행됨 | 사용자는 터미널에 뜬 오류, mapping 복구, host dir 생성, local fallback 중 하나를 보게 됨 | Finder workflow가 folder를 `xpair open-gui`로 넘기고 이후 `xpair launch`, `xpair-launch`가 처리 | continue |
| 2120 | Quick Action 호출 뒤 사용자가 터미널을 닫거나 실행을 중단함 | host probe, mapping 변경, session 생성 없이 종료 | 터미널 프로세스 종료 시 이후 launcher 진행 없음 | terminate |
| 2121 | Finder가 넘긴 client folder가 local에서 사라졌거나 folder가 아님 | GUI 실행 전에 `folder not found`를 보여주고 종료 | `open-gui`와 `launch` 모두 `cd` 실패 시 종료 | continue |
| 21210 | local path 오류를 보고 사용자가 아무 조작도 하지 않음 | mapping, SSH, session 생성 없음 | stderr에 `folder not found`만 남음 | terminate |
| 21211 | 같은 Finder 항목을 다시 실행함 | path가 복구되지 않았으면 같은 오류로 끝남 | `cd`를 다시 시도하고 실패하면 종료 | continue |
| 212110 | 항목이 여전히 없음 | `folder not found`로 종료 | 동일 | terminate |
| 212111 | 항목이 다시 folder로 존재함 | stale 여부를 다시 판단하고 launch로 진행 | `cd` 성공 후 mapping, reach, dir check 단계로 이동 | route to: 211 |
| 212112 | Finder가 folder 외 항목에서 Quick Action을 기대함 | 접근 불가 | Service는 `public.folder`만 받음 | terminate |
| 2122 | mapping은 있으나 mapped host path가 host에 없음 | 사용자는 host dir 생성, 실제 host path로 remap, 취소 중 하나를 선택 | reachable host에서 `[ -d HOST_DIR ]`가 `__NO__`이면 안내와 prompt 표시 | continue |
| 21220 | 사용자가 host dir 생성 또는 remap을 취소함 | remote session 생성 없이 종료 | `cancelled: directory missing on host` 또는 `cancelled.` 출력 | terminate |
| 21221 | 사용자가 빈 host dir 생성을 선택함 | 새 프로젝트일 때만 host에 빈 dir을 만들고 remote launch를 계속 | `ssh mkdir -p HOST_DIR` 실행 | continue |
| 212210 | host dir 생성이 실패함 | 실패를 보여주고 종료 | `mkdir failed` 후 종료 | terminate |
| 212211 | host dir 생성이 성공함 | 생성된 host path 기준 remote setup으로 이동 | 이후 remote script가 같은 `HOST_DIR`로 session 생성 시도 | route to: 211311 |
| 212212 | 생성된 빈 dir에 기존 client 내용 동기화를 기대함 | 접근 불가, content sync는 별도 도구 책임 | 안내도 `new project only`로 제한 | terminate |
| 21222 | 사용자가 실제 content가 있는 host path로 remap을 선택함 | 올바른 host path를 입력하고 mapping을 갱신 | `map_register_interactive`가 host path 확인 후 `cmd_map add` 실행 | continue |
| 212220 | remap 입력을 취소함 | mapping 변경 없이 종료 | `cancelled.` 또는 map 등록 실패 | terminate |
| 212221 | 입력한 host path가 존재하고 mapping 저장이 성공함 | 새 mapping 기준 launch를 계속하거나 다음 실행에서 정상 mapped flow로 이동 | `mapping added` 출력 | route to: 211 |
| 212222 | 입력한 host path도 없고 생성하지 않음 | stale mapping 상태가 유지되고 종료 | path 재입력, 생성, 취소 prompt에서 취소 | terminate |
| 2123 | SSH reachability가 실패함 | SSH 오류를 보여주고 Tailscale 보정 뒤에도 실패하면 local fallback으로 이동 | `ssh BatchMode true` 실패 후 tailscale exit-node 시도 또는 local fallback | continue |
| 21230 | 사용자가 reach 실패 메시지에서 터미널을 닫음 | session 생성 없이 종료 | fallback 실행 전 종료 가능 | terminate |
| 21231 | Tailscale CLI가 없음 | 자동 보정 불가를 알리고 local fallback 시도 | `tailscale CLI not found` 후 `falling back to local` 출력 | continue |
| 212310 | local fallback도 실행 불가함 | local engine 또는 tmux 오류를 보여주고 종료 | `engine not found` 또는 `tmux not found` 계열 오류 | terminate |
| 212311 | local fallback이 실행됨 | remote 대신 local session을 열고 status bar로 local임을 구분 | `launch_local`이 tmux 또는 tmux-aqua session 생성 | route to: 311 |
| 212312 | 사용자가 remote session이 열린 것으로 오해함 | 터미널 title과 stderr가 local fallback임을 보여줘야 함 | `falling back to local (LOCAL_HOST) automatically` 출력 | terminate |
| 21232 | Tailscale online exit-node 후보가 있음 | exit-node 설정 후 reach를 다시 확인 | `sudo tailscale set --exit-node=...` 후 재시도 | continue |
| 212320 | exit-node 설정 뒤 사용자가 중단함 | session 생성 없이 종료 | 재시도 또는 fallback 전 종료 | terminate |
| 212321 | 재시도에서 SSH reach가 성공함 | host dir check와 remote setup으로 진행 | 두 번째 `reach`가 성공하면 fallback 없이 계속 | route to: 211311 |
| 212322 | 재시도 후에도 SSH reach가 실패함 | local fallback으로 이동 | `cannot reach HOST` 후 `launch_local` | route to: 21231 |
| 212323 | exit-node 후보가 없음 | 보정 없이 local fallback으로 이동 | `no online exit-node candidate` 후 reach 재실패 시 fallback | route to: 21231 |
| 2124 | SSH host auth가 실패함 | `Permission denied`, key, host-key 오류를 그대로 보여주고 local fallback으로 이동 | reach 함수가 stderr를 숨기지 않음 | continue |
| 21240 | 사용자가 auth 실패 메시지만 보고 종료함 | remote setup 없음, local fallback 전이면 session 없음 | 오류와 launch log 경로만 남음 | terminate |
| 21241 | BatchMode key auth가 거부됨 | 비밀번호 prompt 없이 실패하고 fallback을 시도 | `BatchMode=yes`라 대화형 비밀번호 입력 없음 | continue |
| 212410 | local fallback 실행이 실패함 | auth 실패와 local 실행 실패를 모두 터미널과 log에 남김 | EXIT trap이 `claude-launch.err.log` 경로 표시 | terminate |
| 212411 | local fallback 실행이 성공함 | host 인증 복구 전까지 local session으로 작업 가능 | `launch_local`로 전환 | route to: 311 |
| 212412 | 사용자가 Finder Quick Action 안에서 SSH 비밀번호 입력을 기대함 | 접근 불가 | Quick Action path는 key auth만 사용 | terminate |
| 21242 | host key mismatch 또는 known_hosts 오류가 발생함 | 원문 SSH 오류를 보여주고 remote 실행은 막음 | stderr가 terminal과 `claude-launch.err.log`에 기록 | continue |
| 212420 | 사용자가 known_hosts를 고치지 않음 | remote session 생성 없음 | 같은 오류 반복 | terminate |
| 212421 | 사용자가 별도 터미널에서 SSH 신뢰 문제를 고친 뒤 재실행함 | 정상 mapped launch로 다시 판단 | 다음 실행의 reach 결과에 따름 | route to: 211 |
| 212422 | Quick Action이 known_hosts 수정 UI를 제공하길 기대함 | 접근 불가 | launcher는 수정 UI 없이 오류만 노출 | terminate |
| 2125 | reach는 됐지만 host dir check SSH가 불안정함 | 최대 3회 재시도하고 실패가 지속되면 local fallback | `dir-check ssh attempt i/3 failed` 출력 | continue |
| 21250 | 재시도 중 사용자가 터미널을 닫음 | host dir 생성, fallback, session 생성 없이 종료 | 중단 시 이후 단계 없음 | terminate |
| 21251 | 3회 안에 dir check 결과를 받음 | `__YES__`면 remote, `__NO__`면 stale host path 처리 | marker 출력 기준으로 분기 | continue |
| 212513 | 결과가 `__YES__`임 | remote setup으로 진행 | host dir 존재 확인 완료 | route to: 211311 |
| 212511 | 결과가 `__NO__`임 | stale host path branch로 이동 | create 또는 cancel prompt 표시 | route to: 2122 |
| 212512 | 결과 marker가 깨지거나 비어 있음 | 남은 재시도를 계속함 | marker 없으면 attempt 메시지 출력 | continue |
| 21252 | dir check SSH가 3회 모두 실패함 | host가 불안정하므로 local fallback으로 이동 | `dir-check ssh failed 3 times` 출력 | continue |
| 212520 | local fallback도 실패함 | fallback 실패 오류와 log 경로를 보여주고 종료 | local engine 또는 tmux 오류 | terminate |
| 212521 | local fallback이 성공함 | remote 대신 local session을 열고 사용자가 local임을 확인 | `launch_local` 실행 | route to: 311 |
| 212522 | 사용자가 자동 local fallback을 원하지 않음 | 현행은 선택 없이 fallback함 | 명시 취소 prompt 없음 | terminate |
| 2126 | remote setup 단계에서 host 측 준비가 실패함 | terminal에 setup 실패 원인을 보여주고 remote attach를 만들지 않음 | SSH setup output을 표시하고 exit code로 종료 | continue |
| 21260 | 사용자가 remote setup 실패 메시지를 보고 종료함 | mosh attach 없이 종료 | `remote setup failed` 또는 원문 stderr 표시 | terminate |
| 21261 | 선택한 engine이 host에 없음 | engine 설치 안내 후 종료 | `claude/codex/opencode not found on host` exit 11 | continue |
| 212610 | 사용자가 engine을 설치하지 않음 | 같은 오류 반복 | 동일 | terminate |
| 212611 | host에 engine을 설치한 뒤 재실행함 | remote setup을 다시 시도 | 다음 실행에서 `command -v` 통과 가능 | route to: 211311 |
| 212612 | client에만 engine을 설치하고 재실행함 | remote engine 오류는 해소되지 않음 | engine check는 host에서 실행 | terminate |
| 21262 | XpairHost tmux-aqua server가 host에서 안 뜸 | 앱 설치, 권한, LaunchAgent 문제를 보여주고 종료 | `XpairHost host ... not running` exit 13 | continue |
| 212620 | 사용자가 host app 문제를 고치지 않음 | remote session 생성 없음 | 같은 exit 13 반복 | terminate |
| 212621 | host app과 권한을 복구한 뒤 재실행함 | remote setup 재시도 | `tm has-session` 통과 가능 | route to: 211311 |
| 212622 | Quick Action에서 host 권한 부여를 기대함 | 접근 불가 | 권한 부여는 host 물리 화면 또는 VNC에서 해야 함 | terminate |
| 21263 | setup SSH는 연결됐지만 session marker를 받지 못함 | remote setup 실패로 종료하고 log를 남김 | `failed to extract remote session name` 또는 SSH_RC die | continue |
| 212630 | 사용자가 메시지만 보고 종료함 | attach 없음 | stderr와 `claude-launch.err.log`만 남음 | terminate |
| 212631 | 재실행해 marker를 정상 수신함 | mosh 또는 ssh attach 단계로 이동 | `__SESSION__:<name>` 추출 성공 | route to: 2114 |
| 212632 | marker 없는 상태에서 mosh attach를 기대함 | 접근 불가 | session name 없으면 attach하지 않음 | terminate |

다음 실행 ID
- 212512
