# 31141211

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31141211 | cwd가 적용된 plain tmux 세션 생성 명령으로 진행함 | agent shell이 요청한 project directory 기준으로 실행되어야 함 | `tmux new-session ... -c "$PROJECT_DIR" "bash $T"` 호출 직전 상태 | continue |
| 311412110 | 사용자가 plain tmux 세션 생성 명령 실행 전에 종료함 | 세션 생성, attach, agent shell 시작 없음 | `new-session` 호출 전 종료되면 이후 단계 없음 | terminate |
| 311412111 | plain tmux 세션 생성 명령이 실행됨 | 현재 Mac의 plain tmux 서버 안에 새 session 생성을 시도함 | `tmux new-session -d -s "$TSESS" -c "$PROJECT_DIR" "bash $T"` 실행 | continue |
| 3114121110 | `new-session` 실행이 실패함 | 실패 로그를 남기고 attach로 넘어가지 않음 | nonzero 종료로 launcher가 중단되고 tmux 오류가 노출됨 | terminate |
| 3114121111 | `new-session` 실행이 성공함 | 새 plain tmux session이 detached 상태로 생성되고 후속 handoff 준비로 넘어감 | `RP_SESSION=$TSESS`와 생성 로그 기록 단계로 진행 | continue |
| 31141211110 | 생성 직후 사용자가 종료함 | 생성된 plain tmux session은 detached로 남고 다음 실행에서 기존 세션 attach로 라우팅됨 | attach 전 종료 시 session만 남음 | terminate |
| 31141211111 | session 생성 로그를 기록함 | Xpair 표면과 진단 로그가 새 session 이름을 추적할 수 있어야 함 | `rp_log ... SESSION: created host=local name=$TSESS` 기록 | continue |
| 31141211112 | session title 출력으로 진행함 | 터미널과 Xpair 표면에서 현재 session name이 보임 | OSC title 출력 단계로 이동 | continue |
| 31141211113 | 생성된 session을 바로 인수 연결하려 함 | 사용자는 새로 만든 plain tmux agent shell을 볼 수 있어야 함 | `tmux attach -d -t "=$TSESS"` handoff로 진행 | route to: 31141312 |
| 3114121112 | 생성은 성공했지만 agent shell이 즉시 종료됨 | 빈 session이나 종료된 pane으로 남지 않고 실패 안내 또는 재시도 흐름으로 가야 함 | shell script 실패는 tmux pane 종료 또는 오류 로그로 관찰됨 | route to: 3147 |
| 311412112 | 대상 session 이름이 생성 시점에 이미 생긴 경합 상태가 됨 | 중복 생성하지 않고 기존 detached session attach 분기로 보내야 함 | `duplicate session`류 오류가 발생할 수 있음 | route to: 31142 |

다음 실행 ID
- 31141211111
- 31141211112
