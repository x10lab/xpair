# 31141311

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31141311 | session title을 출력함 | Xpair IDE와 terminal 표면에서 plain tmux session name을 식별할 수 있어야 함 | OSC title `\033]2;$TSESS\007` 출력 | continue |
| 311413110 | 사용자가 title 출력 직후 attach 전에 종료함 | 새 plain tmux session은 detached 상태로 남고 이 분기는 끝남 | `tmux attach` 호출 전 종료하면 생성된 session만 남음 | terminate |
| 311413111 | 사용자가 표시된 session title을 확인함 | 현재 local plain tmux session으로 붙는 흐름임을 알 수 있어야 함 | terminal title 또는 Xpair 표면에 `$TSESS`가 노출됨 | continue |
| 311413112 | terminal이 OSC title을 표시하지 않음 | title 표시만 누락되고 attach 흐름은 계속 진행해야 함 | terminal 설정에 따라 window title 반영이 보이지 않을 수 있음 | continue |
| 311413113 | 사용자가 표시된 title을 보고 다른 session으로 전환하려 함 | 이미 생성된 session을 이 분기에서 바꾸지 않고 session 선택 흐름으로 보내야 함 | 새 session 선택 UI 없이 현재 `$TSESS` attach만 이어짐 | route to: 131 |
| 3114131110 | title 확인 뒤 사용자가 attach 전에 종료함 | 새 plain tmux session은 detached 상태로 남고 이 분기는 끝남 | `attach -d` 호출 전 종료 | terminate |
| 3114131111 | 표시된 title과 생성 session이 일치함 | 사용자는 곧 붙을 session이 방금 만든 plain tmux session임을 확인함 | `$TSESS` 기준 title과 attach target이 같음 | continue |
| 3114131112 | 사용자가 title이 local plain tmux session임을 확인하고 XpairHost 권한 상속을 기대함 | plain tmux 경로 범위를 벗어나므로 XpairHost 기반 실행 흐름으로 다시 보내야 함 | 현재 경로는 `tmux-aqua`나 host app subtree를 사용하지 않음 | route to: 31133 |
| 3114131120 | title 미표시 상태에서 사용자가 종료함 | session은 detached 상태로 남고 title 미표시는 이후 흐름을 만들지 않음 | attach 전 종료면 생성된 session만 남음 | terminate |
| 3114131121 | title은 보이지 않지만 attach handoff가 실행됨 | title 표시 여부와 무관하게 새 plain tmux agent shell로 붙어야 함 | OSC title 표시 실패 뒤에도 attach 명령은 계속 실행됨 | continue |
| 3114131122 | title 미표시 때문에 사용자가 session 식별을 포기함 | 생성된 session은 남기고 사용자는 session 목록에서 다시 선택해야 함 | session name은 로그와 tmux session에는 남음 | route to: 131 |
| 31141311110 | title 일치 확인 후 attach 전에 종료함 | 새 plain tmux session은 detached 상태로 남음 | attach 호출 전 종료 | terminate |
| 31141311111 | title 일치 상태에서 attach handoff로 진행함 | 사용자는 방금 만든 plain tmux agent shell을 볼 수 있어야 함 | `tmux attach -d -t "=$TSESS"` 호출로 넘어감 | route to: 31141312 |
| 31141311210 | title 미표시 상태의 attach가 실패함 | title 문제가 아니라 attach 실패로 안내한 뒤 이 분기는 끝남 | `tmux attach` 오류가 표시됨 | terminate |
| 31141311211 | title 미표시 상태에서도 attach가 성공함 | 사용자는 title 없이도 새 agent shell 화면에서 입력할 수 있어야 함 | tmux client 전환은 title 표시 여부와 독립적으로 성공 | route to: 31141312 |
| 31141311212 | attach 후에도 session name을 확인할 수 없음 | session 내부 상태는 유지하고 session 목록 또는 로그에서 식별해야 함 | shell 화면에는 별도 title 재출력이 없을 수 있음 | route to: 131 |

다음 실행 ID
