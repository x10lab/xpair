# 211422

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 211422 | `mosh`가 없어 ssh TTY fallback으로 remote tmux에 붙음 | 기존 detached host session에 ssh `-t`로 attach를 시도 | parent flow에서 `mosh` 부재 시 ssh fallback attach로 기록됨 | continue |
| 2114220 | 사용자가 ssh attach 시작 전에 터미널을 닫음 | 기존 host session은 detached 상태로 남고 attach 없이 종료 | ssh 실행 전 종료면 remote 변화 없음 | terminate |
| 2114221 | ssh TTY 연결 시도가 시작됨 | host SSH 인증과 TTY 할당을 거쳐 tmux attach 명령으로 이동 | `ssh -t ... tmux-aqua attach -d` fallback 경로 | continue |
| 2114222 | ssh 인증, host reachability, 키 확인에서 실패함 | CLI SSH/auth 실패 안내 흐름으로 이동 | mosh fallback 이후 ssh도 실패하면 attach 불가 | route to: 314 |
| 2114223 | ssh는 연결됐지만 remote tmux server나 session을 찾지 못함 | host app, tmux-aqua, session 상태 점검 흐름으로 이동 | attach 대상이 없으면 remote setup 실패와 같은 오류가 됨 | route to: 314 |
| 2114224 | ssh attach가 성공해 tmux 화면이 표시됨 | 사용자는 기존 detached session 화면을 이어서 봄 | ssh TTY가 foreground를 차지하고 tmux-aqua attach 화면을 표시 | continue |
| 2114225 | 사용자가 attach 중 Xpair 범위 밖 터미널 기능으로 빠짐 | 접근 불가, Xpair userflow 하위 탐색 차단 | shell, terminal profile, window 관리 기능은 Xpair launch 흐름 밖 | terminate |
| 21142210 | ssh 연결 대기 중 사용자가 중단함 | attach 없이 종료하고 host session은 detached로 유지 | Ctrl-C 또는 창 닫기로 ssh 프로세스 종료 | terminate |
| 21142211 | SSH 인증이 통과하고 TTY가 할당됨 | remote tmux attach 명령을 실행 | ssh 접속 뒤 지정 명령 실행 단계 | continue |
| 21142212 | SSH가 비대화형으로 처리 못 하는 입력을 요구함 | 인증 실패 또는 host 설정 문제로 보고 종료 | Finder Quick Action 표면에서는 password prompt를 해결하지 못함 | route to: 314 |
| 21142213 | SSH 연결은 됐지만 remote 명령 응답을 기다림 | attach 준비 상태를 유지하다 성공 또는 실패로 분기 | 네트워크 지연 시 terminal이 대기 화면에 머묾 | continue |
| 21142240 | tmux 화면 표시 직후 사용자가 터미널을 닫음 | host session은 detached 상태로 남음 | ssh 종료 시 tmux client가 detach됨 | terminate |
| 21142241 | 기존 agent session 화면이 표시됨 | mapped host path 기준 기존 대화가 입력 가능한 상태가 됨 | tmux attach가 이전 session pane을 보여줌 | continue |
| 21142242 | 사용자가 agent 화면에 입력함 | 이후는 Claude, Codex, OpenCode 자체 대화라 Xpair launch 하위 탐색을 종료 | parent flow의 agent 입력 종료 규칙과 같음 | terminate |
| 21142243 | 사용자가 tmux detach를 실행함 | host session은 detached로 남고 다음 mapped launch에서 take-over 가능 | detach 후 동일 detached branch로 돌아갈 수 있음 | route to: 21142 |
| 21142244 | ssh attach 중 네트워크가 끊김 | mosh가 없으므로 terminal attach는 끊기고 host session은 detached 상태로 남아야 함 | ssh fallback은 roaming 복구 없이 연결 종료될 수 있음 | continue |
| 211422110 | TTY 할당 직후 사용자가 터미널을 닫음 | tmux attach 실행 전이면 기존 session만 detached로 유지 | ssh client 종료 | terminate |
| 211422111 | remote tmux attach 명령이 실행됨 | 기존 detached session을 현재 ssh TTY에 붙임 | `attach -d`로 이전 client를 떼고 현재 TTY에 붙음 | continue |
| 211422112 | attach 대상 session이 실행 사이에 사라짐 | 새 session 생성 또는 remote setup 실패 흐름으로 이동 | session 없음은 detached attach 실패로 처리됨 | route to: 21141 |
| 211422113 | terminal 크기와 title이 attach 화면에 반영됨 | 사용자는 같은 session을 현재 창 크기로 봄 | ssh TTY 크기 기준으로 tmux 화면이 다시 그림 | continue |
| 211422130 | remote 명령 응답 대기 중 사용자가 중단함 | attach 없이 종료하고 host session은 detached로 유지 | ssh 프로세스 종료 | terminate |
| 211422131 | remote tmux 명령이 응답함 | tmux attach 성공 여부에 따라 화면 표시로 이어짐 | ssh command가 tmux-aqua attach를 실행 | continue |
| 211422132 | remote 명령이 실패하거나 timeout됨 | SSH/auth/host 실패 흐름으로 이동 | marker 없음 또는 nonzero 종료는 remote failure 취급 | route to: 314 |
| 211422410 | agent 화면을 보고 아무 입력도 하지 않음 | session은 ssh TTY에 attached 상태로 유지 | 입력이 없으면 현재 pane 유지 | terminate |
| 211422411 | 사용자가 기존 화면을 확인하고 입력 가능 상태로 둠 | 다음 user action은 agent 입력, detach, 연결 끊김 중 하나로 이어짐 | 동일 화면에서 대기 | continue |
| 211422412 | 표시된 화면이 Xpair 범위 밖 VSCodium 또는 shell 작업으로 이동함 | 접근 불가, 하위 탐색 차단 | tmux 내부 임의 작업은 Xpair launch 흐름 밖 | terminate |
| 211422440 | 네트워크 단절 뒤 사용자가 재시도하지 않음 | local terminal은 종료되고 host session은 남음 | ssh attach 종료 | terminate |
| 211422441 | 사용자가 같은 folder에서 Finder Quick Action을 다시 실행함 | detached session take-over attach로 돌아감 | 다음 실행은 기존 session 감지 branch로 진입 | route to: 21142 |
| 211422442 | 단절 뒤 host reachability도 실패함 | CLI SSH/auth 실패 흐름으로 이동 | 재연결 불가 상태 | route to: 314 |

다음 실행 ID
- 211422111
- 211422113
- 211422131
- 211422411
