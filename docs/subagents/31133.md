# 31133

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 31133 | aqua 대상 세션이 없어 새 세션 생성으로 진입함 | 같은 project의 `_1` aqua 세션을 만들고 기본 재개 설정으로 붙을 준비를 함 | `NEED_CREATE=1`, `CONT=1` 상태에서 생성 경로 실행 | continue |
| 311330 | 사용자가 aqua 새 세션 생성 전에 종료함 | 세션 생성, title 출력, attach 없음 | `tm_local new-session` 전 종료되면 이후 단계 없음 | terminate |
| 311331 | 세션 이름과 재개 설정을 준비함 | local host와 project slug/hash로 `SESS`를 고정하고 이전 대화 재개를 허용함 | `SESS="${LOCAL_PROJ}_${N}"`, `CONT=1` 유지 | continue |
| 3113310 | 세션 이름 준비 뒤 사용자가 종료함 | 새 aqua 세션 없음 | 임시 script 작성 전 종료 | terminate |
| 3113311 | 재개 script가 작성됨 | agent가 같은 cwd에서 이전 기록을 우선 재개하고 없으면 새 대화로 시작해야 함 | `CLAUDE_WARP_RC=$SESS`, `CL_CONTINUE=1`, `respawn_body`를 임시 파일에 기록 | continue |
| 31133111 | 재개 가능한 기록이 있음 | 같은 project의 마지막 대화를 이어서 agent shell을 시작함 | Claude는 recorded id로 `--resume`, Codex는 `resume --last`, Opencode는 `--continue` | route to: 21151 |
| 31133112 | 재개 가능한 기록이 없음 | 같은 session 이름으로 새 blank agent shell을 시작함 | `CL_CONTINUE=1`이어도 recorded id가 없으면 각 engine의 새 실행으로 이동 | route to: 21151 |
| 311332 | project cwd guard를 확인함 | 세션은 요청한 project directory에서 열려야 함 | `[ -d "$PROJECT_DIR" ]` 확인 뒤 `new-session -c "$PROJECT_DIR"` 사용 | continue |
| 3113320 | project directory가 사라짐 | 오류를 보이고 aqua 세션을 만들지 않음 | `project directory missing` 후 exit 1 | terminate |
| 3113321 | project directory가 존재함 | 새 aqua 세션의 cwd가 요청 project로 고정됨 | `tm_local new-session ... -c "$PROJECT_DIR"` 인자를 유지 | continue |
| 31133210 | cwd 설정 뒤 사용자가 종료함 | 세션 attach 없이 종료 | 생성 또는 attach 전 종료 | terminate |
| 31133211 | cwd가 적용된 세션 생성 명령으로 진행함 | agent shell이 해당 project 기준으로 떠야 함 | `bash $T`가 `$PROJECT_DIR` cwd에서 실행되도록 요청 | continue |
| 311333 | `tmux-aqua new-session`을 실행함 | XpairHost 권한을 상속하는 aqua 서버 안에 새 session을 생성함 | `tm_local new-session -d -s "$SESS" -c "$PROJECT_DIR" "bash $T"` 실행 | continue |
| 3113330 | `new-session`이 실패함 | 실패 로그와 pause를 남기고 attach하지 않음 | `set -e`로 launcher가 실패 종료 | terminate |
| 3113331 | `new-session`이 성공함 | session 생성 로그를 남기고 attach handoff로 넘어감 | `RP_SESSION=$SESS`, `rp_log ... SESSION: created` 실행 | continue |
| 31133311 | session title을 출력함 | Xpair IDE와 terminal 표면에서 session name을 식별할 수 있어야 함 | OSC title `\033]2;$SESS\007` 출력 | continue |
| 31133312 | 생성 직후 사용자가 종료함 | aqua session은 detached 상태로 남고 다음 실행에서 붙을 수 있음 | attach exec 전 종료하면 생성된 session만 남음 | route to: 31134 |
| 31133313 | aqua attach handoff가 실행됨 | 사용자는 새로 만든 agent shell을 보고 입력할 수 있음 | `tmux-aqua ... attach -d -t "=$SESS"`로 foreground 전환 | route to: 21151 |

다음 실행 ID
- 31133211
- 31133311
