# 123432

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 123432 | 같은 project의 `_1`이 detached라 새 세션을 시작함 | 기존 `_1`을 `attach -d`로 take over함 | host tmux session이 있으면 `NEED_CREATE=0` | continue |
| 1234320 | 사용자가 takeover attach가 열리기 전에 Xpair 터미널 또는 표면을 닫음 | 새 세션은 만들지 않고 기존 `_1`은 detached로 남음 | attach 실행 전 중단이면 기존 host tmux session만 유지됨 | terminate |
| 1234321 | Xpair가 같은 project의 detached `_1`을 attach 대상으로 확정함 | `_2`를 만들거나 새 대화를 시작하지 않고 기존 `_1`에 붙을 준비를 함 | host tmux session 확인으로 생성 경로를 건너뜀 | continue |
| 12343210 | 사용자가 대상 확정 뒤 attach 전 단계에서 종료함 | 새 세션 생성 없음, 기존 `_1`은 detached로 유지됨 | `attach -d` 호출 전 종료면 상태 변화 없음 | terminate |
| 12343211 | attach transport가 mosh로 선택됨 | 사용자는 roaming 가능한 터미널로 기존 `_1` 화면에 붙어야 함 | mosh가 있으면 remote tmux `attach -d`를 실행함 | continue |
| 123432110 | mosh attach 직전에 사용자가 터미널을 닫음 | 기존 `_1`은 detached 상태로 남고 새 세션은 없음 | mosh 실행 전 종료면 host tmux session만 유지됨 | terminate |
| 123432111 | mosh attach가 성공함 | 사용자는 기존 `_1`의 agent 화면을 이어서 보고 입력할 수 있음 | foreground가 mosh client로 전환되고 기존 session에 붙음 | continue |
| 123432112 | mosh attach가 실패함 | attach 실패를 표시하고 복구 흐름으로 이동 | mosh 또는 remote attach 실패는 remote 실패 처리로 이어짐 | route to: 314 |
| 12343212 | attach transport가 ssh TTY fallback으로 선택됨 | mosh 없이도 기존 `_1`에 붙어야 함 | mosh가 없으면 ssh TTY로 remote tmux `attach -d`를 실행함 | continue |
| 123432120 | ssh attach 직전에 사용자가 터미널을 닫음 | 기존 `_1`은 detached 상태로 남고 새 세션은 없음 | ssh 실행 전 종료면 host tmux session만 유지됨 | terminate |
| 123432121 | ssh attach가 성공함 | 사용자는 기존 `_1`의 agent 화면을 이어서 보고 입력할 수 있음 | foreground가 ssh TTY로 전환되고 기존 session에 붙음 | continue |
| 123432122 | ssh attach가 실패함 | attach 실패를 표시하고 복구 흐름으로 이동 | ssh 또는 remote attach 실패는 remote 실패 처리로 이어짐 | route to: 314 |
| 12343213 | attach 대상 `_1`이 확정 후 사라지거나 stale 상태가 됨 | 새 세션으로 조용히 대체하지 말고 실패를 보여줘야 함 | attach 시점 실패로 remote 실패 처리에 합류함 | route to: 314 |
| 1234322 | 사용자가 detached takeover 대신 새 대화를 기대함 | 이 분기는 기존 `_1` takeover가 원칙이라 fresh 시작 흐름으로 보내야 함 | fresh는 attached 상태나 명시 fresh 조건에서만 선택됨 | route to: 123431 |
| 1234323 | 같은 project 세션 시작 중 Xpair 범위 밖의 VSCodium 기능으로 빠짐 | 접근 불가 | 세션 takeover와 무관한 기본 IDE 표면은 탐색하지 않음 | terminate |

다음 실행 ID
- 123432111
- 123432121
