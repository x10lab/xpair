# 423

| ID | 동작 | 예상 | 현행 | 플래그 |
|---|---|---|---|---|
| 423 | Host가 준비됐지만 연결된 Xpair 클라이언트가 없음 | 메뉴와 상태가 no-client 대기 상태를 보이고, Host는 계속 실행되며 Xpair 클라이언트 실행을 안내함 | `startServing()` 뒤 tmux host와 광고는 유지되고 `ConnectedClients.list()`가 비면 Clients 영역에 없음 상태가 표시됨 | continue |
| 4230 | 사용자가 Host 메뉴를 열지 않거나 더 진행하지 않음 | Host는 idle 상태로 계속 실행되고 클라이언트 연결을 기다림 | `hostTimer`와 `tickTimer`가 유지되며 세션과 클라이언트 상태만 갱신됨 | terminate |
| 4231 | 사용자가 메뉴바에서 no-client 상태를 확인함 | 연결 클라이언트가 없다는 읽기 전용 표시만 보이고 세션 선택은 요구되지 않음 | `Clients` 헤더 아래 `   (none connected)` 비활성 항목을 추가함 | continue |
| 42310 | no-client 상태만 보고 기다림 | Host는 종료되지 않고 다음 heartbeat 또는 메뉴 재열림까지 대기함 | 메뉴 항목은 비활성이고 `ensureServer()` watchdog은 계속 동작함 | terminate |
| 42311 | stale 클라이언트 기록이 남아 있는 상태에서 메뉴를 다시 엶 | 90초 초과 heartbeat는 연결 클라이언트로 보이지 않아야 함 | `ConnectedClients.list()`가 `ts` age가 90초를 넘는 파일을 제외함 | continue |
| 423110 | stale 기록이 만료됨 | Clients 영역이 no-client 상태로 돌아감 | 만료된 파일은 삭제하지 않지만 목록에서 빠져 `(none connected)`가 표시됨 | terminate |
| 423111 | stale 기록이 아직 90초 안임 | 최근 heartbeat가 있는 클라이언트는 연결된 것으로 표시됨 | age가 90초 이하면 `name (user)` 행으로 표시됨 | route to: 421 |
| 42312 | no-client 상태에서 Sessions 영역을 확인함 | Xpair client 선택 없이 Host의 기존 tmux 세션 상태만 읽을 수 있음 | `Sessions.list()` 결과가 없으면 `(no active sessions)`, 있으면 Attached 또는 Detached로 표시됨 | continue |
| 423120 | 세션이 없음 | 새 프로젝트나 세션 조작 없이 대기함 | `Sessions.list()`가 빈 배열이면 disabled 없음 행만 표시됨 | terminate |
| 423121 | 기존 세션 행이 보이지만 클라이언트가 없음 | 세션 종료 확인 외 프로젝트 attach 흐름으로 확장하지 않음 | 세션 클릭은 `Terminate session` 모달만 열 수 있음 | terminate |
| 423122 | 사용자가 세션 종료 모달을 취소함 | 기존 세션 상태를 유지하고 no-client Host 메뉴로 돌아감 | `Cancel`이면 `Sessions.kill()`을 호출하지 않음 | terminate |
| 423123 | 사용자가 세션 종료를 확인함 | 해당 tmux 세션만 종료되고 Host는 계속 실행됨 | `Sessions.kill(name)`이 호출되고 앱 자체는 종료되지 않음 | terminate |
| 4232 | 사용자가 연결 안내 또는 설정 계열 메뉴를 엶 | no-client 복구를 돕는 안내나 설정만 열리고 Host는 계속 실행됨 | Host 메뉴에 `Connect…`, `Set up…`, `Permissions…`, `Check for Updates…`, `About Xpair`, `Quit`가 있음 | continue |
| 42320 | `Connect…`를 열지 않음 | 연결 안내 창 없음, Host는 no-client 상태 유지 | 메뉴 선택이 없으면 상태 변화 없음 | terminate |
| 42321 | `Connect…`를 클릭함 | 클라이언트 연결 안내 온보딩이 열리고 Xpair 클라이언트 실행으로 이어질 수 있음 | `OnboardingWindow(mode: .grantOnly, initialStep: "connect")`를 표시함 | continue |
| 423210 | 연결 안내 창을 닫음 | Host는 종료되지 않고 no-client 대기로 돌아감 | grant-only 온보딩은 닫혀도 앱을 종료하지 않음 | terminate |
| 423211 | 사용자가 안내에 따라 Xpair 클라이언트를 시작함 | Host-side no-client 흐름을 끝내고 클라이언트 연결 흐름으로 이동함 | 클라이언트가 heartbeat를 쓰면 Host의 Clients 목록에 잡힘 | route to: 422 |
| 423212 | 안내는 열었지만 클라이언트를 시작하지 않음 | 연결된 클라이언트 없음 상태가 유지됨 | `ConnectedClients.list()`는 계속 빈 배열을 반환함 | terminate |
| 42322 | `Set up…` 또는 `Permissions…`를 엶 | 설정 또는 권한 온보딩을 볼 수 있지만 클라이언트 없이 프로젝트 선택으로 확장하지 않음 | `openSetup()`과 `grantPermissions()`가 grant-only 온보딩을 띄움 | continue |
| 423220 | 설정 또는 권한 창을 닫음 | Host는 메뉴바 대기 상태로 돌아감 | grant-only 창 닫기는 실행 중 앱을 종료하지 않음 | terminate |
| 423221 | 권한 상태를 확인하거나 시스템 설정을 엶 | 권한 확인만 진행되고 no-client 상태 자체는 변하지 않음 | 권한 요청과 System Settings 열기는 연결 클라이언트 목록을 만들지 않음 | terminate |
| 423222 | 설정 온보딩을 완료함 | Host는 계속 실행되고 클라이언트 연결은 별도 시작이 필요함 | 완료 콜백은 창 참조만 비우며 serving 상태를 유지함 | terminate |
| 42323 | 업데이트 또는 About 같은 도움 표면을 엶 | 정보 확인만 가능하고 Xpair client 선택이나 Host 프로젝트 조작은 없음 | `checkUpdates()` 또는 `about()`이 모달, 업데이트, GitHub 링크만 처리함 | continue |
| 423230 | 업데이트 확인을 취소하거나 끝냄 | Host는 no-client 대기로 돌아감 | 업데이트 확인은 연결 클라이언트 상태를 바꾸지 않음 | terminate |
| 423231 | About에서 OK를 누름 | 정보 모달만 닫힘 | `OK`는 URL을 열지 않고 종료함 | terminate |
| 423232 | About에서 Open GitHub를 누름 | 브라우저로 저장소 도움말을 열고 Host는 계속 실행됨 | `NSWorkspace.shared.open(https://github.com/{GH_REPO})` 호출 | terminate |
| 4233 | 사용자가 로그나 status를 확인함 | no-client 진단 정보만 확인하고 Host는 계속 실행됨 | README의 `xpair status`, 로그 경로, 앱 status/menu 정보가 진단 표면임 | continue |
| 42330 | 상태를 확인하지 않음 | 진단 출력 없음, no-client 대기 유지 | 상태 조회 명령이나 메뉴 조작이 없으면 변화 없음 | terminate |
| 42331 | `xpair status` 또는 메뉴 상태를 확인함 | 앱 PID, host server, heartbeat age 같은 Host 상태를 확인함 | README는 `xpair status`를 Host app과 heartbeat 진단 명령으로 안내함 | terminate |
| 42332 | 로그 위치를 확인함 | `~/.xpair/host/logs/xpair.log`에서 Host 로그를 볼 수 있음 | README troubleshooting이 로그 경로를 안내함 | terminate |
| 42333 | 도움말 문서를 확인함 | 설치, 클라이언트 시작, 문제 해결 안내를 보고 no-client 해결을 준비함 | README usage와 troubleshooting이 `xpair launch`, `doctor`, `status`를 안내함 | terminate |
| 4234 | 사용자가 Host를 종료하려 함 | 메뉴에 노출된 종료만 허용되고, 종료 후에는 Host가 클라이언트를 기다리지 않음 | `Quit` 메뉴가 `NSApplication.terminate(_:)`에 연결됨 | continue |
| 42340 | Quit를 누르지 않음 | Host는 계속 실행되고 no-client 대기 상태 유지 | 종료 액션 없음 | terminate |
| 42341 | Quit 메뉴 또는 `q` 단축키를 실행함 | XpairHost 앱이 종료됨 | `NSApplication.terminate(_:)`가 호출됨 | terminate |
| 42342 | tmux host만 stop하려고 함 | 현재 메뉴에는 별도 stop host 항목이 없으므로 접근 불가 | `rebuildMenu()`에 restart 또는 stop host 항목은 없음 | terminate |

다음 실행 ID
- 421
- 422
