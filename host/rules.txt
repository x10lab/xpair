# RemotePair approve rules v2 — runtime location: ~/.remote-pair/rules.txt
# One line = one approval dialog.  id <TAB> marker <TAB> action      (lines starting with "#" are comments, edits apply immediately)
#   marker = unique OCR text that appears only in that dialog (substring match, case-insensitive)
#   action = ocr:<label|label..>  (find that button text via OCR and click it)
#          | key:<combo>          (send keys — cmd+return, return, esc …)
1Password	Access Requested	ocr:Authorize|Authorize Once|Always Allow|Allow|Approve|Confirm|승인|한 번 승인|허용|확인
Claude for Chrome	New permissions required	key:cmd+return|return
# e.g.) add a new dialog:
# MyApp	Grant access?	ocr:Allow|허용
# Some Dialog	Confirm action	key:return
